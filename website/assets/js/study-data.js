/**
 * Full-text search index + Q&A flashcards per guide.
 * AUTO-GENERATED by prepare-deploy.js — do not edit by hand.
 */
window.STUDY_DATA = {"00_Topic_List.md":{"title":"Complete Roadmap","trackId":"utility","trackTitle":"Planning","sections":[{"id":"complete-interview-preparation-roadmap","heading":"Complete Interview Preparation Roadmap","level":1,"text":""},{"id":"for-java-backend-developer-3-years-experience-senior-role","heading":"For Java Backend Developer | 3+ Years Experience → Senior Role","level":3,"text":""},{"id":"how-to-use-this-guide","heading":"HOW TO USE THIS GUIDE","level":2,"text":"Read each file in order Each topic goes from Beginner → Intermediate → Advanced Revise daily as your checklist Mark topics: Not started In progress Done"},{"id":"file-index","heading":"FILE INDEX","level":2,"text":"File Topics Covered Java 8–21, OOP, Collections, Concurrency, JVM, Design Patterns, §22 wait/sleep/join, ThreadLocal, class loaders, deep/shallow copy Spring Core, Boot, REST, Security, JPA, Microservices, §14 JPA/Hibernate deep dive, §15 Spring Q&A, §16 annotations cheat sheet Scalability, Caching, Databases, Messaging, Real world designs, §14 Order tracking, Rate limiter, URL shortener, Redis KV, Auth OOP Design, SOLID, Design Patterns, LLD problems, §8 Order Management System Arrays, Strings, Trees, Graphs, DP, Greedy, Searching, Sorting Java tricks, Bit manipulation, Math shortcuts, Template code AI Agents, MCP servers, prompt & context engineering, RAG, Beginner → Pro React.js — JSX, Hooks, Router, Redux, Performance, TypeScript, Testing, React 18/19, Q&A Real interview Q&A — company wise actual technical round questions with full answers StackReady Web UI — responsive dashboard + markdown reader ( ) — deploy:"},{"id":"section-1-java-beginner-to-advanced","heading":"SECTION 1 — JAVA (Beginner to Advanced)","level":2,"text":""},{"id":"11-java-core","heading":"1.1 Java Core","level":3,"text":"[ ] JDK vs JRE vs JVM [ ] Data Types, Wrappers, Autoboxing [ ] Operators, Control Flow [ ] String, StringBuilder, StringBuffer (immutability) [ ] Arrays & Varargs [ ] Static keyword (variables, methods, blocks, nested classes) [ ] , , [ ] Pass by value vs Pass by reference [ ] Type casting (implicit, explicit, ClassCastException)"},{"id":"12-object-oriented-programming","heading":"1.2 Object-Oriented Programming","level":3,"text":"[ ] Class, Object, Constructor (default, parameterized, copy) [ ] and keywords [ ] Inheritance (single, multilevel, hierarchical — NO multiple) [ ] Polymorphism (Compile time: overloading Runtime: overriding) [ ] Encapsulation (getters/setters, access modifiers) [ ] Abstraction (abstract class vs interface) [ ] Interface default & static methods (Java 8+) [ ] Covariant return types [ ] Object class methods ( , , , , , )"},{"id":"13-java-collections-framework","heading":"1.3 Java Collections Framework","level":3,"text":"[ ] Iterable → Collection → List/Set/Queue hierarchy [ ] ArrayList vs LinkedList vs Vector [ ] HashSet vs LinkedHashSet vs TreeSet [ ] HashMap vs LinkedHashMap vs TreeMap vs Hashtable [ ] HashMap internal working (hashing, buckets, load factor, rehashing) [ ] ConcurrentHashMap internal working [ ] PriorityQueue, ArrayDeque, Stack [ ] Collections utility class [ ] Comparable vs Comparator [ ] Fail fast vs Fail safe iterators"},{"id":"14-java-generics","heading":"1.4 Java Generics","level":3,"text":"[ ] Generic classes and methods [ ] Bounded type parameters ( ) [ ] Wildcards ( , , ) [ ] Type erasure"},{"id":"15-exception-handling","heading":"1.5 Exception Handling","level":3,"text":"[ ] Checked vs Unchecked exceptions [ ] Exception hierarchy (Throwable → Error / Exception) [ ] try with resources (AutoCloseable) [ ] Multi catch blocks [ ] Custom exceptions (best practices) [ ] throws vs throw"},{"id":"16-java-8-features-most-important","heading":"1.6 Java 8 Features (MOST IMPORTANT)","level":3,"text":"[ ] Lambda expressions [ ] Functional interfaces ( , built in: Predicate, Function, Consumer, Supplier, BiFunction) [ ] Stream API (map, filter, reduce, collect, flatMap, distinct, sorted, limit, skip) [ ] Terminal vs Intermediate operations [ ] Optional class [ ] Default and static methods in interface [ ] Method references (4 types) [ ] (toList, toMap, groupingBy, partitioningBy, joining) [ ] Date/Time API (LocalDate, LocalTime, LocalDateTime, ZonedDateTime, Duration, Period)"},{"id":"17-java-921-features","heading":"1.7 Java 9–21 Features","level":3,"text":"[ ] Java 9: Modules, , , Stream improvements [ ] Java 10: keyword (local variable type inference) [ ] Java 11: methods (isBlank, strip, lines, repeat), in lambda [ ] Java 14: Records (preview), Switch expressions [ ] Java 15: Text blocks [ ] Java 16: Records (stable), pattern matching [ ] Java 17: Sealed classes (LTS) [ ] Java 21: Virtual Threads (Project Loom), Record Patterns, Sequenced Collections (LTS)"},{"id":"18-multithreading-concurrency","heading":"1.8 Multithreading & Concurrency","level":3,"text":"[ ] Thread lifecycle (NEW, RUNNABLE, BLOCKED, WAITING, TIMED WAITING, TERMINATED) [ ] Creating threads: class vs vs [ ] keyword (method & block level) [ ] keyword [ ] wait(), notify(), notifyAll() [ ] Deadlock, Livelock, Starvation — causes & prevention [ ] package [ ] ExecutorService, ThreadPoolExecutor, ScheduledExecutorService [ ] and [ ] CountDownLatch, CyclicBarrier, Semaphore, Phaser [ ] AtomicInteger, AtomicReference, AtomicLong [ ] ReentrantLock, ReadWriteLock, StampedLock [ ] ForkJoinPool and parallel streams [ ] Thread safe collections (CopyOnWriteArrayList, ConcurrentHashMap) [ ] Virtual Threads (Java 21)"},{"id":"19-jvm-internals","heading":"1.9 JVM Internals","level":3,"text":"[ ] JVM Architecture (Class Loader, Runtime Data Areas, Execution Engine) [ ] Class loading (Bootstrap, Extension, Application classloaders) [ ] JVM Memory: Heap (Young Gen: Eden+Survivor, Old Gen), Stack, Method Area, PC Register, Native Stack [ ] Garbage Collection algorithms: Serial, Parallel, CMS, G1, ZGC, Shenandoah [ ] GC roots and reachability [ ] String Pool (intern()) [ ] JIT Compiler [ ] Memory leaks in Java — causes and fixes [ ] JVM tuning flags ( , , , etc.)"},{"id":"110-design-patterns","heading":"1.10 Design Patterns","level":3,"text":"[ ] Creational : Singleton (all variants), Factory, Abstract Factory, Builder, Prototype [ ] Structural : Adapter, Bridge, Composite, Decorator, Facade, Flyweight, Proxy [ ] Behavioral : Chain of Responsibility, Command, Iterator, Mediator, Memento, Observer, State, Strategy, Template Method, Visitor [ ] Anti patterns to avoid"},{"id":"section-2-spring-boot","heading":"SECTION 2 — SPRING BOOT","level":2,"text":""},{"id":"21-spring-core","heading":"2.1 Spring Core","level":3,"text":"[ ] IoC and DI (Inversion of Control, Dependency Injection) [ ] Bean lifecycle (instantiation → populate properties → init → use → destroy) [ ] Bean scopes: singleton, prototype, request, session, application, websocket [ ] , , [ ] , , , , [ ] , [ ] ApplicationContext vs BeanFactory [ ] Spring AOP (Aspect, Advice, Pointcut, JoinPoint, Weaving) [ ] deep dive (propagation, isolation levels, rollback rules)"},{"id":"22-spring-boot","heading":"2.2 Spring Boot","level":3,"text":"[ ] Auto configuration mechanism [ ] (= + + ) [ ] vs [ ] Profiles ( , ) [ ] Externalized configuration ( , ) [ ] Spring Boot Actuator (endpoints, health, metrics, custom endpoints) [ ] Embedded server (Tomcat, Jetty, Undertow) [ ] Spring Boot Starter dependencies [ ] SpringApplication lifecycle"},{"id":"23-spring-rest","heading":"2.3 Spring REST","level":3,"text":"[ ] , , , , , [ ] , , , [ ] — status codes and headers [ ] Exception handling: , , [ ] Content negotiation [ ] HATEOAS [ ] API versioning strategies"},{"id":"24-spring-data-jpa","heading":"2.4 Spring Data JPA","level":3,"text":"[ ] JPA vs Hibernate vs Spring Data JPA [ ] Entity, , , , [ ] Relationships: , , , [ ] Fetch types: EAGER vs LAZY [ ] CascadeType, orphanRemoval [ ] JpaRepository, CrudRepository, PagingAndSortingRepository [ ] JPQL and native queries ( ) [ ] Named queries [ ] with JPA [ ] N+1 problem and solutions (JOIN FETCH, EntityGraph, batch fetching) [ ] Pagination and Sorting ( , ) [ ] Optimistic vs Pessimistic locking ( ) [ ] Auditing ( , ) [ ] Criteria API"},{"id":"25-spring-security","heading":"2.5 Spring Security","level":3,"text":"[ ] Authentication vs Authorization [ ] , filter order [ ] UserDetailsService, UserDetails [ ] Password encoding (BCrypt) [ ] JWT authentication (end to end flow) [ ] OAuth2 / OpenID Connect [ ] , , [ ] CSRF, CORS configuration [ ] Method level security [ ] Spring Security 6.x changes"},{"id":"26-microservices-with-spring","heading":"2.6 Microservices with Spring","level":3,"text":"[ ] Microservices principles (12 factor app) [ ] Spring Cloud components: Config Server, Eureka (Service Discovery), Gateway, Ribbon/LoadBalancer, Feign Client, Hystrix/Resilience4j [ ] Circuit breaker pattern [ ] Saga pattern (choreography vs orchestration) [ ] API Gateway pattern [ ] Service mesh concepts (Istio, Envoy) [ ] Distributed tracing (Zipkin, Sleuth) [ ] Inter service communication: REST vs gRPC vs Messaging [ ] Event driven architecture with Kafka/RabbitMQ [ ] Docker & Kubernetes basics for microservices"},{"id":"27-spring-annotations-cheat-sheet-in-02_springbootmd-16","heading":"2.7 Spring annotations cheat sheet (in 02_SpringBoot.md §16)","level":3,"text":"[ ] Spring annotations cheat sheet — core, Boot, REST, JPA, validation, security, testing"},{"id":"section-3-system-design-hld","heading":"SECTION 3 — SYSTEM DESIGN (HLD)","level":2,"text":"[ ] Scalability: Vertical vs Horizontal scaling [ ] Load Balancing (Round Robin, Least Connections, Consistent Hashing) [ ] Caching (L1/L2/L3, CDN, Redis, Memcached, cache invalidation strategies) [ ] Database scaling (Read replicas, Sharding, Partitioning) [ ] SQL vs NoSQL — when to use which [ ] CAP Theorem, BASE, ACID [ ] Consistency patterns (strong, eventual, causal) [ ] Message Queues (Kafka, RabbitMQ, SQS) [ ] Content Delivery Network (CDN) [ ] Rate Limiting algorithms (Token Bucket, Leaky Bucket, Sliding Window) [ ] Bloom Filters [ ] Consistent Hashing [ ] Distributed ID generation (UUID, Snowflake, ULID) [ ] Service Discovery [ ] API Gateway design [ ] Event Sourcing & CQRS [ ] Distributed Transactions (2PC, Saga) [ ] Distributed Locks (Redis Redlock) [ ] Real world designs : URL Shortener, Rate Limiter, Notification System, Chat App, News Feed, Search Autocomplete, Uber/Lyft, Netflix/YouTube, Twitter, Payment System"},{"id":"section-4-system-design-lld","heading":"SECTION 4 — SYSTEM DESIGN (LLD)","level":2,"text":"[ ] SOLID principles (with Java examples) [ ] DRY, KISS, YAGNI principles [ ] UML: Class, Sequence, Activity, Use Case diagrams [ ] All 23 GoF Design Patterns (Creational, Structural, Behavioral) [ ] Real world LLD problems : [ ] Parking Lot [ ] Library Management System [ ] Elevator System [ ] BookMyShow / Movie Ticket Booking [ ] Amazon / E commerce Cart & Order [ ] Hotel Reservation System [ ] ATM Machine [ ] Chess Game [ ] Snake and Ladder [ ] Vending Machine [ ] Ride Sharing (Uber) [ ] Food Delivery (Swiggy/Zomato) [ ] Splitwise [ ] Logging Framework"},{"id":"section-5-dsa-for-35-yoe-roles","heading":"SECTION 5 — DSA (for 3–5 YOE roles)","level":2,"text":""},{"id":"data-structures","heading":"Data Structures","level":3,"text":"[ ] Arrays (1D, 2D, prefix sum, sliding window, two pointers) [ ] Strings (palindrome, anagram, substring problems) [ ] Linked List (singly, doubly, circular; reverse, detect cycle, merge) [ ] Stack (monotonic stack) [ ] Queue (BFS, circular queue) [ ] Deque / Sliding Window Maximum [ ] Binary Tree (traversals, BST, balanced trees) [ ] Heap / Priority Queue (min heap, max heap, heap sort) [ ] Hashing (HashMap, HashSet patterns) [ ] Trie (prefix tree) [ ] Union Find (Disjoint Set Union) [ ] Segment Tree, Fenwick Tree (BIT) — optional but good to know"},{"id":"algorithms","heading":"Algorithms","level":3,"text":"[ ] Sorting: Bubble, Selection, Insertion, Merge, Quick, Heap, Counting, Radix [ ] Binary Search (and variations: first/last occurrence, rotated array, answer space) [ ] BFS, DFS (graphs + trees) [ ] Topological Sort (Kahn's + DFS) [ ] Dijkstra's, Bellman Ford, Floyd Warshall [ ] Prim's, Kruskal's (MST) [ ] Dynamic Programming patterns: [ ] 0/1 Knapsack [ ] Unbounded Knapsack [ ] LCS, LIS, Edit Distance [ ] Matrix Chain Multiplication [ ] Interval DP [ ] Bitmask DP [ ] Digit DP [ ] Tree DP [ ] Greedy algorithms [ ] Backtracking (permutations, combinations, N Queens, Sudoku) [ ] Divide & Conquer"},{"id":"complexity","heading":"Complexity","level":3,"text":"[ ] Big O, Big Ω, Big Θ notation [ ] Time & Space complexity of all standard algorithms [ ] Amortized complexity"},{"id":"section-6-competitive-programming-tricks","heading":"SECTION 6 — COMPETITIVE PROGRAMMING TRICKS","level":2,"text":"[ ] Fast I/O in Java [ ] Bit manipulation tricks (set/unset/toggle bit, count set bits, power of 2, XOR tricks) [ ] Modular arithmetic (mod inverse, Fermat's little theorem) [ ] Sieve of Eratosthenes [ ] GCD, LCM, Extended Euclidean [ ] Matrix exponentiation [ ] Two pointers / Sliding window patterns [ ] Binary search on answer [ ] Monotonic stack/queue patterns [ ] Java specific: TreeMap, TreeSet for ordered operations [ ] Custom comparator tricks [ ] Memoization template (top down DP) [ ] Tabulation template (bottom up DP)"},{"id":"interview-question-banks","heading":"INTERVIEW QUESTION BANKS","level":2,"text":""},{"id":"java-hot-questions","heading":"Java Hot Questions","level":3,"text":"1. What happens when capacity exceeds load factor? 2. Difference between and for Strings 3. Why is String immutable? 4. How does achieve thread safety? 5. What is the difference between and ? 6. Explain chaining 7. What is the difference between and ? 8. How does GC work in Java? Which GC would you use for low latency? 9. What are virtual threads? How do they differ from platform threads? 10. Explain Java Memory Model (JMM) and"},{"id":"spring-boot-hot-questions","heading":"Spring Boot Hot Questions","level":3,"text":"1. How does Spring Boot auto configuration work? 2. What is the difference between , , ? 3. Explain propagation levels with examples 4. What is N+1 problem and how to solve it? 5. How does JWT authentication work end to end? 6. Explain circuit breaker pattern with Resilience4j 7. How to handle distributed transactions in microservices? 8. What is the difference between and ? 9. How does Spring AOP work internally? 10. Explain Spring Security filter chain"},{"id":"system-design-hot-questions","heading":"System Design Hot Questions","level":3,"text":"1. Design a URL shortener (like bit.ly) 2. Design a rate limiter 3. Design WhatsApp / chat system 4. Design Netflix (video streaming) 5. Design Twitter news feed 6. Design an e commerce system 7. How would you design a notification service? 8. Design a distributed cache 9. Design a search autocomplete system 10. How would you scale a database to handle millions of users?"},{"id":"daily-study-plan-8-week-roadmap","heading":"DAILY STUDY PLAN (8-Week Roadmap)","level":2,"text":"Week Focus Area Week 1 Java Core + OOP + Collections Week 2 Java 8 Features + Generics + Exception Handling Week 3 Multithreading + JVM + Design Patterns Week 4 Spring Boot + REST + JPA Week 5 Spring Security + Microservices + Spring Cloud Week 6 System Design HLD (concepts + 5 designs) Week 7 System Design LLD (SOLID + 5 problems) Week 8 DSA Revision + CP Tricks + Mock Interviews"},{"id":"section-6-reactjs-beginner-to-advanced","heading":"SECTION 6 — REACT.JS (Beginner to Advanced)","level":2,"text":""},{"id":"61-react-fundamentals","heading":"6.1 React Fundamentals","level":3,"text":"[ ] What is React, SPA, Virtual DOM, reconciliation [ ] JSX rules, fragments, expressions [ ] Functional vs class components [ ] Props (read only), state, one way data flow [ ] Lifting state up, state colocation"},{"id":"62-hooks-critical","heading":"6.2 Hooks (Critical)","level":3,"text":"[ ] useState — functional updates [ ] useEffect — deps, cleanup, common mistakes [ ] useContext, useReducer, useRef [ ] useMemo, useCallback — when to use [ ] useLayoutEffect, useId [ ] useTransition, useDeferredValue (React 18) [ ] Custom hooks — useFetch, useDebounce [ ] Rules of Hooks"},{"id":"63-ui-patterns","heading":"6.3 UI Patterns","level":3,"text":"[ ] Event handling, SyntheticEvent [ ] Controlled vs uncontrolled forms [ ] Lists, keys, conditional rendering [ ] Error boundaries, Portals, forwardRef"},{"id":"64-ecosystem","heading":"6.4 Ecosystem","level":3,"text":"[ ] React Router v6 — routes, nested, protected [ ] Redux Toolkit vs Zustand vs Context [ ] Performance — React.memo, lazy, Suspense, virtualization [ ] TypeScript with React [ ] Testing — Jest, React Testing Library [ ] Vite, Next.js, SSR vs SSG vs CSR"},{"id":"65-advanced-interview","heading":"6.5 Advanced & Interview","level":3,"text":"[ ] React 18/19 — concurrent, Server Components [ ] Compound components, HOC, render props [ ] Code puzzles (batching, stale closure, infinite loop) [ ] Todo app, debounce, infinite scroll coding problems [ ] Full Q&A bank in"},{"id":"section-7-ai-agents-mcp-in-14_ai_agents_and_mcp_for_developersmd","heading":"SECTION 7 — AI AGENTS & MCP (in 14_AI_Agents_and_MCP_For_Developers.md)","level":2,"text":"[ ] §23 Memory bloating in agentic AI [ ] §23 Partial failures in agentic AI [ ] §23 Hallucinations in agentic AI [ ] §23 Performance issues in agentic AI"},{"id":"section-8-java-multithreading-deep-dive-in-16_java_multithreading_deep_divemd","heading":"SECTION 8 — JAVA MULTITHREADING DEEP DIVE (in 16_Java_Multithreading_Deep_Dive.md)","level":2,"text":"[ ] Process vs Thread — memory sharing model [ ] When to use threads, Amdahl's Law, CPU vs I/O pool sizing [ ] Thread lifecycle & states (sleep vs wait lock behavior) [ ] Creating threads — Thread, Runnable, Callable, ExecutorService [ ] Concurrency models (pool, producer consumer, fork join, pipeline, scatter gather) [ ] Synchronization — synchronized, volatile, Locks, Atomics/CAS [ ] Java Memory Model & happens before [ ] wait/notify & BlockingQueue producer consumer [ ] Thread pools & ExecutorService lifecycle [ ] ThreadPoolExecutor internals (core/max/queue/rejection policy) [ ] Callable, Future & CompletableFuture pipelines [ ] Concurrent collections (ConcurrentHashMap, CopyOnWriteArrayList) [ ] ForkJoinPool & parallel streams [ ] Virtual threads (Java 21, Project Loom) [ ] Deadlock, livelock, starvation — prevention & diagnosis [ ] Real world multithreading patterns (partitioned queues, batching, async callback) [ ] Multithreading best practices & cooperative interruption [ ] Multithreading interview Q&A bank"},{"id":"section-9-java-streams-deep-dive-in-17_java_streams_deep_divemd","heading":"SECTION 9 — JAVA STREAMS DEEP DIVE (in 17_Java_Streams_Deep_Dive.md)","level":2,"text":"[ ] Stream vs Collection, pipeline anatomy, single use [ ] Creating streams (of, iterate, generate, IntStream, Files.lines) [ ] Intermediate ops (filter, map, sorted, distinct, limit, takeWhile) [ ] Terminal ops (collect, reduce, count, match, find) [ ] Collectors (groupingBy, partitioningBy, toMap, joining, teeing) [ ] flatMap & nested data flattening [ ] Primitive streams (IntStream/LongStream/DoubleStream) [ ] Optional — map, filter, orElse, orElseGet, ifPresent [ ] Parallel streams — when to use and pitfalls [ ] Lazy evaluation & short circuiting [ ] Stream common pitfalls [ ] Real world stream examples (grouping, indexing, top N) [ ] Streams interview Q&A bank"},{"id":"section-10-java-spring-annotations-deep-dive-in-18_annotations_deep_divemd","heading":"SECTION 10 — JAVA & SPRING ANNOTATIONS DEEP DIVE (in 18_Annotations_Deep_Dive.md)","level":2,"text":"[ ] What annotations are & how frameworks use them [ ] Built in Java annotations (@Override, @Deprecated, @FunctionalInterface) [ ] Meta annotations (@Target, @Retention, @Inherited, @Repeatable) [ ] Creating custom annotations [ ] Retention policies & reading annotations via reflection [ ] Compile time annotation processing (Lombok, MapStruct) [ ] Core Spring annotations (@Component, @Autowired, @Bean, @Configuration) [ ] Spring Boot annotations (@SpringBootApplication, @ConfigurationProperties, @Async, @Scheduled) [ ] Spring Web/REST annotations (@RestController, @GetMapping, @RequestBody) [ ] Spring Data JPA annotations (@Entity, @Id, relationships, @Query) [ ] Validation annotations (@Valid, @NotNull, @Size, @Email) [ ] Transaction & AOP annotations (@Transactional, @Aspect, advice) [ ] Testing annotations (@SpringBootTest, @MockBean, slices) [ ] Annotations interview Q&A bank"},{"id":"section-11-nodejs-in-19_node_js_complete_guidemd","heading":"SECTION 11 — NODE.JS (in 19_Node_JS_Complete_Guide.md)","level":2,"text":"[ ] What is Node.js, V8, libuv, non blocking I/O [ ] Event loop phases, microtasks vs macrotasks [ ] Modules — CommonJS vs ES Modules [ ] npm, package.json, semver, lockfile [ ] Callbacks, Promises, async/await, Promise.all [ ] Core modules (fs, path, http, events) [ ] Streams & Buffers, backpressure, pipeline [ ] HTTP server & Express basics [ ] Middleware, routing, async error handling [ ] REST API design with Express (status codes, validation) [ ] Databases — Mongoose, pg pool, parameterized queries [ ] Auth (JWT, bcrypt) & security checklist [ ] Error handling & graceful shutdown [ ] Performance, clustering & worker threads [ ] Testing Node apps (Jest, Supertest) [ ] TypeScript with Node.js [ ] Node production best practices [ ] Node coding problems (promisify, mapLimit, retry, debounce) [ ] Node.js interview Q&A bank"},{"id":"section-12-apache-kafka-in-20_kafka_deep_divemd","heading":"SECTION 12 — APACHE KAFKA (in 20_Kafka_Deep_Dive.md)","level":2,"text":"[ ] What Kafka is — distributed commit log, when to use [ ] Brokers, topics, partitions, replication, ISR, controller [ ] Producers — acks, batching, partitioning, idempotence [ ] Consumers & consumer groups, partition assignment [ ] Rebalancing — cooperative, static membership, tuning [ ] Offsets & delivery semantics (at least once default) [ ] Exactly once — idempotent producer & transactions [ ] Ordering guarantees (per partition, keying) [ ] Retry topics & dead letter queues [ ] Outbox pattern (transactional event publishing) [ ] Backpressure & flow control (pull, pause/resume) [ ] Consumer lag monitoring & operations [ ] Schema registry & compatibility modes [ ] Spring Kafka in practice [ ] Design: 50,000 events/sec system [ ] Kafka interview Q&A bank"},{"id":"section-13-kubernetes-docker-cloud-native-in-21_kubernetes_docker_cloudnativemd","heading":"SECTION 13 — KUBERNETES, DOCKER & CLOUD-NATIVE (in 21_Kubernetes_Docker_CloudNative.md)","level":2,"text":"[ ] Docker fundamentals — images, layers, containers [ ] Production Dockerfiles (multi stage, non root) [ ] Docker networking, volumes, compose [ ] Kubernetes architecture (control plane, nodes) [ ] Pods, ReplicaSets & Deployments [ ] Services & Ingress [ ] ConfigMaps & Secrets [ ] Liveness, readiness & startup probes [ ] Resource requests, limits & QoS (OOMKilled) [ ] Horizontal Pod Autoscaler (+ KEDA, Cluster Autoscaler) [ ] Rolling deploys & rollbacks [ ] StatefulSets & persistent storage (PV/PVC) [ ] AWS for backend (EKS/ECS/S3/RDS/SQS/IAM) [ ] CI/CD to Kubernetes (GitHub Actions, GitOps) [ ] Troubleshooting pods (CrashLoopBackOff etc.) [ ] Kubernetes/Docker interview Q&A bank"},{"id":"section-14-resilience-distributed-patterns-in-22_resilience_distributed_patternsmd","heading":"SECTION 14 — RESILIENCE & DISTRIBUTED PATTERNS (in 22_Resilience_Distributed_Patterns.md)","level":2,"text":"[ ] Why resilience — cascading failure, fallacies [ ] Timeouts & deadline propagation [ ] Retries & exponential backoff + jitter [ ] Circuit breaker (CLOSED/OPEN/HALF OPEN) [ ] Bulkhead isolation [ ] Rate limiting (token/leaky/sliding window) [ ] Resilience4j in practice [ ] Idempotency keys [ ] Distributed locks (Redis/Redisson, fencing) [ ] Saga pattern (choreography vs orchestration) [ ] Distributed ID generation (Snowflake) [ ] CAP, strong vs eventual consistency [ ] Graceful degradation & fallbacks [ ] Resilience interview Q&A bank"},{"id":"section-15-observability-production-debugging-in-23_observability_production_debuggingmd","heading":"SECTION 15 — OBSERVABILITY & PRODUCTION DEBUGGING (in 23_Observability_Production_Debugging.md)","level":2,"text":"[ ] Observability vs monitoring [ ] Three pillars — metrics, logs, traces [ ] Metrics with Prometheus & PromQL [ ] Dashboards with Grafana [ ] Structured logging (JSON, levels, no PII) [ ] Log aggregation (ELK & Loki) [ ] Distributed tracing (OpenTelemetry & Jaeger) [ ] Correlation IDs / trace propagation [ ] RED & USE methods (golden signals) [ ] SLI, SLO, SLA & error budgets [ ] Alerting on symptoms, actionable alerts [ ] Debugging: a service is slow in prod [ ] Debugging: errors, memory, CPU [ ] Observability interview Q&A bank"},{"id":"section-16-jvm-performance-advanced-concurrency-in-24_jvm_performance_advanced_concurrencymd","heading":"SECTION 16 — JVM PERFORMANCE & ADVANCED CONCURRENCY (in 24_JVM_Performance_Advanced_Concurrency.md)","level":2,"text":"[ ] JVM memory areas (heap, metaspace, stacks) [ ] Generational GC (young/old, minor/full) [ ] Garbage collectors — Serial to ZGC [ ] G1 vs ZGC — choosing & tuning [ ] Reading GC logs [ ] Key JVM flags (container aware sizing) [ ] Heap dumps & memory leak hunting (MAT) [ ] Thread dumps & CPU profiling (flame graphs) [ ] Diagnostic tooling — jstack, jmap, Arthas [ ] ThreadPoolExecutor tuning [ ] CompletableFuture patterns [ ] Virtual threads — when & pinning pitfalls [ ] Lock free & atomics (CAS, LongAdder, ABA) [ ] JIT, escape analysis & warmup [ ] JVM performance interview Q&A bank"},{"id":"section-17-redis-caching-strategy-in-25_redis_caching_strategymd","heading":"SECTION 17 — REDIS & CACHING STRATEGY (in 25_Redis_Caching_Strategy.md)","level":2,"text":"[ ] Why cache & what Redis is [ ] Redis data structures (String/Hash/ZSet/Stream) [ ] Caching patterns overview [ ] Cache aside (lazy loading) [ ] Write through & write behind [ ] Eviction policies (LRU/LFU) [ ] TTL & expiration design (jitter) [ ] Cache stampede & hot keys [ ] Cache penetration & avalanche [ ] Cache vs DB consistency (invalidate) [ ] Redis as a distributed lock [ ] Redis as a rate limiter [ ] Persistence, HA & cluster (RDB/AOF/Sentinel) [ ] Spring Cache with Redis [ ] Redis/caching interview Q&A bank"},{"id":"section-18-sql-at-scale-in-26_sql_at_scalemd","heading":"SECTION 18 — SQL AT SCALE (in 26_SQL_at_Scale.md)","level":2,"text":"[ ] How a query executes (optimizer, stats) [ ] Indexing deep dive (B tree, composite, covering) [ ] Reading EXPLAIN plans [ ] The N+1 problem & fixes [ ] Query optimization techniques (sargable, keyset) [ ] Transactions & isolation levels [ ] Optimistic vs pessimistic locking [ ] Connection pooling & HikariCP sizing [ ] Read replicas & read/write splitting [ ] Partitioning (range/hash, pruning) [ ] Sharding (shard key, hot shards) [ ] SQL vs NoSQL & denormalization [ ] Schema migrations at scale (expand contract) [ ] SQL at scale interview Q&A bank"},{"id":"section-19-design-patterns-in-java-in-27_design_patterns_javamd","heading":"SECTION 19 — DESIGN PATTERNS IN JAVA (in 27_Design_Patterns_Java.md)","level":2,"text":"[ ] Why design patterns matter — vocabulary, decoupling, trade offs [ ] SOLID principles recap (SRP, OCP, LSP, ISP, DIP) [ ] Pattern categories & how to choose (creational/structural/behavioral) [ ] Singleton — variants & thread safety (Java) [ ] Factory Method (Java) [ ] Abstract Factory — product families (Java) [ ] Builder — fluent, immutable objects (Java) [ ] Prototype — cloning & deep copy (Java) [ ] Adapter — wrap incompatible APIs (Java) [ ] Bridge — abstraction vs implementation (Java) [ ] Composite — tree structures (Java) [ ] Decorator — stackable responsibilities (Java) [ ] Facade — simplify a subsystem (Java) [ ] Flyweight — share immutable state (Java) [ ] Proxy — control access (lazy/security/AOP) (Java) [ ] Chain of Responsibility — handler pipeline (Java) [ ] Command — encapsulate requests, queue/undo (Java) [ ] Iterator — traverse without exposing internals (Java) [ ] Mediator — centralize interactions (Java) [ ] Memento — snapshot & restore state (Java) [ ] Observer — publish/subscribe (Java) [ ] State — behaviour by lifecycle state (Java) [ ] Strategy — interchangeable algorithms (Java) [ ] Template Method — algorithm skeleton (Java) [ ] Visitor — operations over a type hierarchy (Java) [ ] Interpreter — small grammar/DSL (Java) [ ] Enterprise & Spring patterns (DI, Repository, DTO, Proxy/AOP) [ ] Anti patterns to avoid (God object, singleton abuse, patternitis) [ ] Java design patterns interview Q&A bank"},{"id":"section-20-design-patterns-in-python-in-28_design_patterns_pythonmd","heading":"SECTION 20 — DESIGN PATTERNS IN PYTHON (in 28_Design_Patterns_Python.md)","level":2,"text":"[ ] Design patterns the Pythonic way [ ] Python idioms that replace patterns [ ] Creational patterns overview (Python) [ ] Singleton — module/metaclass (Python) [ ] Factory Method — dict dispatch / classmethod (Python) [ ] Abstract Factory — Protocol based families (Python) [ ] Builder — kwargs/dataclasses/fluent (Python) [ ] Prototype — copy/deepcopy (Python) [ ] Adapter — duck typing wrapper (Python) [ ] Bridge — composition with Protocol (Python) [ ] Composite — recursive trees (Python) [ ] Decorator — @decorator vs structural (Python) [ ] Facade — module/class entry point (Python) [ ] Flyweight — slots + lru cache (Python) [ ] Proxy — getattr / cached property (Python) [ ] Chain of Responsibility — middleware list (Python) [ ] Command — closures / functools.partial (Python) [ ] Iterator — generators & yield (Python) [ ] Mediator — coordinator (Python) [ ] Memento — deepcopy / pickle state (Python) [ ] Observer — callbacks / signals (Python) [ ] State — state machine objects (Python) [ ] Strategy — pass a Callable (Python) [ ] Template Method — ABC or HOF (Python) [ ] Visitor — functools.singledispatch (Python) [ ] Interpreter — expression tree / DSL (Python) [ ] Python design patterns interview Q&A bank"},{"id":"section-21-python-interview-questions-in-29_python_interview_questionsmd","heading":"SECTION 21 — PYTHON INTERVIEW QUESTIONS (in 29_Python_Interview_Questions.md)","level":2,"text":"[ ] Python core language & data model (is vs ==, mutability, args, mutable defaults) [ ] Python data structures & time complexity [ ] Python OOP, MRO/C3, super, dunder methods, metaclasses, descriptors [ ] Python decorators (wraps, retry/backoff, stateful, lru cache) [ ] Python generators & iterators (yield, yield from, lazy) [ ] The GIL & concurrency (threading vs multiprocessing vs asyncio) [ ] Async/await with asyncio (gather, tasks, blocking, timeouts) [ ] Python memory management & GC (refcount, cyclic GC, del , slots ) [ ] Python performance & optimization (profiling, comprehensions) [ ] Type hints, dataclasses & Pydantic, Protocol [ ] Standard library idioms (collections, itertools, functools) [ ] Modern Python 3.10–3.13 (match, walrus, free threaded) [ ] Exceptions & context managers (with, async, exception groups, EAFP) [ ] Python testing & tooling (pytest, ruff, uv, mypy) [ ] Python tricky output & gotchas (floats, list refs, closures) [ ] Pythonic coding problems (Counter, dedupe, group, LRU) Next Step : Open and start reading!"}],"qa":[]},"01_Java_Core_to_Advanced.md":{"title":"Java Core to Advanced","trackId":"java-core","trackTitle":"Java","sections":[{"id":"java-core-to-advanced","heading":"Java — Core to Advanced","level":1,"text":""},{"id":"complete-interview-guide-beginner-advanced","heading":"Complete Interview Guide | Beginner → Advanced","level":3,"text":""},{"id":"1-jvm-jdk-jre","heading":"1. JVM, JDK, JRE","level":2,"text":"JDK JRE JVM Purpose Develop + Run Run only Execute bytecode Contains JRE + tools JVM + libraries Runtime engine Use case Developers End users Inside JRE How Java code runs: Platform Independence : Java is WORA (Write Once Run Anywhere) because bytecode is platform independent. The JVM is platform specific. Theory. Think of Java as a two stage translation: your source code becomes bytecode (a portable instruction set, like a recipe written in a universal language), and each platform's JVM translates that bytecode into native machine code for that CPU/OS. You ship one ; Windows, Linux, and macOS each run it through their own JVM. Python and Node.js are often described similarly, but Java's bytecode + verified class loading is stricter and more portable across vendors. How it works. compiles → files containing opcodes (e.g., , ). The ClassLoader reads bytes, verifies them (no buffer overflows, valid references), and places class metadata in Metaspace while creating Class objects on the heap. The Execution Engine starts in interpreter mode (slow but instant startup), profiles which methods run hot, and the JIT compiler (C1/C2) recompiles those hot paths to native code — this is why Java gets faster after warmup. Example. A method in is loaded once; every call to a hot loop inside it may eventually run as native x86/ARM instructions, not interpreted bytecode. Interview angle. \"Is Java compiled or interpreted?\" → Both : compiled to bytecode, then interpreted + JIT compiled at runtime. \"Difference between JDK and JRE?\" → JDK = develop + run; JRE = run only (largely superseded — modern JDK bundles everything)."},{"id":"2-data-types-memory","heading":"2. Data Types & Memory","level":2,"text":""},{"id":"primitive-types","heading":"Primitive Types","level":3,"text":"Type Size Default Range 1 byte 0 128 to 127 2 bytes 0 32,768 to 32,767 4 bytes 0 2^31 to 2^31 1 8 bytes 0L 2^63 to 2^63 1 4 bytes 0.0f ±3.4e38 8 bytes 0.0d ±1.7e308 2 bytes '\\u0000' 0 to 65,535 1 bit false true/false"},{"id":"wrapper-classes","heading":"Wrapper Classes","level":3,"text":"Interview Trap : Integer cache is 128 to 127. Beyond that, returns false."},{"id":"pass-by-value","heading":"Pass by Value","level":3,"text":"Theory. Java is always pass by value . For primitives, the value itself is copied. For objects, the reference value (memory address) is copied — like handing someone a copy of a house key. They can use their copy to enter and rearrange furniture (mutate the object), but if they get a new key to a different house ( ), your original key still points to the old house. How it works. When you call , the stack frame gets a copy of the reference. Both caller and callee point to the same heap object. Reassigning the parameter ( ) only changes the callee's copy — the caller's reference is untouched. Mutating through the reference ( ) affects the shared object. Example. Pitfall. Interviewers love: \"Java is pass by reference for objects\" — wrong . It's pass by value of the reference. Confusing this leads to bugs when you expect reassignment inside a method to affect the caller."},{"id":"3-object-oriented-programming","heading":"3. Object-Oriented Programming","level":2,"text":""},{"id":"the-4-pillars","heading":"The 4 Pillars","level":3,"text":"3.1 Encapsulation 3.2 Inheritance 3.3 Polymorphism Compile time (Method Overloading): Runtime (Method Overriding): Rules for overriding: Same method name, same params Return type: same OR covariant (subtype) Access modifier: same OR more permissive Cannot override , , methods Can throw narrower checked exceptions 3.4 Abstraction Abstract Class: Interface: Abstract class vs Interface: Abstract Class Interface Instantiation No No Constructor Yes No Instance variables Yes No (only constants) Multiple inheritance No Yes Methods Abstract + concrete Abstract + default + static (Java 8+) Use when \"is a\" with shared code \"can do\" / capability"},{"id":"35-object-class-methods","heading":"3.5 Object Class Methods","level":3,"text":"Golden Rule : If two objects are equal ( returns true), they MUST have the same . The reverse is NOT required (hash collisions allowed)."},{"id":"36-static-keyword","heading":"3.6 Static Keyword","level":3,"text":""},{"id":"37-final-keyword","heading":"3.7 final keyword","level":3,"text":""},{"id":"38-nested-classes","heading":"3.8 Nested Classes","level":3,"text":""},{"id":"4-string-deep-dive","heading":"4. String Deep Dive","level":2,"text":""},{"id":"string-immutability","heading":"String Immutability","level":3,"text":"Theory. Once created, a String's character sequence cannot change. returns a new String; the original is untouched. This is why concatenation in a loop creates many throwaway objects. How it works. Strings store a (or in older JDKs) and cache. The JVM maintains a String Pool (in heap since Java 7) for string literals — in code is interned. compares references; compares content. Why String is immutable? 1. Thread safety (can be shared without synchronization) 2. String Pool (reuse is possible only if immutable) 3. Security (passwords, file paths can't be modified) 4. HashCode caching (used as HashMap keys safely)"},{"id":"string-vs-stringbuilder-vs-stringbuffer","heading":"String vs StringBuilder vs StringBuffer","level":3,"text":"String StringBuilder StringBuffer Mutability Immutable Mutable Mutable Thread Safety Yes (immutable) No Yes (synchronized) Performance Slow (concatenation) Fastest Slower than SB Use case Constants, keys Single threaded Multi threaded"},{"id":"important-string-methods","heading":"Important String Methods","level":3,"text":""},{"id":"5-java-collections-framework","heading":"5. Java Collections Framework","level":2,"text":""},{"id":"hierarchy","heading":"Hierarchy","level":3,"text":""},{"id":"51-arraylist-vs-linkedlist","heading":"5.1 ArrayList vs LinkedList","level":3,"text":""},{"id":"52-hashmap-internal-working-critical-interview-topic","heading":"5.2 HashMap Internal Working (CRITICAL INTERVIEW TOPIC)","level":3,"text":"Theory. A HashMap is an array of buckets . Given a key, you compute a hash, map it to a bucket index, and look up the value in O(1) average time. Collisions (two keys, same bucket) are handled by a linked list (Java 7) or linked list → Red Black Tree (Java 8+). The trade off: no ordering, and you must implement + correctly for custom keys. For full depth — Java 7 vs 8 differences, resize mechanics, custom key puzzles, RB tree conditions → see Section 13 and Section 14. Example — bucket index with capacity 16: Why override both and for custom keys?"},{"id":"53-concurrenthashmap","heading":"5.3 ConcurrentHashMap","level":3,"text":"For full depth — Java 7 segments vs Java 8 CAS, atomic operations, resize, vs synchronizedMap → see Section 15."},{"id":"54-fail-fast-vs-fail-safe","heading":"5.4 Fail-Fast vs Fail-Safe","level":3,"text":"Theory. Fail fast iterators detect that the collection was structurally modified (add/remove outside the iterator) during iteration and throw immediately — better than silent corruption. Fail safe iterators work on a snapshot or weakly consistent view — no exception, but you may miss concurrent updates or see stale data. How it works. tracks (incremented on add/remove). The iterator remembers ; if they differ → exception. copies the entire array on every write; the iterator holds a reference to the old array — reads are fast, writes are expensive. Interview angle. Enhanced for loop uses an iterator under the hood — that's why inside fails but works."},{"id":"55-comparable-vs-comparator","heading":"5.5 Comparable vs Comparator","level":3,"text":""},{"id":"56-priorityqueue","heading":"5.6 PriorityQueue","level":3,"text":""},{"id":"6-generics","heading":"6. Generics","level":2,"text":""},{"id":"why-generics","heading":"Why Generics?","level":3,"text":""},{"id":"bounded-type-parameters","heading":"Bounded Type Parameters","level":3,"text":""},{"id":"wildcards-pecs-rule","heading":"Wildcards — PECS Rule","level":3,"text":"Theory. Generics are invariant in Java: is NOT a , even though Dog extends Animal. If it were, you could put a Cat into a through the Animal view — type safety would break. Wildcards ( , ) let you write flexible APIs without losing safety. How it works — PECS (Producer Extends, Consumer Super): — you can read as Number (producer), but cannot (except null) because you don't know if the list is or . — you can write Integers (consumer), but reads return (only safe common type). Example. Copy numbers from a source list into a destination list: Interview angle. is NOT a supertype of . Use or PECS wildcards for polymorphic collection parameters."},{"id":"type-erasure","heading":"Type Erasure","level":3,"text":"Theory. Java generics were added in Java 5 for backward compatibility with pre generics bytecode. The compiler checks types at compile time, then erases type parameters to their bounds (or ) in the file. At runtime, and are both just — the JVM never sees vs . How it works. The compiler inserts casts where needed ( ) and may generate bridge methods for overriding with generics. Type info survives only in metadata used by reflection ( ), not in raw bytecode. Example. Pitfall. Erasure is why you get at runtime if you bypass generics with raw types: Interview angle. \"Why can't I do ?\" → No at runtime after erasure. Reified generics (Kotlin, C ) keep type info at runtime; Java chose erasure for migration cost."},{"id":"7-exception-handling","heading":"7. Exception Handling","level":2,"text":"Theory. Java separates recoverable failures (checked exceptions — caller must handle) from programming bugs (unchecked ). Errors ( ) indicate the JVM itself is in trouble — don't catch and swallow them."},{"id":"hierarchy","heading":"Hierarchy","level":3,"text":""},{"id":"try-with-resources-java-7","heading":"try-with-resources (Java 7+)","level":3,"text":""},{"id":"custom-exception-best-practices","heading":"Custom Exception Best Practices","level":3,"text":""},{"id":"exception-chaining","heading":"Exception Chaining","level":3,"text":""},{"id":"8-java-8-features","heading":"8. Java 8 Features","level":2,"text":"Interview note: Java 8 is the most asked version. For complete feature list + keyword deep dive → Section 17 and Section 20."},{"id":"81-lambda-expressions","heading":"8.1 Lambda Expressions","level":3,"text":""},{"id":"82-functional-interfaces","heading":"8.2 Functional Interfaces","level":3,"text":""},{"id":"83-stream-api-most-important","heading":"8.3 Stream API (MOST IMPORTANT)","level":3,"text":"Theory. A Stream is not a data structure — it's a lazy pipeline of operations over a source (collection, array, I/O). Think of it like an assembly line: intermediate steps (filter, map) don't run until a terminal operation (collect, count) pulls the trigger. This enables fusion (fewer passes) and short circuiting ( stops early). How it works. Intermediate ops return a new Stream and are lazy . The source is traversed once when a terminal op runs. transforms each element; maps then flattens (Stream of Streams → Stream). uses a Collector with a supplier, accumulator, and combiner (for parallel). Example — mental trace: Pitfall. Never put side effects in streams ( inside ) — order is undefined in parallel, and it's hard to debug. Don't use parallel streams for small lists or I/O — overhead dominates."},{"id":"84-optional","heading":"8.4 Optional","level":3,"text":"Theory. is a container that may or may not hold a value — it pushes null handling to the type system so callers explicitly handle absence. It's not meant for fields or method parameters in domain models (overuse adds ceremony); it's ideal for return types where \"no result\" is valid. How it works. throws if x is null; wraps null as empty. Terminal methods like , , extract the value or substitute. / chain without null checks — if empty, the chain short circuits to empty. Pitfall. Never do without checking — use with a meaningful message. Don't use Optional in serialized APIs or JPA entities."},{"id":"85-method-references","heading":"8.5 Method References","level":3,"text":""},{"id":"86-datetime-api","heading":"8.6 Date/Time API","level":3,"text":""},{"id":"87-collectors-cheat-sheet","heading":"8.7 Collectors — Cheat Sheet","level":3,"text":""},{"id":"9-java-921-features","heading":"9. Java 9–21 Features","level":2,"text":""},{"id":"java-9","heading":"Java 9","level":3,"text":""},{"id":"java-10","heading":"Java 10","level":3,"text":""},{"id":"java-11","heading":"Java 11","level":3,"text":""},{"id":"java-141516-records-switch-text-blocks","heading":"Java 14/15/16 — Records, Switch, Text Blocks","level":3,"text":""},{"id":"java-16-pattern-matching-instanceof","heading":"Java 16 — Pattern Matching instanceof","level":3,"text":""},{"id":"java-17-sealed-classes","heading":"Java 17 — Sealed Classes","level":3,"text":""},{"id":"java-21-virtual-threads-project-loom","heading":"Java 21 — Virtual Threads (Project Loom)","level":3,"text":"Theory. Platform threads map 1:1 to OS threads ( 1MB stack each) — creating 10,000 for 10,000 concurrent HTTP requests exhausts memory. Virtual threads are JVM managed, lightweight ( few KB), scheduled onto a small pool of carrier (platform) threads. When a virtual thread blocks on I/O, it unmounts from its carrier so another virtual thread can run — you write simple blocking code but get event loop like scalability. How it works. The JVM continuation mechanism saves the virtual thread's stack at blocking points ( , ). The carrier thread runs another ready virtual thread. On I/O completion, the virtual thread is rescheduled. Pinning occurs when blocking inside — the carrier can't be released; use instead. Example. 1 million virtual threads each sleeping 1 second — feasible. 1 million platform threads — ."},{"id":"java-21-sequenced-collections","heading":"Java 21 — Sequenced Collections","level":3,"text":""},{"id":"10-multithreading-concurrency","heading":"10. Multithreading & Concurrency","level":2,"text":""},{"id":"101-thread-lifecycle","heading":"10.1 Thread Lifecycle","level":3,"text":""},{"id":"102-creating-threads","heading":"10.2 Creating Threads","level":3,"text":""},{"id":"103-synchronized","heading":"10.3 synchronized","level":3,"text":"Theory. Every Java object has an intrinsic monitor lock (sometimes called a \"mutex\"). ensures mutual exclusion (only one thread in the critical section) and visibility (writes inside the block are visible to the next thread that acquires the same lock). It's reentrant — a thread holding the lock can enter another synchronized block on the same object without deadlocking itself. How it works. compiles to roughly: . Without synchronization, two threads can both LOAD the same value (e.g., 5), both ADD 1, both STORE 6 — one increment is lost . makes the read modify write atomic relative to other threads on that lock. Example. Two threads each call 1000 times on an unsynchronized counter → result is often less than 2000 . With , you get exactly 2000. Interview angle. on an instance method locks ; on a static method locks the object. Prefer smaller lock scopes (synchronized block on a dedicated lock object) over locking entire methods."},{"id":"104-volatile","heading":"10.4 volatile","level":3,"text":"For full depth — JMM, volatile vs Atomic vs synchronized, double checked locking, solving concurrency without synchronized → Section 16."},{"id":"105-waitnotify","heading":"10.5 wait/notify","level":3,"text":""},{"id":"106-executorservice-thread-pools","heading":"10.6 ExecutorService & Thread Pools","level":3,"text":"Theory. Creating a platform thread costs 1MB stack and an OS scheduler slot. A thread pool reuses a fixed set of workers, queuing excess work — the standard pattern for server side concurrency. Unbounded thread creation under load leads to . How it works. Submit a task → pool assigns an idle worker or queues the task. Workers loop: pull task from queue → execute → pull next. stops accepting new tasks; waits for the queue to drain. Pitfall. uses an unbounded queue — under sustained overload, the queue grows until heap OOM. Production code should use explicit with bounded queue + rejection policy."},{"id":"107-completablefuture","heading":"10.7 CompletableFuture","level":3,"text":"Theory. blocks and offers no composition. is a promise you can chain: transform results, combine independent calls, handle errors, and set timeouts — all without blocking the calling thread until you explicitly / . How it works. runs a task on an executor (default: shared — avoid for blocking I/O ). runs synchronously on the completing thread; dispatches to another pool. is flatMap — when the next step is itself async. waits for all; returns when the first completes. Example — scatter gather latency: Three downstream calls each take 200ms. Sequential = 600ms. Parallel with ≈ 200ms (slowest call)."},{"id":"108-locks-javautilconcurrentlocks","heading":"10.8 Locks (java.util.concurrent.locks)","level":3,"text":""},{"id":"109-synchronization-aids","heading":"10.9 Synchronization Aids","level":3,"text":""},{"id":"1010-atomic-classes","heading":"10.10 Atomic Classes","level":3,"text":""},{"id":"1011-deadlock-livelock-starvation","heading":"10.11 Deadlock, Livelock, Starvation","level":3,"text":""},{"id":"1012-forkjoinpool-parallel-streams","heading":"10.12 ForkJoinPool & Parallel Streams","level":3,"text":""},{"id":"1013-thread-safe-collections","heading":"10.13 Thread-Safe Collections","level":3,"text":""},{"id":"1014-virtual-threads-java-21-concurrency-view","heading":"10.14 Virtual Threads (Java 21) — Concurrency View","level":3,"text":"For full depth on all of the above, see (thread pools, CompletableFuture, virtual threads, real world patterns) and (GC, profiling, lock free)."},{"id":"11-jvm-internals-garbage-collection","heading":"11. JVM Internals & Garbage Collection","level":2,"text":""},{"id":"111-jvm-memory-areas","heading":"11.1 JVM Memory Areas","level":3,"text":"Theory. The JVM splits memory into thread private areas (each thread has its own stack — like a personal notepad for method calls) and shared areas (heap — the warehouse where all objects live). Understanding this explains (too many nested calls), (too many objects), and why local variables are thread safe by default. How it works. Stack (per thread): Each method call pushes a stack frame containing local primitives, references to heap objects, and operand stack for bytecode. When calls , a new frame is pushed; when returns, it's popped. References on the stack point to heap objects; the objects themselves are on the heap. Heap (shared): All objects and arrays. Young Gen (Eden + Survivors) for short lived objects; Old Gen for long lived. This is where GC runs. Metaspace (shared, native): Class metadata, static variables (since Java 8, replaced PermGen). Grows in native memory, not the Java heap."},{"id":"112-object-lifecycle-in-heap","heading":"11.2 Object Lifecycle in Heap","level":3,"text":"Theory. Generational GC exploits the observation that most objects die young (temporary strings, request DTOs, iterator objects). Splitting the heap into Young and Old lets the JVM run cheap, frequent collections on the Young gen and expensive, rare collections on Old. How it works — worked example. Suppose Eden is full and two Minor GCs have already happened: 1. → allocated in Eden . 2. Eden fills → Minor GC : live objects copied to Survivor S0 , age = 1; dead objects reclaimed. 3. More allocations in Eden → another Minor GC: S0 survivors with age 1 move to S1 (age 2); new Eden survivors go to S0. 4. After 15 survivor cycles (default ), object is promoted to Old Gen. 5. Old Gen full → Full GC (Stop The World) — all application threads pause while GC marks and compacts. Pitfall. Promoting too many objects too fast (e.g., massive caching) causes frequent Full GCs and multi second pauses. Tune / or fix object lifetime in code."},{"id":"113-garbage-collectors","heading":"11.3 Garbage Collectors","level":3,"text":"GC Algorithm STW Pauses Best For Serial Mark Sweep Compact Yes Single core, small heaps Parallel Parallel Mark Sweep Yes Multi core, throughput focus CMS Concurrent Mark Sweep Short Low latency (deprecated Java 9) G1 (default Java 9+) Region based Short, predictable Balanced throughput + latency ZGC (Java 15+) Concurrent, colored pointers < 1ms Very low latency, large heaps Shenandoah Concurrent compaction < 10ms Low latency"},{"id":"114-memory-leaks-in-java","heading":"11.4 Memory Leaks in Java","level":3,"text":"Common causes: 1. Static collections accumulating objects without removal 2. Unclosed resources (connections, streams) without try with resources 3. ThreadLocal not removed (especially in thread pools) 4. Event listeners never deregistered 5. Inner class holding reference to outer class unintentionally 6. Caches without eviction policy"},{"id":"12-design-patterns","heading":"12. Design Patterns","level":2,"text":""},{"id":"121-creational-patterns","heading":"12.1 Creational Patterns","level":3,"text":"Singleton (All Variants) Builder Pattern Factory & Abstract Factory"},{"id":"122-structural-patterns","heading":"12.2 Structural Patterns","level":3,"text":"Decorator Pattern Proxy Pattern"},{"id":"123-behavioral-patterns","heading":"12.3 Behavioral Patterns","level":3,"text":"Observer Pattern Strategy Pattern Template Method Pattern"},{"id":"13-deep-dive-hashmap-internals-java-7-vs-8","heading":"13. DEEP DIVE: HashMap Internals (Java 7 vs 8+)","level":2,"text":""},{"id":"131-why-hashmap-exists-the-problem-it-solves","heading":"13.1 Why HashMap Exists — The Problem It Solves","level":3,"text":"Array gives O(1) access by index but only when you know the index. TreeMap gives O(log n) sorted access. HashMap gives average O(1) key→value lookup by computing an index from the key's hash code. The trade off: no ordering guarantee, hash collisions, and you must implement + correctly for custom keys."},{"id":"132-core-data-structures-java-8-source-level-mental-model","heading":"13.2 Core Data Structures (Java 8+ source-level mental model)","level":3,"text":"Key fields explained: — array of buckets (not the entries themselves; each slot is head of a list or tree) — how full before resize (0.75 = resize at 75% capacity = 12 entries when capacity is 16) — ; when , resize happens Power of 2 capacity enables instead of slow"},{"id":"133-hash-computation-why-xor-with-upper-bits","heading":"13.3 Hash Computation — Why XOR with Upper Bits?","level":3,"text":"Why? When capacity is small (e.g. 16), only lower 4 bits of hash determine bucket index. Without spreading high bits, keys with similar patterns (e.g. keys 0,16,32...) all land in the same bucket → linked list → O(n). XOR ing upper 16 bits into lower bits distributes keys more evenly across buckets even when capacity is small. Null key: Always goes to bucket index 0. Only one null key allowed (Map contract)."},{"id":"134-put-operation-step-by-step-java-8","heading":"13.4 PUT Operation — Step by Step (Java 8+)","level":3,"text":""},{"id":"135-resize-operation-what-actually-happens","heading":"13.5 RESIZE Operation — What Actually Happens","level":3,"text":"When : 1. Create new array of double the capacity (16 → 32 → 64...) 2. Recompute index for every entry (O(n) — expensive!) 3. Java 8 optimization: entries either stay at same index OR move by exactly offset (because capacity doubled, one more bit participates in index calculation) 4. During resize, Java 8 splits each bucket into lo and hi chains based on whether the new bit is 0 or 1 Interview trap: \"Resize happens at 75% full\" — technically when where . With capacity 16, threshold is 12, so resize on 13th entry."},{"id":"136-java-7-vs-java-8-critical-differences","heading":"13.6 Java 7 vs Java 8 — Critical Differences","level":3,"text":"Aspect Java 7 Java 8+ Collision structure Singly linked list only Linked list → Red Black Tree Insert on collision Head insert (new node first) Tail insert (new node last) Worst case get/put O(n) — long chains O(log n) — tree when chain ≥ 8 Resize Rehash all entries Optimized lo/hi split Hash function 4 shifts + XOR (more complex) Simpler: Tree threshold N/A Treeify at 8, untreeify at 6 Min capacity for tree N/A Must have capacity ≥ 64 to treeify Why Java 7 used head insert: Historically believed to improve locality for recently added entries (often accessed soon). Java 8 switched to tail insert because head insert could create infinite loops during concurrent resize in multi threaded misuse (HashMap is NOT thread safe). Tail insert + improved resize fixed the famous JDK 7 HashMap infinite loop bug. Why NOT treeify when capacity < 64? Resizing is cheaper than maintaining trees on small maps. If you have 8 collisions in a bucket but total capacity is only 16, it's better to just resize and spread entries than pay tree overhead."},{"id":"137-get-operation-step-by-step","heading":"13.7 GET Operation — Step by Step","level":3,"text":"Important: HashMap checks first (fast int compare), then (potentially expensive). Custom keys must ensure: if then ."},{"id":"138-hashmap-custom-key-interview-output-puzzle","heading":"13.8 HashMap Custom Key — Interview Output Puzzle","level":3,"text":"Case Output Why No override compares references; different objects Only equals Different → different bucket; never finds key Both correct Same bucket, matches Mutable key trap: Rule: Never mutate fields used in / while object is in a HashMap/HashSet."},{"id":"139-hashmap-vs-hashtable-vs-concurrenthashmap-vs-collectionssynchronizedmap","heading":"13.9 HashMap vs Hashtable vs ConcurrentHashMap vs Collections.synchronizedMap","level":3,"text":"HashMap Hashtable synchronizedMap ConcurrentHashMap Thread safe No Yes (every method synchronized) Yes (mutex on each op) Yes (fine grained) Null key/value 1 null key, many null values Not allowed Follows backing map Not allowed Iterator Fail fast Fail fast Fail fast Weakly consistent Performance (reads) Fastest Slow Slow Fast (no read lock) Performance (writes) Fastest Slow Slow Good (bucket level)"},{"id":"14-deep-dive-red-black-tree-in-hashmap","heading":"14. DEEP DIVE: Red-Black Tree in HashMap","level":2,"text":""},{"id":"141-why-a-tree-inside-hashmap","heading":"14.1 Why a Tree Inside HashMap?","level":3,"text":"When many keys hash to the same bucket (bad , adversarial input, or hash collision attack), linked list traversal becomes O(n). Java 8 converts chains of length ≥ 8 to a Red Black Tree for O(log n) worst case — but only when total map capacity ≥ 64."},{"id":"142-what-is-a-red-black-tree","heading":"14.2 What is a Red-Black Tree?","level":3,"text":"A self balancing Binary Search Tree with these rules: 1. Every node is RED or BLACK 2. Root is BLACK 3. All leaves (NIL) are BLACK 4. RED node cannot have RED child (no two consecutive reds) 5. Every path from node to descendant leaf has same number of BLACK nodes Result: Height ≤ 2 × log₂(n+1) → guaranteed O(log n) search, insert, delete."},{"id":"143-treenode-in-hashmap","heading":"14.3 TreeNode in HashMap","level":3,"text":"TreeNode also maintains pointer — so converting tree → list (untreeify) is O(n) without re sorting."},{"id":"144-treeify-untreeify-conditions","heading":"14.4 Treeify / Untreeify Conditions","level":3,"text":"Scenario walkthrough:"},{"id":"145-how-tree-search-works-in-get","heading":"14.5 How Tree Search Works in get()","level":3,"text":"Critical: TreeNode ordering uses if keys implement it, otherwise uses (class name comparison). Mixed key types in same bucket can cause on treeify."},{"id":"146-interview-questions-on-rb-tree-in-hashmap","heading":"14.6 Interview Questions on RB Tree in HashMap","level":3,"text":"Q: Why Red Black Tree and not AVL Tree? A: AVL is stricter balanced (faster lookups) but more rotations on insert/delete. HashMap buckets are small; RB tree's looser balance = fewer rotations = better insert performance. Q: What is the worst case time complexity of HashMap get() in Java 8? A: O(log n) per bucket when treeified. Average remains O(1) assuming good hash distribution. Q: Can HashMap degenerate to O(n) even in Java 8? A: Yes — if capacity stays < 64 and all keys collide, it stays a linked list."},{"id":"15-deep-dive-concurrenthashmap-complete","heading":"15. DEEP DIVE: ConcurrentHashMap (Complete)","level":2,"text":""},{"id":"150-what-concurrenthashmap-is-and-fine-grained-locking","heading":"15.0 What ConcurrentHashMap Is (and Fine-Grained Locking)","level":3,"text":"What it is. (CHM) is a thread safe implementation of in . It lets many threads read and write concurrently without you writing any synchronization , and without the performance penalty of locking the entire map on every operation. Why it exists. You have three options for a map shared across threads: Option Thread safe? Problem No Data corruption / infinite loops under concurrent writes / Yes Locks the whole map on every / → threads queue up, no real parallelism Yes None of the above — high concurrency The first is unsafe; the second is safe but slow because it's a single lock for the entire structure. solves both. How it works (modern Java 8+), in short: The map is an array of buckets (like ). Reads ( ) are lock free — they acquire no lock at all ( fields guarantee threads see the latest values). Writes ( ) lock only the single bucket being modified, via on that bucket's head node. If the target bucket is empty, it inserts with a CAS (compare and swap) — no lock needed at all. So two threads writing to different buckets never block each other; they run truly in parallel. It also forbids keys/values, so a returning always means \"absent\" (never \"present but null\") — important reasoning in concurrent code. Fine grained vs coarse grained locking This is the core idea that makes CHM fast. Granularity = how much you lock at once. Coarse grained locking = one big lock for the whole data structure. Example: / — every operation locks the entire map. Analogy: a house with one bathroom and one key — only one person can use any of the plumbing at a time. Simple, but everyone waits in line. Fine grained locking = many small locks, each guarding a small piece. Example: locks only the individual bucket being written. Analogy: a house where each bathroom has its own key — many people use different bathrooms at once; they only wait if they want the same one. The win: with fine grained locking, contention happens only when two threads touch the same bucket , which is rare with a goo"},{"id":"151-the-problem-concurrenthashmap-solves","heading":"15.1 The Problem ConcurrentHashMap Solves","level":3,"text":"and lock the entire map for every read and write. Under high concurrency, threads queue up on one lock → throughput collapses. (CHM) allows concurrent reads and fine grained writes."},{"id":"152-java-7-segment-locking-lock-striping","heading":"15.2 Java 7 — Segment Locking (Lock Striping)","level":3,"text":"Segment count = default 16 → up to 16 threads can write to different segments simultaneously constructor param sets segment count (rounded to power of 2) Read operations: mostly lock free (volatile reads on HashEntry chain) Write operations: lock only the relevant segment Limitation: Hot segment (many keys hashing to same segment) becomes bottleneck."},{"id":"153-java-8-node-array-cas-synchronized-bucket-head","heading":"15.3 Java 8+ — Node Array + CAS + synchronized Bucket Head","level":3,"text":"Java 8 completely rewrote CHM. No more segments. Structure mirrors HashMap but with concurrency primitives: Key insight: Lock granularity = single bucket head , not entire map or segment."},{"id":"154-cas-compare-and-swap-explained","heading":"15.4 CAS (Compare-And-Swap) Explained","level":3,"text":"Theory. CAS is a hardware atomic instruction : \"If memory at location X equals expected, set it to newValue; else fail.\" No lock, no kernel call — threads spin retry on contention. This is the foundation of lock free counters and ConcurrentHashMap's empty bucket inserts. How it works. On x86 this maps to . The CPU guarantees the read compare write is atomic for that memory word. Under high contention, many threads retry → CAS spin storms . mitigates by spreading updates across cells. Example — lost update vs CAS: CHM uses CAS for: initializing table, inserting into empty bucket, updating size counters ( array — similar to )."},{"id":"155-read-operations-why-no-lock","heading":"15.5 Read Operations — Why No Lock?","level":3,"text":"has no block. and are → visibility guaranteed. Iterator is weakly consistent — never throws ."},{"id":"156-concurrenthashmap-atomic-operations","heading":"15.6 ConcurrentHashMap Atomic Operations","level":3,"text":""},{"id":"157-concurrenthashmap-vs-synchronized-hashmap","heading":"15.7 ConcurrentHashMap vs synchronized HashMap","level":3,"text":"synchronizedMap ConcurrentHashMap Lock scope Entire map Per bucket / CAS Reads Locked Lock free Iterator Fail fast, needs external sync Weakly consistent Null key/value Allowed (HashMap) Not allowed size() Exact Approximate under concurrency Why no nulls in CHM? (and why can allow them) returning is ambiguous : it could mean \"key absent\" or \"key present, value is null.\" This ambiguity exists in too — the difference is whether you can reliably resolve it. (single threaded) — you disambiguate with a second call: Because a isn't shared across threads, nothing changes between and , so the two step check is trustworthy. That's why allows one null key and null values. (multi threaded) — that same two step check is a race condition . Between your returning and your , another thread could insert or remove that key, so no longer reflects the state at the time of your . There is no atomic way to distinguish \"absent\" from \"null value\" in a concurrent map. Rather than ship an unresolvable ambiguity, the designers forbid — so always means \"absent,\" unambiguously. Interview one liner. \" allows a null value because a single thread can safely disambiguate it with . In that follow up check is racy — the map can change between the two calls — so null is banned to keep meaning exactly one thing: absent.\""},{"id":"158-concurrenthashmap-resize-java-8","heading":"15.8 ConcurrentHashMap Resize (Java 8)","level":3,"text":"Multiple threads can help resize simultaneously. Uses field as coordination flag. placed in old buckets points to new table while migration in progress."},{"id":"16-deep-dive-volatile-vs-atomic-variables","heading":"16. DEEP DIVE: volatile vs Atomic Variables","level":2,"text":""},{"id":"161-the-java-memory-model-jmm","heading":"16.1 The Java Memory Model (JMM)","level":3,"text":"Theory. Modern CPUs and compilers reorder and cache memory accesses for speed. Without rules, Thread B might never see Thread A's write, or see a partially constructed object. The JMM defines happens before — a partial ordering guarantee. If action A happens before B, then B is guaranteed to see all of A's effects. How it works. Each thread may keep values in CPU cache or registers. A memory barrier (inserted by , lock acquire/release, or ) forces flush/invalidate so other threads see fresh values. The compiler cannot reorder instructions across a happens before edge. Key happens before rules: Program order within one thread. happens before the next on the same monitor. Write to happens before every subsequent read of that variable. happens before any action in the started thread. All actions in thread T happen before returns successfully. Example — visibility bug without sync: Interview angle. \"volatile vs synchronized?\" → volatile = visibility + ordering, no mutual exclusion. synchronized = visibility + mutual exclusion. Neither makes safe alone — use or a lock."},{"id":"162-volatile-visibility-not-atomicity","heading":"16.2 volatile — Visibility, NOT Atomicity","level":3,"text":"volatile guarantees: visibility, ordering (memory barrier), happens before. volatile does NOT guarantee: atomicity of compound operations like ."},{"id":"163-atomic-variables-visibility-and-atomicity","heading":"16.3 Atomic Variables — Visibility AND Atomicity","level":3,"text":""},{"id":"164-volatile-vs-synchronized-vs-atomic","heading":"16.4 volatile vs synchronized vs Atomic","level":3,"text":"Feature volatile synchronized Atomic Visibility Yes Yes Yes Atomicity (compound) No Yes Yes (single var) Mutual exclusion No Yes No (CAS retry) Use case flags critical sections counters"},{"id":"165-double-checked-locking-why-volatile-is-required","heading":"16.5 Double-Checked Locking — Why volatile is Required","level":3,"text":"Theory. Double checked locking tries to avoid synchronizing on every call after initialization. Without , the JVM may reorder instructions so another thread sees a non null reference to an incompletely constructed object. How it works. is not atomic: (1) allocate memory, (2) run constructor, (3) assign reference. Another thread can see step 3 before step 2 completes. on prevents this reordering. Example — broken without volatile: Without , another thread may see partially constructed object (reference assigned before constructor finishes)."},{"id":"166-solving-concurrency-without-synchronized","heading":"16.6 Solving Concurrency Without synchronized","level":3,"text":"1. — CHM, BlockingQueue, Semaphore 2. Atomic classes — lock free counters/state 3. , 4. Immutable objects — no shared mutable state 5. — thread confinement 6. Database locking — , optimistic"},{"id":"17-deep-dive-default-keyword-in-interfaces-java-8","heading":"17. DEEP DIVE: default Keyword in Interfaces (Java 8)","level":2,"text":""},{"id":"171-why-default-methods-were-added","heading":"17.1 Why default Methods Were Added","level":3,"text":"Before Java 8, adding a method to an interface broke all implementing classes . With millions of lines using , , etc., this was impossible. methods allow adding new behavior without breaking existing implementations."},{"id":"172-syntax-and-rules","heading":"17.2 Syntax and Rules","level":3,"text":""},{"id":"173-diamond-problem-resolution","heading":"17.3 Diamond Problem Resolution","level":3,"text":"Resolution order: Class method wins → subinterface default → compile error if ambiguous."},{"id":"174-real-jdk-examples","heading":"17.4 Real JDK Examples","level":3,"text":""},{"id":"175-polymorphism-with-default-interview-puzzle","heading":"17.5 Polymorphism with default — Interview Puzzle","level":3,"text":""},{"id":"18-real-interview-code-puzzles-traps","heading":"18. Real Interview Code Puzzles & Traps","level":2,"text":""},{"id":"181-main-method-signature","heading":"18.1 main() Method Signature","level":3,"text":"Keyword Purpose JVM (outside class) must access entry point Runs without creating object Returns nothing Fixed entry point name Command line arguments"},{"id":"182-string-pool-deep-explanation","heading":"18.2 String Pool — Deep Explanation","level":3,"text":"Why bypasses pool: always creates separate heap object even if literal exists in pool. User input ( ) creates heap strings, not pool literals. : moves/returns pooled reference → is true."},{"id":"183-integer-caching","heading":"18.3 Integer Caching","level":3,"text":""},{"id":"184-arraysaslist-trap","heading":"18.4 Arrays.asList() Trap","level":3,"text":""},{"id":"185-error-vs-exception-vs-runtimeexception","heading":"18.5 Error vs Exception vs RuntimeException","level":3,"text":"Error Checked Exception Unchecked (RuntimeException) Examples OOM, StackOverflow IOException, SQLException NPE, IllegalArgumentException Catch? Should NOT Must handle/declare Optional Cause JVM/system External conditions Programming bugs"},{"id":"186-marker-interface","heading":"18.6 Marker Interface","level":3,"text":""},{"id":"187-fail-fast-vs-fail-safe","heading":"18.7 Fail-Fast vs Fail-Safe","level":3,"text":"Fail Fast (ArrayList, HashMap): check → Fail Safe (CHM, CopyOnWriteArrayList): iterator on snapshot, no exception"},{"id":"19-coding-problems-with-solutions","heading":"19. Coding Problems with Solutions","level":2,"text":""},{"id":"191-word-frequency-count","heading":"19.1 Word Frequency Count","level":3,"text":""},{"id":"192-group-anagrams","heading":"19.2 Group Anagrams","level":3,"text":""},{"id":"193-best-time-to-buy-and-sell-stock","heading":"19.3 Best Time to Buy and Sell Stock","level":3,"text":""},{"id":"194-longest-substring-with-k-distinct-characters","heading":"19.4 Longest Substring with K Distinct Characters","level":3,"text":""},{"id":"195-valid-sudoku","heading":"19.5 Valid Sudoku","level":3,"text":""},{"id":"196-stock-tracker-priorityqueue","heading":"19.6 Stock Tracker — PriorityQueue","level":3,"text":""},{"id":"197-booking-system-semaphore-db-lock","heading":"19.7 Booking System — Semaphore + DB Lock","level":3,"text":"Use to limit concurrent requests + for row level seat locking. See Section 16.6."},{"id":"20-tough-interview-qa-bank","heading":"20. Tough Interview Q&A Bank","level":2,"text":""},{"id":"core-java","heading":"Core Java","level":3,"text":"Q: What is OOPS? A: Encapsulation, Inheritance, Polymorphism, Abstraction — code reuse, security, maintainability via objects. Q: Array vs ArrayList vs LinkedList? Array ArrayList LinkedList Structure Fixed contiguous Dynamic array Doubly linked nodes get(i) O(1) O(1) O(n) add(end) N/A if full O(1) amortized O(1) add(middle) O(n) O(n) O(n) find + O(1) link Cache locality Best Good Poor Q: Comparable vs Comparator? A: Comparable = natural order inside class ( ). Comparator = external flexible ordering ( ), multiple sort strategies."},{"id":"collections-concurrency","heading":"Collections & Concurrency","level":3,"text":"Q: HashMap all versions? → Section 13 Q: Red Black Tree in HashMap? → Section 14 Q: ConcurrentHashMap deep dive? → Section 15 Q: volatile vs Atomic? → Section 16 Q: default keyword Java 8? → Section 17 Q: Arrays.asList() remove? → UnsupportedOperationException (fixed size) Q: Resource allocation without synchronized? → CHM, Semaphore, Atomic, ReentrantLock, immutable objects, DB locks"},{"id":"java-8-17","heading":"Java 8 / 17","level":3,"text":"Q: All Java 8 features? 1. Lambda expressions 2. Functional interfaces 3. Stream API 4. Optional 5. Method references 6. default methods in interfaces 7. Static methods in interfaces 8. java.time API 9. CompletableFuture 10. Parallel array sorting 11. StringJoiner, Base64 Q: Java 8 vs 17? Records, Sealed classes, Pattern matching, Text blocks, Switch expressions, ZGC production, HttpClient (11), var (10), modules (9)."},{"id":"spring-commonly-asked-with-java","heading":"Spring (commonly asked with Java)","level":3,"text":"Q: @Valid vs @Validated? = JSR 380 standard. = Spring, supports validation groups. Q: @Transactional propagation? REQUIRED (default), REQUIRES NEW, NESTED, SUPPORTS, NOT SUPPORTED, MANDATORY, NEVER. Q: ApplicationContext vs BeanFactory? ApplicationContext = superset with events, i18n, AOP, eager init. Q: Circular dependency fix? Redesign, , setter injection, . Q: JPA vs CrudRepository? CrudRepository = basic CRUD. JpaRepository extends with flush, paging, batch delete. Q: Bean lifecycle? Instantiate → inject → → use → ."},{"id":"architecture","heading":"Architecture","level":3,"text":"Q: Monolithic vs Microservices? Single deploy vs independent services; trade off complexity vs scaling isolation. Q: Saga orchestration vs choreography? Orchestration = central coordinator. Choreography = event driven, decentralized. Q: gRPC vs REST? gRPC = HTTP/2, Protobuf, typed, streaming, fast inter service. REST = HTTP/JSON, human readable, browser friendly."},{"id":"dsa-in-java-rounds","heading":"DSA in Java Rounds","level":3,"text":"Problem Technique Structure Valid Sudoku HashSet row/col/box HashSet Buy/sell stock Single pass min variables K distinct substring Sliding window HashMap Group anagrams Sorted key HashMap Word frequency groupingBy Stream Multi event stock Greedy + heap PriorityQueue Booking concurrency Semaphore + DB lock Semaphore"},{"id":"hibernate-orm","heading":"Hibernate / ORM","level":3,"text":"Q: What is Hibernate? A: ORM framework mapping Java classes to DB tables, objects to rows. Features: automatic mapping, HQL, caching, lazy loading, transaction management. Reduces JDBC boilerplate, database independence."},{"id":"interview-qa-java","heading":"INTERVIEW Q&A — JAVA","level":2,"text":"Q: Why is String immutable in Java? A: 1) String Pool requires immutability for safe sharing. 2) Thread safe without synchronization. 3) Safe for use as HashMap keys (hashCode never changes). 4) Security — passwords and file paths can't be altered. Q: Difference between and ? A: compares references (memory address). compares content. For , compares characters. Always use for objects. Q: What happens when HashMap reaches 75% capacity? A: Rehashing occurs. A new array of double the capacity is created, all entries are re computed (hash, index) and moved. This is O(n) operation. That's why initial capacity matters for performance critical code. For full internals see Section 13. Q: Why override when overriding ? A: HashMap and HashSet use to find the bucket. If two equal objects have different hashCodes, they land in different buckets and returns null even though the key \"exists\". Q: ConcurrentHashMap vs synchronized HashMap? A: locks the entire map for every operation. uses CAS and segment/bucket level locking for much better concurrency. Also, doesn't allow null keys/values. For full depth see Section 15. Q: Why does ConcurrentHashMap forbid null keys/values when HashMap allows them? A: returning null is ambiguous — \"key absent\" or \"value is null\". In (single threaded) you resolve it with , and nothing changes between the two calls, so it's safe — that's why HashMap allows one null key and null values. In that follow up is a race: another thread can insert/remove the key between the and the check, so there's no atomic way to disambiguate. Null is banned so always means exactly \"absent\". Q: What is Compare And Swap (CAS)? A: CAS is an atomic CPU instruction (e.g., ) that takes a memory location, an expected old value, and a new value: it sets the location to the new value only if it currently equals the expected value, otherwise it fails — all in one indivisible step. It's the foundation of lock free concurrency (used by / and by for empty bucket inserts): you read, c"},{"id":"22-additional-core-java-interview-qa","heading":"22. Additional Core Java Interview Q&A","level":2,"text":""},{"id":"multithreading-concurrency","heading":"Multithreading & Concurrency","level":3,"text":"Q: Difference between , , and ? Class Purpose Wait for notification on a lock Pause current thread for N ms Wait for another thread to finish Releases lock? Yes (must be in ) No No Woken by / Time expires Target thread dies Use case Producer consumer, coordination Delay, backoff Main waits for worker Q: Explain vs ? A: See Section 16. = visibility + ordering only; no mutual exclusion; still unsafe. = visibility + mutual exclusion; only one thread in block; can cause deadlock if misused. Use volatile for flags; synchronized or for compound operations. Q: Explain ThreadLocal with a use case? A: Each thread gets its own copy of a variable — no sharing, no locking. Use cases: User context in web apps, (not thread safe), transaction IDs, per request logging MDC. Q: Scenario — Payment gateway: multiple transactions update account balance in parallel. How to ensure thread safety? A: Layered approach: 1. optimistic locking on entity — retry on 2. Pessimistic lock — or 3. / database — atomic SQL, no read modify write race 4. Serialize per account — partition Kafka by so one consumer per account 5. Idempotency keys — prevent duplicate charges on retry Never: without a lock."},{"id":"jvm-memory","heading":"JVM & Memory","level":3,"text":"Q: Explain class loading mechanism (Bootstrap, Extension, Application classloader)? Delegation model: Child asks parent first. Parent loads if it can; else child tries. Prevents reloading from your JAR. Custom classloader: Spring, Tomcat, OSGi use custom loaders for hot deploy / isolation. Q: How does Garbage Collection work in the JVM? A: See Section 11. Summary: objects created in Eden → Minor GC moves survivors to Survivor → promoted to Old Gen after age threshold → Major/Full GC when Old Gen full. Collectors: G1 (default Java 9+), ZGC, Shenandoah for low latency. GC roots: stack references, static fields, JNI refs. Unreachable objects are collected. Tune with , , ."},{"id":"object-exceptions","heading":"Object & Exceptions","level":3,"text":"Q: Deep copy vs Shallow copy? Shallow copy Deep copy Primitive fields Copied Copied Object references Same object shared New objects created Risk Mutating nested object affects both Independent copies Serialization ( ) can deep copy if all nested objects are serializable. Libraries: Apache Commons , JSON round trip. Q: Difference between Checked vs Unchecked exceptions? Checked Unchecked Extends (not RuntimeException) Compile time Must catch or declare Optional Examples , , custom Use when Recoverable external failure Programming bug / business rule violation"},{"id":"object-methods-already-in-qa-expanded","heading":"Object Methods (already in Q&A — expanded)","level":3,"text":"Q: How does differ from ? Why override with ? A: compares references. compares content (if overridden). hashCode contract: equal objects → same hashCode. Needed for / — different hashCode → different bucket → returns null. See Sections 13, 18, and INTERVIEW Q&A above. Q: How does ConcurrentHashMap achieve thread safety internally? A: See Section 15. Java 8+: no full map lock; CAS for empty buckets; on bucket head for collisions; reads; weakly consistent iterator."}],"qa":[{"q":"Why Red-Black Tree and not AVL Tree?","a":"AVL is stricter balanced (faster lookups) but more rotations on insert/delete. HashMap buckets are small; RB tree's looser balance = fewer rotations = better insert performance."},{"q":"What is the worst-case time complexity of HashMap get() in Java 8?","a":"O(log n) per bucket when treeified. Average remains O(1) assuming good hash distribution."},{"q":"Can HashMap degenerate to O(n) even in Java 8?","a":"Yes — if capacity stays < 64 and all keys collide, it stays a linked list."},{"q":"What is OOPS?","a":"Encapsulation, Inheritance, Polymorphism, Abstraction — code reuse, security, maintainability via objects."},{"q":"Comparable vs Comparator?","a":"Comparable = natural order inside class (`compareTo`). Comparator = external flexible ordering (`compare`), multiple sort strategies."},{"q":"All Java 8 features?","a":"1. Lambda expressions  2. Functional interfaces  3. Stream API  4. Optional\n5. Method references  6. **default methods in interfaces**  7. Static methods in interfaces\n8. java.time API  9. CompletableFuture  10. Parallel array sorting  11. StringJoiner, Base64"},{"q":"Java 8 vs 17?","a":"Records, Sealed classes, Pattern matching, Text blocks, Switch expressions, ZGC production, HttpClient (11), var (10), modules (9)."},{"q":"@Valid vs @Validated?","a":"`@Valid` = JSR-380 standard. `@Validated` = Spring, supports validation groups."},{"q":"@Transactional propagation?","a":"REQUIRED (default), REQUIRES_NEW, NESTED, SUPPORTS, NOT_SUPPORTED, MANDATORY, NEVER."},{"q":"ApplicationContext vs BeanFactory?","a":"ApplicationContext = superset with events, i18n, AOP, eager init."},{"q":"Circular dependency fix?","a":"Redesign, `@Lazy`, setter injection, `ObjectProvider`."},{"q":"JPA vs CrudRepository?","a":"CrudRepository = basic CRUD. JpaRepository extends with flush, paging, batch delete."},{"q":"Bean lifecycle?","a":"Instantiate → inject → `@PostConstruct` → use → `@PreDestroy`."},{"q":"Monolithic vs Microservices?","a":"Single deploy vs independent services; trade-off complexity vs scaling isolation."},{"q":"Saga orchestration vs choreography?","a":"Orchestration = central coordinator. Choreography = event-driven, decentralized."},{"q":"gRPC vs REST?","a":"gRPC = HTTP/2, Protobuf, typed, streaming, fast inter-service. REST = HTTP/JSON, human-readable, browser-friendly."},{"q":"What is Hibernate?","a":"ORM framework mapping Java classes to DB tables, objects to rows. Features: automatic mapping, HQL, caching, lazy loading, transaction management. Reduces JDBC boilerplate, database independence."},{"q":"Why is String immutable in Java?","a":"1) String Pool requires immutability for safe sharing. 2) Thread-safe without synchronization. 3) Safe for use as HashMap keys (hashCode never changes). 4) Security — passwords and file paths can't be altered."},{"q":"Difference between `==` and `equals()`?","a":"`==` compares references (memory address). `equals()` compares content. For `String`, `equals()` compares characters. Always use `equals()` for objects."},{"q":"What happens when HashMap reaches 75% capacity?","a":"Rehashing occurs. A new array of double the capacity is created, all entries are re-computed (hash, index) and moved. This is O(n) operation. That's why initial capacity matters for performance-critical code. For full internals see [Section 13](#13-deep-dive-hashmap-internals-java-7-vs-8)."},{"q":"Why override `hashCode()` when overriding `equals()`?","a":"HashMap and HashSet use `hashCode()` to find the bucket. If two equal objects have different hashCodes, they land in different buckets and `get()` returns null even though the key \"exists\"."},{"q":"ConcurrentHashMap vs synchronized HashMap?","a":"`Collections.synchronizedMap()` locks the entire map for every operation. `ConcurrentHashMap` uses CAS and segment/bucket-level locking for much better concurrency. Also, `ConcurrentHashMap` doesn't allow null keys/values. For full depth see [Section 15](#15-deep-dive-concurrenthashmap-complete)."},{"q":"Why does ConcurrentHashMap forbid null keys/values when HashMap allows them?","a":"`get()` returning null is ambiguous — \"key absent\" or \"value is null\". In `HashMap` (single-threaded) you resolve it with `containsKey()`, and nothing changes between the two calls, so it's safe — that's why HashMap allows one null key and null values. In `ConcurrentHashMap` that follow-up `containsKey()` is a race: another thread can insert/remove the key between the `get()` and the check, so there's no atomic way to disambiguate. Null is banned so `get()==null` always means exactly \"absent\"."},{"q":"What is Compare-And-Swap (CAS)?","a":"CAS is an atomic CPU instruction (e.g., `CMPXCHG`) that takes a memory location, an expected old value, and a new value: it sets the location to the new value only if it currently equals the expected value, otherwise it fails — all in one indivisible step. It's the foundation of lock-free concurrency (used by `AtomicInteger`/`AtomicLong` and by `ConcurrentHashMap` for empty-bucket inserts): you read, compute, then CAS, retrying in a loop if it fails. Benefits: no locks, no deadlock. Downsides: the ABA problem (fix with `AtomicStampedReference`) and CPU spin under high contention (mitigated by `LongAdder`)."},{"q":"What is the difference between `Callable` and `Runnable`?","a":"`Runnable.run()` returns void and cannot throw checked exceptions. `Callable.call()` returns a value of generic type and can throw checked exceptions. `Callable` is used with `ExecutorService.submit()` and returns a `Future`."},{"q":"Explain `volatile`.","a":"`volatile` ensures memory visibility — writes to a volatile variable are immediately visible to all threads. It prevents CPU cache and compiler reordering. It does NOT ensure atomicity. Use `AtomicInteger` for atomic compound operations. For full depth see [Section 16](#16-deep-dive-volatile-vs-atomic-variables)."},{"q":"What are virtual threads?","a":"Introduced in Java 21 (Project Loom). Lightweight threads managed by the JVM, not OS. Platform threads have ~1MB stack; virtual threads have ~few KB. You can create millions of virtual threads. During blocking I/O, the JVM \"parks\" the virtual thread and uses the carrier thread for other work. Ideal for I/O-bound, not CPU-bound tasks."},{"q":"How does G1GC work?","a":"G1 divides the heap into equal-sized regions (1–32MB). Regions are dynamically assigned to Eden, Survivor, or Old. G1 tracks \"liveness\" of each region and collects the most garbage-dense ones first (hence \"Garbage First\"). This gives predictable, configurable pause times (`-XX:MaxGCPauseMillis=200`)."},{"q":"Explain `volatile` vs `synchronized`?","a":"See [Section 16](#16-deep-dive-volatile-vs-atomic-variables). **`volatile`** = visibility + ordering only; no mutual exclusion; `i++` still unsafe. **`synchronized`** = visibility + mutual exclusion; only one thread in block; can cause deadlock if misused. Use volatile for flags; synchronized or `Atomic*` for compound operations."},{"q":"Explain ThreadLocal with a use case?","a":"Each thread gets its **own copy** of a variable — no sharing, no locking."},{"q":"How does Garbage Collection work in the JVM?","a":"See Section 11. Summary: objects created in Eden → Minor GC moves survivors to Survivor → promoted to Old Gen after age threshold → Major/Full GC when Old Gen full. Collectors: G1 (default Java 9+), ZGC, Shenandoah for low latency. GC roots: stack references, static fields, JNI refs. Unreachable objects are collected. Tune with `-Xms`, `-Xmx`, `-XX:+UseG1GC`."},{"q":"How does `equals()` differ from `==`? Why override `hashCode()` with `equals()`?","a":"`==` compares references. `equals()` compares content (if overridden). **hashCode contract:** equal objects → same hashCode. Needed for `HashMap`/`HashSet` — different hashCode → different bucket → `get()` returns null. See Sections 13, 18, and INTERVIEW Q&A above."},{"q":"How does ConcurrentHashMap achieve thread safety internally?","a":"See [Section 15](#15-deep-dive-concurrenthashmap-complete). Java 8+: no full-map lock; CAS for empty buckets; `synchronized` on bucket head for collisions; `volatile` reads; weakly consistent iterator."}]},"02_SpringBoot.md":{"title":"Spring Boot","trackId":"spring-stack","trackTitle":"Spring Boot","sections":[{"id":"spring-boot-complete-interview-guide","heading":"Spring Boot — Complete Interview Guide","level":1,"text":""},{"id":"core-boot-rest-jpa-security-microservices","heading":"Core → Boot → REST → JPA → Security → Microservices","level":3,"text":""},{"id":"1-spring-core-ioc-and-di","heading":"1. Spring Core — IoC and DI","level":2,"text":"In simple terms. Without Spring, you write everywhere — you control object creation. With Spring, you say \"I need a PaymentService\" and Spring creates it and hands it to you . That's the whole idea. IoC (Inversion of Control) = you stop creating objects; Spring does it for you. DI (Dependency Injection) = how Spring gives them to you — through the constructor (best), a setter, or a field. Kitchen analogy. Old way: every chef buys their own ingredients. Spring way: a central pantry delivers ingredients to each station — chefs just cook. Bean = any object Spring manages (your , , etc.). Spring keeps them in a container ( ) and wires them together at startup. Why it matters for interviews: IoC is the idea ; DI is the mechanism . Spring uses DI to achieve IoC. Common mistake: Still writing inside a class — you skip Spring entirely (no transactions, no easy mocking in tests)."},{"id":"inversion-of-control-ioc","heading":"Inversion of Control (IoC)","level":3,"text":""},{"id":"dependency-injection-types","heading":"Dependency Injection Types","level":3,"text":"How it works. Spring supports three injection styles. Constructor injection runs at bean creation time — dependencies are , set once, visible in the constructor signature (great for tests: ). Setter injection runs after construction — useful for optional dependencies that may change. Field injection uses reflection to set private fields — convenient but hides dependencies and makes testing harder without Spring Test or reflection. Style When to use Testability Constructor Required deps (default choice) Best — pass mocks in constructor Setter Optional / reconfigurable deps Good Field Avoid in new code Poor — hidden deps Interview angle. Always say constructor injection for mandatory dependencies. Mention that since Spring 4.3+, on a single constructor is optional."},{"id":"applicationcontext-vs-beanfactory","heading":"ApplicationContext vs BeanFactory","level":3,"text":"In simple terms. Both hold your Spring beans. is what Spring Boot uses — it creates beans at startup so config errors show up immediately. is the older, minimal version (lazy loading). In interviews: \"I use ApplicationContext; BeanFactory is legacy.\" Feature BeanFactory ApplicationContext Bean creation Lazy (on first request) Eager (at startup) Events No Yes ( ) Internationalization No Yes ( ) AOP Limited Full support Annotation support No Yes Use Legacy, memory constrained Always use this"},{"id":"qualifier-and-primary","heading":"@Qualifier and @Primary","level":3,"text":"In simple terms. Two beans implement the same interface → Spring doesn't know which one to inject. Fix with to pick one, or to mark the default."},{"id":"2-bean-lifecycle-scopes","heading":"2. Bean Lifecycle & Scopes","level":2,"text":"In simple terms. A Spring bean isn't just \"new and done.\" Spring: (1) calls your constructor, (2) injects dependencies, (3) runs , (4) wraps the bean in a proxy (for , etc.), then it's ready. On shutdown: . Scope = how many copies exist. Default is — one instance shared by the whole app. = new instance every time you ask for it. Trap: Putting a bean inside a — you get only one prototype instance, not a fresh one each call. Use or to fix."},{"id":"bean-lifecycle","heading":"Bean Lifecycle","level":3,"text":""},{"id":"bean-scopes","heading":"Bean Scopes","level":3,"text":"Scope Meaning When to use One per app (default) Stateless services New every injection Stateful / per use objects One per HTTP request Web apps One per user session Login specific data"},{"id":"3-spring-aop","heading":"3. Spring AOP","level":2,"text":"In simple terms. Some logic repeats everywhere — logging, security checks, transactions. AOP lets you write it once and Spring runs it around your methods automatically. How it works (plain English): Spring wraps your bean in a proxy . When someone calls your method from outside, the proxy runs extra code first (e.g., open transaction), then your method, then cleanup (commit/rollback). The 1 trap: Calling inside the same class skips the proxy — so on won't work. Fix: inject and call ."},{"id":"concepts","heading":"Concepts","level":3,"text":""},{"id":"spring-aop-example","heading":"Spring AOP Example","level":3,"text":"Example walkthrough. When is called: 1. advice logs \"Before: createOrder\" 2. advice starts timer, calls (actual method) 3. Method returns 4. logs the result 5. logs elapsed time If an exception is thrown, runs instead of . Interview angle. is most powerful — you can skip the method entirely, change arguments, or wrap exceptions. is implemented as advice on the proxy."},{"id":"pointcut-expression-syntax","heading":"Pointcut Expression Syntax","level":3,"text":""},{"id":"how-spring-aop-works-proxy-based","heading":"How Spring AOP Works (Proxy-based)","level":3,"text":"How it works — step by step. 1. Spring creates your bean. 2. sees it needs transaction/logging advice. 3. If implements an interface → JDK dynamic proxy (implements same interfaces, delegates to target). 4. If no interface → CGLIB generates a subclass that overrides public methods and intercepts calls. 5. Container stores the proxy , not the raw object. Injected references are proxies. Limitations. Spring AOP only intercepts public methods on Spring managed beans . Private methods, methods, and calls from inside the same class are invisible to the proxy."},{"id":"4-spring-boot-fundamentals","heading":"4. Spring Boot Fundamentals","level":2,"text":"In simple terms. Spring Boot = Spring + batteries included . You get: An embedded Tomcat (no separate server install) Auto configuration (Boot sets up DataSource, JSON, etc. based on what's on your classpath) Starters (one dependency pulls in everything you need for web, JPA, security…) What happens when you run ? 1. Spring starts and scans your package for , , etc. 2. Boot reads its auto config list and creates default beans (only if you didn't define your own) 3. Tomcat starts on port 8080 4. Your app is ready Auto config in one sentence: Add + set DB URL in YAML → Boot creates a connection pool for you. Define your own → Boot backs off and uses yours."},{"id":"springbootapplication","heading":"@SpringBootApplication","level":3,"text":""},{"id":"auto-configuration","heading":"Auto-Configuration","level":3,"text":"Boot only creates a bean if: The needed library is on the classpath ( ) You haven't already defined that bean ( ) A property says so ( ) Debug tip: Run with to see which auto configs matched and which were skipped."},{"id":"configuration-files","heading":"Configuration Files","level":3,"text":"How it works. Spring Boot loads or from classpath root, then profile specific files ( ) when is set. Later sources override earlier ones: default → profile → env vars → command line args. Example. resolves from environment at runtime — never commit secrets to YAML."},{"id":"profiles","heading":"Profiles","level":3,"text":"In simple terms. Same JAR, different settings per environment. for local, for production. Activate with ."},{"id":"externalized-configuration","heading":"Externalized Configuration","level":3,"text":"In simple terms. Don't hard code URLs or passwords in Java. Put them in or environment variables. — read one property — bind a whole group of related settings to a Java class (preferred when you have many)"},{"id":"5-spring-rest-api","heading":"5. Spring REST API","level":2,"text":"In simple terms. = a class that handles HTTP requests and returns JSON (not HTML pages). You map URLs to methods with , , etc. Request flow: Browser/client → Tomcat → → finds your controller method → runs it → Jackson converts result to JSON → response. Quick difference: returns HTML view names; always returns JSON in the body. Use when you need a specific status code (201, 404, etc.)."},{"id":"request-handling","heading":"Request Handling","level":3,"text":""},{"id":"validation","heading":"Validation","level":3,"text":"Theory. Never trust client input. Bean Validation (JSR 380) declaratively rejects bad data before your service layer runs. How it works. on triggers validation after JSON deserialization. Failures throw — handle globally in . on nested objects cascades validation to fields too. Pitfall. Validation annotations on the entity ( ) do not run automatically on REST endpoints — they run only when you explicitly the object."},{"id":"global-exception-handling","heading":"Global Exception Handling","level":3,"text":"Theory. Without centralized handling, every controller duplicates try/catch blocks. applies methods across all controllers. How it works. Spring walks the exception handler chain: most specific handler first ( → 404), then generic ( → 500). Return a consistent JSON error shape so clients can parse + reliably. Interview angle. Order handlers from specific to general. Never expose stack traces to clients in production — log server side only."},{"id":"http-status-codes-to-remember","heading":"HTTP Status Codes to Remember","level":3,"text":""},{"id":"6-spring-data-jpa","heading":"6. Spring Data JPA","level":2,"text":"Theory. JPA (Java Persistence API) is a specification for ORM — mapping Java objects to relational tables. Hibernate is the default implementation in Spring Boot. Spring Data JPA adds repository interfaces so you write instead of boilerplate JDBC. How it works — read path. 1. Controller calls . 2. Spring Data generates a proxy implementing your interface. 3. Proxy builds JPQL/SQL, Hibernate executes via JDBC/HikariCP. 4. Rows map to objects in the persistence context (1st level cache). 5. Lazy associations ( ) load only when accessed inside an open transaction. Pitfall. Accessing outside a method → ."},{"id":"entity-design","heading":"Entity Design","level":3,"text":""},{"id":"relationships","heading":"Relationships","level":3,"text":"Theory. JPA models associations with , , etc. The owning side has the foreign key column ( on ). The inverse side uses . How it works — bidirectional sync. JPA does not automatically update both sides. Always use helper methods ( ) so and stay consistent — otherwise orphan rows or broken FKs appear. Example. has many . FK lives on table. is the owning side; is inverse ( ). Interview angle. Default fetch: = EAGER (often bad — override to LAZY), = LAZY. Always prefer LAZY + explicit fetch when needed."},{"id":"repository-methods","heading":"Repository Methods","level":3,"text":"How it works — derived queries. Spring parses method names: → . for joins/complex logic. queries need because they bypass the persistence context. Example. generates automatically."},{"id":"n1-problem-and-solutions","heading":"N+1 Problem and Solutions","level":3,"text":"Theory. The N+1 problem is a performance trap: one query loads N parent rows, then N additional queries load each child/lazy association when accessed in a loop. Example — the numbers. How it works. LAZY loading triggers a SELECT on first access. JOIN FETCH or loads associations in the same query. DTO projections select only needed columns — fastest for read heavy APIs. Interview angle. Always mention you detect N+1 via Hibernate , logs, or APM (many similar SELECTs). Fix at query level, not by switching everything to EAGER."},{"id":"locking","heading":"Locking","level":3,"text":"Theory. Concurrent updates to the same row (two users booking the last seat) need coordination. Optimistic = assume no conflict, detect at commit. Pessimistic = lock row in DB before update. Example — optimistic flow. 1. Thread A reads . 2. Thread B reads . 3. Thread A saves → succeeds → version becomes 4. 4. Thread B saves → matches 0 rows → . Interview angle. Optimistic for low contention (profile updates). Pessimistic for high contention (inventory, seat booking)."},{"id":"7-spring-security","heading":"7. Spring Security","level":2,"text":"Theory. Spring Security protects your app via a filter chain — a pipeline of servlet filters that run before your controller. Authentication = who are you? Authorization = what can you do? How it works — JWT request flow. 1. Client sends . 2. extracts token, validates signature + expiry. 3. Loads from DB/cache, builds object. 4. Stores it in (ThreadLocal — available to whole request thread). 5. checks / URL rules. 6. Controller runs with authenticated principal available. Pitfall. is ThreadLocal — don't pass auth context to async threads without explicit propagation ( )."},{"id":"security-filter-chain","heading":"Security Filter Chain","level":3,"text":""},{"id":"spring-security-6x-configuration","heading":"Spring Security 6.x Configuration","level":3,"text":"How it works. is a replacing old . Each lambda configures one concern: CSRF, CORS, session policy, URL authorization, custom filters. Filters added with run in declared order. Example. REST APIs typically: disable CSRF (no cookies), stateless sessions, JWT filter before username/password filter, permit , protect everything else."},{"id":"jwt-authentication-complete-flow","heading":"JWT Authentication — Complete Flow","level":3,"text":"Theory. JWT (JSON Web Token) enables stateless auth — the server doesn't store sessions; the signed token carries identity and claims. How it works — token structure. — payload contains (username), , . Server verifies signature with secret/public key; tampering invalidates the token. Example — login to protected call. 1. → validate → return JWT. 2. Client stores token (httpOnly cookie preferred over localStorage for XSS resistance). 3. with → filter validates → 200 with data. Interview angle. Refresh tokens for short lived access tokens. Never put secrets in JWT payload (it's base64, not encrypted)."},{"id":"method-security","heading":"Method Security","level":3,"text":"Theory. URL level security ( ) is coarse. Method security ( ) enforces rules on individual service methods — e.g., users can only read their own orders. How it works. creates AOP proxies around beans. SpEL expressions like access method args and the current user. checks the return value after execution. Pitfall. Method security only works on Spring managed beans called through the proxy — same self invocation trap as ."},{"id":"8-spring-transactions","heading":"8. Spring Transactions","level":2,"text":"Theory. A transaction groups multiple DB operations into one atomic unit — all commit or all roll back. tells Spring to start/commit/rollback a transaction around the method via AOP proxy. How it works. When you call : 1. Proxy starts transaction (or joins existing one per propagation). 2. Gets JDBC connection from HikariCP, binds to current thread. 3. All repo calls in that thread share the same connection/transaction. 4. On success → commit. On unchecked exception → rollback (by default). Analogy. A shopping cart: either everything checks out together or nothing does — you don't pay for items that failed to ship."},{"id":"transactional-deep-dive","heading":"@Transactional Deep Dive","level":3,"text":"Propagation Levels Example — REQUIRED vs REQUIRES NEW. Interview angle. uses a second connection from the pool — overuse can exhaust the pool under load. Isolation Levels Theory. Isolation controls what one transaction sees while another is in flight. Higher isolation = fewer anomalies, more locking/contention. Example — non repeatable read. TX1 reads balance=$100. TX2 updates balance to $50 and commits. TX1 reads again → $50. With , TX1 would still see $100 until it commits. Interview angle. Know the three anomalies (dirty, non repeatable, phantom) and which level prevents each. Most Spring apps use DB default ( on PostgreSQL). Common Mistakes"},{"id":"9-microservices-with-spring-cloud","heading":"9. Microservices with Spring Cloud","level":2,"text":"Theory. Microservices split a monolith into independently deployable services (order, payment, inventory). Spring Cloud provides patterns for problems monoliths solve in process: service discovery, config, routing, resilience. How it works — typical request. 1. Client → API Gateway ( ). 2. Gateway resolves via Eureka, load balances. 3. Order service calls payment via Feign (declarative HTTP client). 4. If payment is down, circuit breaker returns fallback instead of hanging. 5. Async events (order created) go to Kafka for inventory/shipping. Interview angle. Microservices trade simplicity for operational complexity — only split when team scale or independent deployment truly requires it."},{"id":"architecture-overview","heading":"Architecture Overview","level":3,"text":""},{"id":"service-discovery-eureka","heading":"Service Discovery — Eureka","level":3,"text":"Theory. In a dynamic environment (Kubernetes, auto scaling), service IPs change constantly. Service discovery lets clients ask \"where is order service?\" instead of hard coding URLs. How it works. Each service registers with Eureka on startup ( , host, port). Clients resolve → Eureka returns healthy instances → client side load balancer picks one. Pitfall. Eureka has a cache delay — a crashed instance may receive traffic briefly until heartbeat timeout. Pair with client side retries and circuit breakers."},{"id":"feign-client-declarative-rest","heading":"Feign Client (Declarative REST)","level":3,"text":"Theory. Feign turns HTTP calls into Java interface methods — no manual URL building. Spring Cloud adds service discovery integration and fallbacks. How it works. At runtime, Feign generates a proxy. resolves the base URL via Eureka. Fallback class runs when circuit opens or call fails. Example. → HTTP POST to a discovered payment service instance → deserialize ."},{"id":"circuit-breaker-resilience4j","heading":"Circuit Breaker — Resilience4j","level":3,"text":"Theory. When a downstream service fails repeatedly, continuing to call it wastes threads and cascades failure. A circuit breaker stops calls after a failure threshold and fails fast. How it works — state machine. Example. Payment service down → after 5 of 10 calls fail, circuit opens → order service immediately returns \"payment queued\" fallback instead of waiting 30s per request. Interview angle. Circuit breaker + retry + timeout work together — retry transient errors, break on sustained failure, timeout prevents thread starvation."},{"id":"api-gateway-spring-cloud-gateway","heading":"API Gateway — Spring Cloud Gateway","level":3,"text":""},{"id":"saga-pattern-distributed-transactions","heading":"Saga Pattern — Distributed Transactions","level":3,"text":""},{"id":"kafka-integration","heading":"Kafka Integration","level":3,"text":""},{"id":"10-spring-boot-actuator","heading":"10. Spring Boot Actuator","level":2,"text":"Theory. Actuator exposes production ready endpoints for health checks, metrics, and debugging — essential for Kubernetes liveness/readiness probes and Prometheus scraping. How it works. registers endpoints under . By default only is exposed over HTTP; opens the rest (lock down in prod with security). Example — Kubernetes probe. Liveness: → 200 UP keeps pod alive. Readiness: checks DB connectivity before receiving traffic. Pitfall. Exposing or publicly leaks secrets and internals — restrict via Spring Security or network policy."},{"id":"11-interview-qa","heading":"11. Interview Q&A","level":2,"text":"This section is preserved in full — nothing removed. Additional questions are in Section 13 only. Q: How does Spring Boot auto configuration work? A: Spring Boot reads from all JARs on the classpath. Each listed class is a annotated with annotations. For example, only applies and . If you define your own bean, Spring's auto config is skipped. Q: Difference between @Component, @Service, @Repository, @Controller? A: All four are specializations of and enable component scanning. Semantically: adds persistence exception translation (converts SQL exceptions to Spring's ). marks business logic. marks MVC controllers. = + . They're all equivalent for DI purposes but carry semantic meaning. Q: Explain @Transactional propagation with a practical example? A: Consider calling . If has , it runs in its own transaction. If fails and rolls back, the audit log is still saved. If had (default), the log would roll back along with the order. Q: What is the N+1 problem? A: When fetching a list of N entities and then accessing a lazily loaded relationship on each, you get 1 initial query + N additional queries. Fix: use JOIN FETCH in JPQL, annotation, or DTO projections to load exactly what you need in one query. Q: How does JWT authentication work end to end? A: 1) User sends username/password to . 2) Server validates credentials against UserDetailsService. 3) Server generates JWT (header.payload.signature) signed with secret key. 4) Client stores JWT (localStorage or httpOnly cookie). 5) Client sends JWT in header. 6) extracts token, validates signature and expiration, loads UserDetails, sets SecurityContext. 7) Request proceeds to controller. Q: What is the difference between and ? A: is synchronous and blocking — thread waits until response arrives. is reactive and non blocking — thread is freed while waiting, better for high concurrency scenarios. is being deprecated. Spring recommends even for synchronous use ( can be called). Q: Explain Spring AOP and when self invocation is a p"},{"id":"12-deep-dive-tough-topics","heading":"12. DEEP DIVE: Tough Topics","level":2,"text":""},{"id":"121-valid-vs-validated","heading":"12.1 @Valid vs @Validated","level":3,"text":"Theory. Both trigger validation, but they come from different layers and support different features. Mixing them up is a common interview trap. How it works. is the JSR 380 standard — Hibernate Validator executes constraints when Spring sees it on a method parameter. is Spring's wrapper that adds validation groups — same DTO, different rules for create vs update. Example. On create, must be null. On update, is required. Groups let one serve both endpoints without duplicate classes. Source JSR 380 (javax/jakarta.validation) Spring specific Where Method params, fields, nested objects Class level, method level Groups No group support Supports validation groups Cascade Validates nested objects Same via on fields Interview answer: Use for simple request validation. Use when you need validation groups or class level method validation ( on service class + on method params)."},{"id":"122-transactional-propagation-all-modes","heading":"12.2 @Transactional Propagation — All Modes","level":3,"text":"Propagation Behavior (default) Join existing transaction; create new if none Always new transaction; suspend existing Nested transaction with savepoint inside existing Use transaction if exists; else non transactional Suspend existing; run non transactional Must have transaction; else Must NOT have transaction; else Interview trap: creates a new DB connection from pool. Overuse can exhaust connection pool."},{"id":"123-applicationcontext-vs-beanfactory-deep","heading":"12.3 ApplicationContext vs BeanFactory — Deep","level":3,"text":"Feature BeanFactory ApplicationContext Bean creation Lazy (on first ) Eager (most singletons at startup) Events No i18n No AOP Limited Full proxy support Web support No Annotation processing No , Use today Almost never directly Always (Spring Boot uses this)"},{"id":"124-circular-dependency-how-it-happens-and-fixes","heading":"12.4 Circular Dependency — How It Happens and Fixes","level":3,"text":"Solutions (in order of preference): 1. Redesign — extract shared logic to that both depend on 2. @Lazy — inject proxy, real bean created on first use 3. Setter/field injection — Spring can create empty object first, then inject (not recommended for new code) 4. ObjectProvider / Provider Spring Boot 2.6+: circular references disallowed by default ( to override — avoid)."},{"id":"125-jpa-vs-crudrepository-vs-jparepository","heading":"12.5 JPA vs CrudRepository vs JpaRepository","level":3,"text":"JPA (specification) CrudRepository JpaRepository What Standard for ORM mapping Spring Data interface Extends CrudRepository Level EntityManager, JPQL CRUD only CRUD + paging + flush Use Custom complex queries Simple CRUD Most Spring Boot apps"},{"id":"126-database-connectivity-in-spring-boot","heading":"12.6 Database Connectivity in Spring Boot","level":3,"text":"Flow: Controller → Service → Repository → Hibernate → JDBC → HikariCP → PostgreSQL Interview points: HikariCP = fastest connection pool, default in Boot 2+ in prod; use Flyway/Liquibase for migrations N+1 fix: , , DTO projection Read/write splitting: routes to replica (with routing DataSource)"},{"id":"127-testng-vs-junit","heading":"12.7 TestNG vs JUnit","level":3,"text":"JUnit 5 TestNG Default in Spring Boot Yes No (add dependency) Annotations , , Parallel execution Limited (extensions) Built in Test groups Tags ( ) Groups ( ) Dependencies No native test order Data providers XML suites No Interview answer: Use JUnit 5 for Spring Boot (default, better ecosystem). TestNG when you need complex suite XML, test dependencies, or heavy parallel group execution."},{"id":"128-http-vs-https-how-it-works","heading":"12.8 HTTP vs HTTPS — How It Works","level":3,"text":"Key concepts for interviews: Symmetric encryption — same key encrypt/decrypt (fast, used for data) Asymmetric encryption — public/private key pair (used during handshake) Certificate — binds domain to public key, signed by CA TLS 1.2/1.3 — modern versions; SSL is deprecated"},{"id":"129-grpc-vs-rest-vs-sql","heading":"12.9 gRPC vs REST vs SQL","level":3,"text":"REST gRPC SQL Type API architectural style RPC framework Query language Protocol HTTP/1.1 or HTTP/2 HTTP/2 N/A (talks to DB) Payload JSON (text) Protocol Buffers (binary) Structured queries Contract OpenAPI/Swagger (optional) .proto file (strict) DB schema Streaming Limited (SSE, WebSocket) Unary, server, client, bidirectional Result sets Browser support Native Needs grpc web proxy N/A Best for Public APIs, CRUD Inter service, high perf Data persistence When to use gRPC in Spring: microservice to microservice calls where latency and throughput matter. Use REST for external/public APIs."},{"id":"1210-monolithic-vs-microservices-spring-context","heading":"12.10 Monolithic vs Microservices (Spring Context)","level":3,"text":"Monolithic Microservices Deployment Single JAR/WAR Independent services Spring setup One Multiple Boot apps + Cloud Transactions Single Saga / eventual consistency Communication In process method calls REST / gRPC / Kafka Config Spring Cloud Config Discovery N/A Eureka / Consul Gateway N/A Spring Cloud Gateway Failure One bug can crash all Isolated blast radius"},{"id":"1211-saga-orchestration-vs-choreography-deep","heading":"12.11 Saga — Orchestration vs Choreography (Deep)","level":3,"text":""},{"id":"1212-multithreading-resource-allocation-in-spring","heading":"12.12 Multithreading Resource Allocation in Spring","level":3,"text":"Problem: Multiple threads competing for DB connections, seats, inventory. Solutions without : 1. Database pessimistic lock: or 2. Optimistic lock: field on entity 3. Redis distributed lock: , with TTL 4. Semaphore (in process): limit concurrent bookings 5. Kafka partitioning: one consumer per partition = ordered processing 6. @Transactional isolation: (last resort — slow)"},{"id":"13-additional-interview-qa-new-questions-only","heading":"13. Additional Interview Q&A (New Questions Only)","level":2,"text":"Note: Section 11 above is unchanged — all 8 original Q&A items are preserved in full. This section adds only new questions not covered in Section 11. For deep dives see Section 12. Q: What is Hibernate? A: Hibernate is an ORM (Object Relational Mapping) framework for Java. It maps Java classes to database tables and Java objects to table rows. It manages CRUD operations automatically, provides HQL (Hibernate Query Language) for database independent queries, supports first level and second level caching, lazy loading of associations, and transaction management via JPA/ . It reduces JDBC boilerplate (no manual , , row mapping) and provides database independence — switch PostgreSQL to MySQL with minimal code changes. In Spring Boot, Hibernate is the default JPA provider behind . Q: Explain the Spring bean lifecycle in detail? A: Full lifecycle: 1. Instantiation — Spring calls constructor via reflection 2. Populate properties — dependencies injected 3. Aware interfaces — , , callbacks 4. BeanPostProcessor.postProcessBeforeInitialization() — pre init customization 5. Initialization — → → custom 6. BeanPostProcessor.postProcessAfterInitialization() — post init (AOP proxies created here!) 7. Bean ready — in use 8. Destruction — → → custom on context shutdown Q: How to resolve circular dependency in Spring? A: Constructor injection circular deps fail at startup (Boot 2.6+ by default). Solutions in order: 1. Redesign — extract shared logic to a third bean (best) 2. — inject proxy; real bean created on first use: 3. Setter/field injection — Spring instantiates empty object first, then injects (not recommended for new code) 4. — deferred lookup: when needed Avoid in production — it hides design problems. Q: @Valid vs @Validated — difference? A: is JSR 380 standard — use on , nested objects; cascades to nested fields; no validation groups. is Spring specific — supports validation groups ( ), can be placed on class/method for method parameter validation. Use for simple REST requ"},{"id":"14-jpa-hibernate-deep-dive","heading":"14. JPA / Hibernate Deep Dive","level":2,"text":""},{"id":"141-get-vs-load-hibernate","heading":"14.1 get() vs load() (Hibernate)","level":3,"text":"Theory. These answer different questions: \"Does this row exist right now?\" vs \"I need a reference to this row for a FK/association.\" How it works. hits the DB immediately and returns if missing. returns a lazy proxy — no SELECT until you access a field. Hibernate uses the proxy to hold a FK without loading the full row. Example — FK assignment without extra query. Hit DB immediately? Yes — SELECT now No — returns proxy If ID not found Returns Throws (when proxy accessed) Use when You need to know if row exists You trust ID exists, only need reference Spring Data equivalent → → lazy proxy Interview trap: Use only when ID is valid (e.g. FK assignment). Use when existence is uncertain."},{"id":"142-first-level-vs-second-level-cache","heading":"14.2 First Level vs Second Level Cache","level":3,"text":"1st Level 2nd Level Scope Per /session Per /JVM Enabled Always What's cached Entities in current persistence context Entity data by ID Invalidation Session closes TTL, explicit evict, entity update Query cache (separate): caches query result IDs — use sparingly, invalidation is tricky."},{"id":"143-optimistic-vs-pessimistic-locking","heading":"14.3 Optimistic vs Pessimistic Locking","level":3,"text":"Optimistic — assume no conflict; detect at commit with : Pessimistic — lock row in DB before update: Optimistic Pessimistic Lock when At commit At read Performance Better (no DB lock held) Worse (blocks other writers) Conflicts Retry on exception Prevented upfront Use when Low contention, read heavy High contention (stock, seats)"},{"id":"144-dirty-checking","heading":"14.4 Dirty Checking","level":3,"text":"Theory. Hibernate's killer feature: you load an entity, change a field, and at commit Hibernate auto generates — no explicit needed for managed entities. How it works. When you , Hibernate stores a snapshot of field values in the persistence context. On flush/commit, it diffs current values vs snapshot — changed fields become SQL clauses. Pitfall. Detached entities (loaded in one transaction, modified after it closed) are invisible to dirty checking — you must or call to reattach. Hibernate tracks all managed (persistent) entities in the persistence context. On or commit, it compares current field values with snapshot taken at load — if different, generates automatically. You don't call UPDATE manually. Detached entity — changes NOT tracked unless you :"},{"id":"145-entity-lifecycle-states","heading":"14.5 Entity Lifecycle States","level":3,"text":"State Meaning In persistence context? Transient , not saved No Persistent Managed by Hibernate in TX Yes — dirty checking active Detached Was persistent, session closed No — changes ignored Removed called, pending flush Yes until flush"},{"id":"146-scenario-product-catalog-concurrent-stock-updates","heading":"14.6 Scenario — Product catalog: concurrent stock updates","level":3,"text":"Problem: Multiple users buy last items simultaneously → overselling. Solution (layered): Best for high contention stock: pessimistic or atomic SQL:"},{"id":"15-additional-spring-spring-boot-qa","heading":"15. Additional Spring & Spring Boot Q&A","level":2,"text":"Q: Difference between and ? Where On a class On a method in class Who creates object Spring scans & instantiates class You write the method body Use when Your own class ( , etc.) 3rd party classes you can't annotate ( , ) Q: What is AOP? Real world use case? A: Aspect Oriented Programming — apply cross cutting logic (logging, security, transactions) without duplicating code in every method. Spring AOP uses proxies. Real use cases: , , , audit logging, metrics, retry logic. Q: How does Spring handle circular dependencies? A: See Section 13 and 12.4. Constructor injection cycles fail. , setter injection, or redesign. Q: How to secure REST APIs (JWT, OAuth2)? A: JWT — stateless; login returns signed token; client sends ; server validates signature + expiry. Good for SPAs/mobile. OAuth2 — delegate auth to provider (Google, Okta); flows: Authorization Code (web), Client Credentials (service to service). Spring Security OAuth2 Resource Server validates tokens. Session based — server stores session; cookie based; traditional web apps. Q: How does Spring Boot auto configuration work? A: See Section 11 Q&A. → → reads → applies configs only when classpath/properties match. Q: vs ? Format flat Hierarchical YAML Readability Fine for few keys Better for nested config Multi doc No separator Lists Awkward Native syntax Both work identically — Spring Boot loads both. Use profiles : , + . Externalize secrets via env vars: . Q: ApplicationContext vs BeanFactory? A: See Section 13. ApplicationContext = always use in Boot. Q: Scenario — Order system: inventory, payment, invoice as independent steps. Use Spring Events / AOP? A: Spring Events (loose coupling — preferred for independent services): AOP — for cross cutting concerns on the same call (logging, audit, ), not for business workflow between services. For distributed microservices use Kafka events instead of in process Spring Events."},{"id":"16-spring-annotations-simple-cheat-sheet","heading":"16. Spring Annotations — Simple Cheat Sheet","level":2,"text":"In simple terms. Annotations are labels on your code that tell Spring what to do. You don't call Spring APIs — you just mark classes and methods, and Spring reads them at startup. For full depth on custom annotations, meta annotations, and reflection → see ."},{"id":"161-core-register-beans-inject-dependencies","heading":"16.1 Core — register beans & inject dependencies","level":3,"text":"Annotation What it does (plain English) Example \"Spring, manage this class as a bean\" Generic bean Same as , for business logic Same as , for DB access (+ exception translation) Handles web requests, returns view names (HTML) MVC pages + always return JSON body REST APIs Class that defines methods Method returns an object Spring should manage bean \"Inject this dependency\" (constructor preferred) Constructor param Pick which bean when several exist Default bean when multiple match Main Inject a property from YAML/env Create bean only when first used Heavy startup beans Run this method after injection (init) Load cache Run before bean is removed (cleanup) Close connections New instance every time (not singleton) Per task processor"},{"id":"162-spring-boot-app-setup-config","heading":"16.2 Spring Boot — app setup & config","level":3,"text":"Annotation What it does Example Main class: scan components + auto config Turn on Boot auto config (inside above) Rarely used alone Scan packages for etc. Default: main class package Bind YAML group to a Java class Bean only active in that profile Dev only Bean only if property is set Feature flags Turn on jobs Cron tasks Run method on a schedule Nightly cleanup Turn on methods Background work Run method on a thread pool Send email async Turn on Cache layer Cache method result Remove from cache After update/delete"},{"id":"163-web-rest-map-http-to-java","heading":"16.3 Web / REST — map HTTP to Java","level":3,"text":"Annotation What it does Example Base URL for all methods in class Class level path Handle HTTP GET List / read Handle HTTP POST Create Handle HTTP PUT Full update Handle HTTP PATCH Partial update Handle HTTP DELETE Remove Value from URL Query string Pagination, filters JSON body → Java object POST payload HTTP header value Cookie value Session id Set HTTP status on method Not found Return value → response body On methods Trigger validation on object With Handle exception in one controller Local errors Global exception handlers App wide + JSON responses REST error format Allow CORS from browser Frontend on other port"},{"id":"164-jpa-database-map-java-tables","heading":"16.4 JPA / Database — map Java ↔ tables","level":3,"text":"Annotation What it does Example This class = a database table Table name (if different from class) Primary key field Auto generate PK , Column name, nullable, length One parent → many children Order → OrderItems Many children → one parent OrderItem → Order Many to many with join table Student ↔ Course One to one User ↔ Profile FK column name Store enum as text (not number) Optimistic locking column Prevent lost updates DB operations in one transaction Service methods Custom JPQL/SQL on repository UPDATE/DELETE query Bulk update Fetch related data in one query Fix N+1"},{"id":"165-validation-check-input-before-processing","heading":"16.5 Validation — check input before processing","level":3,"text":"Annotation What it checks Must not be null String not null/empty/whitespace Collection/string not empty Length or collection size / Number range Valid email format Matches regex / Date in past/future Run validation (on or nested object)"},{"id":"166-security-auth-permissions","heading":"16.6 Security — auth & permissions","level":3,"text":"Annotation What it does Turn on Spring Security Check role before method runs Check after method returns Role based access JSR 250 role check"},{"id":"167-transactions-aop","heading":"16.7 Transactions & AOP","level":3,"text":"Annotation What it does All DB ops succeed or all roll back Read only — performance hint New separate transaction Roll back on checked exceptions too Class containing cross cutting advice / / When advice runs around methods Remember: only works when called through the Spring proxy — not via inside the same class."},{"id":"168-testing","heading":"16.8 Testing","level":3,"text":"Annotation What it does Load full application context (integration test) Test only web layer (controller slice) Test only JPA layer Replace a Spring bean with a Mockito mock Partial mock of a real bean Use test profile config JUnit test method"},{"id":"169-quick-interview-recap-most-asked-annotations","heading":"16.9 Quick interview recap — most-asked annotations","level":3,"text":"If they ask… Say this annotation Mark a REST API class Inject dependency Constructor + (no field ) Read config from YAML or DB transaction Validate request body + etc. Custom SQL on repository Global error handling + Main Boot class Run code after startup or Async background task + One liner for interviews: \"Spring annotations are metadata — I use stereotypes ( , ) to register beans, constructor injection for dependencies, + mapping annotations for APIs, for DB boundaries, and + Bean Validation for input checks.\""}],"qa":[{"q":"uick difference:","a":"`@Controller` returns HTML view names; `@RestController` always returns JSON in the body. Use `ResponseEntity` when you need a specific status code (201, 404, etc.)."},{"q":"How does Spring Boot auto-configuration work?","a":"Spring Boot reads `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` from all JARs on the classpath. Each listed class is a `@Configuration` annotated with `@Conditional*` annotations. For example, `DataSourceAutoConfiguration` only applies `@ConditionalOnClass(DataSource.class)` and `@ConditionalOnMissingBean(DataSource.class)`. If you define your own `DataSource` bean, Spring's auto-config is skipped."},{"q":"Difference between @Component, @Service, @Repository, @Controller?","a":"All four are specializations of `@Component` and enable component scanning. Semantically: `@Repository` adds persistence exception translation (converts SQL exceptions to Spring's `DataAccessException`). `@Service` marks business logic. `@Controller` marks MVC controllers. `@RestController` = `@Controller` + `@ResponseBody`. They're all equivalent for DI purposes but carry semantic meaning."},{"q":"Explain @Transactional propagation with a practical example?","a":"Consider `OrderService.createOrder()` calling `AuditService.logAction()`. If `logAction()` has `REQUIRES_NEW`, it runs in its own transaction. If `createOrder()` fails and rolls back, the audit log is still saved. If `logAction()` had `REQUIRED` (default), the log would roll back along with the order."},{"q":"What is the N+1 problem?","a":"When fetching a list of N entities and then accessing a lazily-loaded relationship on each, you get 1 initial query + N additional queries. Fix: use JOIN FETCH in JPQL, `@EntityGraph` annotation, or DTO projections to load exactly what you need in one query."},{"q":"How does JWT authentication work end-to-end?","a":"1) User sends username/password to `/auth/login`. 2) Server validates credentials against UserDetailsService. 3) Server generates JWT (header.payload.signature) signed with secret key. 4) Client stores JWT (localStorage or httpOnly cookie). 5) Client sends JWT in `Authorization: Bearer <token>` header. 6) `JwtAuthFilter` extracts token, validates signature and expiration, loads UserDetails, sets SecurityContext. 7) Request proceeds to controller."},{"q":"What is the difference between `WebClient` and `RestTemplate`?","a":"`RestTemplate` is synchronous and blocking — thread waits until response arrives. `WebClient` is reactive and non-blocking — thread is freed while waiting, better for high-concurrency scenarios. `RestTemplate` is being deprecated. Spring recommends `WebClient` even for synchronous use (`block()` can be called)."},{"q":"Explain Spring AOP and when self-invocation is a problem?","a":"Spring AOP creates proxy objects around your beans. Advice only applies when a method is called through the proxy. When `serviceA.methodA()` calls `this.methodB()` inside the same class, it bypasses the proxy, so `@Transactional`, `@Cacheable`, etc. on `methodB` are ignored. Solution: inject the bean's own proxy using `@Autowired private ServiceA self` and call `self.methodB()`."},{"q":"What is Circuit Breaker and when to use it?","a":"Circuit Breaker prevents cascade failures. When a downstream service is failing, the circuit \"opens\" and calls fail fast without hitting the service. After a wait period, it goes to HALF_OPEN to probe recovery. Use when: calling external/unreliable services, preventing thread pool exhaustion from slow downstream, implementing graceful degradation with fallback responses."},{"q":"What is Hibernate?","a":"Hibernate is an ORM (Object-Relational Mapping) framework for Java. It maps Java classes to database tables and Java objects to table rows. It manages CRUD operations automatically, provides HQL (Hibernate Query Language) for database-independent queries, supports first-level and second-level caching, lazy loading of associations, and transaction management via JPA/`EntityManager`. It reduces JDBC boilerplate (no manual `Connection`, `PreparedStatement`, row mapping) and provides database independence — switch PostgreSQL to MySQL with minimal code changes. In Spring Boot, Hibernate is the default JPA provider behind `spring-boot-starter-data-jpa`."},{"q":"Explain the Spring bean lifecycle in detail?","a":"Full lifecycle:\n1. **Instantiation** — Spring calls constructor via reflection\n2. **Populate properties** — `@Autowired` dependencies injected\n3. **Aware interfaces** — `BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` callbacks\n4. **BeanPostProcessor.postProcessBeforeInitialization()** — pre-init customization\n5. **Initialization** — `@PostConstruct` → `InitializingBean.afterPropertiesSet()` → custom `init-method`\n6. **BeanPostProcessor.postProcessAfterInitialization()** — post-init (AOP proxies created here!)\n7. **Bean ready** — in use\n8. **Destruction** — `@PreDestroy` → `DisposableBean.destroy()` → custom `destroy-method` on context shutdown"},{"q":"How to resolve circular dependency in Spring?","a":"Constructor injection circular deps **fail at startup** (Boot 2.6+ by default). Solutions in order:\n1. **Redesign** — extract shared logic to a third bean (best)\n2. **`@Lazy`** — inject proxy; real bean created on first use: `public ServiceA(@Lazy ServiceB b)`\n3. **Setter/field injection** — Spring instantiates empty object first, then injects (not recommended for new code)\n4. **`ObjectProvider<T>`** — deferred lookup: `bProvider.getObject()` when needed\nAvoid `spring.main.allow-circular-references=true` in production — it hides design problems."},{"q":"@Valid vs @Validated — difference?","a":"`@Valid` is JSR-380 standard — use on `@RequestBody`, nested objects; cascades to nested `@Valid` fields; no validation groups. `@Validated` is Spring-specific — supports **validation groups** (`@Validated(CreateGroup.class)`), can be placed on class/method for method-parameter validation. Use `@Valid` for simple REST request validation; `@Validated` when same DTO needs different rules for create vs update. See [Section 12.1](#121-valid-vs-validated) for code examples."},{"q":"@Transactional propagation modes — all of them?","a":"- **REQUIRED** (default) — join existing TX or create new\n- **REQUIRES_NEW** — always new TX; suspend existing (audit log survives parent rollback)\n- **NESTED** — nested TX with savepoint inside existing\n- **SUPPORTS** — use TX if exists; else run non-transactional\n- **NOT_SUPPORTED** — suspend existing TX; run non-transactional\n- **MANDATORY** — must have existing TX; else `TransactionException`\n- **NEVER** — must NOT have TX; else `TransactionException`"},{"q":"ApplicationContext vs BeanFactory?","a":"`BeanFactory` is the basic IoC container — **lazy** bean creation (bean created on first `getBean()`). `ApplicationContext` extends `BeanFactory` and adds: eager singleton init, event publishing (`ApplicationEventPublisher`), i18n (`MessageSource`), full AOP support, annotation processing (`@Autowired`, `@PostConstruct`), web support (`WebApplicationContext`). Spring Boot always uses `ApplicationContext` — never use `BeanFactory` directly in modern apps. See [Section 12.3](#123-applicationcontext-vs-beanfactory--deep)."},{"q":"JPA vs CrudRepository vs JpaRepository?","a":"**JPA** is the Java persistence specification (entities, `EntityManager`, JPQL). **CrudRepository** is a Spring Data interface providing `save()`, `findById()`, `findAll()`, `deleteById()`, `count()`. **JpaRepository** extends CrudRepository and adds `flush()`, `saveAndFlush()`, `deleteInBatch()`, `findAll(Pageable)`, `getReferenceById()`. In practice: extend `JpaRepository` for most Spring Boot apps; use custom `@Query` for complex joins. See [Section 12.5](#125-jpa-vs-crudrepository-vs-jparepository)."},{"q":"Saga orchestration vs choreography?","a":"**Choreography** — event-driven, no central coordinator. OrderService publishes `OrderCreated` → PaymentService reacts → publishes `PaymentCompleted` → InventoryService reacts. On failure, compensating events (`PaymentFailed` → OrderService cancels). Pros: loose coupling. Cons: hard to trace, complex compensation, event cycles. **Orchestration** — central `OrderSagaOrchestrator` calls each service step-by-step; runs compensations in reverse on failure. Pros: clear flow, easy monitoring. Cons: orchestrator is a dependency (must be HA). Section 9 also covers this. See [Section 12.11](#1211-saga--orchestration-vs-choreography-deep)."},{"q":"gRPC vs REST — when to use which in Spring?","a":"**REST** — HTTP/JSON, human-readable, browser-friendly, OpenAPI docs, best for **public/external APIs**. **gRPC** — HTTP/2, Protocol Buffers (binary, smaller/faster), strongly typed `.proto` contract, supports streaming, best for **inter-service communication** inside microservices where performance matters. Spring Boot: use `@RestController` for REST; use `grpc-spring-boot-starter` for gRPC. SQL is not comparable — it's the database query layer, not an API protocol. See [Section 12.9](#129-grpc-vs-rest-vs-sql)."},{"q":"HTTP vs HTTPS — how does it work?","a":"**HTTP** (port 80) — plaintext request/response over TCP; anyone on the network can read traffic. **HTTPS** (port 443) — HTTP + TLS: (1) TCP handshake, (2) TLS handshake — Client Hello → Server Hello + SSL certificate → client verifies CA → key exchange → symmetric session key established, (3) all HTTP traffic encrypted with AES. Asymmetric crypto used only during handshake; symmetric used for data (faster). See [Section 12.8](#128-http-vs-https--how-it-works)."},{"q":"TestNG vs JUnit — which for Spring Boot?","a":"**JUnit 5** is Spring Boot default (`spring-boot-starter-test`). Annotations: `@Test`, `@BeforeEach`, `@ParameterizedTest`. Extensions model for custom behavior. **TestNG** — test groups, `@Test(dependsOnMethods)`, `@DataProvider`, parallel execution via XML suites, better for complex test orchestration. Use JUnit 5 for standard Spring Boot projects. Use TestNG only if you need test dependencies, group-based CI pipelines, or XML suite configs. See [Section 12.7](#127-testng-vs-junit)."},{"q":"How to handle concurrent booking without synchronized?","a":"Layered approach:\n1. **Optimistic locking** — `@Version` on entity; `OptimisticLockException` if concurrent update\n2. **Pessimistic DB lock** — `@Lock(PESSIMISTIC_WRITE)` or `SELECT FOR UPDATE`\n3. **Redis distributed lock** — `SET key NX EX 600` for seat hold during payment\n4. **Semaphore** — limit concurrent DB operations in-process\n5. **Kafka partitioning** — ordered processing per partition\n6. **`ReentrantLock`** instead of `synchronized` (more flexible, no virtual-thread pinning)\nSee [Section 12.12](#1212-multithreading-resource-allocation-in-spring) for code."},{"q":"How does database connectivity work in Spring Boot?","a":"`application.yml` datasource properties → `DataSourceAutoConfiguration` → **HikariCP** connection pool (default) → `EntityManagerFactory` (Hibernate/JPA) → `JpaTransactionManager` → Spring Data **Repository** proxies → `@Transactional` service layer → JDBC → database. Key config: `spring.datasource.url`, Hikari `maximum-pool-size`, `spring.jpa.hibernate.ddl-auto=validate` in prod, Flyway/Liquibase for migrations. Flow: `Controller → @Transactional Service → Repository → Hibernate → HikariCP → PostgreSQL`. See [Section 12.6](#126-database-connectivity-in-spring-boot)."},{"q":"uery cache","a":"(separate): caches query result IDs — use sparingly, invalidation is tricky."},{"q":"What is AOP? Real-world use case?","a":"**Aspect-Oriented Programming** — apply cross-cutting logic (logging, security, transactions) **without duplicating code** in every method. Spring AOP uses proxies."},{"q":"How does Spring handle circular dependencies?","a":"See Section 13 and [12.4](#124-circular-dependency--how-it-happens-and-fixes). Constructor injection cycles fail. `@Lazy`, setter injection, or redesign."},{"q":"How to secure REST APIs (JWT, OAuth2)?","a":"- **JWT** — stateless; login returns signed token; client sends `Authorization: Bearer <token>`; server validates signature + expiry. Good for SPAs/mobile.\n- **OAuth2** — delegate auth to provider (Google, Okta); flows: Authorization Code (web), Client Credentials (service-to-service). Spring Security OAuth2 Resource Server validates tokens.\n- **Session-based** — server stores session; cookie-based; traditional web apps."},{"q":"How does Spring Boot auto-configuration work?","a":"See Section 11 Q&A. `@SpringBootApplication` → `@EnableAutoConfiguration` → reads `META-INF/spring/...AutoConfiguration.imports` → applies `@Conditional*` configs only when classpath/properties match."},{"q":"ApplicationContext vs BeanFactory?","a":"See Section 13. ApplicationContext = always use in Boot."}]},"03_System_Design_HLD.md":{"title":"High Level Design","trackId":"system-design","trackTitle":"System Design","sections":[{"id":"system-design-high-level-design-hld","heading":"System Design — High Level Design (HLD)","level":1,"text":""},{"id":"complete-guide-concepts-real-world-systems","heading":"Complete Guide: Concepts + Real-World Systems","level":3,"text":""},{"id":"1-how-to-approach-hld-interviews","heading":"1. How to Approach HLD Interviews","level":2,"text":""},{"id":"the-reshaded-framework","heading":"The RESHADED Framework","level":3,"text":""},{"id":"step-by-step-45-min-interview","heading":"Step-by-Step (45 min interview)","level":3,"text":""},{"id":"key-questions-to-ask","heading":"Key Questions to Ask","level":3,"text":"Theory. The first 5 minutes set up the rest of the interview. Weak requirements → wrong architecture. Always clarify functional (what features) and non functional (scale, latency, consistency) before drawing boxes. Example (worked estimation — \"Design a photo sharing feed\"). Clarify: 10M DAU, each posts 0.5 photos/day, views 50 photos/day. Write QPS: 10M × 0.5 / 86,400 ≈ 58 writes/sec (photos). Read QPS: 10M × 50 / 86,400 ≈ 5,800 reads/sec . Storage/day: 5M photos × 2 MB avg = 10 TB/day raw — likely need compression + CDN, not DB blob storage. Bandwidth: 5,800 reads/sec × 200 KB (thumbnail) ≈ 1.1 GB/sec peak — CDN mandatory. This math tells you: read heavy, CDN + object storage + cache, not a monolithic DB holding images. Interview angle. State assumptions out loud: \"I'll assume 10M DAU unless you say otherwise.\" Interviewers often correct you — that dialogue is part of the test."},{"id":"2-core-concepts-scalability","heading":"2. Core Concepts — Scalability","level":2,"text":""},{"id":"vertical-vs-horizontal-scaling","heading":"Vertical vs Horizontal Scaling","level":3,"text":"Theory. Vertical scaling hits a hardware ceiling (largest instance has finite CPU/RAM) and creates a single point of failure . Horizontal scaling trades simplicity for unlimited theoretical capacity — but requires stateless app design, load balancing, and distributed data stores. Example. API needs 8K RPS. One 4 core server handles 2K RPS → need 4 servers behind a load balancer (horizontal). Alternative: one 16 core machine (vertical) — simpler but one failure takes down everything; 16 core machine may cost more than 4× 4 core due to exponential pricing. Interview angle. Start vertical for MVP; go horizontal when single node limits or HA requirements demand it."},{"id":"load-balancing","heading":"Load Balancing","level":3,"text":"Theory. A load balancer is the traffic cop in front of stateless app servers. It terminates connections (optional SSL), picks a backend, and health checks instances. Without it, one server gets all traffic and others sit idle — or a dead server still receives requests. How it works (L4 vs L7). L4 sees IP + port only — fast, protocol agnostic. L7 parses HTTP — can route to order service and to user service on the same port. L7 enables sticky sessions via cookies. Example. 3 app servers, each handles 2K RPS max. Total need: 5K RPS. LB with round robin spreads 1,667 RPS each — within capacity. One server dies → LB stops routing to it after failed health checks ( 5–30s detection interval). Pitfall. Round robin with unequal request cost (one endpoint is 10× heavier) → one server overloaded while others idle. Use least connections or weighted routing instead. Interview angle. \"Where does the load balancer sit?\" → Between clients and app tier. Not between app and DB (that's connection pooling / read replicas / sharding)."},{"id":"cdn-content-delivery-network","heading":"CDN (Content Delivery Network)","level":3,"text":"Theory. A CDN puts copies of content geographically close to users . Without it, a user in Asia hitting a US origin adds 150 ms RTT per request. With CDN edge in Asia: 20–50 ms . How it works. DNS resolves to nearest edge (Anycast or GeoDNS). Edge checks cache; on miss, fetches from origin (one slow request), caches locally, serves subsequent users in that region from edge. Example. 1M users load a 500 KB JS bundle. Without CDN: 1M × 500 KB = 500 GB from origin. With CDN (90% hit rate after warm up): 100 GB from origin + 400 GB served from edges — origin bandwidth drops 5×, user latency drops 3–10×. Interview angle. CDN for static assets always. For API responses , only if cacheable (short TTL, user agnostic data like product catalog). Never CDN cache personalized/authenticated responses without careful cache key design."},{"id":"3-databases","heading":"3. Databases","level":2,"text":""},{"id":"sql-vs-nosql","heading":"SQL vs NoSQL","level":3,"text":"Aspect SQL (Relational) NoSQL Schema Fixed, predefined Flexible, dynamic Scaling Vertical (mostly) Horizontal (designed for) Transactions ACID BASE (mostly) Joins Yes Limited or none Consistency Strong Eventual (configurable) Best for Complex queries, transactions High scale, flexible schema"},{"id":"when-to-use-which","heading":"When to Use Which","level":3,"text":""},{"id":"database-replication","heading":"Database Replication","level":3,"text":""},{"id":"database-sharding","heading":"Database Sharding","level":3,"text":"Theory. Sharding is the last resort for write/storage scale. Before sharding, exhaust: query tuning, indexes, read replicas, caching, connection pooling, and table partitioning. Example (capacity math). Single PostgreSQL primary: 5K writes/sec sustainable, 2 TB practical limit before maintenance pain. Need 20K writes/sec and 10 TB → at least 4 write shards (rough order of magnitude — validate with load tests). How replication fits. 1 primary + 3 read replicas: writes still capped at primary rate ( 5K/sec), reads scale to 4× (each replica 10K reads/sec with proper indexing) ≈ 40K read/sec total. Interview angle. Always say: \"I'd start with a single DB + replicas + cache. Shard when metrics prove we can't scale vertically or replicate reads enough.\""},{"id":"database-indexing","heading":"Database Indexing","level":3,"text":""},{"id":"4-caching","heading":"4. Caching","level":2,"text":""},{"id":"caching-levels","heading":"Caching Levels","level":3,"text":""},{"id":"cache-strategies","heading":"Cache Strategies","level":3,"text":"Theory. Cache aside (lazy loading) is the default because you only cache what's actually read — no wasted memory on write through for cold data. The app owns invalidation logic: write DB → delete cache key . Example (read ratio math). Product page: 50K reads/sec, 100 writes/sec. Cache hit rate 95% → DB sees 2,500 reads/sec + 100 writes/sec instead of 50,100 reads/sec. Redis handles 47,500 reads/sec at 1 ms each. Pitfall. Cache aside without TTL jitter on hot keys → stampede when key expires (see Cache Problems below). Always pair with mutex or logical expiry for viral content."},{"id":"cache-invalidation-strategies","heading":"Cache Invalidation Strategies","level":3,"text":""},{"id":"cache-problems-and-solutions","heading":"Cache Problems and Solutions","level":3,"text":""},{"id":"redis-deep-dive","heading":"Redis Deep Dive","level":3,"text":""},{"id":"5-message-queues","heading":"5. Message Queues","level":2,"text":""},{"id":"why-use-message-queues","heading":"Why Use Message Queues?","level":3,"text":""},{"id":"kafka-deep-dive","heading":"Kafka Deep Dive","level":3,"text":"Theory. Kafka is a distributed commit log , not a traditional message queue. Messages are retained after consumption — consumers track their own offset (position). This enables replay, multiple consumer groups reading the same data independently, and decoupling producers from consumer speed. How it works (throughput). One broker with 3 partitions can handle 100K–1M msgs/sec depending on message size and replication. Partition count = max parallelism per consumer group (one consumer per partition). Need 12 parallel consumers → at least 12 partitions. Example (ordering). Order events for must be processed in sequence → publish all events with key=42 → same partition → strict order. Events for different orders can parallelize across partitions. Interview angle. \"Kafka vs RabbitMQ?\" → Kafka: high throughput, replay, log retention, stream processing. RabbitMQ: task queues, complex routing, message deleted after ack — better for RPC style job dispatch."},{"id":"kafka-vs-rabbitmq","heading":"Kafka vs RabbitMQ","level":3,"text":"Feature Kafka RabbitMQ Model Log based (pull) Message broker (push) Throughput Very high (millions/sec) High (tens of thousands/sec) Message retention Configurable (days) Deleted after consumption Replay Yes (rewind offset) No Ordering Per partition Per queue Routing Topic/partition Exchange + binding rules (fanout, direct, topic, headers) Use case Event streaming, log aggregation Task queues, RPC, complex routing"},{"id":"6-cap-theorem-consistency","heading":"6. CAP Theorem & Consistency","level":2,"text":""},{"id":"cap-theorem","heading":"CAP Theorem","level":3,"text":"Theory (common misconception). CAP is not \"pick 2 and drop 1 forever.\" It's: during a network partition , you must choose between consistency and availability. During normal operation, many systems provide both. P is non optional in distributed systems — networks fail. How it works (partition scenario). Primary and replica lose connectivity. CP system: reject writes until quorum confirms (unavailable to some clients). AP system: accept writes on both sides → conflict when partition heals (availability over consistency). Example. Payment ledger during partition: CP — block writes until primary reachable (prefer downtime over double spend). Social media \"like count\": AP — show approximate count, reconcile later. Interview angle. \"Is PostgreSQL CA or CP?\" → Single node: CA (no partition). With synchronous replication quorum: CP during partition (writes blocked if can't reach quorum)."},{"id":"acid-vs-base","heading":"ACID vs BASE","level":3,"text":""},{"id":"consistency-patterns","heading":"Consistency Patterns","level":3,"text":"Theory. Most production systems pick a pragmatic middle ground — not full strong consistency everywhere (too slow), not pure eventual everywhere (bad UX for own writes). Example (read your writes). User posts a comment, immediately refreshes feed. Route that user's reads to primary (or session sticky replica) for 30s after write — they see their comment. Other users on replicas see it after replication lag ( 100 ms–2 s). Example (monotonic reads). User on replica A reads . Next read routed to replica B which is behind → sees . Violates monotonic reads. Fix: sticky routing — same user always hits same replica (or primary for financial data). Interview angle. Name the consistency level your design provides and why it's enough — \"eventual for feed, read your writes for profile edits.\""},{"id":"7-rate-limiting","heading":"7. Rate Limiting","level":2,"text":""},{"id":"why-rate-limit","heading":"Why Rate Limit?","level":3,"text":""},{"id":"algorithms","heading":"Algorithms","level":3,"text":"Theory. Rate limiting protects your service and downstream dependencies from abuse and accidental retry storms. In distributed systems, the counter must live in shared storage (Redis) — per instance counters are ineffective behind a load balancer. Example (boundary attack on fixed window). Limit: 100 req/min. User sends 100 requests at 00:00:59 and 100 at 00:01:00 → 200 requests in 2 seconds despite \"100/minute\" rule. Sliding window or token bucket closes this gap. Example (token bucket). Bucket size 100, refill 10/sec. User sends burst of 100 → allowed. Then sustained 10/sec → allowed. Sustained 50/sec → bucket drains in 2s, then 429 until tokens refill. Good for APIs that tolerate short bursts. Interview angle. Always mention 429 Too Many Requests + header + idempotency for retried writes."},{"id":"distributed-rate-limiting","heading":"Distributed Rate Limiting","level":3,"text":""},{"id":"8-consistent-hashing","heading":"8. Consistent Hashing","level":2,"text":""},{"id":"problem-with-regular-hashing","heading":"Problem with Regular Hashing","level":3,"text":""},{"id":"consistent-hashing","heading":"Consistent Hashing","level":3,"text":"Theory. Regular hashing remaps almost all keys when N changes (add/remove server). Consistent hashing limits remapping to K/N keys (K = total keys) — critical for caches and distributed stores where moving data is expensive. How it works (numeric example). Ring from 0 to 2³²−1. Three servers at positions 0, 1.4B, 2.8B (hashed from their IDs). Key \"user:42\" hashes to 500M → clockwise → lands on server at 1.4B. Add server D at 2B → only keys between 1.4B and 2B move from old server to D ( 25% of keys with 4 servers, not 100%). Remove server → its keys move to next clockwise neighbor only. Example (virtual nodes). 3 physical servers with 1 vnode each → uneven load (one server might get 50% of keys by unlucky hash clustering). Each physical server gets 150 vnodes on the ring → load within 10% of perfect balance. Pitfall. Consistent hashing doesn't solve hot keys — a popular key still lands on one node. Combine with key replication or local caching for hot spots. Interview angle. Draw the ring. Show that adding a 4th node moves 1/4 of keys, not all of them. Mention vnodes for even distribution."},{"id":"9-distributed-systems-concepts","heading":"9. Distributed Systems Concepts","level":2,"text":""},{"id":"distributed-id-generation","heading":"Distributed ID Generation","level":3,"text":"How it works (Snowflake). 64 bit ID: 41 bit timestamp (ms since epoch) + 10 bit machine ID + 12 bit sequence. IDs sort chronologically — great for B tree inserts (no random UUID fragmentation). At 4096 IDs/ms per machine, one machine generates 4M IDs/sec — far more than most services need. Example. Order IDs: , — time sortable, URL safe as decimal string, fits in . Compare to UUIDv4: random, 36 chars, bad for clustered index insert order. Pitfall (clock skew). Machine clock goes backward → duplicate ID risk. NTP sync + refuse to generate until clock catches up. Interview mention shows depth."},{"id":"bloom-filter","heading":"Bloom Filter","level":3,"text":"Theory. A Bloom filter answers set membership with no false negatives but possible false positives . You never wrongly reject a valid key, but you might think a key exists when it doesn't ( 1% with proper sizing). How it works. Bit array of size m, k hash functions. Insert \"user:42\": hash to positions 3, 17, 99 → set those bits to 1. Check \"user:99\": hash to 3, 44, 91 → bit 44 is 0 → definitely not in set (skip DB). Check \"user:42\": all bits 1 → probably in set (query DB to confirm). Example. 10M registered usernames, Bloom filter 12 MB (vs 10M × 50 bytes = 500 MB for a hash set). Signup flow: check Bloom first → if \"no\", skip DB uniqueness check. False positive 1% → one extra DB lookup occasionally. Interview angle. \"Why not just use Redis SET?\" → Memory. Bloom filter is 10–50× smaller for large sets where false positives are acceptable."},{"id":"distributed-locks","heading":"Distributed Locks","level":3,"text":""},{"id":"event-sourcing-cqrs","heading":"Event Sourcing & CQRS","level":3,"text":""},{"id":"10-real-world-system-designs","heading":"10. Real-World System Designs","level":2,"text":""},{"id":"101-url-shortener-like-bitly","heading":"10.1 URL Shortener (like bit.ly)","level":3,"text":"Requirements: 100M URLs shortened per day, 10B redirections per day URLs expire after 5 years, custom aliases supported Low latency reads (< 10ms) Estimation: Write QPS: 100M / 86400 ≈ 1,200 writes/sec Read QPS: 10B / 86400 ≈ 115,000 reads/sec (100x read heavy) Storage: 100M 365 5 500 bytes ≈ 91 TB over 5 years Design: Database Schema:"},{"id":"102-rate-limiter-design-the-service","heading":"10.2 Rate Limiter (Design the Service)","level":3,"text":"Requirements: Support multiple rate limiting rules (per user, per IP, per endpoint) Low latency (< 1ms overhead) Distributed (multiple servers) Rule changes without restart Design:"},{"id":"103-whatsapp-chat-system","heading":"10.3 WhatsApp / Chat System","level":3,"text":"Requirements: 1:1 and group messaging (max 100 members) Message delivery receipts (sent ✓, delivered ✓✓, read ✓✓ blue) Online/offline status File/media sharing End to end encryption Estimation: 500M DAU, average 40 messages/day = 20B messages/day Message size average 100 bytes → 2 TB/day message data Active connections: 500M persistent connections Design:"},{"id":"104-netflix-video-streaming","heading":"10.4 Netflix / Video Streaming","level":3,"text":"Requirements: Upload and stream videos Multiple resolutions (360p, 720p, 1080p, 4K) Support millions concurrent viewers Personalized recommendations Low buffering Design:"},{"id":"105-twitter-news-feed-facebook-feed","heading":"10.5 Twitter News Feed / Facebook Feed","level":3,"text":"Requirements: Post tweets (text, images, videos) Follow other users News feed: see tweets from followed users Real time updates Trending topics Feed Generation Approaches: Trending Topics:"},{"id":"106-design-a-notification-system","heading":"10.6 Design a Notification System","level":3,"text":"Requirements: Send notifications via Email, SMS, Push notification Support multiple providers (failover) 10M notifications/day At least once delivery User notification preferences Design:"},{"id":"107-design-a-search-autocomplete-system","heading":"10.7 Design a Search Autocomplete System","level":3,"text":"Requirements: Return top K suggestions as user types (each keystroke) Suggestions ranked by popularity/relevance < 100ms latency Support 10M queries/day Design:"},{"id":"108-uber-ride-sharing","heading":"10.8 Uber / Ride Sharing","level":3,"text":"Requirements: Rider requests ride Match with nearest available driver Real time location tracking Price estimation Trip management, payments Design:"},{"id":"common-interview-tradeoffs","heading":"COMMON INTERVIEW TRADEOFFS","level":2,"text":"Decision Option A Option B When to choose Consistency Strong Eventual Banking: Strong; Social media: Eventual Storage SQL NoSQL Complex queries: SQL; High scale flexible: NoSQL Caching In memory (Caffeine) Distributed (Redis) Single instance: In memory; Multi instance: Redis Messaging Synchronous REST Async Kafka Simple: REST; Decoupled + scale: Kafka ID generation UUID Snowflake Not sorted: UUID; Time sorted + compact: Snowflake Feed generation Fan out on write Fan out on read Low followers: write; Celebrities: read or hybrid Search SQL LIKE Elasticsearch Simple: SQL; Full text, ranking: ES"},{"id":"estimation-cheat-sheet","heading":"ESTIMATION CHEAT SHEET","level":2,"text":""},{"id":"11-deep-dive-protocols-architecture","heading":"11. DEEP DIVE: Protocols & Architecture","level":2,"text":""},{"id":"111-http-vs-https-full-working","heading":"11.1 HTTP vs HTTPS — Full Working","level":3,"text":"Interview points: HTTPS does NOT hide destination IP/domain (SNI visible) Certificate pinning — app trusts only specific cert (mobile apps) HSTS — browser forces HTTPS for domain TLS 1.3 — faster handshake (1 RTT), removed weak ciphers"},{"id":"112-grpc-vs-rest-vs-sql","heading":"11.2 gRPC vs REST vs SQL","level":3,"text":"Dimension REST gRPC SQL What is it? Architectural style RPC framework Data query language Transport HTTP/1.1 or HTTP/2 HTTP/2 only JDBC/ODBC to DB Format JSON/XML Protocol Buffers (binary) Relational tuples Schema Optional (OpenAPI) Required (.proto) DB schema (DDL) Performance Good Better (binary, HTTP/2 multiplexing) Depends on query/index Streaming SSE, WebSocket Native bidirectional streaming Cursor/result set Code gen Optional Required (stubs from .proto) ORM generates entities Use case Public APIs, browsers Microservice internal calls Persistence layer"},{"id":"113-monolithic-vs-microservices-deep-comparison","heading":"11.3 Monolithic vs Microservices — Deep Comparison","level":3,"text":"Aspect Monolithic Microservices Codebase Single repo, single deploy unit Multiple repos/services Scaling Scale entire application Scale individual bottlenecks Technology One stack Polyglot possible Data Usually one shared DB Database per service Transactions ACID easy Distributed saga, eventual consistency Deployment Simple, all or nothing Independent, CI/CD per service Debugging Single process, easy trace Distributed tracing (Jaeger, Zipkin) Team Small team ideal Conway's law — team per service Latency In process calls (μs) Network calls (ms) Failure Blast radius = entire app Isolated per service When monolith: MVP, small team (<10), simple domain, low scale. When microservices: Independent scaling needs, large teams, clear bounded contexts, mature DevOps."},{"id":"114-saga-pattern-orchestration-vs-choreography","heading":"11.4 Saga Pattern — Orchestration vs Choreography","level":3,"text":""},{"id":"115-error-vs-exception-in-distributed-systems","heading":"11.5 Error vs Exception in Distributed Systems","level":3,"text":"Error (system level): service crash, OOM, network partition — design for retry, circuit breaker, fallback Exception (application level): invalid input, not found — return 4xx; business rule violation — return 422 Checked vs unchecked matters in Java code; in HLD discuss HTTP status codes and idempotency instead Theory. In distributed HLD, classify failures as transient (retry) vs permanent (don't retry). Transient: 503, timeout, connection reset. Permanent: 400 bad input, 404 not found, 409 conflict. Example. Payment service returns 503 → order service retries 3× with exponential backoff (1s, 2s, 4s), then publishes for manual review. Payment returns 400 invalid card → no retry, return error to user immediately. How it works (idempotency). Retries cause duplicate requests. with → server stores result keyed by UUID; duplicate POST returns same response without double charging. Interview angle. \"What happens if the client times out but server succeeded?\" → Idempotency keys + at least once delivery with deduplication on server."},{"id":"12-deep-dive-booking-concurrency-systems","heading":"12. DEEP DIVE: Booking & Concurrency Systems","level":2,"text":""},{"id":"121-movieticketseat-booking-system","heading":"12.1 Movie/Ticket/Seat Booking System","level":3,"text":"Requirements: Browse shows, select seats, book, pay No double booking (two users same seat) Handle 10K concurrent users during sale opening Seat map real time availability High Level Design: Concurrency strategy (layered): 1. API Gateway rate limiting — token bucket per user/IP (prevent thundering herd) 2. Redis distributed lock — (10 min hold during payment) 3. DB pessimistic lock — inside transaction 4. Optimistic locking — column; update fails if version changed 5. Queue based booking — high demand sales: user enters queue (Kafka), processed in order Semaphore analogy (application level): limit 100 concurrent DB transactions to protect connection pool. Theory. Seat booking is a classic compare and swap at scale problem: many users compete for finite resources. No single trick is enough — you layer defenses from edge to DB. How it works (why layered locks). 1. Rate limit at gateway — stops 100K bots from reaching booking service; humans only ( 10K). 2. Redis lock — fast in memory check; fails in <1 ms if seat taken (no DB hit for obvious conflicts). 3. DB pessimistic lock — authoritative; prevents double book even if two Redis locks race (TTL expiry edge case). 4. Queue (optional) — for flash sales, serialize access so DB sees orderly 100 concurrent bookings, not 10K simultaneous. Example (timeline). 10K users grab seat A1 at sale open. Gateway rate limit → 2K reach service/sec. Redis: first wins; 9,999 get instant \"seat taken.\" Winner gets 10 min to pay. Payment timeout → lock expires → seat released. Pitfall. Redis lock without DB validation → two users both think they hold the seat if lock expires during slow payment. Always confirm with DB transaction before marking CONFIRMED. Interview angle. \"Why not only DB lock?\" → DB lock per seat selection under 10K concurrent users exhausts connection pool and creates lock wait chains. Redis filters 99% of conflicts before DB."},{"id":"122-stock-price-tracker-multiple-events-priority-queue","heading":"12.2 Stock Price Tracker — Multiple Events (Priority Queue)","level":3,"text":"Theory. When events arrive out of order or from multiple sources, you need a structure that always gives you the next event to process by timestamp or price priority. For processing multiple buy/sell events in time order: Min heap for buy prices (want lowest buy) Max heap for sell prices (want highest sell) Or single PriorityQueue ordered by timestamp for event replay Note on order books. In a real matching engine, bids (buy side) typically use a max heap (highest buyer first) and asks (sell side) use a min heap (lowest seller first). The bullets above describe generic price sorted processing; adapt heap direction to whether you want min or max at the top for your use case. How it works (matching engine sketch). Buy order at $100 → push to max heap of bids (highest bid wins). Sell order at $99 → if best bid ≥ $99, match (trade executes); else push sell to min heap of asks. Events with timestamps: PriorityQueue ordered by ensures deterministic replay even when two events share a timestamp. Example. Events arrive: . t=1: bid heap = {$100}. t=2: sell $99 ≤ best bid $100 → match at $99.5 or $100 (price time priority rules vary). t=3: bid $98 — no match if lowest ask $98. Interview angle. \"How do you handle events arriving out of order?\" → Buffer in a priority queue keyed by event timestamp; process when watermark passes (allow small delay window) or use sequence numbers per instrument."},{"id":"13-tough-interview-qa-bank","heading":"13. Tough Interview Q&A Bank","level":2,"text":"Q: HTTP vs HTTPS? A: HTTP = plaintext over TCP port 80. HTTPS = HTTP + TLS on port 443. TLS handshake exchanges certificate, establishes symmetric session key, then encrypts all traffic. See Section 11.1. Q: gRPC vs REST? A: REST = HTTP/JSON, human readable, browser friendly. gRPC = HTTP/2 + Protobuf, strongly typed, streaming, faster for inter service. SQL is not comparable — it's the database query layer. Q: Monolithic vs microservices? A: Monolith = single deploy, shared DB, simple ops. Microservices = independent deploy/scale, DB per service, distributed transactions via Saga. See Section 11.3. Q: Saga orchestration vs choreography? A: Choreography = event driven, decentralized. Orchestration = central coordinator with explicit compensation. See Section 11.4. Q: How to prevent double booking? A: Redis distributed lock + DB pessimistic/optimistic lock + idempotent booking API + payment timeout release. Q: CAP theorem? A: In partition, choose Consistency (CP) or Availability (AP). CA only without partition. Q: Strong vs eventual consistency? A: Strong = read always latest write (banking). Eventual = replicas converge (social feeds). Q: Cache invalidation strategies? A: TTL, write through, write behind, cache aside (lazy load + invalidate on update). Q: Rate limiting algorithms? A: Token bucket (burst allowed), leaky bucket (smooth rate), fixed/sliding window. Q: How does consistent hashing help? A: Adding/removing servers only remaps K/n keys (not all keys). Used in distributed caches, load balancers."},{"id":"14-system-design-interview-problems","heading":"14. System Design Interview Problems","level":2,"text":""},{"id":"141-design-an-e-commerce-order-tracking-system","heading":"14.1 Design an E-Commerce Order Tracking System","level":3,"text":"Requirements: Track order from placement → payment → shipped → delivered. Real time status for users. 10M orders/day. Read heavy. Estimation. Write QPS: 10M / 86,400 ≈ 116 orders/sec (creates + status updates maybe 3× → 350 writes/sec peak). Read QPS: Assume each order checked 5× during lifecycle, 10M active orders/day viewed → 580 reads/sec average; peak 3× ≈ 1,750 reads/sec . Storage: 10M orders/day × 365 × 500 bytes/order row ≈ 1.8 TB/year for orders table; event log similar — partition/archive after 1 year. High Level Design: Data model: Flow: 1. Order placed → write to + publish to Kafka 2. Each service (payment, warehouse, shipping) publishes status events 3. Tracking service consumes events → updates + 4. User polls → cache in Redis (TTL 30s) Scale: Read replicas for DB; Redis cache hot orders; WebSocket/SSE for live updates on tracking page. APIs:"},{"id":"142-design-a-rate-limiter","heading":"14.2 Design a Rate Limiter","level":3,"text":"Requirements: Limit API to N requests per user per minute. Distributed (multiple API servers). Low latency. Algorithms: Algorithm How Burst? Token bucket Tokens refill at fixed rate; request consumes 1 token Yes (bucket capacity) Leaky bucket Requests queue, processed at fixed rate No Fixed window Count requests per minute window Boundary spike at window edge Sliding window Count in rolling last 60 seconds Smoother Recommended: Token bucket or sliding window log in Redis . Architecture: Where to enforce: API Gateway (Kong, NGINX, Spring Cloud Gateway) or sidecar. Return + header. Follow up: Rate limit per IP vs per API key vs per user tier (free=100/min, premium=1000/min)."},{"id":"143-design-a-url-shortener-like-bitly","heading":"14.3 Design a URL Shortener (like bit.ly)","level":3,"text":"Requirements: Shorten long URL; redirect; 100M URLs; 10:1 read/write ratio; custom aliases optional. APIs: Design: Short code generation: Base62 encode auto increment ID (a zA Z0 9) → 7 chars = 62^7 URLs Or hash long URL + collision check Custom alias: check uniqueness in DB DB schema: Redirect: Use 301 (permanent, cached by browser) or 302 (track every click — most shorteners use 302 for analytics). Scale: Redis cache 80/20 hot URLs; DB sharding by hash; CDN for redirect edge."},{"id":"144-design-a-distributed-key-value-store-like-redis","heading":"14.4 Design a Distributed Key-Value Store (like Redis)","level":3,"text":"Requirements: / / by key; high throughput; replication; eventual consistency OK for some use cases. Core components: Partitioning: Consistent hashing — key → hash ring → responsible node. Virtual nodes for even distribution. Replication: Primary replica per partition. Writes to primary → async replicate. Read from replica (eventual consistency) or primary (strong). Data structures on disk: Redis uses in memory + optional AOF/RDB persistence. For disk based KV: LSM tree (RocksDB) or B tree. CAP trade off: AP for cache (availability + partition tolerance); CP variant for strong consistency (Raft consensus per shard — like etcd). Operations: Failure handling: Primary dies → replica promoted (leader election). Hinted handoff during temporary node failure. Not building Redis from scratch in 45 min — explain: in memory hash table + single threaded event loop + optional persistence + cluster mode with hash slots (16384 slots)."},{"id":"145-design-a-user-authentication-system","heading":"14.5 Design a User Authentication System","level":3,"text":"Requirements: Register, login, logout; secure password storage; session or JWT; OAuth social login; 10M users. Architecture: Registration flow: 1. → hash password ( bcrypt/Argon2 , never plain text) 2. Store user + 3. Send verification email with signed token 4. → set Login flow (JWT): 1. { email, password } 2. Verify bcrypt hash 3. Issue access token (15 min) + refresh token (7 days, stored in DB/Redis) 4. Client sends 5. On expiry → with refresh token → new access token Login flow (Session): 1. Validate credentials → create session ID → store in Redis 2. Set secure cookie 3. Each request: validate session ID in Redis OAuth2 social login: Security checklist: bcrypt passwords (cost factor 12+) HTTPS only Rate limit login (brute force protection) MFA optional (TOTP) Refresh token rotation Token blacklist on logout (Redis) CSRF protection for cookie sessions DB schema:"}],"qa":[{"q":"HTTP vs HTTPS?","a":"HTTP = plaintext over TCP port 80. HTTPS = HTTP + TLS on port 443. TLS handshake exchanges certificate, establishes symmetric session key, then encrypts all traffic. See Section 11.1."},{"q":"gRPC vs REST?","a":"REST = HTTP/JSON, human-readable, browser-friendly. gRPC = HTTP/2 + Protobuf, strongly typed, streaming, faster for inter-service. SQL is not comparable — it's the database query layer."},{"q":"Monolithic vs microservices?","a":"Monolith = single deploy, shared DB, simple ops. Microservices = independent deploy/scale, DB per service, distributed transactions via Saga. See Section 11.3."},{"q":"Saga orchestration vs choreography?","a":"Choreography = event-driven, decentralized. Orchestration = central coordinator with explicit compensation. See Section 11.4."},{"q":"How to prevent double booking?","a":"Redis distributed lock + DB pessimistic/optimistic lock + idempotent booking API + payment timeout release."},{"q":"CAP theorem?","a":"In partition, choose Consistency (CP) or Availability (AP). CA only without partition."},{"q":"Strong vs eventual consistency?","a":"Strong = read always latest write (banking). Eventual = replicas converge (social feeds)."},{"q":"Cache invalidation strategies?","a":"TTL, write-through, write-behind, cache-aside (lazy load + invalidate on update)."},{"q":"Rate limiting algorithms?","a":"Token bucket (burst allowed), leaky bucket (smooth rate), fixed/sliding window."},{"q":"How does consistent hashing help?","a":"Adding/removing servers only remaps K/n keys (not all keys). Used in distributed caches, load balancers."}]},"04_System_Design_LLD.md":{"title":"Low Level Design","trackId":"system-design","trackTitle":"System Design","sections":[{"id":"system-design-low-level-design-lld","heading":"System Design — Low Level Design (LLD)","level":1,"text":""},{"id":"solid-principles-design-patterns-real-world-problems","heading":"SOLID Principles + Design Patterns + Real-World Problems","level":3,"text":""},{"id":"1-solid-principles","heading":"1. SOLID Principles","level":2,"text":"Theory. SOLID is five object oriented design principles (popularized by Robert C. Martin) that together produce code which is easy to change, test, and extend — the foundation of good Low Level Design. Most design pattern choices and \"is this a good design?\" judgments trace back to one of these principles, so treat them as a review checklist: a violation almost always shows up as code that's hard to modify without breaking something elsewhere. The mnemonic spells SOLID — S ingle Responsibility (a class has one reason to change), O pen/Closed (extend without modifying), L iskov Substitution (subtypes are drop in replacements), I nterface Segregation (many small interfaces over one fat one), and D ependency Inversion (depend on abstractions, not concretions)."},{"id":"s-single-responsibility-principle","heading":"S — Single Responsibility Principle","level":3,"text":"A class should have only ONE reason to change."},{"id":"o-openclosed-principle","heading":"O — Open/Closed Principle","level":3,"text":"Open for extension, Closed for modification."},{"id":"l-liskov-substitution-principle","heading":"L — Liskov Substitution Principle","level":3,"text":"Subtypes must be substitutable for their base types without breaking the program."},{"id":"i-interface-segregation-principle","heading":"I — Interface Segregation Principle","level":3,"text":"Clients should not be forced to depend on interfaces they don't use."},{"id":"d-dependency-inversion-principle","heading":"D — Dependency Inversion Principle","level":3,"text":"High level modules should not depend on low level modules. Both should depend on abstractions."},{"id":"2-other-design-principles","heading":"2. Other Design Principles","level":2,"text":""},{"id":"3-design-patterns","heading":"3. Design Patterns","level":2,"text":"Theory. Design patterns are named, reusable solutions to recurring design problems — a shared vocabulary so that saying \"use a Strategy here\" instantly conveys a whole structure to another engineer. They aren't code to copy blindly but templates you adapt to your context; reaching for patterns you don't need is itself an anti pattern (remember YAGNI). The classic 23 Gang of Four patterns split into three families based on what problem they solve: Creational patterns control how objects are created (Singleton, Factory Method, Abstract Factory, Builder, Prototype); Structural patterns control how objects are composed into larger structures (Adapter, Decorator, Facade, Proxy, Composite, Flyweight, Bridge); and Behavioral patterns control how objects interact and distribute responsibility (Strategy, Observer, Command, State, Template Method, Chain of Responsibility, Iterator, Mediator, and more). The real skill is recognizing which problem you have, then picking the pattern that fits."},{"id":"creational-patterns","heading":"Creational Patterns","level":3,"text":"Singleton Factory Method Abstract Factory Builder Prototype"},{"id":"structural-patterns","heading":"Structural Patterns","level":3,"text":"Adapter Decorator Proxy Facade Composite Flyweight"},{"id":"behavioral-patterns","heading":"Behavioral Patterns","level":3,"text":"Observer Strategy Command Chain of Responsibility Template Method State Iterator Mediator"},{"id":"4-lld-problems-with-solutions","heading":"4. LLD Problems with Solutions","level":2,"text":""},{"id":"problem-1-parking-lot","heading":"Problem 1: Parking Lot","level":3,"text":"Requirements: Multiple floors, multiple spots per floor Spot types: Motorcycle, Compact, Large Vehicles: Motorcycle, Car, Truck Ticket on entry, billing on exit Track availability in real time"},{"id":"problem-2-elevator-system","heading":"Problem 2: Elevator System","level":3,"text":""},{"id":"problem-3-bookmyshow-movie-ticket-booking","heading":"Problem 3: BookMyShow (Movie Ticket Booking)","level":3,"text":""},{"id":"problem-4-atm-machine","heading":"Problem 4: ATM Machine","level":3,"text":""},{"id":"problem-5-splitwise","heading":"Problem 5: Splitwise","level":3,"text":""},{"id":"problem-6-chess-game","heading":"Problem 6: Chess Game","level":3,"text":""},{"id":"5-how-to-approach-lld-interviews","heading":"5. How to Approach LLD Interviews","level":2,"text":""},{"id":"step-by-step-process-30-40-min","heading":"Step-by-Step Process (30-40 min)","level":3,"text":""},{"id":"common-patterns-in-lld-problems","heading":"Common Patterns in LLD Problems","level":3,"text":""},{"id":"interview-tips","heading":"Interview Tips","level":3,"text":""},{"id":"6-deep-dive-booking-system-lld","heading":"6. DEEP DIVE: Booking System LLD","level":2,"text":""},{"id":"61-requirements-clarification","heading":"6.1 Requirements Clarification","level":3,"text":""},{"id":"62-class-diagram-core-entities","heading":"6.2 Class Diagram (Core Entities)","level":3,"text":""},{"id":"63-full-implementation-sketch","heading":"6.3 Full Implementation Sketch","level":3,"text":""},{"id":"64-design-patterns-used","heading":"6.4 Design Patterns Used","level":3,"text":"Pattern Where Why Singleton BookingSystem One central coordinator Strategy Pricing (Regular vs Premium vs Weekend) OCP — add pricing without modifying booking State SeatStatus, BookingStatus Clear state transitions Observer Notify user on confirm/cancel Decouple notification Factory Create different ticket types Encapsulate creation"},{"id":"65-concurrency-without-synchronized-keyword","heading":"6.5 Concurrency Without synchronized Keyword","level":3,"text":"Alternatives demonstrated: 1. for seat registry 2. for limiting concurrent DB writes 3. per seat (finer than synchronized method) 4. Database as source of truth 5. Immutable object once confirmed"},{"id":"66-interview-follow-up-questions","heading":"6.6 Interview Follow-Up Questions","level":3,"text":"Q: How to handle user selecting 4 seats but only 3 available? A: All or nothing lock — try lock all, if any fails release all already locked (rollback). Q: How to extend for multiple theatres? A: → → hierarchy. BookingSystem becomes facade per theatre or single system with theatreId. Q: How to add VIP pricing? A: Strategy pattern — interface, , , ."},{"id":"7-tough-interview-qa-bank","heading":"7. Tough Interview Q&A Bank","level":2,"text":"Q: Explain all SOLID principles with one example each? A: SRP — one class one job. OCP — extend via interface not modify. LSP — subclass substitutable. ISP — small interfaces. DIP — depend on abstractions. Q: Singleton in LLD — when and pitfalls? A: Use for ParkingLot, BookingSystem, ATM (one instance). Thread safe: enum or holder pattern. Testability: consider dependency injection instead. Q: Strategy vs State pattern? A: Strategy — client chooses algorithm (pricing). State — object behavior changes with internal state (ATM states). Q: How to design Parking Lot LLD? A: ParkingLot (singleton) → Floor → Spot. Vehicle types (Bike, Car, Truck) map to spot types. Strategy for pricing. Factory for vehicle creation. Q: How to design Elevator LLD? A: Elevator state machine (IDLE, MOVING UP, MOVING DOWN). Request queue (SCAN algorithm). Observer for floor arrival display. Q: How to design Splitwise LLD? A: User, Expense, Split (equal/exact/percentage). Minimize transactions via greedy debt settlement. Graph of who owes whom. Q: Composition vs Inheritance? A: Prefer composition (HAS A) over inheritance (IS A) unless true subtype relationship. Composition is more flexible (OCP). Q: How to make LLD thread safe? A: synchronized, ReentrantLock, ConcurrentHashMap, immutable objects, atomic operations, or delegate to DB locks."},{"id":"8-order-management-system-lld","heading":"8. Order Management System LLD","level":2,"text":"Scenario (interview): Inventory update, payment deduction, and invoice generation must happen as independent steps. Design the LLD."},{"id":"81-requirements","heading":"8.1 Requirements","level":3,"text":""},{"id":"82-class-diagram","heading":"8.2 Class Diagram","level":3,"text":""},{"id":"83-implementation-observer-event-pattern","heading":"8.3 Implementation — Observer / Event pattern","level":3,"text":""},{"id":"84-aop-for-cross-cutting-not-workflow","heading":"8.4 AOP for Cross-Cutting (not workflow)","level":3,"text":"AOP for: logging, audit, metrics, . Events for: business workflow between independent steps."},{"id":"85-concurrency-stock-like-product-catalog","heading":"8.5 Concurrency — stock like product catalog","level":3,"text":"Or optimistic with retry — see Section 14.6."},{"id":"86-interview-talking-points","heading":"8.6 Interview talking points","level":3,"text":"Topic Answer Why events not direct calls? Loose coupling; add notification listener without changing OrderService Monolith vs microservices In process events for monolith; Kafka for microservices Payment fails? → release inventory (compensation) Idempotency? as idempotency key on payment API Patterns used Observer (events), State (order status), Strategy (payment methods)"}],"qa":[{"q":"How to handle user selecting 4 seats but only 3 available?","a":"All-or-nothing lock — try lock all, if any fails release all already locked (rollback)."},{"q":"How to extend for multiple theatres?","a":"`Theatre` → `Screen` → `Seat` hierarchy. BookingSystem becomes facade per theatre or single system with theatreId."},{"q":"How to add VIP pricing?","a":"Strategy pattern — `PricingStrategy` interface, `RegularPricing`, `VIPPricing`, `WeekendPricing`."},{"q":"Explain all SOLID principles with one example each?","a":"SRP — one class one job. OCP — extend via interface not modify. LSP — subclass substitutable. ISP — small interfaces. DIP — depend on abstractions."},{"q":"Singleton in LLD — when and pitfalls?","a":"Use for ParkingLot, BookingSystem, ATM (one instance). Thread-safe: enum or holder pattern. Testability: consider dependency injection instead."},{"q":"Strategy vs State pattern?","a":"Strategy — client chooses algorithm (pricing). State — object behavior changes with internal state (ATM states)."},{"q":"How to design Parking Lot LLD?","a":"ParkingLot (singleton) → Floor → Spot. Vehicle types (Bike, Car, Truck) map to spot types. Strategy for pricing. Factory for vehicle creation."},{"q":"How to design Elevator LLD?","a":"Elevator state machine (IDLE, MOVING_UP, MOVING_DOWN). Request queue (SCAN algorithm). Observer for floor arrival display."},{"q":"How to design Splitwise LLD?","a":"User, Expense, Split (equal/exact/percentage). Minimize transactions via greedy debt settlement. Graph of who owes whom."},{"q":"Composition vs Inheritance?","a":"Prefer composition (HAS-A) over inheritance (IS-A) unless true subtype relationship. Composition is more flexible (OCP)."},{"q":"How to make LLD thread-safe?","a":"synchronized, ReentrantLock, ConcurrentHashMap, immutable objects, atomic operations, or delegate to DB locks."}]},"05_DSA.md":{"title":"Data Structures & Algorithms","trackId":"dsa","trackTitle":"DSA & Algorithms","sections":[{"id":"data-structures-algorithms-dsa","heading":"Data Structures & Algorithms (DSA)","level":1,"text":""},{"id":"complete-guide-for-java-backend-developers-interview-focused","heading":"Complete Guide for Java Backend Developers | Interview Focused","level":3,"text":""},{"id":"1-complexity-analysis","heading":"1. Complexity Analysis","level":2,"text":"Theory — why we measure with Big O. We can't compare algorithms by stopwatch (hardware differs), so we count how the number of operations grows as input size n grows . Big O describes the worst case upper bound on that growth — it ignores constants and lower order terms because they stop mattering as n gets large (O(2n+5) is just O(n)). The goal in interviews is to pick the algorithm whose curve grows slowest for the expected input size: O(log n) and O(n) scale to millions; O(n²) collapses past a few thousand; O(2ⁿ)/O(n!) only work for tiny n. Always state both time and space complexity — a fast algorithm that needs O(n) extra memory may be the wrong trade off. Also know amortized cost (e.g., is O(1) on average even though an occasional resize is O(n))."},{"id":"big-o-reference-table","heading":"Big-O Reference Table","level":3,"text":"Algorithm Best Average Worst Space Binary Search O(1) O(log n) O(log n) O(1) Linear Search O(1) O(n) O(n) O(1) Bubble Sort O(n) O(n²) O(n²) O(1) Quick Sort O(n log n) O(n log n) O(n²) O(log n) Merge Sort O(n log n) O(n log n) O(n log n) O(n) Heap Sort O(n log n) O(n log n) O(n log n) O(1) DFS/BFS O(V+E) O(V+E) O(V+E) O(V) Dijkstra O((V+E) log V) O((V+E) log V) O(V)"},{"id":"common-complexities","heading":"Common Complexities","level":3,"text":""},{"id":"2-arrays-strings","heading":"2. Arrays & Strings","level":2,"text":"Theory. An array is a contiguous block of memory , so element i is found by simple address arithmetic ( ) — that's why indexing is O(1) and arrays are cache friendly (neighbours are physically adjacent). The cost is that inserting/deleting in the middle is O(n) because everything after must shift. Most array interview problems are really about avoiding the naive O(n²) double loop by exploiting structure. The three patterns below cover the majority: Two pointers — when data is sorted (or you compare from both ends), move two indices toward/with each other to get O(n) instead of checking all pairs. Sliding window — for \"best/longest/shortest contiguous subarray/substring\", grow and shrink a window so each element is visited at most twice → O(n). Prefix sum — precompute cumulative sums once so any range sum query is O(1)."},{"id":"key-patterns","heading":"Key Patterns","level":3,"text":"Two Pointers Sliding Window Prefix Sum Important Array Problems"},{"id":"3-linked-list","heading":"3. Linked List","level":2,"text":"Theory. A linked list stores elements as nodes , each holding a value plus a pointer to the next node (and too in a doubly linked list). Because nodes are scattered in memory, there's no index arithmetic: reaching the i th element is O(n), but once you hold a node, inserting or deleting is O(1) (just relink pointers). Prefer a linked list over an array when you do many insertions/removals at known positions and rarely need random access. Two techniques solve most interview questions: the dummy head node (so you don't special case the first element when inserting/deleting), and slow/fast pointers — advancing one pointer twice as fast as the other to find the middle, detect a cycle (Floyd's algorithm), or locate the n th node from the end in a single pass."},{"id":"4-stack-queue","heading":"4. Stack & Queue","level":2,"text":"Theory. A stack is LIFO (last in, first out) — like a stack of plates; you only touch the top. A queue is FIFO (first in, first out) — like a line at a counter. Both give O(1) push/pop. Use a stack whenever the problem has nesting or \"undo/most recent\" semantics: matching brackets, expression evaluation, function call frames, backtracking, and DFS. Use a queue for level by level / first come processing : BFS, scheduling, buffering. The monotonic stack (keeping elements in increasing or decreasing order) is the key trick for \"next greater/smaller element\" and histogram problems — it turns an O(n²) scan into O(n) because each index is pushed and popped at most once. In Java use for both (faster than / )."},{"id":"5-trees-bst","heading":"5. Trees & BST","level":2,"text":"Theory. A tree is a hierarchical structure of nodes with one root and no cycles; each node points to its children. A binary tree has at most two children per node. A Binary Search Tree (BST) adds an ordering invariant: everything in the left subtree is smaller, everything in the right is larger — which makes search/insert/delete O(h) where h is the height (O(log n) when balanced, but O(n) if it degenerates into a linked list, which is why self balancing trees like Red Black/AVL exist). Master the four traversals : inorder (left root right — gives sorted order in a BST), preorder (root first — good for copying/serializing), postorder (children before parent — good for deleting/computing sizes), and level order (BFS with a queue). Most tree problems are solved with recursion : solve for the children, then combine — the trick is identifying what each call should return up to its parent (e.g., height, whether it's balanced, the LCA)."},{"id":"6-heaps","heading":"6. Heaps","level":2,"text":"Theory. A heap is a complete binary tree (filled left to right) kept as a flat array, satisfying the heap property: in a min heap every parent ≤ its children (so the minimum is always at the root); a max heap is the mirror. It gives O(1) peek of the min/max and O(log n) insert/extract — but it is not fully sorted, only the root is guaranteed. Reach for a heap whenever you repeatedly need the \"best\" element so far: top K problems (keep a min heap of size k → O(n log k)), merging k sorted lists, scheduling by priority, or running medians (two heaps). Java's is a min heap by default; pass for a max heap. Building a heap from an existing array is O(n) ( ), cheaper than n inserts."},{"id":"7-graphs","heading":"7. Graphs","level":2,"text":"Theory. A graph is a set of vertices connected by edges ; it generalizes trees (a tree is just an acyclic connected graph). Edges may be directed or undirected , weighted or not. Representation matters: an adjacency list ( ) is the default — O(V+E) space, efficient for sparse graphs; an adjacency matrix is O(V²) but gives O(1) edge lookup. The two core traversals are BFS (queue, explores level by level → gives the shortest path in unweighted graphs) and DFS (stack/recursion → natural for cycle detection, topological sort, connected components). Build up from there: Dijkstra (BFS + a priority queue) for shortest paths with non negative weights; topological sort (Kahn's indegree method or DFS) for ordering dependencies in a DAG; Union Find for connectivity/cycle questions in undirected graphs. Recognising \"this is a graph problem\" (grid cells, dependencies, networks) is half the battle."},{"id":"8-hashing","heading":"8. Hashing","level":2,"text":"Theory. A hash table stores key→value pairs in an array, using a hash function to convert each key into an array index. This gives O(1) average insert/lookup/delete — the single most useful trick in interviews for trading space to save time. Collisions (two keys hashing to the same bucket) are handled by chaining (a list per bucket) or open addressing; Java 8+ converts long chains into balanced trees, so worst case is O(log n). The mental pattern: whenever a brute force solution does \"for each element, search the rest\" (O(n²)), ask \"can I remember what I've seen in a HashMap/HashSet?\" — that usually collapses it to O(n) (e.g., Two Sum, longest consecutive sequence, anagram grouping, frequency counts). Caveat: a custom key class must override both and consistently, and keys must be immutable."},{"id":"9-sorting-algorithms","heading":"9. Sorting Algorithms","level":2,"text":"Theory. Sorting is the warm up step for countless problems (it enables two pointers, binary search, greedy, and dedup). Comparison based sorts cannot beat O(n log n) in the worst case. Know the trade offs: Merge sort is always O(n log n) and stable (keeps equal elements in original order) but needs O(n) extra memory — it's how Java sorts objects ( on / ). Quick sort is O(n log n) average, in place, and cache friendly, but degrades to O(n²) on bad pivots — it's how Java sorts primitives (a dual pivot variant). Heap sort is O(n log n) in place but not stable. When the input is special — small integer range — a non comparison counting/radix sort achieves O(n). In interviews, you rarely write a sort from scratch; you call the library sort and a custom , then reason about stability and complexity."},{"id":"10-binary-search","heading":"10. Binary Search","level":2,"text":"Theory. Binary search repeatedly halves a sorted search space, giving O(log n) — 30 steps suffice for a billion elements. The classic form finds a target in a sorted array, but the powerful interview idea is \"binary search on the answer\" : when the answer is a number in a known range and there's a monotonic check ( is false for small x and becomes true past some threshold), you binary search that threshold even though there's no array to search (e.g., minimum ship capacity, minimum eating speed, smallest feasible time). Two implementation hazards to recite: compute mid as to avoid integer overflow , and be deliberate about boundaries ( vs , whether to move to or ) to avoid infinite loops — the \"first/last occurrence\" variants below show the standard templates."},{"id":"11-dynamic-programming","heading":"11. Dynamic Programming","level":2,"text":"Theory. Dynamic Programming (DP) solves a big problem by combining solutions to overlapping subproblems , storing each subproblem's answer so it's computed only once. Two conditions must hold: optimal substructure (the optimal answer is built from optimal answers of smaller pieces) and overlapping subproblems (the same smaller pieces recur). There are two equivalent styles: top down memoization (write the natural recursion, then cache results) and bottom up tabulation (fill a table from base cases upward). The hard part is always defining the state — what do the indices of your array mean, and what's the transition (recurrence) from smaller states? A reliable recipe: (1) write the brute force recursion, (2) notice repeated calls, (3) add a cache, (4) optionally convert to a table and shrink the dimensions (many 2D DPs only need the previous row → 1D). The patterns below (1D, knapsack, subsequence, grid, interval) cover most interview DP."},{"id":"patterns","heading":"Patterns","level":3,"text":"1D DP 0/1 Knapsack Subsequence Problems 2D Grid DP Interval DP"},{"id":"12-greedy-algorithms","heading":"12. Greedy Algorithms","level":2,"text":"Theory. A greedy algorithm builds the answer by always taking the locally best choice at each step, never reconsidering. It's simple and fast (often just a sort + single pass), but it's only correct when the problem has the greedy choice property : a local optimum provably leads to the global optimum. That proof is the catch — greedy gives wrong answers when future consequences matter (that's when you need DP instead). The usual tell is a sort by the right key followed by a sweep: sort intervals by end time for maximum non overlapping selection, sort by start time + a heap for \"minimum meeting rooms\", track a running reach for \"jump game\". In an interview, after proposing greedy, briefly justify why the local choice is safe (an exchange argument) or give a counterexample and switch to DP."},{"id":"13-backtracking","heading":"13. Backtracking","level":2,"text":"Theory. Backtracking is systematic brute force : it explores the tree of all possible choices via DFS, and as soon as a partial solution can't possibly work, it prunes that branch and backs up to try the next option. The universal shape is choose → explore (recurse) → un choose — you mutate the state, recurse, then undo the mutation so the next sibling starts clean. Use it for problems asking to generate all combinations/permutations/subsets, or to find a valid configuration under constraints (N Queens, Sudoku, word search). It's inherently exponential (there are exponentially many candidates), so good pruning (skipping invalid choices early) is what makes it fast enough. If the question instead asks for a count or an optimal value rather than enumerating everything, consider DP."},{"id":"14-trie","heading":"14. Trie","level":2,"text":"Theory. A trie (prefix tree) stores strings by sharing common prefixes along a tree path — each edge is a character, and a flag marks where a complete word ends. Lookups, inserts, and prefix checks cost O(L) where L is the word length, independent of how many words are stored — that's why it beats a HashMap for prefix queries like autocomplete, spell check, and \"does any stored word start with X\". The trade off is memory (a child array/map per node). Reach for a trie whenever a problem is about prefixes, dictionaries, or matching many strings at once (e.g., word search boards, IP routing, replacing words by roots)."},{"id":"15-union-find-dsu","heading":"15. Union-Find (DSU)","level":2,"text":"Theory. Union Find (Disjoint Set Union) tracks a collection of elements partitioned into disjoint groups , supporting two operations: (which group is x in?) and (merge two groups). With the two standard optimizations — path compression (flatten the tree during ) and union by rank/size (always attach the smaller tree under the larger) — each operation is almost O(1) (inverse Ackermann, effectively constant). It's the go to for connectivity questions on undirected graphs: counting connected components, detecting a cycle (a whose endpoints already share a root closes a cycle), Kruskal's MST, and \"are these two nodes connected?\" queries that arrive online. Choose Union Find over BFS/DFS when edges are added incrementally or you only care about grouping, not paths."},{"id":"problem-pattern-map","heading":"PROBLEM-PATTERN MAP","level":2,"text":"Problem Type Pattern Data Structure Find pair with sum Two pointers / HashMap Array / Map Longest subarray Sliding window Array Shortest path (unweighted) BFS Queue Shortest path (weighted) Dijkstra Priority Queue Detect cycle in directed graph DFS with colors Graph Detect cycle in undirected Union Find DSU Topological order Kahn's / DFS Graph + Queue Kth largest element Min heap of size k Priority Queue Prefix search Trie TrieNode Range sum query Prefix sum Array Next greater element Monotonic stack Stack LCS, Edit distance DP 2D array Subset / Permutation Backtracking Recursion Count inversions Merge sort Array Frequent elements HashMap + Heap Map + PQ"},{"id":"16-real-interview-problems-with-solutions","heading":"16. Real Interview Problems (With Solutions)","level":2,"text":""},{"id":"161-valid-sudoku-asked-in-multiple-rounds","heading":"16.1 Valid Sudoku (Asked in multiple rounds)","level":3,"text":""},{"id":"162-best-time-to-buy-and-sell-stock-one-transaction","heading":"16.2 Best Time to Buy and Sell Stock (One Transaction)","level":3,"text":""},{"id":"163-best-time-to-buysell-at-most-2-transactions","heading":"16.3 Best Time to Buy/Sell — At Most 2 Transactions","level":3,"text":""},{"id":"164-longest-substring-with-k-distinct-characters","heading":"16.4 Longest Substring with K Distinct Characters","level":3,"text":""},{"id":"165-group-anagrams","heading":"16.5 Group Anagrams","level":3,"text":""},{"id":"166-word-frequency-count","heading":"16.6 Word Frequency Count","level":3,"text":""},{"id":"167-stock-price-multiple-events-priorityqueue","heading":"16.7 Stock Price — Multiple Events (PriorityQueue)","level":3,"text":""},{"id":"168-two-pointer-max-loss-stock-variants","heading":"16.8 Two Pointer — Max Loss / Stock Variants","level":3,"text":""},{"id":"17-tough-interview-qa-bank","heading":"17. Tough Interview Q&A Bank","level":2,"text":"Q: Array vs ArrayList vs LinkedList internal working? Array ArrayList LinkedList Memory Contiguous block Dynamic array (1.5x grow) Scattered nodes with prev/next get(i) O(1) O(1) O(n) add(end) O(1) if space O(1) amortized O(1) add(i) O(n) shift O(n) shift O(n) traverse + O(1) link remove(i) O(n) O(n) O(n) Cache Best Good Worst Q: When to use HashMap vs TreeMap vs LinkedHashMap? A: HashMap — O(1) avg, no order. TreeMap — sorted O(log n). LinkedHashMap — insertion/access order O(1). Q: HashMap custom key — what to override? A: Both and . Never mutate key fields after insertion. Q: BFS vs DFS — when to use? A: BFS — shortest path unweighted, level order. DFS — cycle detect, path find, connected components, backtracking. Q: DP vs Greedy — how to decide? A: Greedy if local optimal = global optimal (proof needed). DP if overlapping subproblems + optimal substructure. Q: Time complexity of HashMap operations? A: O(1) average put/get/remove. O(log n) worst case Java 8+ with treeification. Q: How to find cycle in linked list? A: Floyd's tortoise hare — slow moves 1, fast moves 2; if they meet, cycle exists. Q: Top K elements — best approach? A: Min heap of size K — O(n log k). Or QuickSelect — O(n) average."}],"qa":[{"q":"When to use HashMap vs TreeMap vs LinkedHashMap?","a":"HashMap — O(1) avg, no order. TreeMap — sorted O(log n). LinkedHashMap — insertion/access order O(1)."},{"q":"HashMap custom key — what to override?","a":"Both `equals()` and `hashCode()`. Never mutate key fields after insertion."},{"q":"BFS vs DFS — when to use?","a":"BFS — shortest path unweighted, level-order. DFS — cycle detect, path find, connected components, backtracking."},{"q":"DP vs Greedy — how to decide?","a":"Greedy if local optimal = global optimal (proof needed). DP if overlapping subproblems + optimal substructure."},{"q":"Time complexity of HashMap operations?","a":"O(1) average put/get/remove. O(log n) worst case Java 8+ with treeification."},{"q":"How to find cycle in linked list?","a":"Floyd's tortoise-hare — slow moves 1, fast moves 2; if they meet, cycle exists."},{"q":"Top K elements — best approach?","a":"Min-heap of size K — O(n log k). Or QuickSelect — O(n) average."}]},"06_CP_Tricks.md":{"title":"Java CP Tricks","trackId":"dsa","trackTitle":"DSA & Algorithms","sections":[{"id":"competitive-programming-tricks-shortcuts-templates","heading":"Competitive Programming — Tricks, Shortcuts & Templates","level":1,"text":""},{"id":"java-edition-for-interviews-cp-contests","heading":"Java Edition | For Interviews + CP Contests","level":3,"text":""},{"id":"1-fast-io-in-java","heading":"1. Fast I/O in Java","level":2,"text":""},{"id":"2-bit-manipulation","heading":"2. Bit Manipulation","level":2,"text":"Theory. Bit manipulation treats an integer as a fixed array of 32 (or 64) bits and uses the bitwise operators ( , , , , , ) to compute things far faster than arithmetic or loops. It matters in interviews and contests for three reasons: speed (each operation is a single CPU instruction), space (a bitmask packs up to 32 boolean flags into one int — the basis of bitmask DP and subset enumeration), and a family of classic identities you'll be expected to recognize — XOR to cancel duplicate values, to drop the lowest set bit, and to isolate it. Also know the JDK built ins so you don't reinvent them: , , ."},{"id":"the-essential-bit-tricks","heading":"The Essential Bit Tricks","level":3,"text":""},{"id":"xor-tricks","heading":"XOR Tricks","level":3,"text":""},{"id":"subset-generation-with-bits","heading":"Subset Generation with Bits","level":3,"text":""},{"id":"3-math-number-theory","heading":"3. Math & Number Theory","level":2,"text":""},{"id":"gcd-lcm","heading":"GCD, LCM","level":3,"text":""},{"id":"modular-arithmetic","heading":"Modular Arithmetic","level":3,"text":""},{"id":"prime-numbers","heading":"Prime Numbers","level":3,"text":""},{"id":"combinations-permutations","heading":"Combinations & Permutations","level":3,"text":""},{"id":"4-string-tricks","heading":"4. String Tricks","level":2,"text":""},{"id":"5-array-collection-tricks","heading":"5. Array & Collection Tricks","level":2,"text":""},{"id":"6-two-pointers-sliding-window-templates","heading":"6. Two Pointers & Sliding Window Templates","level":2,"text":""},{"id":"7-binary-search-templates","heading":"7. Binary Search Templates","level":2,"text":""},{"id":"8-dynamic-programming-templates","heading":"8. Dynamic Programming Templates","level":2,"text":"Theory. Every dynamic programming solution comes down to the same three decisions: define the state (what does each index/parameter represent?), write the transition (how is a state built from smaller states?), and set the base cases . The templates below are the two ways to fill that in. Top down (recursion + a memo cache) is written in the natural order of the problem and is usually easier to reason about. Bottom up (a table filled from the base cases upward) avoids recursion depth limits and often allows a space optimization down to one or two rows. Choose top down when the recurrence is clearer; choose bottom up when you need to save memory or stack space."},{"id":"top-down-memoization","heading":"Top-Down (Memoization)","level":3,"text":""},{"id":"bottom-up-tabulation","heading":"Bottom-Up (Tabulation)","level":3,"text":""},{"id":"dp-common-patterns","heading":"DP Common Patterns","level":3,"text":""},{"id":"9-graph-templates","heading":"9. Graph Templates","level":2,"text":""},{"id":"10-java-specific-tricks","heading":"10. Java-Specific Tricks","level":2,"text":""},{"id":"collections-power-usage","heading":"Collections Power Usage","level":3,"text":""},{"id":"integerlong-tricks","heading":"Integer/Long Tricks","level":3,"text":""},{"id":"string-conversion-tricks","heading":"String Conversion Tricks","level":3,"text":""},{"id":"11-problem-recognition-cheat-sheet","heading":"11. Problem Recognition Cheat Sheet","level":2,"text":""},{"id":"look-for-these-keywords-use-this-approach","heading":"Look for These Keywords → Use This Approach","level":3,"text":"Keywords in Problem Approach \"Subarray with max/min sum\" Sliding Window / Kadane's \"Two numbers that sum to target\" Two Pointers (sorted) / HashMap \"K closest elements / Kth largest\" Heap (Priority Queue) \"All subsets / all combinations\" Backtracking / Bitmask \"All permutations\" Backtracking \"Shortest path / minimum hops\" BFS \"Longest path in DAG\" Topological Sort + DP \"Minimum spanning tree\" Kruskal's (sort edges) / Prim's \"Connected components / cycle detection\" Union Find / DFS \"Count distinct / frequency\" HashMap \"Range sum query\" Prefix Sum / Segment Tree \"Prefix matching / autocomplete\" Trie \"Overlapping intervals\" Sort + Greedy / Stack \"Matrix path / islands\" DFS/BFS on grid \"Minimum cost / optimal choice at each step\" Greedy \"Minimize/maximize with overlapping subproblems\" Dynamic Programming \"Count ways / number of paths\" DP \"Find element in sorted array\" Binary Search \"Next greater element\" Monotonic Stack \"Sliding window maximum\" Monotonic Deque \"Top K frequent / K smallest\" Heap / QuickSelect \"Parentheses matching / valid sequence\" Stack \"Single non duplicate / XOR problems\" Bit Manipulation \"Expression evaluation / calculator\" Stack"},{"id":"complexity-based-selection","heading":"Complexity-Based Selection","level":3,"text":""},{"id":"12-common-mistakes-to-avoid","heading":"12. Common Mistakes to Avoid","level":2,"text":""},{"id":"quick-reference-card","heading":"QUICK REFERENCE CARD","level":2,"text":""},{"id":"13-real-interview-problem-templates","heading":"13. Real Interview Problem Templates","level":2,"text":""},{"id":"131-valid-sudoku-one-liner-set-trick","heading":"13.1 Valid Sudoku — One-Liner Set Trick","level":3,"text":""},{"id":"132-stock-buysell-template","heading":"13.2 Stock Buy/Sell — Template","level":3,"text":""},{"id":"133-sliding-window-k-distinct-template","heading":"13.3 Sliding Window — K Distinct Template","level":3,"text":""},{"id":"134-group-anagrams-key-generation","heading":"13.4 Group Anagrams — Key Generation","level":3,"text":""},{"id":"135-frequency-count-stream-one-liner","heading":"13.5 Frequency Count — Stream One-Liner","level":3,"text":""},{"id":"136-two-pointer-opposite-ends-template","heading":"13.6 Two Pointer — Opposite Ends Template","level":3,"text":""},{"id":"137-monotonic-stack-next-greater-element","heading":"13.7 Monotonic Stack — Next Greater Element","level":3,"text":""},{"id":"138-binary-search-lower-bound-template","heading":"13.8 Binary Search — Lower Bound Template","level":3,"text":""},{"id":"139-priorityqueue-top-k-merge-k-lists","heading":"13.9 PriorityQueue — Top K / Merge K Lists","level":3,"text":""},{"id":"1310-union-find-connectivity-template","heading":"13.10 Union-Find — Connectivity Template","level":3,"text":""},{"id":"14-tough-interview-qa-bank","heading":"14. Tough Interview Q&A Bank","level":2,"text":"Q: Integer caching trap in Java CP? A: → true. → false. Always use for objects. Q: Arrays.asList() — can you remove? A: No. Fixed size backed by array. / throw . Wrap in . Q: String pool — ? A: false. creates heap object. Literal is in pool. Use or . Q: Comparator — why not ? A: Integer overflow when . Use . Q: How to avoid TLE in Java? A: Fast I/O (BufferedReader/PrintWriter), StringBuilder not , not bubble, right complexity, not nested loops. Q: When to use long instead of int? A: Products, sums over 10^5 elements, graph distances, modulo arithmetic intermediate values. Q: Modulo with negative numbers in Java? A: is negative. Use . Q: Recognize sliding window problem? A: Keywords: \"longest/shortest subarray/substring\", \"at most K distinct\", \"sum ≤ target\", contiguous elements. Q: Recognize binary search on answer? A: \"Minimize maximum\", \"find minimum capacity/speed\", monotonic feasibility function."}],"qa":[{"q":"Integer caching trap in Java CP?","a":"`Integer a = 127, b = 127` → `a==b` true. `128, 128` → false. Always use `.equals()` for objects."},{"q":"Arrays.asList() — can you remove?","a":"No. Fixed-size backed by array. `add()`/`remove()` throw `UnsupportedOperationException`. Wrap in `new ArrayList<>()`."},{"q":"String pool — `new String(\"x\") == \"x\"`?","a":"false. `new` creates heap object. Literal is in pool. Use `.equals()` or `.intern()`."},{"q":"Comparator — why not `(a,b) -> a-b`?","a":"Integer overflow when `a=MIN_VALUE`. Use `Integer.compare(a,b)`."},{"q":"How to avoid TLE in Java?","a":"Fast I/O (BufferedReader/PrintWriter), StringBuilder not `+=`, `Arrays.sort` not bubble, right complexity, `HashMap` not nested loops."},{"q":"When to use long instead of int?","a":"Products, sums over 10^5 elements, graph distances, modulo arithmetic intermediate values."},{"q":"Modulo with negative numbers in Java?","a":"`(-5 % MOD)` is negative. Use `((x % MOD) + MOD) % MOD`."},{"q":"Recognize sliding window problem?","a":"Keywords: \"longest/shortest subarray/substring\", \"at most K distinct\", \"sum ≤ target\", contiguous elements."},{"q":"Recognize binary search on answer?","a":"\"Minimize maximum\", \"find minimum capacity/speed\", monotonic feasibility function."}]},"07_Python_Core_to_Advanced.md":{"title":"Python Core to Advanced","trackId":"python-stack","trackTitle":"Python Stack","sections":[{"id":"python-core-to-advanced","heading":"Python — Core to Advanced","level":1,"text":""},{"id":"complete-interview-guide-beginner-advanced","heading":"Complete Interview Guide | Beginner → Advanced","level":3,"text":""},{"id":"1-python-fundamentals","heading":"1. Python Fundamentals","level":2,"text":""},{"id":"python-execution-model","heading":"Python Execution Model","level":3,"text":""},{"id":"variables-dynamic-typing","heading":"Variables & Dynamic Typing","level":3,"text":""},{"id":"mutable-vs-immutable","heading":"Mutable vs Immutable","level":3,"text":""},{"id":"python-naming-conventions","heading":"Python Naming Conventions","level":3,"text":""},{"id":"2-data-types-data-structures","heading":"2. Data Types & Data Structures","level":2,"text":""},{"id":"numbers","heading":"Numbers","level":3,"text":""},{"id":"strings","heading":"Strings","level":3,"text":""},{"id":"lists","heading":"Lists","level":3,"text":""},{"id":"tuples","heading":"Tuples","level":3,"text":""},{"id":"dictionaries","heading":"Dictionaries","level":3,"text":""},{"id":"sets","heading":"Sets","level":3,"text":""},{"id":"collections-module-essential","heading":"Collections Module — Essential","level":3,"text":""},{"id":"3-functions-functional-programming","heading":"3. Functions & Functional Programming","level":2,"text":"Theory. Functions in Python are first class objects — you can pass them as arguments, return them from other functions, and store them in variables or data structures. That single fact is what makes decorators, callbacks, and the functional tools below possible. Python's calling model is \"pass by object reference\" : the function gets a reference to the same object the caller holds, so rebinding a parameter ( ) doesn't affect the caller, but mutating a mutable argument ( ) does — the root cause of the infamous mutable default argument bug. Python also lets you shape a function's calling convention precisely: / for variadic functions, and the and markers to force positional only or keyword only arguments for clearer, more stable APIs. Closures (inner functions that capture variables from their enclosing scope) and helpers like and let you build and specialize behavior without writing classes."},{"id":"function-fundamentals","heading":"Function Fundamentals","level":3,"text":""},{"id":"lambda-functions","heading":"Lambda Functions","level":3,"text":""},{"id":"functional-tools","heading":"Functional Tools","level":3,"text":""},{"id":"closures","heading":"Closures","level":3,"text":""},{"id":"4-object-oriented-programming","heading":"4. Object-Oriented Programming","level":2,"text":"Theory. Python is multi paradigm but fully object oriented — even classes are objects. Several things differ from Java and trip people up: instance attributes live in a per instance , so they're dynamic (you can add them at runtime) unless you opt into ; there is no real — a leading underscore is a convention and a double underscore only triggers name mangling; and operator/built in behavior is customized through dunder (\"magic\") methods ( , , , , , , …) that hook your objects into the language. Python supports multiple inheritance , resolved deterministically by the MRO (Method Resolution Order via C3 linearization); walks the MRO, not merely the literal parent class. In practice, prefer composition and small mixins over deep hierarchies, expose computed/validated state with , provide alternative constructors with , and use to remove boilerplate ( / / ) for data holding classes."},{"id":"classes-objects","heading":"Classes & Objects","level":3,"text":""},{"id":"inheritance","heading":"Inheritance","level":3,"text":""},{"id":"abstract-classes","heading":"Abstract Classes","level":3,"text":""},{"id":"dataclasses-python-37","heading":"Dataclasses (Python 3.7+)","level":3,"text":""},{"id":"dunder-magic-methods","heading":"Dunder (Magic) Methods","level":3,"text":""},{"id":"5-iterators-generators-comprehensions","heading":"5. Iterators, Generators & Comprehensions","level":2,"text":""},{"id":"iterators","heading":"Iterators","level":3,"text":""},{"id":"generators","heading":"Generators","level":3,"text":""},{"id":"comprehensions","heading":"Comprehensions","level":3,"text":""},{"id":"6-decorators-context-managers","heading":"6. Decorators & Context Managers","level":2,"text":"Theory. A decorator is simply a function that takes a function (or class) and returns a replacement; the syntax above is sugar for . Because functions are first class, decorators let you wrap cross cutting behavior — logging, timing, caching, retries, access control — around existing code without editing it (the Wrapper/Proxy pattern). Always apply to the inner wrapper so the original / survive. A decorator that takes arguments is a decorator factory — a function that returns a decorator, i.e. one more level of nesting. A context manager (the statement) guarantees that paired setup/teardown runs even when an exception is thrown: implement / , or more simply write a generator decorated with that s exactly once. Use context managers for anything that must be reliably released — files, locks, DB connections/transactions, timers, or temporary global state."},{"id":"decorators","heading":"Decorators","level":3,"text":""},{"id":"context-managers","heading":"Context Managers","level":3,"text":""},{"id":"7-modules-packages-imports","heading":"7. Modules, Packages & Imports","level":2,"text":""},{"id":"8-error-handling-exceptions","heading":"8. Error Handling & Exceptions","level":2,"text":"Theory. Python favors EAFP — \"easier to ask forgiveness than permission\": attempt the operation and handle the exception if it fails, rather than checking every precondition first (LBYL). Exceptions are objects in a class hierarchy rooted at ; you catch by type and order handlers most specific first , catching the narrowest exception you can genuinely handle instead of a bare . runs only when the block raised nothing, and always runs (use it for cleanup). Define custom exception classes (subclassing a project level base) so callers can react to domain specific failures, and use exception chaining — — to keep the underlying cause visible in the traceback. Never silently swallow exceptions; if you catch only to re raise, a bare preserves the original stack trace."},{"id":"9-concurrency-parallelism","heading":"9. Concurrency & Parallelism","level":2,"text":"Theory. Picking the right concurrency model in Python hinges on the GIL (Global Interpreter Lock): in CPython only one thread executes Python bytecode at a time, so threads do not speed up CPU bound work — but because the GIL is released during I/O, threads do help I/O bound work. That leads to three tools for three situations: for a modest number of blocking I/O tasks; — a single threaded cooperative event loop driven by — for massive I/O concurrency (thousands of connections); and — separate processes, each with its own interpreter and GIL — for true parallelism on CPU bound work. provides one uniform API over both thread and process pools. Two rules to remember: never run a blocking call inside an asyncio event loop (it freezes everything), and protect shared mutable state in threads with locks or hand work off through queues."},{"id":"threading","heading":"Threading","level":3,"text":""},{"id":"concurrentfutures","heading":"concurrent.futures","level":3,"text":""},{"id":"asyncio-asyncawait","heading":"asyncio — Async/Await","level":3,"text":""},{"id":"threading-vs-multiprocessing-vs-asyncio","heading":"Threading vs Multiprocessing vs asyncio","level":3,"text":""},{"id":"10-memory-management-internals","heading":"10. Memory Management & Internals","level":2,"text":""},{"id":"reference-counting-gc","heading":"Reference Counting & GC","level":3,"text":""},{"id":"python-internals","heading":"Python Internals","level":3,"text":""},{"id":"11-python-type-system-annotations","heading":"11. Python Type System & Annotations","level":2,"text":"Theory. Python remains dynamically typed at runtime — type annotations are hints , not enforced by the interpreter. Their value comes from static checkers (mypy, pyright) that read them to catch type bugs before you run, and from IDEs that use them for autocomplete and safe refactoring. Hints turn a large codebase into self documenting, safer code without giving up Python's flexibility. Beyond basic types you'll reach for / for nullable values, / for alternatives, generics via and for reusable containers, for structural typing (duck typing made checkable — \"anything with these methods\"), for a fixed set of allowed values, and / /Pydantic for structured records. Note that runtime validation (as in Pydantic/FastAPI) is a separate concern layered on top of the same annotations."},{"id":"12-testing-in-python","heading":"12. Testing in Python","level":2,"text":""},{"id":"pytest","heading":"pytest","level":3,"text":""},{"id":"13-python-vs-java-key-differences","heading":"13. Python vs Java — Key Differences","level":2,"text":"Aspect Python Java Typing Dynamic (duck typing) Static (strong typing) Compilation Interpreted (bytecode) Compiled to JVM bytecode Performance Slower (CPython) Faster (JIT) Concurrency GIL limits CPU parallelism True multithreading Syntax Concise, indentation based Verbose, braces OOP Multi paradigm, flexible Primarily OOP Memory Automatic GC (refcount + cyclic) Automatic GC (generational) Error handling try/except try/catch Null safety None, no null pointer magic NullPointerException risk Interfaces Protocol (structural) Interface (nominal) Speed to develop Fast Slower (more boilerplate) Ecosystem Data science, ML, scripting Enterprise, Android Package manager pip/poetry/uv Maven/Gradle"},{"id":"14-interview-qa","heading":"14. Interview Q&A","level":2,"text":"Q: What is the GIL and why does Python have it? A: The Global Interpreter Lock (GIL) is a mutex in CPython that allows only one thread to execute Python bytecode at a time. It exists because CPython's memory management (reference counting) is not thread safe. The GIL simplifies the implementation and protects Python objects from concurrent modification. For CPU bound parallel code, use (separate processes, no shared GIL). For I/O bound code, and work well because the GIL is released during I/O operations. Q: Difference between and ? A: compares values (calls ). compares identity (same object in memory, same ). Always use for value comparison. Use only for , , checks ( ). Q: What is a generator and when would you use it? A: A generator is a function that uses to produce values lazily, one at a time. Unlike lists, generators don't store all values in memory. Use generators when: processing large datasets that don't fit in memory, implementing infinite sequences, creating pipelines of transformations, reading large files line by line. Q: How does Python's garbage collection work? A: CPython primarily uses reference counting. Every object has a reference count; when it drops to 0, the object is immediately freed. However, reference counting cannot handle circular references. CPython also has a cyclic garbage collector (generational: gen0, gen1, gen2) that periodically detects and breaks cycles. PyPy uses a different GC (mark and sweep). Q: What are and ? A: collects extra positional arguments into a tuple. collects extra keyword arguments into a dictionary. In function calls, unpacks a sequence as positional args and unpacks a dict as keyword args. accepts any combination. Q: What is the difference between , , and instance methods? A: Instance methods receive (the instance). Class methods receive (the class) — used for alternative constructors or factory methods. Static methods receive neither — they're regular functions scoped to the class. Use for , patterns. Use for "}],"qa":[{"q":"What is the GIL and why does Python have it?","a":"The Global Interpreter Lock (GIL) is a mutex in CPython that allows only one thread to execute Python bytecode at a time. It exists because CPython's memory management (reference counting) is not thread-safe. The GIL simplifies the implementation and protects Python objects from concurrent modification. For CPU-bound parallel code, use `multiprocessing` (separate processes, no shared GIL). For I/O-bound code, `threading` and `asyncio` work well because the GIL is released during I/O operations."},{"q":"Difference between `is` and `==`?","a":"`==` compares values (calls `__eq__`). `is` compares identity (same object in memory, same `id()`). Always use `==` for value comparison. Use `is` only for `None`, `True`, `False` checks (`if x is None`)."},{"q":"What is a generator and when would you use it?","a":"A generator is a function that uses `yield` to produce values lazily, one at a time. Unlike lists, generators don't store all values in memory. Use generators when: processing large datasets that don't fit in memory, implementing infinite sequences, creating pipelines of transformations, reading large files line by line."},{"q":"How does Python's garbage collection work?","a":"CPython primarily uses reference counting. Every object has a reference count; when it drops to 0, the object is immediately freed. However, reference counting cannot handle circular references. CPython also has a cyclic garbage collector (generational: gen0, gen1, gen2) that periodically detects and breaks cycles. PyPy uses a different GC (mark-and-sweep)."},{"q":"What are `*args` and `","a":"kwargs`?**\n`*args` collects extra positional arguments into a tuple. `**kwargs` collects extra keyword arguments into a dictionary. In function calls, `*` unpacks a sequence as positional args and `**` unpacks a dict as keyword args. `def f(*args, **kwargs): ...` accepts any combination."},{"q":"What is the difference between `@staticmethod`, `@classmethod`, and instance methods?","a":"Instance methods receive `self` (the instance). Class methods receive `cls` (the class) — used for alternative constructors or factory methods. Static methods receive neither — they're regular functions scoped to the class. Use `@classmethod` for `from_json()`, `from_dict()` patterns. Use `@staticmethod` for utility functions related to the class but not needing class/instance access."},{"q":"Explain Python decorators.","a":"A decorator is a function that takes a function as input and returns a modified function. They implement the Proxy/Wrapper pattern. Common uses: logging, timing, access control, caching (`@lru_cache`), retry logic, validation. The `@functools.wraps(func)` call inside preserves the original function's metadata (`__name__`, `__doc__`)."},{"q":"What is `__slots__`?","a":"By default, Python stores instance attributes in a `__dict__`. `__slots__ = ['x', 'y']` replaces `__dict__` with a fixed set of attributes, reducing memory usage by ~50-70% for many objects. Drawback: can't add new attributes dynamically; doesn't work well with multiple inheritance unless all classes define `__slots__`."}]},"08_FastAPI_and_Falcon.md":{"title":"FastAPI & Falcon","trackId":"python-stack","trackTitle":"Python Stack","sections":[{"id":"fastapi-falcon-complete-interview-guide","heading":"FastAPI & Falcon — Complete Interview Guide","level":1,"text":""},{"id":"python-web-frameworks-rest-apis-async-production-patterns","heading":"Python Web Frameworks | REST APIs | Async | Production Patterns","level":3,"text":""},{"id":"1-framework-comparison-overview","heading":"1. Framework Comparison Overview","level":2,"text":""},{"id":"2-fastapi-core-concepts","heading":"2. FastAPI — Core Concepts","level":2,"text":"Theory. FastAPI's central idea is that your Python type hints are the single source of truth . You write a path operation function with typed parameters and a Pydantic model for the body, and the framework derives everything from those types: request parsing, validation (returning a clean on bad input), serialization of the response, and the interactive OpenAPI/Swagger docs — with no separate schema to keep in sync. Pydantic models are the foundation: a class of typed fields that validates and coerces incoming data into real Python objects, so your business logic always receives clean, well typed values. Because FastAPI is built on ASGI (via Starlette), endpoints run cooperatively on an event loop, which is what makes it fast for I/O bound APIs."},{"id":"installation-basic-app","heading":"Installation & Basic App","level":3,"text":""},{"id":"path-operations","heading":"Path Operations","level":3,"text":""},{"id":"pydantic-models-foundation-of-fastapi","heading":"Pydantic Models (Foundation of FastAPI)","level":3,"text":""},{"id":"3-fastapi-request-response","heading":"3. FastAPI — Request & Response","level":2,"text":""},{"id":"request-body","heading":"Request Body","level":3,"text":""},{"id":"response-models","heading":"Response Models","level":3,"text":""},{"id":"exception-handling","heading":"Exception Handling","level":3,"text":""},{"id":"middleware","heading":"Middleware","level":3,"text":""},{"id":"4-fastapi-dependency-injection","heading":"4. FastAPI — Dependency Injection","level":2,"text":"Theory. Dependency Injection is FastAPI's mechanism for supplying a route with the things it needs — a DB session, the current user, pagination parameters — without the route having to create them itself. You write a function (or class) that produces the value and reference it with ; FastAPI calls it, resolves any sub dependencies it in turn requires, and injects the result into your handler. Dependencies that double as setup/teardown (open a DB session, it, close it after the response). Within a single request the same dependency is cached by default (called once), and dependencies can be attached at the route, router, or whole app level. The payoff: cross cutting concerns (auth, DB, config) become reusable, composable, and trivially overridable in tests via ."},{"id":"5-fastapi-authentication-security","heading":"5. FastAPI — Authentication & Security","level":2,"text":"Theory. Authentication answers \"who are you?\" and authorization \"what are you allowed to do?\". The dominant pattern for modern APIs is stateless JWT : the user logs in once with credentials, the server verifies them and returns a signed token containing claims (such as the username and an expiry time); the client then sends that token on every subsequent request, and the server simply validates the signature and expiry rather than looking up a server side session — which scales horizontally with no shared session store. Passwords are never stored in plaintext; they're hashed with a deliberately slow algorithm like bcrypt. FastAPI expresses all of this through its DI system: a dependency decodes and validates the token, and you attach it to protected routes so security stays declarative and testable."},{"id":"oauth2-jwt","heading":"OAuth2 + JWT","level":3,"text":""},{"id":"api-key-security","heading":"API Key Security","level":3,"text":""},{"id":"6-fastapi-database-integration","heading":"6. FastAPI — Database Integration","level":2,"text":"Theory. FastAPI is database agnostic — you bring your own toolkit, most commonly SQLAlchemy (the ORM that maps classes to tables) plus Alembic (which versions and migrates your schema). In an async app you use SQLAlchemy's async engine and session so database calls instead of blocking the event loop. The crucial pattern is managing the session lifecycle per request through a dependency: create a session, hand it to the route, and guarantee it's committed and closed afterward — never share one session across requests. To cut latency on hot lookups, put a read through cache (typically Redis) in front of the DB: check the cache first, fall back to the database on a miss, then store the result with a TTL so it expires."},{"id":"sqlalchemy-async","heading":"SQLAlchemy Async","level":3,"text":""},{"id":"alembic-migrations","heading":"Alembic Migrations","level":3,"text":""},{"id":"redis-integration","heading":"Redis Integration","level":3,"text":""},{"id":"7-fastapi-background-tasks-websockets","heading":"7. FastAPI — Background Tasks & WebSockets","level":2,"text":""},{"id":"background-tasks","heading":"Background Tasks","level":3,"text":""},{"id":"websockets","heading":"WebSockets","level":3,"text":""},{"id":"server-sent-events-sse","heading":"Server-Sent Events (SSE)","level":3,"text":""},{"id":"8-fastapi-testing","heading":"8. FastAPI — Testing","level":2,"text":""},{"id":"9-fastapi-advanced-patterns","heading":"9. FastAPI — Advanced Patterns","level":2,"text":""},{"id":"routers-apirouter","heading":"Routers & APIRouter","level":3,"text":""},{"id":"lifespan-events-fastapi-093","heading":"Lifespan Events (FastAPI 0.93+)","level":3,"text":""},{"id":"settings-with-pydantic-settings","heading":"Settings with Pydantic-Settings","level":3,"text":""},{"id":"pagination-pattern","heading":"Pagination Pattern","level":3,"text":""},{"id":"10-falcon-core-concepts","heading":"10. Falcon — Core Concepts","level":2,"text":""},{"id":"installation-basic-app","heading":"Installation & Basic App","level":3,"text":""},{"id":"app-configuration","heading":"App Configuration","level":3,"text":""},{"id":"11-falcon-routing-resources","heading":"11. Falcon — Routing & Resources","level":2,"text":""},{"id":"falcon-errors","heading":"Falcon Errors","level":3,"text":""},{"id":"12-falcon-middleware-hooks","heading":"12. Falcon — Middleware & Hooks","level":2,"text":"Theory. Falcon separates cross cutting logic into two complementary tools. Middleware are classes with / / methods that run for every request in a defined order (and in reverse order on the way out) — ideal for app wide concerns like authentication, CORS, logging, and timing. Hooks ( / ) are decorators attached to a specific responder, for targeted concerns such as validating one endpoint's input or enforcing a content type. The mental split is simple: middleware = global pipeline , hooks = per endpoint guards ."},{"id":"middleware","heading":"Middleware","level":3,"text":""},{"id":"hooks","heading":"Hooks","level":3,"text":""},{"id":"13-falcon-asgi-async","heading":"13. Falcon — ASGI & Async","level":2,"text":""},{"id":"14-fastapi-vs-falcon-vs-flask-vs-django-rest","heading":"14. FastAPI vs Falcon vs Flask vs Django REST","level":2,"text":""},{"id":"feature-matrix","heading":"Feature Matrix","level":3,"text":"Feature FastAPI Falcon Flask Django REST Performance ⭐⭐⭐⭐⭐ ⭐⭐⭐⭐⭐ ⭐⭐⭐ ⭐⭐ Async Native Native (v3) Limited Limited Auto Docs ✅ Swagger + ReDoc ❌ Manual ❌ Manual ✅ Browsable Validation ✅ Pydantic ❌ Manual ❌ Manual ✅ Serializers ORM Any Any Any Django ORM Learning curve Medium Low Low High Boilerplate Low Very Low Low High Community Growing fast Smaller Large Large Use case Modern APIs Perf critical Simple APIs Full apps"},{"id":"when-to-choose-what","heading":"When to Choose What","level":3,"text":""},{"id":"performance-comparison-rps-on-simple-json-endpoint","heading":"Performance Comparison (RPS on simple JSON endpoint)","level":3,"text":""},{"id":"15-production-patterns-deployment","heading":"15. Production Patterns & Deployment","level":2,"text":""},{"id":"uvicorn-gunicorn-setup","heading":"Uvicorn + Gunicorn Setup","level":3,"text":""},{"id":"docker","heading":"Docker","level":3,"text":""},{"id":"rate-limiting-slowapi-for-fastapi","heading":"Rate Limiting (Slowapi for FastAPI)","level":3,"text":""},{"id":"structured-logging","heading":"Structured Logging","level":3,"text":""},{"id":"16-interview-qa","heading":"16. Interview Q&A","level":2,"text":"Q: What makes FastAPI different from Flask? A: FastAPI is built on ASGI (async native), uses Python type hints for automatic request validation via Pydantic, generates OpenAPI docs automatically, and has much better performance for async I/O. Flask is WSGI (synchronous), requires extensions for validation and docs, but has a larger ecosystem and is simpler to learn. Q: How does FastAPI validate request data? A: FastAPI uses Pydantic models. You declare request body as a Pydantic parameter. FastAPI automatically: 1) Parses the JSON body, 2) Validates each field against the type annotation and Field constraints, 3) Returns HTTP 422 with detailed error messages if validation fails, 4) Converts the validated data into the Python model instance for your function. Q: Explain FastAPI's Dependency Injection system. A: declares a dependency. When a route is called, FastAPI resolves the dependency chain: it calls the dependency function, injects its return value into the route. Dependencies can be nested (sub dependencies). By default, within the same request, each dependency is called once and the result is cached. Common uses: DB sessions, authentication, pagination parameters, shared business logic. Q: How does Falcon achieve high performance? A: Falcon has minimal overhead by design — it doesn't include an ORM, template engine, or admin interface. It processes requests with a lean middleware stack. Response/request objects are pre allocated. Error handling is kept at a minimum. The ASGI version adds async I/O capabilities. Falcon avoids the overhead that frameworks like Django add for convenience features. Q: What is Pydantic v2 and what changed from v1? A: Pydantic v2 was rewritten in Rust (via pydantic core), making it 5 50x faster. Key changes: class replaced by dict, replaced by , replaced by / , replaced by . FastAPI 0.100+ uses Pydantic v2 by default. Q: How do you handle database sessions in FastAPI? A: Use a generator dependency with . The DB session is created in"}],"qa":[{"q":"What makes FastAPI different from Flask?","a":"FastAPI is built on ASGI (async-native), uses Python type hints for automatic request validation via Pydantic, generates OpenAPI docs automatically, and has much better performance for async I/O. Flask is WSGI (synchronous), requires extensions for validation and docs, but has a larger ecosystem and is simpler to learn."},{"q":"How does FastAPI validate request data?","a":"FastAPI uses Pydantic models. You declare request body as a Pydantic `BaseModel` parameter. FastAPI automatically: 1) Parses the JSON body, 2) Validates each field against the type annotation and Field constraints, 3) Returns HTTP 422 with detailed error messages if validation fails, 4) Converts the validated data into the Python model instance for your function."},{"q":"Explain FastAPI's Dependency Injection system.","a":"`Depends()` declares a dependency. When a route is called, FastAPI resolves the dependency chain: it calls the dependency function, injects its return value into the route. Dependencies can be nested (sub-dependencies). By default, within the same request, each dependency is called once and the result is cached. Common uses: DB sessions, authentication, pagination parameters, shared business logic."},{"q":"How does Falcon achieve high performance?","a":"Falcon has minimal overhead by design — it doesn't include an ORM, template engine, or admin interface. It processes requests with a lean middleware stack. Response/request objects are pre-allocated. Error handling is kept at a minimum. The ASGI version adds async I/O capabilities. Falcon avoids the overhead that frameworks like Django add for convenience features."},{"q":"What is Pydantic v2 and what changed from v1?","a":"Pydantic v2 was rewritten in Rust (via pydantic-core), making it 5-50x faster. Key changes: `Config` class replaced by `model_config` dict, `orm_mode=True` replaced by `from_attributes=True`, `@validator` replaced by `@field_validator`/`@model_validator`, `schema_extra` replaced by `json_schema_extra`. FastAPI 0.100+ uses Pydantic v2 by default."},{"q":"How do you handle database sessions in FastAPI?","a":"Use a generator dependency with `yield`. The DB session is created in the dependency, `yield`ed to the route function, and closed/committed in the `finally` block after the response is sent. This ensures the session is always cleaned up. For async, use `AsyncSession` with `async_sessionmaker`. Never hold sessions longer than the request lifetime."},{"q":"How does FastAPI handle async vs sync route functions?","a":"If a route is `async def`, FastAPI runs it directly on the event loop. If it's a regular `def`, FastAPI runs it in a thread pool (using `run_in_executor`) to avoid blocking the event loop. Use `async def` for I/O-bound async code. Use `def` for CPU-bound or legacy synchronous code. Mixing is fine and handled automatically."}]},"09_GoLang.md":{"title":"Go Language","trackId":"cloud-tools","trackTitle":"Cloud & Languages","sections":[{"id":"go-golang-complete-interview-guide","heading":"Go (Golang) — Complete Interview Guide","level":1,"text":""},{"id":"beginner-to-advanced-benefits-over-python","heading":"Beginner to Advanced + Benefits Over Python","level":3,"text":""},{"id":"1-go-vs-python-why-go","heading":"1. Go vs Python — Why Go?","level":2,"text":""},{"id":"the-definitive-comparison","heading":"The Definitive Comparison","level":3,"text":"Aspect Go Python Performance 10 100x faster (compiled, native) Slower (interpreted, GIL) Concurrency Native goroutines (M:N threads) GIL limits true parallelism Type System Statically typed, compiled Dynamically typed, runtime checks Memory Manual ish (low GC pauses) Automatic (GC + reference counting) Compilation Compiles to single binary Needs Python runtime installed Startup time Milliseconds Seconds (for heavy libs) Docker image size 5 15 MB (scratch) 200 500 MB Syntax Simple, minimal (25 keywords) Clean, but many ways to do same thing Error handling Explicit ( ) Exceptions OOP Composition via interfaces Full OOP + multiple inheritance Generics Yes (Go 1.18+) Yes (via type hints) Ecosystem Growing, strong for infra Massive, especially ML/data Learning curve Easy to learn, hard to master Easy to learn, easy to start Best for Cloud infra, microservices, CLIs, networking ML, scripting, data, prototyping"},{"id":"where-go-beats-python","heading":"Where Go Beats Python","level":3,"text":""},{"id":"where-python-beats-go","heading":"Where Python Beats Go","level":3,"text":""},{"id":"2-go-fundamentals","heading":"2. Go Fundamentals","level":2,"text":""},{"id":"program-structure","heading":"Program Structure","level":3,"text":""},{"id":"variables","heading":"Variables","level":3,"text":""},{"id":"zero-values","heading":"Zero Values","level":3,"text":""},{"id":"type-conversions","heading":"Type Conversions","level":3,"text":""},{"id":"control-flow","heading":"Control Flow","level":3,"text":""},{"id":"3-data-types-structures","heading":"3. Data Types & Structures","level":2,"text":""},{"id":"arrays-and-slices","heading":"Arrays and Slices","level":3,"text":""},{"id":"maps","heading":"Maps","level":3,"text":""},{"id":"strings-and-runes","heading":"Strings and Runes","level":3,"text":""},{"id":"4-functions-methods","heading":"4. Functions & Methods","level":2,"text":"Theory. Functions are first class values in Go (you can assign, pass, and return them), but the standout feature is multiple return values — idiomatically — which is how Go reports failures without exceptions. Methods are just functions with a receiver , which attaches behavior to a type. The key decision is value vs pointer receiver : a value receiver operates on a copy (safe, but can't mutate the original), while a pointer receiver can modify the struct and avoids copying large values. The rule of thumb: be consistent per type, and prefer a pointer receiver when the method mutates state or the struct is large. (which runs at function return in LIFO order) and closures round out the toolkit for reliable cleanup and stateful callbacks."},{"id":"5-structs-interfaces-oop-in-go","heading":"5. Structs, Interfaces & OOP in Go","level":2,"text":"Theory. Go deliberately has no classes and no inheritance . Its OOP is three simpler pieces: structs (group related data), methods (attach behavior to a type), and interfaces (declare required behavior). Reuse comes from composition via struct embedding — embedding one struct inside another \"promotes\" its fields and methods — instead of an inheritance hierarchy. The defining feature is that interfaces are satisfied implicitly (structural typing) : any type that has the required methods automatically implements the interface, with no keyword. This keeps code loosely coupled — you define a small interface describing exactly what you need and accept any type that fits — which is why idiomatic Go favors many tiny interfaces ( , ) over a few large ones."},{"id":"structs","heading":"Structs","level":3,"text":""},{"id":"methods","heading":"Methods","level":3,"text":""},{"id":"interfaces","heading":"Interfaces","level":3,"text":""},{"id":"generics-go-118","heading":"Generics (Go 1.18+)","level":3,"text":""},{"id":"6-error-handling","heading":"6. Error Handling","level":2,"text":"Theory. Go treats errors as ordinary values , not exceptions. Any function that can fail returns an as its last result, and the caller is expected to check it explicitly ( ). This makes every failure path visible right at the call site — more verbose than try/catch, but errors can't be silently swallowed or invisibly propagate. Add context while preserving the original by wrapping with , then inspect the chain with (does it match a known sentinel value?) or (extract a specific concrete error type). / exist too, but they're reserved for truly unexpected, programmer level bugs — not for normal, expected error conditions."},{"id":"7-goroutines-channels-concurrency","heading":"7. Goroutines & Channels — Concurrency","level":2,"text":"Theory. Concurrency is Go's signature strength. A goroutine is an extremely cheap unit of concurrent execution ( 2 KB initial stack) that the Go runtime multiplexes onto a small pool of OS threads (an M:N scheduler ), so a single program can run millions of them. Goroutines coordinate through channels — typed pipes that pass values between them — embodying Go's motto: \"don't communicate by sharing memory; share memory by communicating.\" Unbuffered channels synchronize sender and receiver (a rendezvous); buffered ones decouple them up to a capacity; and waits on several channel operations at once, which is how you build timeouts and cancellation. When shared mutable state is unavoidable, fall back to the package ( , , , ), and use the package to propagate deadlines and cancellation across goroutine boundaries."},{"id":"goroutines","heading":"Goroutines","level":3,"text":""},{"id":"syncwaitgroup","heading":"sync.WaitGroup","level":3,"text":""},{"id":"channels","heading":"Channels","level":3,"text":""},{"id":"select-statement","heading":"Select Statement","level":3,"text":""},{"id":"sync-package-mutexes","heading":"sync Package — Mutexes","level":3,"text":""},{"id":"context-package","heading":"Context Package","level":3,"text":""},{"id":"worker-pool-pattern","heading":"Worker Pool Pattern","level":3,"text":""},{"id":"8-packages-modules","heading":"8. Packages & Modules","level":2,"text":""},{"id":"9-go-standard-library-deep-dive","heading":"9. Go Standard Library Deep Dive","level":2,"text":""},{"id":"10-building-rest-apis-in-go","heading":"10. Building REST APIs in Go","level":2,"text":""},{"id":"using-nethttp-standard-library","heading":"Using net/http (Standard Library)","level":3,"text":""},{"id":"using-gin-most-popular-web-framework","heading":"Using Gin (Most Popular Web Framework)","level":3,"text":""},{"id":"database-with-sqlx-or-pgx","heading":"Database with sqlx or pgx","level":3,"text":""},{"id":"11-go-vs-java-key-differences","heading":"11. Go vs Java — Key Differences","level":2,"text":"Aspect Go Java OOP Composition (no inheritance) Classical inheritance Interfaces Implicit (structural) Explicit ( ) Error handling Return values Exceptions Concurrency Goroutines + channels Threads + synchronized Null safety nil (explicit check) NullPointerException risk Generics Yes (v1.18+) Yes (v5+) Compilation Fast ( seconds) Slower ( minutes for large projects) Binary Static, no runtime needed Requires JVM Pointers Yes (but no pointer arithmetic) No (only references) Package manager Go modules (built in) Maven/Gradle Verbosity Less verbose More verbose"},{"id":"12-memory-management-in-go","heading":"12. Memory Management in Go","level":2,"text":""},{"id":"13-testing-in-go","heading":"13. Testing in Go","level":2,"text":""},{"id":"14-advanced-go-patterns","heading":"14. Advanced Go Patterns","level":2,"text":""},{"id":"functional-options-pattern","heading":"Functional Options Pattern","level":3,"text":""},{"id":"pipeline-pattern","heading":"Pipeline Pattern","level":3,"text":""},{"id":"15-interview-qa","heading":"15. Interview Q&A","level":2,"text":"Q: What are goroutines and how are they different from OS threads? A: Goroutines are lightweight concurrent execution units managed by the Go runtime, not the OS. OS threads have 1 8MB fixed stack and are expensive to create. Goroutines start with 2KB stack that grows/shrinks dynamically. You can run millions of goroutines. The Go runtime uses an M:N scheduler — M goroutines are multiplexed across N OS threads. Goroutines are cheaper than threads in both creation time and memory. Q: How do channels work in Go? A: Channels are typed conduits for goroutine communication. Unbuffered channels block the sender until a receiver is ready (synchronous rendezvous). Buffered channels allow asynchronous sends up to the buffer capacity. allows waiting on multiple channels simultaneously. Close a channel to signal \"no more values\" — receivers can detect this. Never close a channel from the receiver side, and never close an already closed channel (panic). Q: Explain Go's interface system and why it's different from Java. A: Go interfaces are implicit/structural. A type implements an interface simply by having the required methods — no keyword needed. This enables duck typing in a statically typed language. Java interfaces are nominal — you must explicitly declare . Go's approach allows decoupling: you can define an interface in your package that matches a type in another package without modifying that package. This is called \"satisfy an interface retroactively.\" Q: How does Go handle errors differently from Python/Java? A: Go treats errors as values, not exceptions. Functions return tuples. The caller must explicitly check . This makes error handling visible at every call site. Python/Java use exceptions which can be silently swallowed or propagate up without explicit handling. Go's approach is more verbose but ensures errors are never accidentally ignored. and handle error wrapping/unwrapping. Q: What is the Go GC like compared to JVM? A: Go uses a concurrent, tri color mark and"}],"qa":[{"q":"What are goroutines and how are they different from OS threads?","a":"Goroutines are lightweight concurrent execution units managed by the Go runtime, not the OS. OS threads have ~1-8MB fixed stack and are expensive to create. Goroutines start with ~2KB stack that grows/shrinks dynamically. You can run millions of goroutines. The Go runtime uses an M:N scheduler — M goroutines are multiplexed across N OS threads. Goroutines are cheaper than threads in both creation time and memory."},{"q":"How do channels work in Go?","a":"Channels are typed conduits for goroutine communication. Unbuffered channels block the sender until a receiver is ready (synchronous rendezvous). Buffered channels allow asynchronous sends up to the buffer capacity. `select` allows waiting on multiple channels simultaneously. Close a channel to signal \"no more values\" — receivers can detect this. Never close a channel from the receiver side, and never close an already-closed channel (panic)."},{"q":"Explain Go's interface system and why it's different from Java.","a":"Go interfaces are implicit/structural. A type implements an interface simply by having the required methods — no `implements` keyword needed. This enables duck typing in a statically-typed language. Java interfaces are nominal — you must explicitly declare `implements`. Go's approach allows decoupling: you can define an interface in your package that matches a type in another package without modifying that package. This is called \"satisfy an interface retroactively.\""},{"q":"How does Go handle errors differently from Python/Java?","a":"Go treats errors as values, not exceptions. Functions return `(result, error)` tuples. The caller must explicitly check `if err != nil`. This makes error handling visible at every call site. Python/Java use exceptions which can be silently swallowed or propagate up without explicit handling. Go's approach is more verbose but ensures errors are never accidentally ignored. `errors.Is()` and `errors.As()` handle error wrapping/unwrapping."},{"q":"What is the Go GC like compared to JVM?","a":"Go uses a concurrent, tri-color mark-and-sweep GC with very low STW pauses (typically < 1ms). JVM uses generational GC (G1, ZGC) which has more sophisticated algorithms but can have longer pauses. Go's GC is simpler and more predictable for latency-sensitive workloads. Go doesn't use reference counting (unlike CPython), so circular references are handled automatically."},{"q":"What is the difference between `var` and `:=` in Go?","a":"`var x int = 5` is a full declaration and can be used at package level or inside functions. `:=` is short variable declaration and can only be used inside functions. `:=` infers the type from the right-hand side. At least one variable on the left of `:=` must be new (otherwise use `=`). `var` is required for zero-value initialization without assignment: `var x int` gives x=0."},{"q":"Why is Go better than Python for microservices?","a":"1) **Performance**: Go is 10-100x faster for CPU-bound work. 2) **Concurrency**: Goroutines handle 10,000+ concurrent connections easily; Python's GIL prevents true CPU parallelism. 3) **Deployment**: Go compiles to a single static binary — no Python runtime, no virtualenv, Docker images are ~5MB vs 200MB+. 4) **Memory**: Go uses 10-50x less memory than Python for equivalent workloads. 5) **Predictable latency**: Go GC pauses < 1ms; Python GC less predictable. 6) **Type safety**: Compile-time errors catch bugs Python would find at runtime."}]},"10_Python_System_Design_HLD.md":{"title":"Python HLD","trackId":"system-design","trackTitle":"System Design","sections":[{"id":"system-design-hld-python-edition","heading":"System Design HLD — Python Edition","level":1,"text":""},{"id":"concepts-python-specific-stack-real-world-designs","heading":"Concepts + Python-Specific Stack + Real-World Designs","level":3,"text":""},{"id":"1-python-in-production-architecture","heading":"1. Python in Production Architecture","level":2,"text":""},{"id":"where-python-fits","heading":"Where Python Fits","level":3,"text":""},{"id":"typical-python-backend-stack","heading":"Typical Python Backend Stack","level":3,"text":""},{"id":"2-scalability-with-python","heading":"2. Scalability with Python","level":2,"text":""},{"id":"the-gil-problem-solutions","heading":"The GIL Problem & Solutions","level":3,"text":""},{"id":"scaling-pattern-in-python","heading":"Scaling Pattern in Python","level":3,"text":""},{"id":"3-python-web-servers-deployment","heading":"3. Python Web Servers & Deployment","level":2,"text":""},{"id":"wsgi-vs-asgi","heading":"WSGI vs ASGI","level":3,"text":""},{"id":"docker-deployment","heading":"Docker Deployment","level":3,"text":""},{"id":"4-databases-with-python","heading":"4. Databases with Python","level":2,"text":""},{"id":"sqlalchemy-orm-core","heading":"SQLAlchemy — ORM & Core","level":3,"text":""},{"id":"connection-pooling-read-replicas","heading":"Connection Pooling & Read Replicas","level":3,"text":""},{"id":"database-migrations-alembic","heading":"Database Migrations — Alembic","level":3,"text":""},{"id":"nosql-mongodb-with-motor","heading":"NoSQL — MongoDB with Motor","level":3,"text":""},{"id":"5-caching-with-python","heading":"5. Caching with Python","level":2,"text":""},{"id":"redis-with-redis-py-aioredis","heading":"Redis with redis-py / aioredis","level":3,"text":""},{"id":"in-memory-cache-cachetools-functools","heading":"In-Memory Cache — cachetools / Functools","level":3,"text":""},{"id":"6-task-queues-celery-redis","heading":"6. Task Queues — Celery & Redis","level":2,"text":""},{"id":"celery-setup","heading":"Celery Setup","level":3,"text":""},{"id":"celery-tasks","heading":"Celery Tasks","level":3,"text":""},{"id":"7-message-queues-kafka-rabbitmq-in-python","heading":"7. Message Queues — Kafka & RabbitMQ in Python","level":2,"text":""},{"id":"kafka-with-confluent-kafka-aiokafka","heading":"Kafka with confluent-kafka / aiokafka","level":3,"text":""},{"id":"rabbitmq-with-aio-pika","heading":"RabbitMQ with aio-pika","level":3,"text":""},{"id":"8-microservices-in-python","heading":"8. Microservices in Python","level":2,"text":""},{"id":"service-to-service-communication","heading":"Service-to-Service Communication","level":3,"text":""},{"id":"service-discovery-with-consul","heading":"Service Discovery with Consul","level":3,"text":""},{"id":"distributed-tracing-with-opentelemetry","heading":"Distributed Tracing with OpenTelemetry","level":3,"text":""},{"id":"9-api-design-patterns-in-python","heading":"9. API Design Patterns in Python","level":2,"text":""},{"id":"rate-limiting","heading":"Rate Limiting","level":3,"text":""},{"id":"pagination","heading":"Pagination","level":3,"text":""},{"id":"10-monitoring-logging-observability","heading":"10. Monitoring, Logging & Observability","level":2,"text":""},{"id":"structured-logging","heading":"Structured Logging","level":3,"text":""},{"id":"prometheus-metrics","heading":"Prometheus Metrics","level":3,"text":""},{"id":"health-checks","heading":"Health Checks","level":3,"text":""},{"id":"11-real-world-system-designs-python-stack","heading":"11. Real-World System Designs (Python Stack)","level":2,"text":""},{"id":"design-1-url-shortener-in-python","heading":"Design 1: URL Shortener in Python","level":3,"text":""},{"id":"design-2-notification-service-in-python","heading":"Design 2: Notification Service in Python","level":3,"text":""},{"id":"design-3-real-time-chat-with-websocket-redis-pubsub","heading":"Design 3: Real-Time Chat with WebSocket + Redis Pub/Sub","level":3,"text":""},{"id":"12-python-vs-go-vs-java-for-system-design","heading":"12. Python vs Go vs Java for System Design","level":2,"text":""},{"id":"13-interview-qa","heading":"13. Interview Q&A","level":2,"text":"Q: How do you handle the GIL for CPU bound tasks in Python? A: Use instead of — each process has its own GIL. For ML inference, use . Celery workers are separate processes. Also, NumPy/Pandas release the GIL during their C level computations, so Python threads work fine for numerical work. For extreme performance, consider offloading to Go or Rust microservices. Q: How do you scale a Python FastAPI application? A: 1) Vertical: increase resources, add more Uvicorn workers ( ). 2) Horizontal: deploy multiple instances behind a load balancer (Nginx, AWS ALB), using stateless design — session in Redis, no local state. 3) Async I/O: use routes + DB/Redis clients for high concurrency within each worker. 4) Caching: Redis for frequent reads. 5) Background tasks: Celery for heavy work. Q: How does Celery work and when would you use it? A: Celery is a distributed task queue. Tasks are Python functions decorated with . When is called, Celery serializes the task + args and pushes to a broker (Redis/RabbitMQ). Worker processes pick tasks off the queue and execute them. Results are stored in a backend (Redis/DB). Use for: async email/SMS sending, report generation, image processing, scheduled jobs, anything that shouldn't block the HTTP response. Q: How would you implement idempotency in a Python API? A: Clients send an header (UUID). Server stores in Redis with TTL. On first request: process and store result. On duplicate request: return cached result immediately. For financial transactions, also store in DB with a unique constraint on the idempotency key. Q: How do you prevent cache stampede in Python? A: 1) Mutex lock : use Redis (set if not exists) to allow only one process to recompute. 2) Probabilistic early expiration : before key expires, probabilistically recompute it. 3) Background refresh : a separate task refreshes cache before it expires. Example with Redis lock: acquire lock, check cache again (double check), if still miss fetch from DB, release lock."}],"qa":[{"q":"How do you handle the GIL for CPU-bound tasks in Python?","a":"Use `multiprocessing` instead of `threading` — each process has its own GIL. For ML inference, use `ProcessPoolExecutor`. Celery workers are separate processes. Also, NumPy/Pandas release the GIL during their C-level computations, so Python threads work fine for numerical work. For extreme performance, consider offloading to Go or Rust microservices."},{"q":"How do you scale a Python FastAPI application?","a":"1) Vertical: increase resources, add more Uvicorn workers (`workers = 2 * CPU + 1`). 2) Horizontal: deploy multiple instances behind a load balancer (Nginx, AWS ALB), using stateless design — session in Redis, no local state. 3) Async I/O: use `async def` routes + `async` DB/Redis clients for high concurrency within each worker. 4) Caching: Redis for frequent reads. 5) Background tasks: Celery for heavy work."},{"q":"How does Celery work and when would you use it?","a":"Celery is a distributed task queue. Tasks are Python functions decorated with `@celery.task`. When `.delay()` is called, Celery serializes the task + args and pushes to a broker (Redis/RabbitMQ). Worker processes pick tasks off the queue and execute them. Results are stored in a backend (Redis/DB). Use for: async email/SMS sending, report generation, image processing, scheduled jobs, anything that shouldn't block the HTTP response."},{"q":"How would you implement idempotency in a Python API?","a":"Clients send an `Idempotency-Key` header (UUID). Server stores `key → result` in Redis with TTL. On first request: process and store result. On duplicate request: return cached result immediately. For financial transactions, also store in DB with a unique constraint on the idempotency key."},{"q":"How do you prevent cache stampede in Python?","a":"1) **Mutex lock**: use Redis `SET NX` (set if not exists) to allow only one process to recompute. 2) **Probabilistic early expiration**: before key expires, probabilistically recompute it. 3) **Background refresh**: a separate task refreshes cache before it expires. Example with Redis lock: acquire lock, check cache again (double check), if still miss fetch from DB, release lock."}]},"11_Python_System_Design_LLD.md":{"title":"Python LLD","trackId":"system-design","trackTitle":"System Design","sections":[{"id":"system-design-lld-python-edition","heading":"System Design LLD — Python Edition","level":1,"text":""},{"id":"solid-design-patterns-real-world-problems-in-python","heading":"SOLID + Design Patterns + Real-World Problems in Python","level":3,"text":""},{"id":"1-solid-principles-in-python","heading":"1. SOLID Principles in Python","level":2,"text":"Theory. SOLID is five object oriented design principles that make code easy to change, test, and extend — the backbone of good Low Level Design. The principles are language agnostic, but Python expresses them with lighter machinery than Java: structural typing via (no explicit ), to cut entity boilerplate, and duck typing that often removes the need for formal interfaces. Treat them as a review checklist — a violation almost always appears as code that's hard to modify without breaking something else. The mnemonic spells SOLID : S ingle Responsibility (one reason to change), O pen/Closed (extend without modifying), L iskov Substitution (subtypes are drop in replacements), I nterface Segregation (small, focused interfaces), and D ependency Inversion (depend on abstractions, inject concretions)."},{"id":"s-single-responsibility-principle","heading":"S — Single Responsibility Principle","level":3,"text":""},{"id":"o-openclosed-principle","heading":"O — Open/Closed Principle","level":3,"text":""},{"id":"l-liskov-substitution-principle","heading":"L — Liskov Substitution Principle","level":3,"text":""},{"id":"i-interface-segregation-principle","heading":"I — Interface Segregation Principle","level":3,"text":""},{"id":"d-dependency-inversion-principle","heading":"D — Dependency Inversion Principle","level":3,"text":""},{"id":"2-other-design-principles","heading":"2. Other Design Principles","level":2,"text":""},{"id":"3-design-patterns-in-python","heading":"3. Design Patterns in Python","level":2,"text":"Theory. Design patterns are named, reusable solutions to recurring design problems — a shared vocabulary that conveys an entire structure in a word. The classic Gang of Four catalog splits into Creational (how objects are made — Singleton, Factory, Builder), Structural (how objects are composed — Decorator, Proxy, Adapter, Facade), and Behavioral (how objects interact — Strategy, Observer, State, Template Method, Chain of Responsibility). What's distinctive in Python is that the language already gives you several patterns for free: a module is a natural Singleton , first class functions and decorators replace many Strategy/Decorator class hierarchies, and duck typing/Protocols make the \"program to an interface\" advice almost effortless. So apply patterns where they clarify intent — but reach for Python's built in idioms first rather than porting verbose Java structures."},{"id":"creational-patterns","heading":"Creational Patterns","level":3,"text":"Singleton Factory Method Builder Pattern"},{"id":"structural-patterns","heading":"Structural Patterns","level":3,"text":"Decorator Pattern (via Python's actual decorators) Proxy Pattern Observer Pattern Strategy Pattern Template Method Chain of Responsibility"},{"id":"4-python-specific-oop-patterns","heading":"4. Python-Specific OOP Patterns","level":2,"text":""},{"id":"context-manager-as-resource-pattern","heading":"Context Manager as Resource Pattern","level":3,"text":""},{"id":"descriptor-protocol","heading":"Descriptor Protocol","level":3,"text":""},{"id":"mixin-pattern","heading":"Mixin Pattern","level":3,"text":""},{"id":"repository-pattern","heading":"Repository Pattern","level":3,"text":""},{"id":"5-lld-problems-in-python","heading":"5. LLD Problems in Python","level":2,"text":""},{"id":"problem-1-parking-lot","heading":"Problem 1: Parking Lot","level":3,"text":""},{"id":"problem-2-vending-machine-state-pattern","heading":"Problem 2: Vending Machine (State Pattern)","level":3,"text":""},{"id":"problem-3-library-management-system","heading":"Problem 3: Library Management System","level":3,"text":""},{"id":"problem-4-splitwise","heading":"Problem 4: Splitwise","level":3,"text":""},{"id":"6-how-to-approach-lld-in-python-interviews","heading":"6. How to Approach LLD in Python Interviews","level":2,"text":""},{"id":"python-specific-lld-tips","heading":"Python-Specific LLD Tips","level":3,"text":""},{"id":"7-interview-qa","heading":"7. Interview Q&A","level":2,"text":"Q: How do you implement SOLID principles in Python differently from Java? A: Python uses for interface segregation (structural typing) instead of explicit . Python decorators are the idiomatic way to implement the Decorator pattern. Python's duck typing means you often don't need explicit interfaces for DIP — just type hint against an abstract class or Protocol. reduces boilerplate. The principles are the same, but Python is more concise. Q: How do you make a Python class thread safe? A: Use or around shared state access. Use for thread local storage. For counters, use , . In Python, the GIL protects individual bytecode operations, but compound operations (check then act) still need locks. Use for thread safe producer consumer patterns. Q: When would you use ABC vs Protocol in Python? A: Use when you want enforced abstract methods and want to provide default implementations. Subclasses must explicitly inherit. Use for duck typing — any class with the right methods satisfies the protocol without inheriting. Protocol is more Pythonic and flexible. Use Protocol for function parameters (structural subtyping), use ABC for class hierarchies with shared behavior. Q: How would you implement the Repository pattern in Python with FastAPI? A: Define an abstract with . Create concrete implementations ( , , ). Inject via FastAPI's — switch implementations by changing what's injected. For testing, inject — no database needed. This follows DIP and makes the service layer testable in isolation."}],"qa":[{"q":"How do you implement SOLID principles in Python differently from Java?","a":"Python uses `Protocol` for interface segregation (structural typing) instead of explicit `implements`. Python decorators are the idiomatic way to implement the Decorator pattern. Python's duck typing means you often don't need explicit interfaces for DIP — just type hint against an abstract class or Protocol. `@dataclass` reduces boilerplate. The principles are the same, but Python is more concise."},{"q":"How do you make a Python class thread-safe?","a":"Use `threading.Lock()` or `threading.RLock()` around shared state access. Use `threading.local()` for thread-local storage. For counters, use `threading.Event()`, `threading.Semaphore()`. In Python, the GIL protects individual bytecode operations, but compound operations (check-then-act) still need locks. Use `queue.Queue` for thread-safe producer-consumer patterns."},{"q":"When would you use ABC vs Protocol in Python?","a":"Use `ABC` when you want enforced abstract methods and want to provide default implementations. Subclasses must explicitly inherit. Use `Protocol` for duck typing — any class with the right methods satisfies the protocol without inheriting. Protocol is more Pythonic and flexible. Use Protocol for function parameters (structural subtyping), use ABC for class hierarchies with shared behavior."},{"q":"How would you implement the Repository pattern in Python with FastAPI?","a":"Define an abstract `BaseRepository` with `ABC`. Create concrete implementations (`SQLUserRepository`, `MongoUserRepository`, `InMemoryUserRepository`). Inject via FastAPI's `Depends()` — switch implementations by changing what's injected. For testing, inject `InMemoryUserRepository` — no database needed. This follows DIP and makes the service layer testable in isolation."}]},"12_Python_CP_Tricks.md":{"title":"Python CP Tricks","trackId":"dsa","trackTitle":"DSA & Algorithms","sections":[{"id":"python-competitive-programming-tricks-shortcuts","heading":"Python — Competitive Programming Tricks & Shortcuts","level":1,"text":""},{"id":"complete-cheat-sheet-leetcode-coding-interviews","heading":"Complete Cheat Sheet | LeetCode + Coding Interviews","level":3,"text":""},{"id":"1-fast-io-in-python","heading":"1. Fast I/O in Python","level":2,"text":""},{"id":"2-built-in-power-tools","heading":"2. Built-in Power Tools","level":2,"text":""},{"id":"essential-built-ins","heading":"Essential Built-ins","level":3,"text":""},{"id":"useful-python-idioms","heading":"Useful Python Idioms","level":3,"text":""},{"id":"3-collections-module-cp-arsenal","heading":"3. Collections Module — CP Arsenal","level":2,"text":""},{"id":"4-heapq-priority-queue-tricks","heading":"4. heapq — Priority Queue Tricks","level":2,"text":""},{"id":"5-bisect-binary-search-on-sorted-lists","heading":"5. bisect — Binary Search on Sorted Lists","level":2,"text":""},{"id":"6-itertools-functools","heading":"6. itertools & functools","level":2,"text":""},{"id":"7-string-tricks","heading":"7. String Tricks","level":2,"text":""},{"id":"8-math-number-theory","heading":"8. Math & Number Theory","level":2,"text":""},{"id":"9-bit-manipulation-in-python","heading":"9. Bit Manipulation in Python","level":2,"text":"Theory. Bit manipulation treats an integer as a row of bits and uses the bitwise operators ( , , , , , ) to compute things faster and more compactly than arithmetic. It's worth knowing for speed , for packing flags (a bitmask stores up to N booleans in one int — the basis of bitmask DP and subset enumeration), and for the classic identities interviewers expect: XOR to cancel duplicates, to clear the lowest set bit, to isolate it. One important Python twist: integers have arbitrary precision (no 32/64 bit overflow), which is convenient but means you can't rely on overflow wraparound — and use / to count set bits."},{"id":"10-algorithm-templates","heading":"10. Algorithm Templates","level":2,"text":""},{"id":"two-pointers","heading":"Two Pointers","level":3,"text":""},{"id":"binary-search-templates","heading":"Binary Search Templates","level":3,"text":""},{"id":"monotonic-stack","heading":"Monotonic Stack","level":3,"text":""},{"id":"dfsbfs-on-grid","heading":"DFS/BFS on Grid","level":3,"text":""},{"id":"union-find","heading":"Union-Find","level":3,"text":""},{"id":"11-dynamic-programming-templates","heading":"11. Dynamic Programming Templates","level":2,"text":"Theory. Every dynamic programming solution reduces to three decisions: define the state (what does each parameter mean?), write the transition (how is a state built from smaller ones?), and set the base cases . The templates below show the two ways to implement that. Top down memoization is especially clean in Python thanks to (or ), which turns a plain recursive function into a memoized one with a single decorator. Bottom up tabulation fills a table from the base cases upward and lets you shrink memory to one or two rows. Choose top down when the recurrence reads naturally; choose bottom up when you need to avoid Python's recursion depth limit or save memory."},{"id":"memoization-top-down","heading":"Memoization (Top-Down)","level":3,"text":""},{"id":"tabulation-bottom-up-templates","heading":"Tabulation (Bottom-Up) Templates","level":3,"text":""},{"id":"12-graph-templates","heading":"12. Graph Templates","level":2,"text":""},{"id":"13-python-specific-tricks-one-liners","heading":"13. Python-Specific Tricks & One-Liners","level":2,"text":""},{"id":"14-problem-pattern-recognition","heading":"14. Problem Pattern Recognition","level":2,"text":"Keywords Python Approach Data Structure \"Anagram / permutation\" Counter \"Frequency count\" Counter \"BFS shortest path\" + visited set deque \"Top K elements\" heapq \"Kth largest\" min heap of size k heapq \"Merge K sorted\" heap with (val, list idx, elem idx) heapq \"Sorted range queries\" bisect \"Consecutive elements\" + check set \"Two sum / pair sum\" or for complement set/dict \"Subarray sum = k\" prefix sum + defaultdict \"Interval merging\" sort by start, compare with prev end list \"Cycle detection\" slow/fast pointers — \"Connected components\" UnionFind or DFS+visited UnionFind/set \"Topological order\" Kahn's (BFS indegree) deque \"All subsets\" bitmask — \"All permutations\" itertools \"Combinations\" itertools \"Cartesian product\" itertools \"Memoize recursion\" / functools \"Running total/prefix\" itertools \"Sliding window max\" monotonic deque deque \"Next greater element\" monotonic stack list as stack"},{"id":"15-common-gotchas-mistakes","heading":"15. Common Gotchas & Mistakes","level":2,"text":""},{"id":"quick-reference-card","heading":"QUICK REFERENCE CARD","level":2,"text":""}],"qa":[]},"13_AWS_Complete_Guide.md":{"title":"AWS Complete Guide","trackId":"cloud-tools","trackTitle":"Cloud & Languages","sections":[{"id":"aws-complete-guide-beginner-to-pro","heading":"AWS — Complete Guide: Beginner to Pro","level":1,"text":""},{"id":"all-services-real-life-scenarios-interview-qa-situation-based-answers","heading":"All Services + Real-Life Scenarios + Interview Q&A + Situation-Based Answers","level":3,"text":""},{"id":"part-1-foundations","heading":"PART 1 — FOUNDATIONS","level":3,"text":"1. What is AWS? Cloud Fundamentals 2. IAM — Identity and Access Management 3. Compute — EC2 4. Compute — Lambda (Serverless) 5. Compute — ECS, EKS, Fargate (Containers) 6. Storage — S3 7. Storage — EBS, EFS, FSx"},{"id":"part-2-networking-databases","heading":"PART 2 — NETWORKING & DATABASES","level":3,"text":"8. Networking — VPC Deep Dive 9. Networking — Route 53, CloudFront, API Gateway, ELB 10. Databases — RDS & Aurora 11. Databases — DynamoDB 12. Databases — ElastiCache (Redis/Memcached) 13. Messaging — SQS, SNS, EventBridge, Kinesis"},{"id":"part-3-devops-security-monitoring","heading":"PART 3 — DEVOPS, SECURITY & MONITORING","level":3,"text":"14. Security — KMS, Secrets Manager, WAF, Shield, GuardDuty, Cognito 15. Monitoring — CloudWatch, X Ray, CloudTrail 16. DevOps — CodePipeline, CloudFormation, CDK, Elastic Beanstalk 17. Analytics — Kinesis Data Streams, Glue, Athena, Redshift 18. Other Key Services — Step Functions, AppSync, SES, Cognito"},{"id":"part-4-architecture-interviews","heading":"PART 4 — ARCHITECTURE & INTERVIEWS","level":3,"text":"19. Real World Architecture Patterns 20. AWS Well Architected Framework 21. Interview Questions — Core Concepts 22. Situation Based Interview Q&A 23. AWS Cost Optimization Strategies 24. Quick Reference Cheat Sheet 25. DEEP DIVE: AWS CDK (Infrastructure as Real Code)"},{"id":"part-1-foundations","heading":"PART 1 — FOUNDATIONS","level":1,"text":""},{"id":"1-what-is-aws-cloud-fundamentals","heading":"1. What is AWS? Cloud Fundamentals","level":2,"text":""},{"id":"cloud-computing-models","heading":"Cloud Computing Models","level":3,"text":""},{"id":"cloud-deployment-models","heading":"Cloud Deployment Models","level":3,"text":""},{"id":"aws-global-infrastructure","heading":"AWS Global Infrastructure","level":3,"text":""},{"id":"shared-responsibility-model","heading":"Shared Responsibility Model","level":3,"text":""},{"id":"aws-pricing-model","heading":"AWS Pricing Model","level":3,"text":""},{"id":"2-iam-identity-and-access-management","heading":"2. IAM — Identity and Access Management","level":2,"text":""},{"id":"core-concepts","heading":"Core Concepts","level":3,"text":""},{"id":"policy-types","heading":"Policy Types","level":3,"text":""},{"id":"iam-policy-example","heading":"IAM Policy Example","level":3,"text":""},{"id":"iam-roles-key-scenarios","heading":"IAM Roles — Key Scenarios","level":3,"text":""},{"id":"iam-best-practices","heading":"IAM Best Practices","level":3,"text":""},{"id":"3-compute-ec2","heading":"3. Compute — EC2","level":2,"text":""},{"id":"ec2-instance-types","heading":"EC2 Instance Types","level":3,"text":""},{"id":"ec2-purchasing-options","heading":"EC2 Purchasing Options","level":3,"text":""},{"id":"ec2-key-features","heading":"EC2 Key Features","level":3,"text":""},{"id":"ec2-auto-scaling","heading":"EC2 Auto Scaling","level":3,"text":""},{"id":"real-life-ec2-scenario","heading":"Real-Life EC2 Scenario","level":3,"text":""},{"id":"4-compute-lambda-serverless","heading":"4. Compute — Lambda (Serverless)","level":2,"text":""},{"id":"lambda-fundamentals","heading":"Lambda Fundamentals","level":3,"text":""},{"id":"lambda-triggers-use-cases","heading":"Lambda Triggers & Use Cases","level":3,"text":""},{"id":"lambda-configuration-deep-dive","heading":"Lambda Configuration Deep Dive","level":3,"text":""},{"id":"lambda-best-practices","heading":"Lambda Best Practices","level":3,"text":""},{"id":"lambda-real-life-scenarios","heading":"Lambda Real-Life Scenarios","level":3,"text":""},{"id":"5-compute-ecs-eks-fargate-containers","heading":"5. Compute — ECS, EKS, Fargate (Containers)","level":2,"text":""},{"id":"container-services-overview","heading":"Container Services Overview","level":3,"text":""},{"id":"ecs-key-concepts","heading":"ECS Key Concepts","level":3,"text":""},{"id":"eks-essentials","heading":"EKS Essentials","level":3,"text":""},{"id":"6-storage-s3","heading":"6. Storage — S3","level":2,"text":""},{"id":"s3-fundamentals","heading":"S3 Fundamentals","level":3,"text":""},{"id":"s3-storage-classes-cost-optimization","heading":"S3 Storage Classes (Cost Optimization)","level":3,"text":""},{"id":"s3-security","heading":"S3 Security","level":3,"text":""},{"id":"s3-advanced-features","heading":"S3 Advanced Features","level":3,"text":""},{"id":"7-storage-ebs-efs-fsx","heading":"7. Storage — EBS, EFS, FSx","level":2,"text":""},{"id":"ebs-elastic-block-store","heading":"EBS (Elastic Block Store)","level":3,"text":""},{"id":"efs-elastic-file-system","heading":"EFS (Elastic File System)","level":3,"text":""},{"id":"aws-fsx","heading":"AWS FSx","level":3,"text":""},{"id":"part-2-networking-databases","heading":"PART 2 — NETWORKING & DATABASES","level":1,"text":""},{"id":"8-networking-vpc-deep-dive","heading":"8. Networking — VPC Deep Dive","level":2,"text":""},{"id":"vpc-fundamentals","heading":"VPC Fundamentals","level":3,"text":""},{"id":"vpc-components","heading":"VPC Components","level":3,"text":""},{"id":"vpc-advanced-features","heading":"VPC Advanced Features","level":3,"text":""},{"id":"9-networking-route-53-cloudfront-api-gateway-elb","heading":"9. Networking — Route 53, CloudFront, API Gateway, ELB","level":2,"text":""},{"id":"route-53","heading":"Route 53","level":3,"text":""},{"id":"cloudfront-cdn","heading":"CloudFront (CDN)","level":3,"text":""},{"id":"elastic-load-balancing-elb","heading":"Elastic Load Balancing (ELB)","level":3,"text":""},{"id":"api-gateway","heading":"API Gateway","level":3,"text":""},{"id":"10-databases-rds-aurora","heading":"10. Databases — RDS & Aurora","level":2,"text":""},{"id":"rds-relational-database-service","heading":"RDS (Relational Database Service)","level":3,"text":""},{"id":"aurora","heading":"Aurora","level":3,"text":""},{"id":"11-databases-dynamodb","heading":"11. Databases — DynamoDB","level":2,"text":""},{"id":"dynamodb-fundamentals","heading":"DynamoDB Fundamentals","level":3,"text":""},{"id":"dynamodb-access-patterns","heading":"DynamoDB Access Patterns","level":3,"text":""},{"id":"dynamodb-advanced","heading":"DynamoDB Advanced","level":3,"text":""},{"id":"12-databases-elasticache-redismemcached","heading":"12. Databases — ElastiCache (Redis/Memcached)","level":2,"text":""},{"id":"elasticache-overview","heading":"ElastiCache Overview","level":3,"text":""},{"id":"redis-vs-memcached","heading":"Redis vs Memcached","level":3,"text":""},{"id":"elasticache-redis-architecture","heading":"ElastiCache Redis Architecture","level":3,"text":""},{"id":"13-messaging-sqs-sns-eventbridge-kinesis","heading":"13. Messaging — SQS, SNS, EventBridge, Kinesis","level":2,"text":""},{"id":"sqs-simple-queue-service","heading":"SQS (Simple Queue Service)","level":3,"text":""},{"id":"sns-simple-notification-service","heading":"SNS (Simple Notification Service)","level":3,"text":""},{"id":"eventbridge","heading":"EventBridge","level":3,"text":""},{"id":"kinesis","heading":"Kinesis","level":3,"text":""},{"id":"part-3-devops-security-monitoring","heading":"PART 3 — DEVOPS, SECURITY & MONITORING","level":1,"text":""},{"id":"14-security-kms-secrets-manager-waf-shield-guardduty-cognito","heading":"14. Security — KMS, Secrets Manager, WAF, Shield, GuardDuty, Cognito","level":2,"text":""},{"id":"kms-key-management-service","heading":"KMS (Key Management Service)","level":3,"text":""},{"id":"secrets-manager","heading":"Secrets Manager","level":3,"text":""},{"id":"waf-web-application-firewall","heading":"WAF (Web Application Firewall)","level":3,"text":""},{"id":"shield","heading":"Shield","level":3,"text":""},{"id":"guardduty","heading":"GuardDuty","level":3,"text":""},{"id":"cognito","heading":"Cognito","level":3,"text":""},{"id":"15-monitoring-cloudwatch-x-ray-cloudtrail","heading":"15. Monitoring — CloudWatch, X-Ray, CloudTrail","level":2,"text":""},{"id":"cloudwatch","heading":"CloudWatch","level":3,"text":""},{"id":"x-ray-distributed-tracing","heading":"X-Ray (Distributed Tracing)","level":3,"text":""},{"id":"cloudtrail","heading":"CloudTrail","level":3,"text":""},{"id":"16-devops-codepipeline-cloudformation-cdk-elastic-beanstalk","heading":"16. DevOps — CodePipeline, CloudFormation, CDK, Elastic Beanstalk","level":2,"text":""},{"id":"cicd-on-aws","heading":"CI/CD on AWS","level":3,"text":""},{"id":"cloudformation-infrastructure-as-code","heading":"CloudFormation (Infrastructure as Code)","level":3,"text":""},{"id":"aws-cdk-cloud-development-kit","heading":"AWS CDK (Cloud Development Kit)","level":3,"text":""},{"id":"elastic-beanstalk","heading":"Elastic Beanstalk","level":3,"text":""},{"id":"17-analytics-kinesis-data-streams-glue-athena-redshift","heading":"17. Analytics — Kinesis Data Streams, Glue, Athena, Redshift","level":2,"text":""},{"id":"aws-glue","heading":"AWS Glue","level":3,"text":""},{"id":"athena","heading":"Athena","level":3,"text":""},{"id":"redshift","heading":"Redshift","level":3,"text":""},{"id":"18-other-key-services-step-functions-appsync-ses","heading":"18. Other Key Services — Step Functions, AppSync, SES","level":2,"text":""},{"id":"step-functions","heading":"Step Functions","level":3,"text":""},{"id":"amazon-ses-simple-email-service","heading":"Amazon SES (Simple Email Service)","level":3,"text":""},{"id":"part-4-architecture-interviews","heading":"PART 4 — ARCHITECTURE & INTERVIEWS","level":1,"text":""},{"id":"19-real-world-architecture-patterns","heading":"19. Real-World Architecture Patterns","level":2,"text":""},{"id":"pattern-1-serverless-web-application","heading":"Pattern 1: Serverless Web Application","level":3,"text":""},{"id":"pattern-2-three-tier-web-application-traditional","heading":"Pattern 2: Three-Tier Web Application (Traditional)","level":3,"text":""},{"id":"pattern-3-microservices-with-eks","heading":"Pattern 3: Microservices with EKS","level":3,"text":""},{"id":"pattern-4-data-lake-architecture","heading":"Pattern 4: Data Lake Architecture","level":3,"text":""},{"id":"20-aws-well-architected-framework","heading":"20. AWS Well-Architected Framework","level":2,"text":""},{"id":"21-interview-questions-core-concepts","heading":"21. Interview Questions — Core Concepts","level":2,"text":""},{"id":"compute","heading":"Compute","level":3,"text":"Q: What's the difference between EC2 Reserved Instances and Savings Plans? A: Both offer discounts for commitment. Reserved Instances lock you to a specific instance type and region, offering up to 72% discount. Savings Plans are more flexible — you commit to a minimum $/hour spend but can change instance types, regions, or even use it for Lambda and Fargate (Compute Savings Plan). Savings Plans are generally preferred for new commitments due to flexibility. Q: When would you choose Lambda over EC2? A: Lambda for: event driven, short lived tasks, variable traffic, no idle servers needed, < 15 minute execution. EC2 for: long running processes, stateful apps, GPU workloads, full OS control, predictable steady state traffic where Reserved Instances save more money. Q: Explain EC2 cold start and how to mitigate it. A: Lambda cold start happens when a new container is initialized (first invocation or after scale out). Mitigation: 1) Provisioned Concurrency (keep N warm instances), 2) SnapStart for Java 11+, 3) use lighter runtimes (Python/Node vs Java), 4) minimize package size, 5) lazy load heavy libraries inside handler, 6) avoid VPC if possible (VPC Lambda adds 100ms for ENI creation in older configurations)."},{"id":"storage-databases","heading":"Storage & Databases","level":3,"text":"Q: When would you choose DynamoDB over RDS? A: DynamoDB for: single digit millisecond latency, automatic scaling, no schema migrations, key value/document access patterns, massive scale (millions of RPS), serverless architectures, gaming, IoT, ad tech. RDS for: complex queries (JOINs), transactional integrity across many tables, relational data model, reporting with complex SQL, team familiar with SQL. Q: Explain S3 consistency model. A: Since December 2020, S3 provides strong consistency for all operations. PutObject followed immediately by GetObject returns the new object. ListObjects after PutObject returns the new object in the list. This means no need for eventual consistency workarounds anymore. Q: What is S3 Intelligent Tiering and when should you use it? A: S3 Intelligent Tiering auto moves objects between access tiers based on usage patterns. Objects not accessed for 30 days go to Infrequent Access (lower storage cost), then Archive Instant Access (90 days), optionally Archive and Deep Archive. There's a small monitoring fee ( $0.0025 per 1,000 objects). Use when: access patterns are unpredictable or changing, objects are 128 KB (below that the monitoring fee outweighs savings)."},{"id":"networking","heading":"Networking","level":3,"text":"Q: What is VPC peering vs Transit Gateway? A: VPC Peering: direct 1 to 1 connection, non transitive, cheapest option for few VPCs. Transit Gateway: hub and spoke model, transitive routing, centralized, supports up to 5,000 VPCs. Use peering for 2 5 VPCs; use Transit Gateway for 5+ VPCs or when you need centralized routing, VPN, or Direct Connect integration. Q: What is the difference between Security Groups and NACLs? A: Security Groups are stateful instance level firewalls (return traffic automatically allowed, ALLOW only). NACLs are stateless subnet level firewalls (must explicitly allow both inbound and outbound, ALLOW and DENY rules evaluated in order). Start with Security Groups; add NACLs for subnet level DENY rules (e.g., block specific IP ranges at subnet level)."},{"id":"security","heading":"Security","level":3,"text":"Q: How do you securely store database credentials in AWS? A: Use AWS Secrets Manager. Store credentials as a secret. Enable auto rotation (Lambda function rotates DB password + updates RDS). App retrieves secret at runtime via SDK — never hardcoded. Use IAM roles for the app (EC2/Lambda/ECS) to access Secrets Manager. KMS encrypts the secret at rest. Enable CloudTrail to audit all access to the secret. Q: What is IAM role assumption and when is it used? A: Role assumption is when a principal (user, service, or account) temporarily takes on the permissions of an IAM role by calling STS AssumeRole. Used for: EC2/Lambda accessing AWS services (service roles), cross account access, identity federation (allow corporate AD users to access AWS), privilege escalation patterns, CI/CD pipelines assuming deployment roles."},{"id":"22-situation-based-interview-qa","heading":"22. Situation-Based Interview Q&A","level":2,"text":""},{"id":"situation-1-high-availability","heading":"Situation 1: High Availability","level":3,"text":"\"Your application is running on a single EC2 instance. The business requires 99.99% uptime. How would you redesign it?\" Answer:"},{"id":"situation-2-cost-reduction","heading":"Situation 2: Cost Reduction","level":3,"text":"\"Your AWS bill is $50,000/month and the team wants to reduce it by 30%. How do you approach it?\" Answer:"},{"id":"situation-3-slow-application","heading":"Situation 3: Slow Application","level":3,"text":"\"Users are complaining the application is slow (page load 5 seconds). How do you troubleshoot and fix it?\" Answer:"},{"id":"situation-4-security-breach","heading":"Situation 4: Security Breach","level":3,"text":"\"CloudTrail shows unusual API calls — someone is deleting S3 objects in the middle of the night. What do you do?\" Answer:"},{"id":"situation-5-traffic-spike-black-friday","heading":"Situation 5: Traffic Spike (Black Friday)","level":3,"text":"\"Black Friday is in 2 weeks. You expect 100x normal traffic. How do you prepare your AWS infrastructure?\" Answer:"},{"id":"situation-6-migrate-on-premises-app-to-aws","heading":"Situation 6: Migrate On-Premises App to AWS","level":3,"text":"\"You have a Java Spring Boot app with PostgreSQL database on premises. How do you migrate to AWS?\" Answer:"},{"id":"23-aws-cost-optimization-strategies","heading":"23. AWS Cost Optimization Strategies","level":2,"text":""},{"id":"24-quick-reference-cheat-sheet","heading":"24. Quick Reference Cheat Sheet","level":2,"text":""},{"id":"25-deep-dive-aws-cdk-infrastructure-as-real-code","heading":"25. DEEP DIVE: AWS CDK (Infrastructure as Real Code)","level":2,"text":"Theory. The AWS Cloud Development Kit (CDK) lets you define your cloud infrastructure using a real programming language — Java, TypeScript, Python, C , or Go — instead of writing thousands of lines of YAML/JSON. You write code; the CDK synthesizes it into a CloudFormation template, and CloudFormation provisions the actual resources. So CDK is not a separate provisioning engine — it's a higher level authoring layer on top of CloudFormation . You get loops, conditionals, functions, classes, IDE autocomplete, type checking, unit tests, and package reuse — all the things plain templates lack. Analogy. Writing CloudFormation YAML by hand is like writing assembly: powerful but verbose and repetitive. CDK is the high level language that compiles down to that assembly — you express intent (\"a load balanced Fargate service\") and it generates the hundreds of lines of low level resource definitions for you. Why teams adopt CDK: Less boilerplate — one high level construct can expand into 30+ CloudFormation resources with sensible, secure defaults. Abstraction & reuse — package a \"standard microservice\" pattern as a class and reuse it across teams (like a library). Type safety & IDE help — typos and wrong property types are caught at compile time, not at deploy time 10 minutes later. Still CloudFormation underneath — you keep drift detection, rollback, and change sets; you're not locked into a third party state file."},{"id":"251-the-mental-model-app-stack-construct","heading":"25.1 The Mental Model — App → Stack → Construct","level":3,"text":"CDK apps are a tree of constructs . There are exactly three core concepts: App — the root of the tree; the entry point of your program. When you call , the entire tree is converted to CloudFormation templates. Stack — the unit of deployment. One stack = one CloudFormation stack = created/updated/deleted together. You split infrastructure into multiple stacks (e.g., , , ) to deploy and manage them independently. Construct — the fundamental building block. A construct can represent a single resource (an S3 bucket) or a reusable assembly of many resources (a whole web service). Every construct takes — is its parent in the tree, is a locally unique name, are its configuration. Why matters. CDK builds resource names by walking the tree path, so the you give must be unique within its parent . This path also produces stable logical IDs in the generated CloudFormation — renaming a construct's can cause CloudFormation to delete and recreate the resource, so treat ids as semi permanent."},{"id":"252-construct-levels-l1-l2-l3-a-key-interview-point","heading":"25.2 Construct Levels — L1, L2, L3 (a key interview point)","level":3,"text":"Constructs come in three levels of abstraction. Knowing the difference is a classic CDK interview question. Level Name What it is Example L1 CFN Resources ( ) Raw 1:1 mapping to a CloudFormation resource. Verbose, no defaults. L2 Curated constructs Higher level, with sensible defaults, helper methods, and security best practices baked in. The level you use most. (with , encryption defaults) L3 Patterns Opinionated assemblies of many resources for a complete use case. (ALB + ECS + Fargate + security groups + IAM, in one object) Example of the abstraction payoff. An L3 is 10 lines of code but synthesizes into a VPC wiring, an ECS cluster reference, a task definition, a service, an Application Load Balancer, target groups, listeners, security groups, and IAM roles — easily 300+ lines of CloudFormation. You'd hand write and debug all of that with raw YAML. Pitfall. Beginners reach for L1 ( ) because it looks familiar, then re implement everything L2 already gives you. Default to L2 ; drop to L1 only for a brand new AWS feature CDK hasn't wrapped yet."},{"id":"253-how-cdk-synth-and-cdk-deploy-actually-work","heading":"25.3 How cdk synth and cdk deploy Actually Work","level":3,"text":"The lifecycle, step by step: 1. You run . CDK executes your program (it's just a Java/TS app). 2. Constructs are instantiated, forming the App→Stack→Construct tree. 3. Synthesis : CDK walks the tree and emits a CloudFormation template (plus an assembly of assets) into the folder. This is what does on its own — and it's the command to run to see exactly what will be created . 4. CDK uploads any assets (Docker images, Lambda zip bundles) to a bootstrap S3 bucket / ECR repo. 5. CloudFormation creates a change set (the diff between current and desired state) and applies it — creating, updating, or deleting resources, with automatic rollback on failure. Why exists (common gotcha). Before your first deploy in an account/region, CDK needs a place to store assets and a deployment role. creates this \"CDKToolkit\" stack once. Forgetting it gives a confusing \"this stack uses assets, so the toolkit stack is required\" error."},{"id":"254-worked-example-cdk-in-java-your-stack","heading":"25.4 Worked Example — CDK in Java (your stack)","level":3,"text":"Since you work in Java, here's a realistic stack: an S3 bucket, a DynamoDB table, and a Lambda that reads both — with permissions wired by methods (no hand written IAM JSON). The standout feature here is . Instead of hand writing an IAM policy (and almost always making it too broad), generates the exact least privilege policy and attaches it to the Lambda's role. This single feature prevents a huge class of security mistakes."},{"id":"255-the-same-idea-in-typescript-most-common-in-the-cdk-community","heading":"25.5 The Same Idea in TypeScript (most common in the CDK community)","level":3,"text":"Same structure, same model — only the syntax differs. This is why CDK skills transfer across languages."},{"id":"256-reusable-constructs-the-real-power","heading":"25.6 Reusable Constructs (the real power)","level":3,"text":"Beyond stacks, you create your own constructs to encode your org's standards once and reuse everywhere — the thing raw CloudFormation can't do well. Now any team writes and automatically gets the DLQ + retry policy your platform team mandates. This is how CDK scales good practices across an organization."},{"id":"257-environments-context-configuration","heading":"25.7 Environments, Context & Configuration","level":3,"text":"Environment ( ) — the target account + region for a stack. Specify it explicitly for production code so synthesis is deterministic (region specific resources like AMIs resolve correctly). Context — key/value configuration passed via or , used to vary behavior per environment ( vs ) without changing code. Multi environment pattern — instantiate the same stack class multiple times with different props: This \"same code, different config\" pattern is exactly what's painful in copy pasted YAML and trivial in CDK."},{"id":"258-testing-infrastructure-a-big-differentiator","heading":"25.8 Testing Infrastructure (a big differentiator)","level":3,"text":"Because CDK is real code, you can unit test your infrastructure before deploying — assert the synthesized template contains what you expect. Interview angle. \"How do you test infrastructure?\" — CDK supports fine grained assertions (assert specific resources/properties exist in the synthesized template) and snapshot tests (fail if the template changes unexpectedly). This catches misconfigurations in CI before they ever reach AWS."},{"id":"259-cdk-in-cicd","heading":"25.9 CDK in CI/CD","level":3,"text":"Two common approaches: from a pipeline — simplest; the CI job runs with deploy credentials. CDK Pipelines — a self mutating pipeline construct: you define the pipeline in CDK , and it can update itself when you add stages. Powerful for multi account/multi stage rollouts. A typical flow: PR → + unit tests + (posted to the PR for review) → on merge, to staging → manual approval → to prod. Store credentials in your CI secret store, never in code."},{"id":"2510-cdk-vs-cloudformation-vs-terraform","heading":"25.10 CDK vs CloudFormation vs Terraform","level":3,"text":"CloudFormation CDK Terraform Authoring YAML/JSON Real code (Java/TS/Python…) HCL (its own language) Engine CloudFormation CloudFormation (synthesizes to it) Terraform engine + state file Cloud scope AWS only AWS only Multi cloud Abstraction/reuse Limited (nested stacks) High (constructs, classes) Modules State management Managed by AWS Managed by AWS (via CFN) You manage One liner: \"CDK gives you a real programming language and high level constructs while keeping CloudFormation's managed state and rollback; Terraform is the choice when you need multi cloud and are willing to manage your own state.\""},{"id":"2511-best-practices-common-pitfalls","heading":"25.11 Best Practices & Common Pitfalls","level":3,"text":"Best practices: Default to L2 constructs; build L3/custom constructs to encode standards. Use methods for IAM — never hand write broad policies. Split into multiple stacks by lifecycle (network/data/app) and pass references between them. Always run before deploy and review it like a code change. Unit test templates; pin the CDK library version. Common pitfalls: Forgetting → asset deploys fail mysteriously. Renaming construct s → changes logical IDs → CloudFormation replaces (and can delete) resources. Be deliberate. Hardcoding account/region instead of using /context → breaks across environments. Putting secrets in code/context → use Secrets Manager / SSM Parameter Store and reference them. One giant stack for everything → slow deploys and a blast radius covering unrelated resources."},{"id":"2512-cdk-interview-qa","heading":"25.12 CDK Interview Q&A","level":3,"text":"Q1. What is AWS CDK and how does it relate to CloudFormation? CDK is a framework to define infrastructure in a real programming language; it synthesizes to CloudFormation , which does the actual provisioning. You get code level abstraction and tooling while keeping CloudFormation's managed state, change sets, and rollback. Q2. Explain L1, L2, L3 constructs. L1 ( ) are raw 1:1 CloudFormation mappings; L2 are curated constructs with sensible defaults and helper methods (the everyday choice); L3 are opinionated patterns assembling many resources for a complete use case (e.g., a load balanced Fargate service). Q3. What does do vs ? runs your program and emits the CloudFormation template + assets into (inspect only). synthesizes, uploads assets, creates a change set, and applies it via CloudFormation. Q4. Why is needed? It provisions the one time \"toolkit\" resources (S3 bucket for assets, ECR repo, deploy roles) per account/region that CDK relies on for asset based deploys. Q5. How does CDK handle IAM permissions? Through methods ( , ) that generate least privilege policies and attach them automatically — safer than hand written JSON. Q6. How do you test CDK infrastructure? With the assertions module: synthesize the stack and assert specific resources/properties exist (fine grained assertions) or use snapshot tests to detect unintended template changes — all in CI before deploy. Q7. How do you manage multiple environments (dev/prod)? Instantiate the same stack class with different props/context and explicit (account+region) — \"same code, different configuration\" — rather than duplicating templates. Q8. What's a risk of renaming a construct's id? It changes the resource's CloudFormation logical ID, which can cause CloudFormation to delete and recreate the resource (data loss for stateful resources). Treat ids as stable."}],"qa":[{"q":"What's the difference between EC2 Reserved Instances and Savings Plans?","a":"Both offer discounts for commitment. Reserved Instances lock you to a specific instance type and region, offering up to 72% discount. Savings Plans are more flexible — you commit to a minimum $/hour spend but can change instance types, regions, or even use it for Lambda and Fargate (Compute Savings Plan). Savings Plans are generally preferred for new commitments due to flexibility."},{"q":"When would you choose Lambda over EC2?","a":"Lambda for: event-driven, short-lived tasks, variable traffic, no idle servers needed, < 15-minute execution. EC2 for: long-running processes, stateful apps, GPU workloads, full OS control, predictable steady-state traffic where Reserved Instances save more money."},{"q":"Explain EC2 cold start and how to mitigate it.","a":"Lambda cold start happens when a new container is initialized (first invocation or after scale-out). Mitigation: 1) Provisioned Concurrency (keep N warm instances), 2) SnapStart for Java 11+, 3) use lighter runtimes (Python/Node vs Java), 4) minimize package size, 5) lazy-load heavy libraries inside handler, 6) avoid VPC if possible (VPC Lambda adds ~100ms for ENI creation in older configurations)."},{"q":"When would you choose DynamoDB over RDS?","a":"DynamoDB for: single-digit millisecond latency, automatic scaling, no schema migrations, key-value/document access patterns, massive scale (millions of RPS), serverless architectures, gaming, IoT, ad tech. RDS for: complex queries (JOINs), transactional integrity across many tables, relational data model, reporting with complex SQL, team familiar with SQL."},{"q":"Explain S3 consistency model.","a":"Since December 2020, S3 provides strong consistency for all operations. PutObject followed immediately by GetObject returns the new object. ListObjects after PutObject returns the new object in the list. This means no need for eventual consistency workarounds anymore."},{"q":"What is S3 Intelligent-Tiering and when should you use it?","a":"S3 Intelligent-Tiering auto-moves objects between access tiers based on usage patterns. Objects not accessed for 30 days go to Infrequent Access (lower storage cost), then Archive Instant Access (90 days), optionally Archive and Deep Archive. There's a small monitoring fee (~$0.0025 per 1,000 objects). Use when: access patterns are unpredictable or changing, objects are > 128 KB (below that the monitoring fee outweighs savings)."},{"q":"What is VPC peering vs Transit Gateway?","a":"VPC Peering: direct 1-to-1 connection, non-transitive, cheapest option for few VPCs. Transit Gateway: hub-and-spoke model, transitive routing, centralized, supports up to 5,000 VPCs. Use peering for 2-5 VPCs; use Transit Gateway for 5+ VPCs or when you need centralized routing, VPN, or Direct Connect integration."},{"q":"What is the difference between Security Groups and NACLs?","a":"Security Groups are stateful instance-level firewalls (return traffic automatically allowed, ALLOW only). NACLs are stateless subnet-level firewalls (must explicitly allow both inbound and outbound, ALLOW and DENY rules evaluated in order). Start with Security Groups; add NACLs for subnet-level DENY rules (e.g., block specific IP ranges at subnet level)."},{"q":"How do you securely store database credentials in AWS?","a":"Use AWS Secrets Manager. Store credentials as a secret. Enable auto-rotation (Lambda function rotates DB password + updates RDS). App retrieves secret at runtime via SDK — never hardcoded. Use IAM roles for the app (EC2/Lambda/ECS) to access Secrets Manager. KMS encrypts the secret at rest. Enable CloudTrail to audit all access to the secret."},{"q":"What is IAM role assumption and when is it used?","a":"Role assumption is when a principal (user, service, or account) temporarily takes on the permissions of an IAM role by calling STS AssumeRole. Used for: EC2/Lambda accessing AWS services (service roles), cross-account access, identity federation (allow corporate AD users to access AWS), privilege escalation patterns, CI/CD pipelines assuming deployment roles."},{"q":"What is AWS CDK and how does it relate to CloudFormation?","a":"CDK is a framework to define infrastructure in a real programming language; it **synthesizes to CloudFormation**, which does the actual provisioning. You get code-level abstraction and tooling while keeping CloudFormation's managed state, change sets, and rollback."},{"q":"Explain L1, L2, L3 constructs.","a":"L1 (`Cfn*`) are raw 1:1 CloudFormation mappings; L2 are curated constructs with sensible defaults and helper methods (the everyday choice); L3 are opinionated patterns assembling many resources for a complete use case (e.g., a load-balanced Fargate service)."},{"q":"What does `cdk synth` do vs `cdk deploy`?","a":"`synth` runs your program and emits the CloudFormation template + assets into `cdk.out/` (inspect-only). `deploy` synthesizes, uploads assets, creates a change set, and applies it via CloudFormation."},{"q":"Why is `cdk bootstrap` needed?","a":"It provisions the one-time \"toolkit\" resources (S3 bucket for assets, ECR repo, deploy roles) per account/region that CDK relies on for asset-based deploys."},{"q":"How does CDK handle IAM permissions?","a":"Through `grant*` methods (`grantRead`, `grantReadWriteData`) that generate **least-privilege** policies and attach them automatically — safer than hand-written JSON."},{"q":"How do you test CDK infrastructure?","a":"With the assertions module: synthesize the stack and assert specific resources/properties exist (fine-grained assertions) or use snapshot tests to detect unintended template changes — all in CI before deploy."},{"q":"How do you manage multiple environments (dev/prod)?","a":"Instantiate the same stack class with different props/context and explicit `env` (account+region) — \"same code, different configuration\" — rather than duplicating templates."},{"q":"What's a risk of renaming a construct's id?","a":"It changes the resource's CloudFormation logical ID, which can cause CloudFormation to delete and recreate the resource (data loss for stateful resources). Treat ids as stable."}]},"14_AI_Agents_and_MCP_For_Developers.md":{"title":"AI Agents & MCP","trackId":"ai-mcp","trackTitle":"AI & MCP","sections":[{"id":"ai-agents-mcp-complete-developer-guide","heading":"AI Agents & MCP — Complete Developer Guide","level":1,"text":""},{"id":"beginner-pro-build-agents-mcp-servers-prompts-context","heading":"Beginner → Pro | Build Agents, MCP Servers, Prompts & Context","level":3,"text":"Note: You wrote \"MPC\" — in the AI world the standard term is MCP (Model Context Protocol) . This guide covers MCP. Java topics are in a separate guide: — keep that and this file independent. Important: You do not \"train\" MCP servers like ML models. MCP servers are code you write . Agents are configured via prompts, tools, RAG, and optionally fine tuning. Section 20 explains this fully."},{"id":"foundations","heading":"Foundations","level":3,"text":"1. What Is an AI Agent? 2. Core Concepts Before You Code 3. What Is MCP (Model Context Protocol)? 4. How Agents + MCP + Tools Work Together"},{"id":"build-learn","heading":"Build & Learn","level":3,"text":"5. Tech Stack — What to Learn 6. Learning Roadmap — 12 Weeks (Beginner) 7. Week by Week Hands On Plan 8. Build Your First MCP Server 9. Build Your First Agent (Concept + Code) 10. Cursor Specific: Agents in Your IDE 11. Cursor SDK — Run Agents from Code"},{"id":"beginner-pro-core-skills","heading":"Beginner → Pro (Core Skills)","level":3,"text":"16. Prompt Engineering — Write Effective Prompts 17. Context Engineering — Give AI the Right Information 18. How to Build AI Agents — Complete Guide 19. How to Build MCP Servers — Complete Guide 20. \"Training\" Your Agent — What It Actually Means 21. Beginner → Pro Roadmap & Skill Levels 22. Production Checklist — Pro Level 23. Agentic AI Production Problems — Deep Dive"},{"id":"reference","heading":"Reference","level":3,"text":"12. Project Ideas (Beginner → Advanced) 13. Common Mistakes Beginners Make 14. Interview Q&A for Agent Developers 15. Resources & Next Steps"},{"id":"1-what-is-an-ai-agent","heading":"1. What Is an AI Agent?","level":2,"text":"Theory. An AI agent is software that uses a Large Language Model (LLM) as a reasoning engine but does not stop at generating text. It can take actions in the real world (API calls, database queries, file operations) through a loop: plan → act → observe results → repeat until the user's goal is met. As a backend developer, you already build APIs and services — agents are clients that call those services intelligently based on natural language goals."},{"id":"chatbot-vs-agent","heading":"Chatbot vs Agent","level":3,"text":"Chatbot AI Agent Input User message User goal Output Text reply Actions + result Tools Usually none Can call APIs, DB, files, browser Memory Often session only Can remember context, state, history Loop Single turn Think → Act → Observe → Repeat"},{"id":"the-agent-loop-react-pattern","heading":"The Agent Loop (ReAct Pattern)","level":3,"text":"How it works. ReAct = Re asoning + Act ing. Each iteration, the LLM receives the conversation so far (including prior tool results) and decides either: (a) call a tool with specific arguments, or (b) respond to the user with a final answer. Your orchestrator code executes the tool, appends the result to the message history, and sends everything back to the LLM. This continues until the model stops requesting tools or you hit a max iteration guard. You as a developer build: The tools (functions the agent can call) The MCP servers (standard way to expose tools) The orchestrator (loop that connects LLM + tools) The backend APIs (REST services your tools call) Example — manual trace. User asks \"Cancel order ORD 99 and refund.\" Iteration 1: LLM calls → returns . Iteration 2: LLM calls → returns . Iteration 3: LLM responds in natural language — no more tools. Three LLM round trips for one user request. Pitfall. Without a max iteration limit, a confused model can loop forever calling tools. Always cap at e.g. 10 iterations and escalate to human on timeout."},{"id":"2-core-concepts-before-you-code","heading":"2. Core Concepts Before You Code","level":2,"text":""},{"id":"21-large-language-model-llm","heading":"2.1 Large Language Model (LLM)","level":3,"text":"A model trained on text that predicts the next token Examples: GPT 4, Claude, Gemini, Cursor's Composer You don't train models as an agent developer — you use them via API How it works. An LLM receives a list of messages (system, user, assistant, tool) and generates the next tokens probabilistically. It has no built in memory between API calls — you resend history each time. It also has no direct access to your database or filesystem unless you expose that through tools. Interview angle. \"Do agents think?\" → No consciousness; they pattern match from training data and your prompt context. Reliability comes from good prompts, tools, and guardrails — not from the model \"understanding\" your business."},{"id":"22-prompt","heading":"2.2 Prompt","level":3,"text":"Instructions + context sent to the LLM."},{"id":"23-system-prompt","heading":"2.3 System Prompt","level":3,"text":"Defines agent personality, rules, and constraints. Critical for production."},{"id":"24-tools-function-calling","heading":"2.4 Tools (Function Calling)","level":3,"text":"Functions the LLM can request to run. You define the schema; the LLM decides when to call them. How it works. You send the LLM a JSON schema describing each tool (name, description, parameters). The model does not execute code itself — it returns a structured tool call request: . Your backend validates arguments, runs the real function, and sends the result back as a message. The model then uses that result in its next reasoning step. Pitfall. Vague tool descriptions cause wrong tool selection. \"Gets data\" is useless; \"Fetch order by ID when user asks about a specific order; requires ORD XXXXX format\" is actionable (see Section 16.5). Your backend code executes the function and returns the result to the LLM."},{"id":"25-rag-retrieval-augmented-generation","heading":"2.5 RAG (Retrieval-Augmented Generation)","level":3,"text":"Theory. LLMs know general knowledge from training but not your internal docs, policies, or live data. RAG retrieves relevant document chunks at query time and injects them into the prompt so answers are grounded in your data — without retraining the model. Example. User asks \"What is our refund policy for cancelled orders?\" Without RAG, the model guesses. With RAG: embed the question → search vector DB → top 3 chunks from inserted into prompt → model cites actual policy text. Interview angle. RAG vs fine tuning: RAG when data changes frequently or you need citations; fine tuning when you need a specific output style at scale and have hundreds of labeled examples."},{"id":"26-memory","heading":"2.6 Memory","level":3,"text":"Type Example Short term Current conversation messages Long term User preferences stored in DB Working Agent's current plan / scratchpad"},{"id":"27-orchestration","heading":"2.7 Orchestration","level":3,"text":"Code that manages the agent loop, tool routing, retries, and error handling. Frameworks: LangGraph, CrewAI, or a custom service you write."},{"id":"3-what-is-mcp-model-context-protocol","heading":"3. What Is MCP (Model Context Protocol)?","level":2,"text":""},{"id":"one-sentence-definition","heading":"One-Sentence Definition","level":3,"text":"MCP is an open standard (by Anthropic) that lets AI applications connect to external data and tools in a uniform way — like USB for AI integrations. Theory. Before MCP, every IDE or agent framework built custom integrations for Slack, Postgres, GitHub, etc. MCP defines one protocol (JSON RPC messages over stdio or HTTP) so any MCP compatible client can talk to any MCP server. You write the server once; Cursor, Claude Desktop, or your own agent can all use it."},{"id":"the-problem-mcp-solves","heading":"The Problem MCP Solves","level":3,"text":"Without MCP: With MCP:"},{"id":"mcp-architecture","heading":"MCP Architecture","level":3,"text":""},{"id":"mcp-server-exposes-three-things","heading":"MCP Server Exposes Three Things","level":3,"text":"Primitive What It Does Example Tools Actions the AI can take , Resources Data the AI can read , Prompts Reusable prompt templates"},{"id":"mcp-vs-rest-api","heading":"MCP vs REST API","level":3,"text":"REST API MCP Built for Any client AI agents specifically Discovery OpenAPI/Swagger Built in tool listing Context Client manages Protocol designed for LLM context Standard Many styles One protocol As a developer: You can wrap your existing REST APIs behind an MCP server so agents can use them."},{"id":"mcp-transport-types","heading":"MCP Transport Types","level":3,"text":"Transport Use Case stdio Local — Cursor spawns your server as subprocess HTTP/SSE Remote — server runs on a URL How it works — stdio transport. Cursor runs as a child process. Client and server communicate via stdin/stdout using JSON RPC messages. No network port needed — simple for local dev. HTTP/SSE is for remote/shared servers where multiple clients connect over the network (requires auth in production). Interview angle. MCP is not a replacement for REST — it's a wrapper layer optimized for LLM tool discovery and invocation. Your MCP server typically calls your existing REST APIs internally."},{"id":"4-how-agents-mcp-tools-work-together","heading":"4. How Agents + MCP + Tools Work Together","level":2,"text":""},{"id":"full-flow-example","heading":"Full Flow Example","level":3,"text":""},{"id":"typical-architecture","heading":"Typical Architecture","level":3,"text":"MCP servers are usually built in Python or TypeScript (official SDKs) and call your backend APIs."},{"id":"5-tech-stack-what-to-learn","heading":"5. Tech Stack — What to Learn","level":2,"text":""},{"id":"path-a-recommended-for-beginners-python-first","heading":"Path A — Recommended for Beginners (Python-first)","level":3,"text":"Layer Technology Why LLM API OpenAI / Anthropic API Industry standard Agent framework LangChain or LangGraph Most tutorials, huge community MCP SDK Python package Official MCP server SDK Backend Any REST API you already know Agents need real data Vector DB Chroma (local) → Pinecone/pgvector For RAG IDE Cursor Built in agent + MCP support"},{"id":"path-b-typescript","heading":"Path B — TypeScript","level":3,"text":"Layer Technology MCP SDK Agent Vercel AI SDK, LangChain.js Cursor SDK for programmatic agents"},{"id":"path-c-all-in-one-framework","heading":"Path C — All-in-One Framework","level":3,"text":"Layer Technology Agent + Tools LangGraph, CrewAI, AutoGen MCP Python or TypeScript MCP SDK RAG LlamaIndex, LangChain retrievers"},{"id":"what-to-pick-as-a-beginner","heading":"What to Pick as a Beginner","level":3,"text":""},{"id":"6-learning-roadmap-12-weeks-beginner","heading":"6. Learning Roadmap — 12 Weeks (Beginner)","level":2,"text":""},{"id":"7-week-by-week-hands-on-plan","heading":"7. Week-by-Week Hands-On Plan","level":2,"text":""},{"id":"week-1-python-api-hello-world","heading":"Week 1 — Python + API Hello World","level":3,"text":"Goal: Understand that LLM = API call with messages."},{"id":"week-2-tool-calling-no-framework","heading":"Week 2 — Tool Calling (No Framework)","level":3,"text":"Goal. Understand the agent loop manually before using frameworks — if you can't build this without LangChain, you won't debug production failures. How it works — the four steps: 1. Send user message + tool definitions to LLM API. 2. Parse response: if present, extract function name and JSON arguments. 3. Execute your Python function locally; capture return value as string/JSON. 4. Append assistant message + tool result messages; call LLM again for final answer. Interview angle. \"What's the difference between RAG and tool calling?\" → RAG injects static retrieved text into context; tools execute live code/API calls and return dynamic results."},{"id":"week-3-langchain-agent","heading":"Week 3 — LangChain Agent","level":3,"text":"Build an agent with one tool that calls a REST API:"},{"id":"week-45-backend-api-as-agent-tool","heading":"Week 4–5 — Backend API as Agent Tool","level":3,"text":"Expose a simple REST endpoint (any language/framework) that your agent calls: Your Python agent or MCP server calls these endpoints."},{"id":"week-67-first-mcp-server","heading":"Week 6–7 — First MCP Server","level":3,"text":"See Section 8."},{"id":"week-8-portfolio-project","heading":"Week 8+ — Portfolio Project","level":3,"text":"Pick one from Section 12."},{"id":"8-build-your-first-mcp-server","heading":"8. Build Your First MCP Server","level":2,"text":"Theory. An MCP server is a small program you write and run locally (or deploy remotely). It advertises tools the LLM can call. Cursor acts as the MCP client — it discovers your tools, passes them to the LLM, and when the LLM requests a tool call, Cursor invokes your server and returns the result."},{"id":"prerequisites","heading":"Prerequisites","level":3,"text":""},{"id":"minimal-mcp-server-python-stdio","heading":"Minimal MCP Server (Python, stdio)","level":3,"text":"How it works. registers a function. FastMCP generates the JSON schema from the function signature and docstring. When the LLM chooses , Cursor sends a JSON RPC call to your server; your function runs and returns the result."},{"id":"connect-to-cursor","heading":"Connect to Cursor","level":3,"text":"Create in your project: Restart Cursor → Settings → MCP → your server should appear."},{"id":"test-in-cursor-chat","heading":"Test in Cursor Chat","level":3,"text":""},{"id":"mcp-server-that-wraps-a-rest-api","heading":"MCP Server That Wraps a REST API","level":3,"text":""},{"id":"9-build-your-first-agent-concept-code","heading":"9. Build Your First Agent (Concept + Code)","level":2,"text":"Theory. This section strips away frameworks to show the minimum viable agent : a loop, an LLM API call, tool schemas, and message history. Every framework (LangChain, LangGraph) is structured sugar on top of this pattern."},{"id":"minimal-agent-loop-python","heading":"Minimal Agent Loop (Python)","level":3,"text":"How it works step by step for \"What is the status of order 12345?\": 1. LLM receives user message + tool schemas. 2. LLM returns . 3. Your code runs the tool → . 4. That result is appended as a message. 5. LLM receives updated history → responds with natural language, no tool calls. 6. Loop returns final answer. Pitfall. Production agents need: max iterations, input validation on , error handling when the API fails, and logging of every tool call for audit. This IS an agent — loop, tools, reasoning. Frameworks just add structure around this."},{"id":"10-cursor-specific-agents-in-your-ide","heading":"10. Cursor-Specific: Agents in Your IDE","level":2,"text":""},{"id":"what-cursor-gives-you","heading":"What Cursor Gives You","level":3,"text":"Feature What It Is Agent mode AI that edits files, runs commands, searches codebase MCP Plug external tools into Cursor Rules — persistent instructions for the AI Skills Reusable instruction files for specific tasks Hooks Automate actions on agent events"},{"id":"mcp-in-cursor-mental-model","heading":"MCP in Cursor — Mental Model","level":3,"text":"Examples of existing MCP servers: GitHub (read repos, issues, PRs) Postgres (run SQL) Filesystem (read/write files) Brave Search (web search)"},{"id":"your-goal-as-agent-developer","heading":"Your Goal as Agent Developer","level":3,"text":"Build MCP servers that expose your business capabilities : Query billing system Trigger migrations Search internal docs Create JIRA tickets"},{"id":"11-cursor-sdk-run-agents-from-code","heading":"11. Cursor SDK — Run Agents from Code","level":2,"text":"When you want agents outside the IDE (CI, scripts, bots):"},{"id":"typescript","heading":"TypeScript","level":3,"text":""},{"id":"python","heading":"Python","level":3,"text":""},{"id":"sdk-mcp","heading":"SDK + MCP","level":3,"text":"You can configure MCP servers for SDK agents so programmatic agents use the same tools as Cursor IDE. Docs: cursor.com/docs/sdk"},{"id":"12-project-ideas-beginner-advanced","heading":"12. Project Ideas (Beginner → Advanced)","level":2,"text":""},{"id":"beginner","heading":"Beginner","level":3,"text":"Project What You Learn Calculator MCP Server MCP basics, tool schema Weather Agent Tool calling, external API REST API + Python Agent Connect LLM to a backend"},{"id":"intermediate","heading":"Intermediate","level":3,"text":"Project What You Learn Internal Docs Q&A (RAG) Embeddings, Chroma, chunking Order Support Agent Multi tool agent, backend integration GitHub PR Review Bot Cursor SDK or GitHub MCP SQL Agent MCP + Postgres, guardrails"},{"id":"advanced","heading":"Advanced","level":3,"text":"Project What You Learn Multi agent system Orchestrator + specialist agents Workflow agent Agent triggers multi step business processes Production MCP gateway Auth, rate limits, logging, Docker Custom agent platform Your own UI + agent runtime"},{"id":"portfolio-project-recommendation-start-here","heading":"Portfolio Project Recommendation (Start Here)","level":3,"text":"\"Billing Support Agent\" 1. Backend API — orders, invoices, refunds (any REST framework) 2. MCP server — 3 tools: , , (Python) 3. RAG — embed your API docs into Chroma 4. Cursor integration — test via MCP in IDE 5. README with architecture diagram"},{"id":"13-common-mistakes-beginners-make","heading":"13. Common Mistakes Beginners Make","level":2,"text":"Theory. Most agent failures are software engineering mistakes, not \"AI mysteries.\" The table below maps symptoms to fixes — treat agents like any distributed system with an unpredictable client (the LLM). Mistake Fix Trying to learn ML/training first Agent dev = software engineering + LLM APIs No backend, only prompts Agents need real tools and data Hardcoding API keys Use env vars / secrets manager No input validation on tools Agent can pass bad args — validate everything Infinite agent loops Set max iterations (e.g. 10) Trusting LLM output blindly Always verify tool results Building without measuring Log latency, token cost, success rate"},{"id":"14-interview-qa-for-agent-developers","heading":"14. Interview Q&A for Agent Developers","level":2,"text":"Theory. Interviewers test whether you understand the architecture (loop, tools, context limits) and production risks (hallucination, partial failure, cost) — not whether you've memorized framework APIs. Q: What is an AI agent? A: A system that uses an LLM to reason, call tools, observe results, and loop until a goal is achieved — not just generate text. Q: What is MCP? A: Model Context Protocol — open standard for connecting AI apps to tools, data, and prompts via MCP servers. Q: MCP vs REST API? A: REST is general purpose. MCP is designed for AI — standardized tool discovery, schema, and context for LLMs. Q: What is RAG? A: Retrieve relevant documents from a vector store, inject into prompt, then generate answer — grounds LLM in your data. Q: What is function calling? A: LLM returns a structured request to call a function you defined; your code executes it and returns the result. Q: How do you prevent agents from doing harmful actions? A: Allowlist tools, validate inputs, human in the loop for sensitive ops, rate limits, audit logs, least privilege API keys."},{"id":"15-resources-next-steps","heading":"15. Resources & Next Steps","level":2,"text":""},{"id":"official-docs","heading":"Official Docs","level":3,"text":"Resource URL MCP Specification modelcontextprotocol.io MCP Python SDK github.com/modelcontextprotocol/python sdk Cursor MCP Docs cursor.com/docs/context/mcp Cursor SDK cursor.com/docs/sdk LangChain python.langchain.com"},{"id":"start-today-3-actions","heading":"Start Today — 3 Actions","level":3,"text":""},{"id":"16-prompt-engineering-write-effective-prompts","heading":"16. Prompt Engineering — Write Effective Prompts","level":2,"text":"Prompt engineering is the 1 skill for agent developers. You don't need ML training — you need to write instructions the LLM follows reliably. Theory. The LLM has no persistent memory of your project. Everything it \"knows\" during a request comes from the messages you send: system prompt, retrieved docs, tool results, and user input. Prompt engineering is the discipline of crafting those messages so the model consistently picks the right tools, follows business rules, and formats output correctly."},{"id":"161-anatomy-of-a-production-prompt","heading":"16.1 Anatomy of a Production Prompt","level":3,"text":"Every agent interaction has these layers:"},{"id":"162-system-prompt-template-copy-adapt","heading":"16.2 System Prompt Template (Copy & Adapt)","level":3,"text":""},{"id":"163-prompt-techniques-beginner-to-pro","heading":"16.3 Prompt Techniques — Beginner to Pro","level":3,"text":"Level Technique Example Beginner Be specific Bad: \"Help with orders\" → Good: \"Look up order by ID and explain status in plain English\" Beginner Assign a role \"You are a senior support agent with 10 years experience\" Beginner Set output format \"Respond in JSON: \" Intermediate Few shot examples Show 2–3 input→output examples in the prompt Intermediate Chain of thought \"Think step by step before answering\" Intermediate Constraints as bullets \"NEVER do X. ALWAYS do Y.\" Pro Structured XML/tags Pro Self critique \"Draft answer, then check against rules, then finalize\" Pro Prompt versioning Store prompts in git, A/B test, measure success rate"},{"id":"164-few-shot-prompting-teach-by-example","heading":"16.4 Few-Shot Prompting (Teach by Example)","level":3,"text":"Few shot = \"training\" via examples in the prompt (no model retraining needed)."},{"id":"165-tool-use-prompts-critical-for-agents","heading":"16.5 Tool-Use Prompts (Critical for Agents)","level":3,"text":"The LLM picks tools based on name + description . Bad descriptions = wrong tool calls. Rules for tool descriptions: 1. Say when to use the tool 2. Say when NOT to use it 3. Describe required parameters with examples 4. One tool = one clear job (single responsibility)"},{"id":"166-bad-vs-good-prompts","heading":"16.6 Bad vs Good Prompts","level":3,"text":"Bad Good Why \"Be helpful\" \"Answer billing questions using get invoice tool only\" Vague vs actionable \"Fix the code\" \"Find null pointer risks in OrderService.java, list line numbers, suggest fix\" No scope vs clear scope Long rambling paragraph Structured sections with headers LLMs follow structure better No examples 2–3 examples of desired output Examples beat instructions \"Don't hallucinate\" \"Only state facts returned by tools; if tool returns empty, say no data found\" Negative vs positive instruction"},{"id":"167-prompt-for-the-react-agent-loop","heading":"16.7 Prompt for the ReAct Agent Loop","level":3,"text":""},{"id":"168-prompt-debugging-checklist","heading":"16.8 Prompt Debugging Checklist","level":3,"text":"When your agent behaves badly, check:"},{"id":"17-context-engineering-give-ai-the-right-information","heading":"17. Context Engineering — Give AI the Right Information","level":2,"text":"Context engineering = deciding what information the LLM sees, in what order , and how much . This is as important as prompt writing. Theory. Prompt engineering tells the model how to behave. Context engineering decides what facts it has to work with. A perfect system prompt fails if you inject irrelevant docs, stale API responses, or 50KB of JSON — the model gets confused, costs spike, and early instructions get \"lost in the middle.\""},{"id":"171-what-goes-into-context","heading":"17.1 What Goes Into Context?","level":3,"text":"Source What When to Use System prompt Rules, role, format Always Conversation history Previous messages Multi turn chat RAG chunks Retrieved doc snippets Company knowledge Tool results API/DB responses After each tool call User metadata ID, permissions, locale Personalization MCP Resources Files, schemas, configs Read only reference data"},{"id":"172-context-window-the-hard-limit","heading":"17.2 Context Window — The Hard Limit","level":3,"text":"Every model has a token limit (e.g. 128K tokens). Everything you send counts: Pro tip: More context ≠ better. Irrelevant context confuses the model."},{"id":"173-context-priority-order","heading":"17.3 Context Priority Order","level":3,"text":"Put the most important information where the model pays most attention:"},{"id":"174-how-to-write-effective-context-blocks","heading":"17.4 How to Write Effective Context Blocks","level":3,"text":""},{"id":"175-context-compression-techniques-pro","heading":"17.5 Context Compression Techniques (Pro)","level":3,"text":"Technique How When Summarization LLM summarizes old messages Long conversations RAG top K Only inject top 3–5 relevant chunks Large knowledge bases Structured JSON Replace prose with Tool results Sliding window Keep last 10 messages only Chat apps Semantic cache Reuse answer if similar question asked High traffic bots"},{"id":"176-mcp-resources-as-context","heading":"17.6 MCP Resources as Context","level":3,"text":"MCP Resources let the agent read data without executing an action: Agent reads resource → includes in context → makes better tool calls."},{"id":"177-cursor-rules-skills-as-context","heading":"17.7 Cursor Rules & Skills as Context","level":3,"text":"In Cursor, persistent context lives in: File Purpose Project wide instructions (always injected) Agent behavior for the repo Skills ( ) Task specific playbooks Example rule ( ):"},{"id":"18-how-to-build-ai-agents-complete-guide","heading":"18. How to Build AI Agents — Complete Guide","level":2,"text":"Theory. Building an agent is backend engineering with an LLM in the loop. You define the job, expose tools (your APIs), write guardrails, and implement the orchestration loop. Frameworks help at scale; understanding the raw loop (Section 9) is what separates debuggers from copy pasters."},{"id":"181-agent-building-blocks","heading":"18.1 Agent Building Blocks","level":3,"text":""},{"id":"182-step-by-step-build-an-agent-from-scratch","heading":"18.2 Step-by-Step: Build an Agent from Scratch","level":3,"text":"Step 1 — Define the job (one sentence) Step 2 — List tools the agent needs Step 3 — Write system prompt (see Section 16) Step 4 — Implement tool functions (real API calls) Step 5 — Implement the loop (see Section 9) Step 6 — Add guardrails Step 7 — Test with 20 real scenarios, fix prompts Step 8 — Add logging, metrics, deploy"},{"id":"183-agent-with-langgraph-intermediate","heading":"18.3 Agent with LangGraph (Intermediate)","level":3,"text":""},{"id":"184-multi-agent-systems-pro","heading":"18.4 Multi-Agent Systems (Pro)","level":3,"text":"Orchestrator breaks task into subtasks Specialist agents each have own prompt + tools Handoff via messages or shared state Frameworks: LangGraph, CrewAI, AutoGen"},{"id":"185-agent-skill-levels","heading":"18.5 Agent Skill Levels","level":3,"text":"Level You Can Build Beginner Single tool chatbot with function calling Intermediate Multi tool agent with memory + RAG Advanced Multi agent system with orchestration Pro Production agent platform with evals, monitoring, fallbacks"},{"id":"19-how-to-build-mcp-servers-complete-guide","heading":"19. How to Build MCP Servers — Complete Guide","level":2,"text":"Theory. An MCP server is a thin adapter between an AI client and your backend. The LLM never calls your REST API directly — it calls MCP tools; your server translates those into HTTP/DB operations, validates inputs, and returns structured results. This separation lets you enforce auth, rate limits, and audit logging at the tool boundary."},{"id":"191-what-you-are-actually-building","heading":"19.1 What You Are Actually Building","level":3,"text":"An MCP server is a small program that: 1. Starts when Cursor/agent connects 2. Advertises its tools, resources, and prompts 3. Executes tool calls and returns results 4. It is not trained — you write and deploy it"},{"id":"192-mcp-server-structure","heading":"19.2 MCP Server Structure","level":3,"text":""},{"id":"193-tools-full-example-with-validation","heading":"19.3 Tools — Full Example with Validation","level":3,"text":""},{"id":"194-resources-read-only-context-for-the-agent","heading":"19.4 Resources — Read-Only Context for the Agent","level":3,"text":""},{"id":"195-prompts-reusable-templates-in-mcp","heading":"19.5 Prompts — Reusable Templates in MCP","level":3,"text":"MCP servers can expose prompt templates the client fills in: In Cursor, the user invokes this prompt template → agent gets a well structured starting prompt."},{"id":"196-typescript-mcp-server","heading":"19.6 TypeScript MCP Server","level":3,"text":""},{"id":"197-mcp-server-deployment-pro","heading":"19.7 MCP Server Deployment (Pro)","level":3,"text":"For remote MCP (HTTP/SSE), expose server behind auth (API key, OAuth)."},{"id":"198-mcp-server-debugging","heading":"19.8 MCP Server Debugging","level":3,"text":""},{"id":"199-mcp-skill-levels","heading":"19.9 MCP Skill Levels","level":3,"text":"Level You Can Build Beginner 1–2 tools, stdio, local Cursor Intermediate Tools + resources + prompts, error handling Advanced Multi module server, auth, Docker Pro Remote MCP, gateway, multi tenant, observability"},{"id":"20-training-your-agent-what-it-actually-means","heading":"20. \"Training\" Your Agent — What It Actually Means","level":2,"text":""},{"id":"201-what-you-do-not-train","heading":"20.1 What You Do NOT Train","level":3,"text":"Thing Reality MCP Server You write code — no training Agent loop You write orchestration logic — no training Tool functions Regular programming"},{"id":"202-what-training-usually-means-for-agent-developers","heading":"20.2 What \"Training\" Usually Means for Agent Developers","level":3,"text":"Method What It Is Difficulty When to Use Prompt engineering Better instructions + examples Easy Always — start here Few shot examples Examples inside the prompt Easy Classification, formatting RAG Retrieve your docs at runtime Medium Company knowledge Fine tuning Retrain model weights on your data Hard Unique style/format at scale Evals Test suite measuring agent quality Medium Before production Feedback loop Log failures → improve prompts Medium Continuous improvement 90% of agent developers never fine tune. Prompt + RAG + tools is enough."},{"id":"203-rag-teach-the-agent-your-data-no-training","heading":"20.3 RAG — \"Teach\" the Agent Your Data (No Training)","level":3,"text":""},{"id":"204-fine-tuning-when-you-actually-retrain-pro","heading":"20.4 Fine-Tuning — When You Actually Retrain (Pro)","level":3,"text":"Fine tuning = send labeled examples to OpenAI/Anthropic → get a custom model. Use when: You need a very specific output format thousands of times Prompt + RAG still fails after extensive tuning You have 100+ high quality labeled examples Don't use when: You can solve it with better prompts (try that first) Your data changes frequently (RAG is better) You're a beginner (skip for now)"},{"id":"205-evals-measure-if-your-agent-is-trained-enough","heading":"20.5 Evals — Measure If Your Agent Is \"Trained\" Enough","level":3,"text":"Pro workflow: Run evals on every prompt change. Track score over time."},{"id":"206-improving-agent-quality-over-time-practical-training","heading":"20.6 Improving Agent Quality Over Time (Practical \"Training\")","level":3,"text":"This loop is how pro agent developers \"train\" — no GPUs required."},{"id":"21-beginner-pro-roadmap-skill-levels","heading":"21. Beginner → Pro Roadmap & Skill Levels","level":2,"text":""},{"id":"211-skill-matrix","heading":"21.1 Skill Matrix","level":3,"text":"Skill Beginner Intermediate Pro Prompt writing Basic role + task Few shot, structured, tool prompts Versioned, A/B tested, eval driven Context Dump everything RAG top K, sliding window Compressed, prioritized, cached Agents Single tool loop LangGraph, memory, multi tool Multi agent, orchestration MCP 1 server, 2 tools Resources + prompts + validation Remote, auth, Docker, gateway RAG Chroma local Chunking strategy, reranking Hybrid search, eval on retrieval Production Local only Logging, env vars Evals, monitoring, fallbacks, cost caps \"Training\" Prompt tuning RAG + eval suite Fine tuning + continuous improvement"},{"id":"212-extended-roadmap-6-months-to-pro","heading":"21.2 Extended Roadmap (6 Months to Pro)","level":3,"text":""},{"id":"213-how-you-know-youre-pro","heading":"21.3 How You Know You're \"Pro\"","level":3,"text":""},{"id":"22-production-checklist-pro-level","heading":"22. Production Checklist — Pro Level","level":2,"text":""},{"id":"before-launch","heading":"Before Launch","level":3,"text":""},{"id":"23-agentic-ai-production-problems-deep-dive","heading":"23. Agentic AI Production Problems — Deep Dive","level":2,"text":"Real world agent systems fail in predictable ways. These four problems appear in almost every production agent deployment — know them for interviews and for building reliable systems."},{"id":"231-memory-bloating-in-agentic-ai","heading":"23.1 Memory Bloating in Agentic AI","level":3,"text":"What it is: Every agent turn appends to context — user messages, assistant replies, tool calls, tool results, retrieved RAG chunks, and scratchpad reasoning. Over multiple iterations, the context window fills up. Cost spikes, latency grows, and the model starts forgetting early instructions (\"lost in the middle\" problem). Why agents are worse than chatbots: Chatbot Agent 1 user + 1 assistant turn 5–20 turns per task Small context Tool JSON payloads are huge No history of API responses Full API responses stuffed into prompt Common causes: Appending full tool outputs (10KB JSON) instead of summaries Never trimming conversation history Storing entire RAG corpus in prompt instead of top k chunks Recursive sub agents each passing full parent context downstream No max iteration guard — agent loops until context limit Symptoms: Token usage grows linearly with each tool call Model ignores system prompt after turn 8+ API errors Bill jumps from $0.02/request to $0.50/request Fixes (production patterns): Strategy When to use Sliding window Simple agents, short tasks Summarization Long multi step workflows External memory tool \"Remember user preferences across sessions\" Structured state Pass not full chat log Token budget per tool Hard cap on each tool response size Interview answer: \"Memory bloating happens because agent loops accumulate tool outputs and history in the context window. I mitigate it with sliding windows, summarizing older turns, truncating tool payloads, external memory for long term facts, and hard limits on max iterations and tokens per request.\""},{"id":"232-partial-failures-in-agentic-ai","heading":"23.2 Partial Failures in Agentic AI","level":3,"text":"What it is: An agent runs a multi step plan — call API A, then B, then C. Step B fails (timeout, 500, rate limit). Steps A may have already committed side effects (DB write, payment, email sent). The agent is now in an inconsistent state. Example — order cancellation agent: User sees: order cancelled, no refund, no email. Agent may hallucinate \"refund completed.\" Types of partial failure: Type Example Tool timeout External API slow, agent retries blindly Idempotency violation Retry creates duplicate charge Inconsistent read Tool A updated DB, Tool B reads stale cache Orchestration gap 3 parallel tools, 1 fails — no rollback Human in the loop timeout Agent waits for approval, session expires Fixes: Interview answer: \"Partial failures are inevitable in multi tool agents. I design tools to be idempotent, use explicit workflow state, implement compensating transactions for critical paths, and never let the LLM assume a failed step succeeded — I verify tool responses and surface uncertainty to the user or a human queue.\""},{"id":"233-hallucinations-in-agentic-ai","heading":"23.3 Hallucinations in Agentic AI","level":3,"text":"What it is: The model produces plausible but false information — inventing API responses, tool names that don't exist, fake citations, or claiming an action succeeded when the tool returned an error. Agentic hallucination is worse than chat hallucination: Chat hallucination Agent hallucination Wrong fact in text Wrong action taken or reported as done User can fact check Side effects may already be committed No tool access Model fakes tool output if not constrained Common patterns: 1. Tool hallucination — model calls when only exists 2. Result hallucination — model says \"refund processed\" without calling refund tool 3. Parameter hallucination — invents instead of using user's real ID 4. Source hallucination — RAG cites documents that weren't retrieved 5. Plan hallucination — describes steps it never executed Fixes: Interview answer: \"Agent hallucinations are dangerous because they can drive real side effects. I ground responses in tool outputs, validate tool calls against schemas, verify state before confirming success to the user, use RAG with citations, and run evals that specifically test for 'claimed success without tool call' scenarios.\""},{"id":"234-performance-issues-in-agentic-ai","heading":"23.4 Performance Issues in Agentic AI","level":3,"text":"What it is: Agents are slow and expensive compared to traditional APIs. Each \"think → act → observe\" cycle is at least one LLM call (often 2–10+ seconds). Multiple tools in sequence multiply latency. Unoptimized prompts burn tokens. Latency breakdown (typical agent request): Cost breakdown: Performance problems: Problem Impact Sequential tool calls Latency adds up linearly Huge system prompt Every call pays full prompt cost Large tool schemas 20 tools × 500 tokens = 10K overhead No caching Same RAG query embedded every turn Wrong model size GPT 4 for simple routing tasks Unbounded loops Agent runs 15 iterations on stuck task Optimizations: Metrics to monitor: Metric Target (rule of thumb) P95 latency < 15s for 3 step agent Tokens/request Track trend; alert if 2× baseline Cost/request Dashboard per feature Tool error rate < 2% Iterations to complete Avg < 4 User abandonment Spike = too slow Interview answer: \"Agent performance is dominated by sequential LLM round trips. I parallelize independent tools, use model routing for simple vs complex tasks, cap iterations, cache RAG results, stream responses, and monitor P95 latency and token cost per request with alerts on regression.\""},{"id":"235-interview-qa-agentic-ai-failures","heading":"23.5 Interview Q&A — Agentic AI Failures","level":3,"text":"Q: What is memory bloating in agents? A: Unbounded growth of context from chat history, tool outputs, and RAG chunks across agent loops — causing high cost, latency, and degraded instruction following. Fix with summarization, sliding windows, truncated tool results, and external memory. Q: How do you handle partial failures? A: Idempotent tools, saga/compensating transactions, explicit workflow state, async retry via outbox pattern, and human escalation — never let the LLM assume an uncertain step succeeded. Q: How are agent hallucinations different from chat hallucinations? A: They can trigger or misreport real tool actions. Mitigate with schema validation, grounding in tool output, post action verification, and evals for fake success claims. Q: Why are agents slow? A: Multiple sequential LLM calls plus tool latency. Optimize with parallel tools, model routing, caching, streaming, iteration limits, and async processing for long jobs. Q: Name four production risks of agentic AI. A: Memory bloating, partial failures, hallucinations, performance/cost — plus security (tool injection) as a fifth bonus topic."},{"id":"quick-reference-card","heading":"Quick Reference Card","level":2,"text":"Last updated: June 2026 Level: Beginner → Pro Agents, MCP, Prompts & Context"}],"qa":[{"q":"What is an AI agent?","a":"A system that uses an LLM to reason, call tools, observe results, and loop until a goal is achieved — not just generate text."},{"q":"What is MCP?","a":"Model Context Protocol — open standard for connecting AI apps to tools, data, and prompts via MCP servers."},{"q":"MCP vs REST API?","a":"REST is general-purpose. MCP is designed for AI — standardized tool discovery, schema, and context for LLMs."},{"q":"What is RAG?","a":"Retrieve relevant documents from a vector store, inject into prompt, then generate answer — grounds LLM in your data."},{"q":"What is function calling?","a":"LLM returns a structured request to call a function you defined; your code executes it and returns the result."},{"q":"How do you prevent agents from doing harmful actions?","a":"Allowlist tools, validate inputs, human-in-the-loop for sensitive ops, rate limits, audit logs, least-privilege API keys."},{"q":"What is memory bloating in agents?","a":"Unbounded growth of context from chat history, tool outputs, and RAG chunks across agent loops — causing high cost, latency, and degraded instruction-following. Fix with summarization, sliding windows, truncated tool results, and external memory."},{"q":"How do you handle partial failures?","a":"Idempotent tools, saga/compensating transactions, explicit workflow state, async retry via outbox pattern, and human escalation — never let the LLM assume an uncertain step succeeded."},{"q":"How are agent hallucinations different from chat hallucinations?","a":"They can trigger or misreport real tool actions. Mitigate with schema validation, grounding in tool output, post-action verification, and evals for fake success claims."},{"q":"Why are agents slow?","a":"Multiple sequential LLM calls plus tool latency. Optimize with parallel tools, model routing, caching, streaming, iteration limits, and async processing for long jobs."},{"q":"Name four production risks of agentic AI.","a":"Memory bloating, partial failures, hallucinations, performance/cost — plus security (tool injection) as a fifth bonus topic."}]},"15_React_JS_Complete_Guide.md":{"title":"React.js Complete Guide","trackId":"frontend","trackTitle":"Frontend — React & Angular","sections":[{"id":"reactjs-complete-interview-guide","heading":"React.js — Complete Interview Guide","level":1,"text":""},{"id":"frontend-core-hooks-router-state-performance-pro-beginner-advanced","heading":"Frontend Core → Hooks → Router → State → Performance → Pro | Beginner → Advanced","level":3,"text":""},{"id":"1-what-is-react-how-it-works","heading":"1. What is React & How It Works","level":2,"text":""},{"id":"what-is-react","heading":"What is React?","level":3,"text":"React is a JavaScript library (not a full framework) for building user interfaces. Created by Facebook (Meta). You compose components that describe what the UI should look like for a given state. Theory. If you come from Java or Python backend work, think of React like a view layer that maps data to UI — similar in spirit to how a template engine renders HTML from a model, but the \"template\" is JavaScript and React keeps the UI in sync when the model (state) changes. You do not manually find DOM nodes and update them; you declare what the UI should look like for the current state, and React figures out how to update the browser. What problem it solves. Imperative DOM code (jQuery style) becomes unmaintainable as UIs grow: every button, form, and list item needs manual wiring, and it's easy to leave the DOM out of sync with application state. React's declarative model means: state changes → re run your component function → React patches the DOM. One source of truth, predictable updates. Interview angle. \"Library vs framework\" is a classic opener: React only handles UI. You choose routing (React Router), data fetching (fetch, TanStack Query), and global state (Context, Redux, Zustand) yourself. Next.js wraps React into a full stack framework with SSR and file based routing."},{"id":"spa-single-page-application","heading":"SPA (Single Page Application)","level":3,"text":"One HTML page ( ) JavaScript handles routing and renders different \"views\" without full page reload Faster navigation after initial load SEO harder (Next.js solves this with SSR) How it works. On first visit, the browser downloads a JS bundle, React mounts into a root , and subsequent \"page\" changes are really component swaps driven by the router — no round trip to the server for HTML. The URL still changes (via the History API), so back/forward works. Example. User lands on → React renders . They click \"Settings\" → router changes URL to → React unmounts dashboard subtree and mounts . Data for the new view is fetched via API calls, not a full page reload. Pitfall. Initial bundle size can be large; code splitting (Section 12) and lazy routes mitigate this. Blank screen until JS loads — show a loading shell in ."},{"id":"virtual-dom","heading":"Virtual DOM","level":3,"text":"Real DOM Virtual DOM Browser's actual tree Lightweight JS copy Expensive to update Cheap to create/compare Direct manipulation React batches & optimizes Why Virtual DOM? Minimizes expensive real DOM operations. React 18+ also uses Fiber architecture for interruptible rendering and prioritization. How it works. When state changes, React does not wipe and rebuild the entire page. It runs your component functions again to produce a new virtual tree, then reconciles (diffs) it against the previous tree. Only nodes whose type, props, or children changed get updated in the real DOM. Updating one counter button does not re touch unrelated parts of the page. Example. A list of 100 items where only item 42's label changes: reconciliation matches items by , sees one text node changed, and updates that single DOM text node — not all 100 elements. Interview angle. Virtual DOM is an implementation strategy, not magic — other frameworks use different approaches (e.g., fine grained reactivity). The interview question is usually \"what happens when setState is called?\" → schedule re render → render phase (build VDOM) → commit phase (apply DOM updates)."},{"id":"react-fiber-interview-topic","heading":"React Fiber (Interview topic)","level":3,"text":"Reimplementation of React's core algorithm (React 16+) Enables: concurrent rendering, suspense, priority based updates Work can be paused, resumed, aborted Two phases: Render (pure, can be interrupted) → Commit (applies to DOM, synchronous) How it works. Before Fiber (React 15 and earlier), reconciliation was synchronous — a large update could block the main thread and make typing feel laggy. Fiber breaks work into units linked in a tree. React can pause after a unit, handle a high priority update (e.g., keystroke in a search box), then resume the lower priority work (e.g., filtering a huge list). The render phase is interruptible; the commit phase is not — once React commits, the user sees the update. Example. User types in a search box while a heavy is re rendering. With concurrent features ( ), React keeps the input responsive and defers the expensive list re filter to a lower priority. Pitfall. Do not confuse Fiber with \"Virtual DOM\" — VDOM is the in memory tree; Fiber is the engine that walks and schedules updates to that tree."},{"id":"react-vs-angular-vs-vue","heading":"React vs Angular vs Vue","level":3,"text":"React Angular Vue Type Library Full framework Progressive framework Learning curve Medium Steep Easier State External (Redux, etc.) Built in services Pinia/Vuex Template JSX HTML + directives HTML templates Company use Meta, Netflix, Airbnb Google, enterprise Alibaba, startups"},{"id":"2-jsx-deep-dive","heading":"2. JSX Deep Dive","level":2,"text":"JSX = JavaScript XML. Syntax extension that looks like HTML inside JavaScript. Theory. JSX is not HTML and not understood by browsers directly. Your build tool (Babel, Vite) transforms each JSX tag into a call (or the modern JSX runtime equivalent). That returns a plain JavaScript object describing a UI node — a building block of the Virtual DOM. Why JSX? It keeps markup and logic in one place (colocation). You can use variables, map over arrays, and call functions right where you render — without string templates or separate files."},{"id":"jsx-rules","heading":"JSX Rules","level":3,"text":"1. Return single root (or use Fragment ) 2. Close all tags — , 3. not , not 4. camelCase for attributes — , , 5. JavaScript expressions in only — not statements ( , ) 6. , , , don't render (boolean short circuit)"},{"id":"embedding-expressions","heading":"Embedding Expressions","level":3,"text":""},{"id":"jsx-security-xss","heading":"JSX Security — XSS","level":3,"text":"React escapes values by default before rendering: Danger: — only use with sanitized HTML. How it works. React escapes strings in by converting , , to entities before inserting text nodes. Attackers cannot inject through normal interpolation. bypasses escaping — treat it like in vanilla JS: sanitize with a library (DOMPurify) or avoid entirely. Interview angle. \"Can XSS happen in React?\" → escaped by default; risk is , passing unsanitized HTML from APIs, or rendering URLs in without validation."},{"id":"3-components-functional-vs-class","heading":"3. Components — Functional vs Class","level":2,"text":""},{"id":"functional-component-modern-use-this","heading":"Functional Component (Modern — use this)","level":3,"text":"Theory. A component is a function that returns UI . Every time its parent re renders or its own state changes, React calls the function again with new props/state and compares the output to the previous render."},{"id":"class-component-legacy-know-for-interviews","heading":"Class Component (Legacy — know for interviews)","level":3,"text":"Functional + Hooks Class Simpler, less boilerplate binding issues Hooks for state/effects , lifecycle methods Industry standard since 2019 Legacy codebases Easier to test Verbose"},{"id":"class-lifecycle-know-for-interviews-maps-to-hooks","heading":"Class Lifecycle (know for interviews — maps to hooks)","level":3,"text":"Class Lifecycle When Hook Equivalent Init state, bind initial After first render After re render Before removal cleanup Skip re render? Sync state from props Avoid — derive in render Before DOM update Rare — refs Interview angle. Legacy codebases still use classes. Know that + cleanup in maps to . batching behavior is analogous to multiple setters in one event handler (React 18 batches them into one render)."},{"id":"4-props-state-data-flow","heading":"4. Props, State & Data Flow","level":2,"text":""},{"id":"props-parent-child-read-only","heading":"Props — Parent → Child (Read-only)","level":3,"text":"Props are immutable — child cannot modify props. How it works. Props flow down the component tree like function arguments. If the parent re renders with new props, the child re renders too (unless memoized). The child communicates up by calling callback props ( , ) that the parent passed in — the parent owns the state and updates it. Example. — and are props. Changing them requires the parent to re render with different values; cannot call internally."},{"id":"proptypes-runtime-validation-optional-with-ts","heading":"PropTypes (runtime validation — optional with TS)","level":3,"text":""},{"id":"state-components-own-data","heading":"State — Component's own data","level":3,"text":"Always use functional update when new state depends on old state (especially in async/batched updates). How it works. returns a value and a setter. Calling the setter schedules a re render — it does not mutate the current variable in place. React 18 batches multiple setters in the same event handler into one re render. Outside events (before React 18), each setter could trigger its own render; batching is now default everywhere. Example — why functional updates matter: Pitfall. Reading state immediately after still shows the old value — state updates are asynchronous relative to your function body."},{"id":"one-way-data-flow","heading":"One-Way Data Flow","level":3,"text":""},{"id":"lifting-state-up","heading":"Lifting State Up","level":3,"text":"When two siblings need same data, move state to common parent ."},{"id":"state-colocation","heading":"State Colocation","level":3,"text":"Keep state as close as possible to where it's used. Don't put everything in global store. Example — lifting state up. Two siblings and must stay in sync. Move and to parent; each child receives + props. Same pattern as sharing data between two Java panels via a common controller. Interview angle. \"Prop drilling\" = passing props through many layers. Fixes: Context (Section 9), component composition (pass ), or a state library — but don't reach for Redux until colocation and Context are insufficient."},{"id":"5-event-handling-forms","heading":"5. Event Handling & Forms","level":2,"text":""},{"id":"events-syntheticevent","heading":"Events — SyntheticEvent","level":3,"text":"React wraps native events in for cross browser consistency. Note: In React 17+, events attach to root, not document (event delegation change). How it works. React uses event delegation — one listener on the root catches bubbled events, then React's SyntheticEvent dispatches to your handler. stops default browser behavior (form submit, link navigation). Handlers receive the same SyntheticEvent interface across browsers. Pitfall. Do not call unless you need the event after an async gap (rare). In async code, read synchronously or store it in a variable first."},{"id":"controlled-vs-uncontrolled-components","heading":"Controlled vs Uncontrolled Components","level":3,"text":"Controlled Uncontrolled React state is source of truth DOM is source of truth + to read DOM Preferred for forms File inputs, integrations Example — controlled search box. User types \"ord\" → fires → updates state → React re renders → input shows \"ord\". The React state is always the source of truth; the DOM cannot drift. Uncontrolled is fine for one off reads on submit (e.g., file input) where you don't need live validation."},{"id":"form-submit-pattern","heading":"Form Submit Pattern","level":3,"text":""},{"id":"6-lists-keys-conditional-rendering","heading":"6. Lists, Keys & Conditional Rendering","level":2,"text":"Theory. React has no special template syntax for conditionals — you use plain JavaScript ( , ternary, ) inside JSX expressions . For lists, you transform arrays with and return elements. Each list item needs a stable so reconciliation can match items across renders."},{"id":"conditional-rendering","heading":"Conditional Rendering","level":3,"text":""},{"id":"lists-keys","heading":"Lists & Keys","level":3,"text":"Why ? Helps React identify which items changed, added, removed during reconciliation. How it works. When a list reorders, React uses to decide whether to reuse an existing component instance or create a new one. Wrong keys cause React to reuse the wrong DOM node — internal state (cursor position, expanded/collapsed, input value) sticks to the wrong row. Example. Delete the first item from with index keys: React thinks key is still the first row and updates its content from A→B, leaving C's row with stale internal state. With stable IDs, React unmounts A's row and keeps B and C instances intact. Good keys Bad keys Unique stable ID from DB Array index (if list reorders) UUID generated once on each render Index as key is OK only if list is static and never reorders. Pitfall. — if is , React renders on screen (falsy number). Use or ."},{"id":"7-useeffect-the-most-important-hook","heading":"7. useEffect — The Most Important Hook","level":2,"text":"Theory. React components should be pure during render — given the same props/state, return the same JSX. Side effects (fetching data, subscriptions, timers, manual DOM tweaks) do not belong in the render body. runs after React commits DOM updates, so the browser has painted (for ; runs before paint). Mental model. Render = \"what should be on screen.\" Effect = \"sync with the outside world based on what's on screen.\""},{"id":"dependency-array-rules","heading":"Dependency Array Rules","level":3,"text":"Dependencies Behavior empty Run once on mount (like componentDidMount) Run on mount + when or changes omitted Run on every render (rarely wanted) How it works — effect lifecycle: 1. Component renders (JSX computed). 2. React commits DOM changes. 3. Effect from previous render's cleanup runs (if deps changed or unmounting). 4. New effect runs. Example. dependency: mount with → fetch user 1. User navigates, → cleanup cancels in flight fetch for user 1 → fetch user 2. Without cleanup, a slow response for user 1 could overwrite user 2's data ( race condition )."},{"id":"common-patterns","heading":"Common Patterns","level":3,"text":""},{"id":"useeffect-mistakes-interview-favorites","heading":"useEffect Mistakes (Interview favorites)","level":3,"text":"1. Missing dependencies — stale closures, ESLint warns 2. Infinite loop — without deps control 3. Object/array in deps — new reference every render → infinite loop 4. Using useEffect for derived state — compute in render instead"},{"id":"8-all-react-hooks-complete-reference","heading":"8. All React Hooks (Complete Reference)","level":2,"text":""},{"id":"rules-of-hooks","heading":"Rules of Hooks","level":3,"text":"1. Only call at top level — not inside loops, conditions, nested functions 2. Only call from React functions — components or custom hooks Why these rules exist. React stores hook state in a linked list per component instance, keyed by call order. If you call inside an , the order changes between renders and React attaches state to the wrong hook — corrupt state and crashes. Custom hooks are just functions that also call hooks; the same ordering rules apply. Interview angle. \"Can I call hooks in a regular function?\" → No, only in components or custom hooks. \"Can I call hooks conditionally?\" → No; call unconditionally and put the condition inside the hook or early return before any hooks (move conditional logic below all hooks, or split into child components)."},{"id":"usestate","heading":"useState","level":3,"text":""},{"id":"usereducer-complex-state-logic","heading":"useReducer — complex state logic","level":3,"text":"Use when: multiple sub values, next state depends on previous, complex transitions (like Redux locally). Example. A multi step wizard with , , and — one reducer with actions , , is clearer than five interdependent calls."},{"id":"usecontext","heading":"useContext","level":3,"text":""},{"id":"useref-dom-access-mutable-values","heading":"useRef — DOM access & mutable values","level":3,"text":""},{"id":"usememo-cache-expensive-computation","heading":"useMemo — cache expensive computation","level":3,"text":""},{"id":"usecallback-cache-function-reference","heading":"useCallback — cache function reference","level":3,"text":"How it works. Every render creates new function/object references. does shallow prop compare — if is a new function reference each render, memo is useless. returns the same function reference until dependencies change. does the same for computed values (sorted arrays, filtered lists). Pitfall. Do not wrap everything in / by default — they cost memory and comparison overhead. Profile first; optimize hot paths (large lists, heavy memoized children)."},{"id":"usememo-vs-usecallback","heading":"useMemo vs useCallback","level":3,"text":"useMemo useCallback Returns memoized value Returns memoized function Skip expensive calculation Stable function reference for child props"},{"id":"uselayouteffect","heading":"useLayoutEffect","level":3,"text":"Runs synchronously after DOM mutations, before browser paint Use for: measuring DOM, preventing visual flicker Prefer unless you need synchronous DOM read/write"},{"id":"useid-react-18","heading":"useId (React 18)","level":3,"text":""},{"id":"usetransition-usedeferredvalue-react-18-concurrent","heading":"useTransition & useDeferredValue (React 18 — Concurrent)","level":3,"text":""},{"id":"custom-hooks","heading":"Custom Hooks","level":3,"text":"Custom hook rules: Name starts with , can call other hooks."},{"id":"9-context-api-global-state","heading":"9. Context API & Global State","level":2,"text":"Theory. Context solves prop drilling — passing the same data (theme, logged in user, locale) through many intermediate components that don't use it. A Provider at the top supplies a value; any descendant calls to read it without intermediate props."},{"id":"when-to-use-context","heading":"When to use Context","level":3,"text":"Theme, locale, auth user, feature flags Avoid for frequently changing data (causes all consumers to re render)"},{"id":"context-performance-pattern-split-contexts","heading":"Context Performance Pattern — split contexts","level":3,"text":""},{"id":"context-usereducer-mini-redux","heading":"Context + useReducer (mini Redux)","level":3,"text":""},{"id":"10-react-router-v6","heading":"10. React Router (v6)","level":2,"text":"How it works. In an SPA, the server always serves . React Router listens to URL changes (via History API) and renders the matching component — no full page reload. uses clean URLs ( ); uses for static hosts without server rewrite rules. Interview angle. v6 API is declarative: + replaces v5's + . Nested routes use in the parent layout — the parent stays mounted while child routes swap."},{"id":"nested-routes","heading":"Nested Routes","level":3,"text":""},{"id":"protected-routes","heading":"Protected Routes","level":3,"text":""},{"id":"data-loading-react-router-64","heading":"Data Loading (React Router 6.4+)","level":3,"text":""},{"id":"11-state-management-redux-zustand-rtk","heading":"11. State Management — Redux, Zustand, RTK","level":2,"text":"Theory. Local and Context work until: (a) many distant components need the same frequently updated data, (b) update logic is complex with middleware needs (logging, persistence), or (c) you need time travel debugging. External stores centralize state and make updates predictable — one direction: action → reducer → new state → subscribers re render."},{"id":"when-you-need-external-state-management","heading":"When you need external state management","level":3,"text":"Many components need same data State updates frequently across distant components Complex async flows, caching, optimistic updates"},{"id":"redux-toolkit-rtk-industry-standard","heading":"Redux Toolkit (RTK) — industry standard","level":3,"text":""},{"id":"zustand-simpler-alternative","heading":"Zustand — simpler alternative","level":3,"text":"Redux Toolkit Zustand Context Large apps, devtools Small medium apps Theme, auth Boilerplate (reduced with RTK) Minimal API Built in Middleware, time travel Simple Re render issues at scale"},{"id":"12-performance-optimization","heading":"12. Performance Optimization","level":2,"text":"Theory. React re renders a component when its state changes or its parent re renders (default). Re render ≠ DOM update — React still diffs and may skip DOM writes. Performance work targets unnecessary renders and expensive render work , measured with React DevTools Profiler before optimizing."},{"id":"reactmemo-skip-re-render-if-props-unchanged","heading":"React.memo — skip re-render if props unchanged","level":3,"text":""},{"id":"when-not-to-optimize-prematurely","heading":"When NOT to optimize prematurely","level":3,"text":"Measure first (React DevTools Profiler) Most apps don't need memo everywhere / have their own cost"},{"id":"code-splitting-reactlazy-suspense","heading":"Code Splitting — React.lazy + Suspense","level":3,"text":""},{"id":"virtualization-for-long-lists","heading":"Virtualization for long lists","level":3,"text":"Use or — render only visible rows."},{"id":"key-performance-checklist","heading":"Key Performance Checklist","level":3,"text":"[ ] Stable keys in lists [ ] Avoid inline object/array in props to memoized children [ ] Code split routes [ ] Debounce search inputs [ ] Virtualize 1000+ item lists [ ] Use production build ( )"},{"id":"13-error-boundaries-suspense","heading":"13. Error Boundaries & Suspense","level":2,"text":"Theory. JavaScript errors during render crash the whole React tree by default — white screen. Error boundaries catch render time errors in children and show fallback UI. Suspense lets components \"wait\" for async data or lazy loaded code, showing a fallback until ready."},{"id":"error-boundary-class-only-no-hook-yet","heading":"Error Boundary (class only — no hook yet)","level":3,"text":"Catches: Render errors, lifecycle errors in children. Does NOT catch: Event handlers, async code, SSR, errors in boundary itself."},{"id":"suspense-for-data-fetching-react-18","heading":"Suspense for Data Fetching (React 18+)","level":3,"text":"Works with frameworks (Next.js, Relay). Experimental in plain React."},{"id":"14-react-18-19-features","heading":"14. React 18 & 19 Features","level":2,"text":"How it works — automatic batching. Before React 18, only updates inside React event handlers were batched. Updates inside , promises, or native handlers caused multiple re renders. React 18 batches all updates in the same event loop tick regardless of source — fewer renders, better performance."},{"id":"react-18","heading":"React 18","level":3,"text":"Feature What it does Automatic Batching Multiple setStates in async/event → one re render Concurrent Rendering Interruptible, priority based updates useTransition Mark updates as non urgent useDeferredValue Defer expensive renders Suspense improvements SSR streaming, selective hydration Strict Mode double invoke Effects run twice in dev to find bugs createRoot"},{"id":"react-19-know-for-interviews","heading":"React 19 (know for interviews)","level":3,"text":"Actions — async transitions with pending states ( , ) Server Components — render on server, zero client JS for static parts hook — read promises/context in render Document metadata — , in components Improved hydration — better mismatch errors"},{"id":"strict-mode","heading":"Strict Mode","level":3,"text":"Dev only: double renders, double invokes effects to expose side effects."},{"id":"15-typescript-with-react","heading":"15. TypeScript with React","level":2,"text":"Theory. TypeScript adds compile time checks to props, state, and events — catching typos like or wrong prop types before runtime. For backend developers used to Java interfaces, feels familiar. Prefer for props , avoid in new code (implicit children typing changed)."},{"id":"16-testing-jest-react-testing-library","heading":"16. Testing — Jest & React Testing Library","level":2,"text":""},{"id":"philosophy-test-behavior-not-implementation","heading":"Philosophy — test behavior, not implementation","level":3,"text":""},{"id":"query-priority-rtl","heading":"Query Priority (RTL)","level":3,"text":"1. (best — accessibility) 2. 3. 4. 5. (last resort)"},{"id":"mocking-api","heading":"Mocking API","level":3,"text":""},{"id":"17-build-tools-vite-cra-nextjs","heading":"17. Build Tools — Vite, CRA, Next.js","level":2,"text":"Theory. React code in files must be bundled (combined, transpiled, minified) before browsers can run it. Build tools also provide dev servers with hot module replacement (HMR) — edit a file, see changes without full reload. Tool Use case Vite Modern default — fast HMR, ESM native CRA Legacy — deprecated, don't start new projects Next.js Full stack React — SSR, SSG, API routes, App Router Remix Web standards, nested routing, loaders"},{"id":"vite-project-structure","heading":"Vite project structure","level":3,"text":""},{"id":"nextjs-app-router-react-18","heading":"Next.js App Router (React 18+)","level":3,"text":"SSR vs SSG vs CSR: CSR SSR SSG Render Browser Server per request Build time SEO Poor Good Good TTFB Fast shell Slower Fast CDN Use Dashboards Personalized pages Blogs, marketing"},{"id":"18-advanced-patterns","heading":"18. Advanced Patterns","level":2,"text":""},{"id":"compound-components","heading":"Compound Components","level":3,"text":""},{"id":"render-props","heading":"Render Props","level":3,"text":""},{"id":"hoc-higher-order-component","heading":"HOC (Higher-Order Component)","level":3,"text":"Modern preference: Custom hooks over HOCs."},{"id":"portal-render-outside-parent-dom","heading":"Portal — render outside parent DOM","level":3,"text":""},{"id":"forwarding-refs","heading":"Forwarding Refs","level":3,"text":""},{"id":"19-real-interview-code-puzzles","heading":"19. Real Interview Code Puzzles","level":2,"text":""},{"id":"puzzle-1-what-prints","heading":"Puzzle 1 — What prints?","level":3,"text":"Theory. These puzzles test whether you understand closures , batching , and stale state — not syntax trivia. Trace what value has at each call."},{"id":"puzzle-2-infinite-loop","heading":"Puzzle 2 — Infinite loop?","level":3,"text":""},{"id":"puzzle-3-stale-closure","heading":"Puzzle 3 — Stale closure","level":3,"text":""},{"id":"puzzle-4-key-problem","heading":"Puzzle 4 — Key problem","level":3,"text":""},{"id":"20-coding-problems-with-solutions","heading":"20. Coding Problems with Solutions","level":2,"text":""},{"id":"problem-1-debounced-search-input","heading":"Problem 1 — Debounced Search Input","level":3,"text":"Theory. Typing triggers a state update every keystroke. Calling an API on every keystroke floods the server. Debouncing waits until the user pauses typing (e.g., 300ms) before firing the search — same pattern as backend rate limiting, applied client side."},{"id":"problem-2-todo-app-crud","heading":"Problem 2 — Todo App (CRUD)","level":3,"text":""},{"id":"problem-3-infinite-scroll","heading":"Problem 3 — Infinite Scroll","level":3,"text":""},{"id":"21-tough-interview-qa-bank","heading":"21. Tough Interview Q&A Bank","level":2,"text":"Q: What is reconciliation? A: React's algorithm to compare new Virtual DOM with previous, compute minimal DOM updates. Uses heuristics: different element types → rebuild subtree; same type → update attributes; lists use keys. Q: Why can't we use index as key? A: When list reorders, React reuses wrong component instances, causing state bugs and poor performance. Q: useEffect vs useLayoutEffect? A: useEffect async after paint; useLayoutEffect sync before paint. Use layout when you need to measure/mutate DOM before user sees flicker. Q: How to prevent unnecessary re renders? A: React.memo, useMemo, useCallback, split context, colocate state, virtualization. Profile first. Q: What is lifting state up? A: Moving shared state to closest common ancestor so siblings stay in sync via props. Q: Controlled vs uncontrolled? A: Controlled = React state drives input value. Uncontrolled = DOM holds value, read via ref. Q: What happens when setState is called? A: React schedules re render, batches updates (React 18 batches even in timeouts), creates new VDOM, diffs, commits changes. Q: Redux vs Context? A: Context for simple infrequent global data. Redux/RTK for complex state, middleware, devtools, predictable updates at scale. Q: What is Strict Mode double invoke? A: Dev only: mounts, unmounts, remounts components and runs effects twice to surface impure side effects. Q: SSR hydration mismatch? A: Server HTML ≠ client first render. Causes React warning. Fix: consistent data, suppressClientRender, useEffect for client only code. Q: How does React 18 automatic batching work? A: Multiple setStates in same event loop tick batched into one render — including promises, setTimeout, native handlers. Q: Custom hook benefits? A: Reuse stateful logic, separate concerns, composable, testable without UI."},{"id":"interview-qa-react","heading":"INTERVIEW Q&A — REACT","level":2,"text":"Q: Is React a framework or library? A: Library — UI layer only. You choose router, state, data fetching. Next.js is a framework built on React. Q: What is JSX? A: Syntax sugar for . Not HTML — , camelCase attrs, JS expressions in . Q: Functional vs class components? A: Functional + hooks is standard. Class uses lifecycle methods and . Know both for legacy code. Q: Explain Virtual DOM. A: In memory JS representation of UI. On state change, new VDOM created, diffed with old, minimal real DOM updates applied. Q: What are props? A: Read only inputs from parent to child. Immutable in child. Can pass data and callback functions. Q: What is state? A: Component's private mutable data. State change triggers re render. Use or . Q: What are React Hooks? A: Functions that let functional components use state, lifecycle, refs, context. Must follow rules of hooks. Q: Explain useEffect. A: Runs side effects after render. Cleanup on unmount/re run. Dependency array controls when it runs. Q: What is useContext? A: Subscribe to context value without prop drilling. Good for theme, auth, locale. Q: useMemo vs useCallback? A: useMemo caches a value; useCallback caches a function reference. Q: What is React Router? A: Client side routing library. Maps URLs to components without full page reload. Q: What is Redux? A: Predictable state container. Single store, actions dispatch state changes, reducers produce new state. RTK is modern standard. Q: How do you test React components? A: React Testing Library — query by role/label, simulate user events, assert visible behavior. Mock API with jest.mock. Q: What is code splitting? A: Load components lazily with + to reduce initial bundle size. Q: Error boundary? A: Class component catching render errors in children. Shows fallback UI. Doesn't catch event/async errors. Q: React 18 concurrent features? A: useTransition, useDeferredValue, automatic batching, Suspense SSR improvements, createRoot API. Q: Next.js vs CRA? A: Next.js = SSR/SSG, file routi"}],"qa":[{"q":"What is reconciliation?","a":"React's algorithm to compare new Virtual DOM with previous, compute minimal DOM updates. Uses heuristics: different element types → rebuild subtree; same type → update attributes; lists use keys."},{"q":"Why can't we use index as key?","a":"When list reorders, React reuses wrong component instances, causing state bugs and poor performance."},{"q":"useEffect vs useLayoutEffect?","a":"useEffect async after paint; useLayoutEffect sync before paint. Use layout when you need to measure/mutate DOM before user sees flicker."},{"q":"How to prevent unnecessary re-renders?","a":"React.memo, useMemo, useCallback, split context, colocate state, virtualization. Profile first."},{"q":"What is lifting state up?","a":"Moving shared state to closest common ancestor so siblings stay in sync via props."},{"q":"Controlled vs uncontrolled?","a":"Controlled = React state drives input value. Uncontrolled = DOM holds value, read via ref."},{"q":"What happens when setState is called?","a":"React schedules re-render, batches updates (React 18 batches even in timeouts), creates new VDOM, diffs, commits changes."},{"q":"Redux vs Context?","a":"Context for simple infrequent global data. Redux/RTK for complex state, middleware, devtools, predictable updates at scale."},{"q":"What is Strict Mode double invoke?","a":"Dev-only: mounts, unmounts, remounts components and runs effects twice to surface impure side effects."},{"q":"SSR hydration mismatch?","a":"Server HTML ≠ client first render. Causes React warning. Fix: consistent data, suppressClientRender, useEffect for client-only code."},{"q":"How does React 18 automatic batching work?","a":"Multiple setStates in same event loop tick batched into one render — including promises, setTimeout, native handlers."},{"q":"Custom hook benefits?","a":"Reuse stateful logic, separate concerns, composable, testable without UI."},{"q":"Is React a framework or library?","a":"Library — UI layer only. You choose router, state, data fetching. Next.js is a framework built on React."},{"q":"What is JSX?","a":"Syntax sugar for `React.createElement`. Not HTML — `className`, camelCase attrs, JS expressions in `{}`."},{"q":"Functional vs class components?","a":"Functional + hooks is standard. Class uses lifecycle methods and `this.state`. Know both for legacy code."},{"q":"Explain Virtual DOM.","a":"In-memory JS representation of UI. On state change, new VDOM created, diffed with old, minimal real DOM updates applied."},{"q":"What are props?","a":"Read-only inputs from parent to child. Immutable in child. Can pass data and callback functions."},{"q":"What is state?","a":"Component's private mutable data. State change triggers re-render. Use `useState` or `useReducer`."},{"q":"What are React Hooks?","a":"Functions that let functional components use state, lifecycle, refs, context. Must follow rules of hooks."},{"q":"Explain useEffect.","a":"Runs side effects after render. Cleanup on unmount/re-run. Dependency array controls when it runs."},{"q":"What is useContext?","a":"Subscribe to context value without prop drilling. Good for theme, auth, locale."},{"q":"useMemo vs useCallback?","a":"useMemo caches a value; useCallback caches a function reference."},{"q":"What is React Router?","a":"Client-side routing library. Maps URLs to components without full page reload."},{"q":"What is Redux?","a":"Predictable state container. Single store, actions dispatch state changes, reducers produce new state. RTK is modern standard."},{"q":"How do you test React components?","a":"React Testing Library — query by role/label, simulate user events, assert visible behavior. Mock API with jest.mock."},{"q":"What is code splitting?","a":"Load components lazily with `React.lazy` + `Suspense` to reduce initial bundle size."},{"q":"Error boundary?","a":"Class component catching render errors in children. Shows fallback UI. Doesn't catch event/async errors."},{"q":"React 18 concurrent features?","a":"useTransition, useDeferredValue, automatic batching, Suspense SSR improvements, createRoot API."},{"q":"Next.js vs CRA?","a":"Next.js = SSR/SSG, file routing, API routes, production-ready. CRA deprecated — use Vite for SPA."},{"q":"How would you optimize a slow React app?","a":"Profile → memo/code split/virtualize → fix unnecessary re-renders → optimize images → CDN → server components for static content."},{"q":"What is prop drilling?","a":"Passing props through many intermediate components. Fix: Context, composition, state management library."},{"q":"Composition vs inheritance in React?","a":"React favors composition — combine components via children, render props, hooks. Inheritance rarely used."},{"q":"What is `key` in lists?","a":"Stable unique identifier for reconciliation. Helps React match items across renders."},{"q":"Can hooks be used in class components?","a":"No. Hooks only in functional components and custom hooks."},{"q":"What is `ref` used for?","a":"Access DOM nodes, store mutable values without re-render, forward refs to child DOM."},{"q":"Server Components (React 19)?","a":"Components that run only on server — zero client JS, can access DB directly. Client Components marked with `'use client'`."}]},"16_Java_Multithreading_Deep_Dive.md":{"title":"Java Multithreading Deep Dive","trackId":"java-core","trackTitle":"Java","sections":[{"id":"java-multithreading-deep-dive","heading":"Java Multithreading — Deep Dive","level":1,"text":""},{"id":"from-fundamentals-to-production-patterns-10-years-engineer-perspective","heading":"From Fundamentals to Production Patterns | 10+ Years Engineer Perspective","level":3,"text":""},{"id":"1-process-vs-thread","heading":"1. Process vs Thread","level":2,"text":"Aspect Process Thread Definition An independent program in execution A lightweight unit of execution within a process Memory Own address space (heap, code, data) Shares the process heap & method area; has own stack + PC register Communication IPC (sockets, pipes, shared memory) — expensive Shared memory — cheap but needs synchronization Creation cost High (OS level) Lower (but platform threads still 1MB stack) Crash impact Isolated One bad thread can corrupt shared state of whole process Key takeaway: Threads share heap memory. That sharing is exactly what makes them powerful and dangerous — every shared mutable object is a potential race condition. Theory. A process is an isolated program with its own virtual address space — like a separate apartment with its own furniture. A thread is a worker inside that apartment sharing the same furniture (heap) but with its own desk (stack). Multiple threads can work on shared data simultaneously, but without coordination they step on each other's edits. How it works — OS perspective. The OS scheduler time slices CPU cores among runnable threads. On a 4 core machine, hundreds of RUNNABLE threads take turns — each gets a few milliseconds, then context switches (save registers, load another thread's state). Context switches cost microseconds; too many threads → thrashing, little useful work. Example. An order service with one thread handles 100 req/s. With 4 threads on 4 cores doing CPU work, you might approach 400 req/s (minus overhead). With I/O bound work, you can run far more threads because most are blocked waiting on DB/network, not using CPU. What each thread owns vs shares:"},{"id":"2-why-multithreading","heading":"2. Why Multithreading","level":2,"text":"Theory. Concurrency solves two different problems: throughput (more work per second) and responsiveness (don't block the user while work happens). The right tool depends on whether threads spend time on CPU or waiting on I/O. Use it for: CPU bound parallelism — split heavy computation across cores (image processing, batch math, aggregations). I/O bound concurrency — keep CPU busy while threads wait on DB / network / disk (the most common server use case). Responsiveness — offload long work off the request/UI thread. Throughput — process many independent units (messages, records, requests) at once. Do NOT reach for threads when: The work is trivial — thread creation/coordination overhead exceeds the gain. Tasks are tightly dependent (heavy locking serializes them anyway → you gain nothing). A simpler model exists (async I/O, a message queue, a batch job, DB side processing). Amdahl's Law (interview favorite): Speedup is capped by the serial fraction. If 10% of your work is inherently sequential, the maximum speedup is 10× no matter how many cores you add. Always profile the serial bottleneck before parallelizing. CPU bound vs I/O bound sizing rule of thumb: CPU bound pool size ≈ number of cores ( ). I/O bound pool size ≈ cores × (1 + wait time / compute time) — can be much larger because threads spend time blocked."},{"id":"3-thread-lifecycle","heading":"3. Thread Lifecycle","level":2,"text":"State Meaning Created but not called Eligible to run (running or ready, JVM doesn't distinguish) Waiting to acquire a monitor lock ( ) Indefinitely waiting ( , , ) Waiting with timeout ( , , ) Completed or threw Gotcha: is and does not release held monitor locks. is / and does release the monitor. This distinction is asked constantly. Example. Thread holds lock on , calls — no other thread can access for 5 seconds. If it called instead (inside synchronized), the lock would be released and others could proceed."},{"id":"4-ways-to-create-threads","heading":"4. Ways to Create Threads","level":2,"text":"vs — the classic trap: Calling directly executes the body on the current thread (no new thread). Only asks the JVM to create a new OS/virtual thread and invoke on it. Runnable vs Callable: Runnable Callable<V Returns value No ( ) Yes ( ) Throws checked exc. No Yes Used with , →"},{"id":"5-types-of-multithreading","heading":"5. Types of Multithreading","level":2,"text":"Different concurrency models you'll choose between in real systems: 1. Thread per task — spawn a thread for each unit. Simple, but unbounded threads = OOM / context switch storm. Only safe with virtual threads. 2. Thread pool (bounded) — fixed set of worker threads pull tasks from a queue. The default for servers. Backpressure via a bounded queue. 3. Producer–Consumer — producers push to a , consumers drain it. Decouples ingestion rate from processing rate. 4. Fork–Join (divide & conquer) — recursively split a big task, work stealing across cores. Great for CPU bound, balanced workloads. 5. Pipeline / staged (SEDA) — each stage has its own pool + queue (parse → enrich → persist). Each stage tuned independently. 6. Master–Worker — one coordinator distributes work units to a worker pool and aggregates results. 7. Event loop / reactive — a few threads + non blocking I/O (Netty, WebFlux). High concurrency with few threads. 8. Scatter–Gather — fan out N parallel calls, then join results (e.g., query 5 downstreams, merge). or virtual threads. A senior engineer picks the model based on: workload type (CPU vs I/O), latency vs throughput goals, ordering requirements, and failure isolation needs."},{"id":"6-synchronization","heading":"6. Synchronization","level":2,"text":""},{"id":"61-synchronized","heading":"6.1 synchronized","level":3,"text":"Theory. provides mutual exclusion (one thread at a time) and happens before visibility (writes before unlock are seen after the next lock). Every object is a potential lock (monitor). How it works — why races. At bytecode level, is three steps: (1) read from memory, (2) add 1, (3) write back. Two threads can interleave: makes the entire read modify write atomic relative to other threads holding the same lock. Pitfall. Locking the wrong object (e.g., in each call — different locks!) or holding locks during I/O (blocks all other threads and pins virtual thread carriers). Mutual exclusion via an object's intrinsic monitor lock. Reentrant: a thread already holding the lock can re acquire it. is not atomic (read modify write) — without synchronization, two threads can lose updates."},{"id":"62-volatile","heading":"6.2 volatile","level":3,"text":"Theory. is a visibility contract , not a lock. It tells the JVM: reads and writes of this field go directly to main memory (with appropriate barriers), so no thread sees a stale cached copy. How it works. Without volatile, Thread A may write to its CPU cache; Thread B reads from its cache (still false) and loops forever. inserts a happens before edge: write → subsequent read on any thread. Example — safe shutdown flag: Guarantees visibility and ordering, but not atomicity . Use for flags / publish once references. Do NOT use for — that's still a race (no atomicity)."},{"id":"63-explicit-locks-javautilconcurrentlocks","heading":"6.3 Explicit Locks (java.util.concurrent.locks)","level":3,"text":"Advantages over : (non blocking / with timeout) → avoids deadlock. → can be interrupted while waiting. Fairness option (FIFO ordering). → many concurrent readers, exclusive writer (read heavy caches). → optimistic reads, even faster for read mostly workloads."},{"id":"64-atomic-variables-lock-free","heading":"6.4 Atomic Variables (lock-free)","level":3,"text":"Backed by CAS (Compare And Swap) CPU instructions — no locking, no blocking. Use / for hot counters with many writers. Decision guide: Need Use Visibility of a flag/reference Atomic single variable / Guard a compound action / multiple fields or Read heavy, rare writes / Timeout / interruptible / fairness"},{"id":"7-java-memory-model","heading":"7. Java Memory Model","level":2,"text":"Theory. Without the JMM, you cannot reason about what one thread sees when another writes a variable. CPUs cache values in L1/L2 cache; compilers reorder instructions if they think it's safe within a single thread. The JMM defines when writes become visible across threads via happens before relationships. The JMM defines when a write by one thread becomes visible to another. Without it, the compiler/CPU may reorder or cache values in registers. How it works — a concrete visibility failure: Thread 2's CPU may cache in a register. or inserts a happens before edge so the write is flushed and the read invalidated. happens before relationships (if A happens before B, A's effects are visible to B): Program order within a single thread. of a monitor happens before a subsequent of the same monitor. Write to a happens before every subsequent read of it. happens before any action in the started thread. All actions in a thread happen before another thread's successful on it. Why it matters: \"It works on my machine\" bugs in concurrency are usually missing happens before edges — a value cached in a CPU register or reordered. /locks insert the necessary memory barriers."},{"id":"8-wait-notify-producerconsumer","heading":"8. wait / notify & Producer–Consumer","level":2,"text":"Theory. / are the low level condition variable API built into every Java object. They let a thread wait for a condition while releasing the lock so another thread can make the condition true. Used for hand rolled producer consumer before . How it works. must be called inside — it atomically releases the monitor and adds the thread to the object's wait set. wakes all waiters, who must re acquire the lock before proceeding. Always wait in a while loop — spurious wakeups and multiple waiters mean the condition may still be false when you wake. Low level coordination on an object's monitor. Always call inside and always wait in a loop (guard against spurious wakeups). Modern, preferred approach — (don't hand roll wait/notify): / handle all the waiting/signaling for you. A bounded queue gives natural backpressure so a fast producer can't OOM the JVM."},{"id":"9-thread-pools","heading":"9. Thread Pools","level":2,"text":""},{"id":"why-pools-exist","heading":"Why pools exist","level":3,"text":"Creating a platform thread is expensive ( 1MB stack, OS scheduling). Unbounded thread creation → context switch thrashing and . A pool reuses a fixed set of workers and queues excess work — this is the single most important production concurrency tool."},{"id":"the-executors-factory-shortcuts-and-why-seniors-avoid-them","heading":"The Executors factory shortcuts (and why seniors avoid them)","level":3,"text":"Senior insight: and use an unbounded . Under sustained overload the queue grows until OOM. creates an unbounded number of threads. In production, construct your own with a bounded queue and an explicit rejection policy."},{"id":"lifecycle-always-shut-down","heading":"Lifecycle — always shut down","level":3,"text":"Leaked pools = leaked threads = the app won't exit and memory climbs."},{"id":"10-threadpoolexecutor-internals","heading":"10. ThreadPoolExecutor Internals","level":2,"text":"Every knob you should know cold:"},{"id":"how-a-task-flows-in-the-algorithm-asked-in-interviews","heading":"How a task flows in (the algorithm — asked in interviews)","level":3,"text":"Theory. is a producer consumer system: callers submit tasks (producer), worker threads pull tasks from a queue (consumer). The algorithm decides whether to spawn a new thread or buffer in the queue — getting this wrong means either wasted threads or unbounded memory growth. Worked example — : Submit Action Active threads Queue 1 New core thread T1 1 [] 2 New core thread T2 2 [] 3 Enqueue (cores busy) 2 [task3] 4 Enqueue 2 [task3, task4] 5 Queue full → new thread T3 3 [task3, task4] 6 Queue full → new thread T4 4 [task3, task4] 7 Queue full, at max → RejectedExecutionException (AbortPolicy) 1. If running threads → create a new core thread for the task. 2. Else → try to enqueue the task in . 3. If the queue is full and threads → create a non core thread . 4. If the queue is full and threads → reject via the rejection handler. Counter intuitive consequence: with an unbounded queue, is never reached (step 2 always succeeds), so extra threads never spawn. This is exactly why ignores its max."},{"id":"queue-choices","heading":"Queue choices","level":3,"text":"Queue Behaviour (bounded) Fixed capacity, predictable memory — recommended (unbounded by default) Grows unbounded → OOM risk No buffering — hands off directly; pairs with large max pool Tasks processed by priority"},{"id":"rejection-policies","heading":"Rejection policies","level":3,"text":"Policy Behaviour (default) Throws Runs task on the caller's thread → natural backpressure / slows producer Silently drops the task Drops oldest queued task, retries Production grade pool template: Naming threads is not cosmetic — it makes thread dumps and production incident triage dramatically faster."},{"id":"11-callable-future-completablefuture","heading":"11. Callable, Future & CompletableFuture","level":2,"text":""},{"id":"future-limitations","heading":"Future limitations","level":3,"text":"blocks , you can't chain callbacks, and you can't easily combine multiple futures. fixes all three."},{"id":"completablefuture-the-async-workhorse","heading":"CompletableFuture — the async workhorse","level":3,"text":"Key methods cheat sheet: Method Purpose Map result (sync) Map on another pool Chain dependent async call (flatMap) Combine two independent futures / Wait for all / first / Recover from failure Fail if too slow Always pass your own executor to methods. The default is shared JVM wide; blocking I/O on it starves parallel streams and other CFs across the whole app."},{"id":"12-concurrent-collections","heading":"12. Concurrent Collections","level":2,"text":"Use case Class Notes Map Lock striping (Java 8: CAS + per bin synchronized). No null keys/values. List (read heavy) Every write copies the array — great for rare writes (listeners), terrible for frequent writes Queue (FIFO blocking) / Producer–consumer Deque / Work stealing, both ends Priority Ordered consumption Delayed / scheduled Elements available after a delay Skip list (sorted) Sorted concurrent map atomic compound ops (avoid check then act races): Never do — that's a race. Use / . Why not ? It locks the whole map per operation (coarse) and still needs external sync for iteration. scales far better and offers atomic compound ops."},{"id":"13-forkjoinpool-parallel-streams","heading":"13. ForkJoinPool & Parallel Streams","level":2,"text":"Theory. Fork/Join is divide and conquer parallelism : split a big task into halves until pieces are small enough to compute directly, then combine results. Work stealing lets idle threads steal tasks from busy threads' deques — better CPU utilization when subtasks finish unevenly. How it works. Each worker thread has a deque (double ended queue). It pushes/pops tasks from one end; idle threads steal from the other end. uses the shared — size ≈ . Pitfall. Blocking I/O inside or tasks starves all parallel streams and CompletableFutures using the default pool across the entire JVM. Fork/Join = divide and conquer with work stealing (idle threads steal tasks from busy threads' deques). Parallel streams ride on the shared : Good for CPU bound , large, splittable sources (arrays, ArrayList) with stateless, associative operations. Avoid for I/O bound work or small collections — they pollute the common pool and often run slower than sequential. To isolate parallel stream work, submit it inside your own :"},{"id":"14-virtual-threads","heading":"14. Virtual Threads","level":2,"text":"Theory. Platform threads are expensive OS resources. For I/O heavy services (REST APIs waiting on DB), you want one thread per request for simple code, but can't afford 50,000 OS threads. Virtual threads give you millions of lightweight threads with blocking style code. Java 21 (Project Loom) — JEP 444. Lightweight threads scheduled by the JVM onto a small pool of carrier (platform) threads. How it works. When blocks, the JVM parks the virtual thread: saves its stack frame state, unmounts it from the carrier platform thread, and runs another virtual thread on that carrier. When I/O completes, the virtual thread is unparked and rescheduled. Carrier pool size ≈ number of CPU cores (default). Example — pinning scenario: Interview angle. Virtual threads don't make CPU bound work faster — you still have N cores. They make I/O bound concurrency cheap. Don't pool virtual threads; don't use thread local caches assuming few threads. Cheap : few hundred bytes vs 1MB for platform threads. Millions are feasible. When a virtual thread blocks on I/O , it unmounts from its carrier, freeing it for other work — so the classic \"thread per request, write simple blocking code\" model scales massively. Best for I/O bound services (each request waits on DB/HTTP). Not a speedup for CPU bound work (still limited by cores). Pitfalls: Pinning : a virtual thread inside a block doing blocking I/O pins the carrier (can't unmount). Prefer over around blocking calls. Don't pool virtual threads — create one per task. Pooling defeats the purpose. Don't rely on thread local heavy caching (you may have millions of threads)."},{"id":"15-deadlock-livelock-starvation","heading":"15. Deadlock, Livelock, Starvation","level":2,"text":"Theory. These are progress failures — threads are alive but the system doesn't advance. Deadlock = frozen; livelock = busy but frozen; starvation = one thread never gets a turn. Deadlock — two+ threads each hold a lock the other needs, forever. Worked example: The four Coffman conditions (all must hold): mutual exclusion, hold and wait, no preemption, circular wait. Break any one to prevent deadlock. Prevention strategies: Global lock ordering — always acquire locks in the same order (e.g., by object id). Most common fix. with timeout — back off and retry instead of blocking forever. Reduce lock scope / use a single coarse lock / lock free structures. Livelock — threads keep responding to each other and changing state but make no progress (two people stepping aside in a hallway). Fix with randomized backoff. Starvation — a thread never gets CPU/lock because others monopolize it (e.g., low priority, or unfair locks). Fix with fair locks / fair queues. Diagnosis in prod: take a thread dump ( or ). The JVM explicitly prints and the cycle. threads waiting on monitors point to contention."},{"id":"16-real-world-patterns","heading":"16. Real-World Patterns","level":2,"text":"The following are generic, anonymized patterns drawn from large scale telco/order processing systems. Service names are intentionally omitted."},{"id":"161-high-throughput-eventorder-processing-engine","heading":"16.1 High-Throughput Event/Order Processing Engine","level":3,"text":"A core processing engine consumes a continuous stream of events (orders, charging events, requests) and must process thousands per second with strict ordering per key but parallelism across keys . Pattern used: a bounded thread pool fronted by partitioned queues . Why partitioning: guarantees per entity ordering (e.g., all events for one account processed in sequence) while still using all cores. Backpressure: bounded queues + slow the upstream when the engine falls behind, preventing OOM. Idempotency: because retries happen on failure, each handler is written to be idempotent (safe to reprocess)."},{"id":"162-bulk-data-injector-ingestion-service","heading":"16.2 Bulk Data Injector / Ingestion Service","level":3,"text":"A service that ingests large batches of records (millions) into downstream stores needs maximum throughput without overwhelming the database. Pattern used: producer–consumer with a bounded queue + batching . Batching amortizes DB round trips → orders of magnitude throughput gain. Bounded queue caps memory and provides backpressure when the DB is the bottleneck. Pool size tuned to the DB connection pool size (no point having more writers than connections)."},{"id":"163-asynchronous-compute-callback-service","heading":"16.3 Asynchronous Compute / Callback Service","level":3,"text":"A service that accepts a request, kicks off long running downstream work, and notifies the caller asynchronously (callback / event) rather than holding the request thread. Pattern used: pipelines on a dedicated executor + async callback . Frees the request/HTTP thread instantly → far higher concurrency on the front door. A dedicated bounded executor isolates this work from other subsystems (bulkhead pattern) so a downstream slowdown can't exhaust the shared pool. centralizes success/failure callback dispatch."},{"id":"164-scattergather-aggregation","heading":"16.4 Scatter–Gather Aggregation","level":3,"text":"Fan out to several independent downstreams, then merge — common in aggregation/composition layers. Total latency ≈ slowest call instead of the sum of all calls. Cross cutting production lessons: Bulkheading : give each subsystem its own pool so one slow dependency can't starve everything. Timeouts everywhere : an unbounded blocking call is a latent outage. Metrics : export pool queue depth, active threads, rejection count — these are your earliest overload signals. Graceful shutdown : drain queues and so in flight work isn't lost on deploy. MDC / trace propagation : copy logging context across thread handoffs, or your distributed traces break."},{"id":"17-best-practices","heading":"17. Best Practices","level":2,"text":"Do: Prefer higher level constructs ( , , , concurrent collections) over raw / / . Always use bounded queues and an explicit rejection policy in production. Name your threads (custom ) for debuggable thread dumps. Keep critical sections small ; do I/O outside locks. Make shared objects immutable where possible (immutability eliminates most races). Always in ; always pools. Make task handlers idempotent if they can be retried. Pass an explicit executor to . Don't: Don't call (deprecated, unsafe). Use interruption + a flag. Don't swallow — restore the flag: . Don't share , (use ), or other non thread safe objects across threads. Don't use / blindly in prod (unbounded queue/threads). Don't block inside a or the common . Don't around blocking I/O when using virtual threads (pinning). Interruption — the cooperative cancellation model:"},{"id":"18-interview-qa","heading":"18. Interview Q&A","level":2,"text":"Q1. vs ? adds , timeouts, interruptibility, fairness, and multiple s — at the cost of explicit in . is simpler, JVM optimized (biased/lightweight locking), and auto releases. Use by default; reach for when you need its extra capabilities. Q2. Does make thread safe? No. guarantees visibility/ordering but not atomicity. is read modify write (3 steps). Use / or a lock. Q3. vs ? releases the monitor and waits for ; must be in a block; it's on . keeps all locks and just pauses; it's static on . Both can be interrupted. Q4. Why is risky? It uses an unbounded ; under sustained overload the queue grows until . Build a with a bounded queue + rejection policy. Q5. How does decide to create a thread vs queue? Below core size → new core thread. Else enqueue. Queue full and below max → new thread. Queue full and at max → reject. (With an unbounded queue, max is never reached.) Q6. What's a thread dump and how do you read it? Snapshot of all threads' stacks ( / ). Look for threads (lock contention), on the same monitor (possible bottleneck), and the explicit \"deadlock\" section. Q7. vs ? CHM uses fine grained locking/CAS for high concurrency and offers atomic compound ops ( , ). locks the whole map per op and needs manual sync for iteration. Q8. CAS and the ABA problem? CAS = atomic compare and swap (lock free). ABA: a value changes A→B→A and CAS wrongly succeeds. Fix with a version stamp — . Q9. How do virtual threads change pool sizing? For I/O bound work you no longer size pools to limit threads — you create one virtual thread per task and let the JVM unmount them on blocking. CPU bound work still needs bounded parallelism (cores). Q10. How do you cancel a running task cleanly? Cooperative interruption: call / , check in loops, and propagate (restore the flag). Never . Q11. Producer is faster than consumer — what happens and how to handle? With an unbounded queue → memory grows → OOM. Use a bounded queue ( blocks) for backpressure, or , or scale consumers, or drop/sample if dat"}],"qa":[{"q":"`synchronized` vs `ReentrantLock`?","a":"`ReentrantLock` adds `tryLock`, timeouts, interruptibility, fairness, and multiple `Condition`s — at the cost of explicit `unlock()` in `finally`. `synchronized` is simpler, JVM-optimized (biased/lightweight locking), and auto-releases. Use `synchronized` by default; reach for `ReentrantLock` when you need its extra capabilities."},{"q":"Does `volatile` make `count++` thread-safe?","a":"No. `volatile` guarantees visibility/ordering but not atomicity. `count++` is read-modify-write (3 steps). Use `AtomicInteger`/`LongAdder` or a lock."},{"q":"`wait()` vs `sleep()`?","a":"`wait()` releases the monitor and waits for `notify`; must be in a `synchronized` block; it's on `Object`. `sleep()` keeps all locks and just pauses; it's static on `Thread`. Both can be interrupted."},{"q":"Why is `newFixedThreadPool` risky?","a":"It uses an unbounded `LinkedBlockingQueue`; under sustained overload the queue grows until `OutOfMemoryError`. Build a `ThreadPoolExecutor` with a bounded queue + rejection policy."},{"q":"How does `ThreadPoolExecutor` decide to create a thread vs queue?","a":"Below core size → new core thread. Else enqueue. Queue full and below max → new thread. Queue full and at max → reject. (With an unbounded queue, max is never reached.)"},{"q":"What's a thread dump and how do you read it?","a":"Snapshot of all threads' stacks (`jstack`/`jcmd Thread.print`). Look for `BLOCKED` threads (lock contention), `WAITING` on the same monitor (possible bottleneck), and the explicit \"deadlock\" section."},{"q":"`ConcurrentHashMap` vs `Collections.synchronizedMap`?","a":"CHM uses fine-grained locking/CAS for high concurrency and offers atomic compound ops (`computeIfAbsent`, `merge`). `synchronizedMap` locks the whole map per op and needs manual sync for iteration."},{"q":"CAS and the ABA problem?","a":"CAS = atomic compare-and-swap (lock-free). ABA: a value changes A→B→A and CAS wrongly succeeds. Fix with a version stamp — `AtomicStampedReference`."},{"q":"How do virtual threads change pool sizing?","a":"For I/O-bound work you no longer size pools to limit threads — you create one virtual thread per task and let the JVM unmount them on blocking. CPU-bound work still needs bounded parallelism (cores)."},{"q":"How do you cancel a running task cleanly?","a":"Cooperative interruption: call `future.cancel(true)` / `thread.interrupt()`, check `isInterrupted()` in loops, and propagate `InterruptedException` (restore the flag). Never `Thread.stop()`."},{"q":"Producer is faster than consumer — what happens and how to handle?","a":"With an unbounded queue → memory grows → OOM. Use a **bounded** queue (`put` blocks) for backpressure, or `CallerRunsPolicy`, or scale consumers, or drop/sample if data is lossy-tolerant."},{"q":"What is the difference between concurrency and parallelism?","a":"Concurrency = dealing with many tasks at once (structure — interleaving). Parallelism = doing many tasks at the same instant (execution — multiple cores). You can have concurrency on a single core; parallelism needs multiple cores."}]},"17_Java_Streams_Deep_Dive.md":{"title":"Java Streams Deep Dive","trackId":"java-core","trackTitle":"Java","sections":[{"id":"java-streams-deep-dive","heading":"Java Streams — Deep Dive","level":1,"text":""},{"id":"functional-data-processing-beginner-expert","heading":"Functional Data Processing | Beginner → Expert","level":3,"text":""},{"id":"1-why-streams","heading":"1. Why Streams","level":2,"text":"A Stream is a sequence of elements supporting declarative, functional style operations. It is not a data structure — it carries no storage; it pulls from a source (collection, array, I/O, generator). Collection Stream Stores data Carries data (no storage) External iteration ( ) Internal iteration (you say what , not how ) Reusable Single use (consume once, then it's closed) Eager Lazy (nothing runs until a terminal op) Mutable Encourages immutability / no side effects Anatomy: . Intermediate ops are lazy and return a new Stream; the terminal op triggers execution and produces a result/side effect. Single use trap: reusing a stream throws ."},{"id":"2-creating-streams","heading":"2. Creating Streams","level":2,"text":"For infinite streams ( , ) you must apply a short circuiting op like / ."},{"id":"3-intermediate-operations","heading":"3. Intermediate Operations","level":2,"text":"All return a new Stream and are lazy . Op Purpose Keep matching elements Transform each element Flatten nested streams (1→many) Remove duplicates (uses ) / Order Debug/side effect (don't abuse) / Truncate / drop / (Java 9+) Take/drop while predicate holds To primitive stream (Java 16+) Imperative style 1→many, less allocation than flatMap vs : stops at the first non match (assumes ordered/sorted data); scans everything."},{"id":"4-terminal-operations","heading":"4. Terminal Operations","level":2,"text":"Trigger execution; the stream is consumed after. Op Returns Mutable reduction (List, Map, String...) (Java 16+) Unmodifiable list (shortcut) / Side effect Single value reduction / / / (short circuit) / (short circuit) array must be associative and stateless to be parallel safe."},{"id":"5-collectors-deep-dive","heading":"5. Collectors Deep Dive","level":2,"text":"The most powerful and most asked part. duplicate key trap: without a merge function, duplicate keys throw . Always supply a merge function when keys may collide."},{"id":"6-flatmap-nested-data","heading":"6. flatMap & Nested Data","level":2,"text":"flattens a stream of streams into one stream (one to many). = 1→1; = 1→many flattened."},{"id":"7-primitive-streams","heading":"7. Primitive Streams","level":2,"text":", , avoid boxing overhead and add numeric ops. Prefer primitive streams for heavy numeric work — no boxing per element."},{"id":"8-optional","heading":"8. Optional","level":2,"text":"A container to model \"value or absence\" — avoid and explicit null checks. Best practices: use for return types, not for fields or method parameters. Don't call without checking. Prefer (lazy) over (eager) when the default is expensive."},{"id":"9-parallel-streams","heading":"9. Parallel Streams","level":2,"text":"Runs on the shared (size = cores − 1 by default). Use when: large dataset, splittable source (array/ArrayList), stateless + associative ops, CPU bound work. Avoid when: small data, I/O bound tasks, ordered/stateful ops, or operations with shared mutable state. preserves order (slower); does not in parallel. Production warning: parallel streams hijack the common pool JVM wide. Don't do blocking I/O inside them. To isolate, run inside your own . For I/O bound concurrency, prefer or virtual threads."},{"id":"10-lazy-evaluation","heading":"10. Lazy Evaluation","level":2,"text":"Intermediate ops do nothing until a terminal op runs, and the pipeline processes elements one at a time (not stage by stage). This enables short circuiting . Short circuiting ops: , , , , , . They let you work with infinite streams."},{"id":"11-common-pitfalls","heading":"11. Common Pitfalls","level":2,"text":"Reusing a stream → . Create a fresh one each time. Side effects in / → breaks parallelism and laziness. Keep them pure. Mutating shared state in → race conditions; use / instead. for logic → may be skipped by optimizations; use only for debugging. duplicate keys → supply a merge function. Overusing parallel → often slower than sequential; measure first. without check → . Use . Forgetting to close I/O streams ( ) → resource leak; use try with resources. Premature then re stream → keep the pipeline going instead."},{"id":"12-real-world-examples","heading":"12. Real-World Examples","level":2,"text":""},{"id":"13-interview-qa","heading":"13. Interview Q&A","level":2,"text":"Q1. Stream vs Collection? Collection stores data and is iterated externally; a Stream carries data, is iterated internally, is lazy, and is single use. Q2. Intermediate vs terminal operations? Intermediate ops are lazy and return a Stream ( , ). Terminal ops trigger execution and produce a result/side effect ( , , ). Nothing runs until a terminal op. Q3. vs ? is 1→1 transform. is 1→many and flattens nested streams into one. Q4. Is a stream reusable? No. After a terminal op the stream is consumed; reusing throws . Q5. How does laziness help? It enables short circuiting (stop early via / ) and lets you process infinite streams; elements flow one at a time through the pipeline. Q6. When are parallel streams a bad idea? Small data, I/O bound work, stateful/ordered ops, or shared mutable state. They also share the common pool JVM wide. Q7. vs ? returns the first encounter order element; may return any (better for parallel performance). Q8. requirements for parallel correctness? Identity, associativity, and statelessness; the combiner must be compatible with the accumulator. Q9. How to avoid failures? Provide a merge function for duplicate keys (and optionally a map supplier). Q10. vs ? always evaluates (eager). evaluates only when empty (lazy) — prefer it when the default is expensive."}],"qa":[{"q":"Stream vs Collection?","a":"Collection stores data and is iterated externally; a Stream carries data, is iterated internally, is lazy, and is single-use."},{"q":"Intermediate vs terminal operations?","a":"Intermediate ops are lazy and return a Stream (`filter`, `map`). Terminal ops trigger execution and produce a result/side effect (`collect`, `count`, `forEach`). Nothing runs until a terminal op."},{"q":"`map` vs `flatMap`?","a":"`map` is 1→1 transform. `flatMap` is 1→many and flattens nested streams into one."},{"q":"Is a stream reusable?","a":"No. After a terminal op the stream is consumed; reusing throws `IllegalStateException`."},{"q":"How does laziness help?","a":"It enables short-circuiting (stop early via `findFirst`/`limit`) and lets you process infinite streams; elements flow one-at-a-time through the pipeline."},{"q":"When are parallel streams a bad idea?","a":"Small data, I/O-bound work, stateful/ordered ops, or shared mutable state. They also share the common pool JVM-wide."},{"q":"`findFirst` vs `findAny`?","a":"`findFirst` returns the first encounter-order element; `findAny` may return any (better for parallel performance)."},{"q":"`reduce` requirements for parallel correctness?","a":"Identity, associativity, and statelessness; the combiner must be compatible with the accumulator."},{"q":"How to avoid `toMap` failures?","a":"Provide a merge function for duplicate keys (and optionally a map supplier)."},{"q":"`orElse` vs `orElseGet`?","a":"`orElse(x)` always evaluates `x` (eager). `orElseGet(supplier)` evaluates only when empty (lazy) — prefer it when the default is expensive."}]},"18_Annotations_Deep_Dive.md":{"title":"Java & Spring Annotations Deep Dive","trackId":"spring-stack","trackTitle":"Spring Boot","sections":[{"id":"java-spring-annotations-deep-dive","heading":"Java & Spring Annotations — Deep Dive","level":1,"text":""},{"id":"from-language-basics-to-spring-boot-internals-beginner-expert","heading":"From Language Basics to Spring Boot Internals | Beginner → Expert","level":3,"text":""},{"id":"1-what-are-annotations","heading":"1. What Are Annotations","level":2,"text":"Annotations are metadata attached to code (classes, methods, fields, parameters). They don't directly change program logic — they're read by the compiler , tools , or frameworks (at runtime via reflection) to drive behavior. Three audiences: 1. Compiler — , (discarded after compilation). 2. Build/processing tools — Lombok, MapStruct, Dagger (generate code at compile time). 3. Frameworks at runtime — Spring, JPA, JUnit read them via reflection to wire behavior."},{"id":"2-built-in-java-annotations","heading":"2. Built-in Java Annotations","level":2,"text":"Annotation Purpose Asserts a method overrides a supertype method (compile error if not) Marks API as obsolete; (Java 9+) Silences specific compiler warnings Enforces exactly one abstract method Suppresses unsafe varargs generics warning (Java 14+) Validates serialization related members"},{"id":"3-meta-annotations","heading":"3. Meta-Annotations","level":2,"text":"Annotations that annotate other annotations — the building blocks of custom annotations. Meta annotation Controls Where it can be applied ( , , , , ...) How long it's kept: / / Include in Javadoc Subclasses inherit the (class level) annotation Allow the same annotation multiple times on one element levels (critical): — discarded by compiler (e.g., , Lombok). — kept in but not loaded at runtime (default). — available via reflection at runtime (Spring/JPA need this)."},{"id":"4-creating-custom-annotations","heading":"4. Creating Custom Annotations","level":2,"text":"Rules: elements look like methods, may have defaults, and types are limited to primitives, , , enums, annotations, and arrays of those. A single element named can be set without the name: ."},{"id":"5-retention-reflection","heading":"5. Retention & Reflection","level":2,"text":"Reading a annotation — this is exactly how frameworks work under the hood: A typical custom annotation interceptor pairs with Spring AOP (or a /servlet filter) to apply cross cutting behavior."},{"id":"6-annotation-processing","heading":"6. Annotation Processing","level":2,"text":"Compile time code generation via the Annotation Processing Tool (APT) — . No runtime reflection cost. Examples you already use: Lombok — , , , , generate boilerplate at compile time. MapStruct — generates type safe mappers. Dagger — compile time DI graph. Immutables / AutoValue — value classes. Compile time processing is faster and safer than reflection (errors caught at build, zero runtime overhead)."},{"id":"7-core-spring-annotations","heading":"7. Core Spring Annotations","level":2,"text":"Theory. Spring's whole model is an IoC (Inversion of Control) container : instead of you calling and wiring objects together, you declare what you need and Spring constructs, connects, and manages the objects — called beans — for you. Annotations are how you make those declarations. Stereotype annotations mark a class so component scanning auto registers it as a bean; dependency injection annotations then tell the container how to satisfy each bean's collaborators. Prefer constructor injection over field injection: it keeps beans immutable, makes their dependencies explicit, fails fast if a dependency is missing, and lets you unit test the class without starting the container. Stereotypes (component scanning registers these as beans): Annotation Meaning Generic Spring managed bean Business logic bean (semantic) Data access bean (+ exception translation) / Web controller Class declaring methods Dependency injection & wiring: Annotation Purpose Inject a dependency by type Disambiguate when multiple beans of a type exist Default bean when multiple candidates Inject a property/SpEL value Declare a bean from a method (default), , , Defer bean creation until first use Activate bean only in a profile Force init order / Lifecycle callbacks"},{"id":"8-spring-boot-annotations","heading":"8. Spring Boot Annotations","level":2,"text":"Theory. Spring Boot exists to remove the manual configuration that plain Spring demanded. The headline annotation bundles three behaviors — configuration, component scanning, and auto configuration — and auto configuration is the magic: Boot inspects your classpath and properties and conditionally creates sensible default beans (a when a JDBC driver is present, a JSON mapper for Jackson, etc.) using annotations. The annotations below either feed that mechanism ( binds external config to typed objects) or opt in to production features — scheduling, async execution, caching — so you get capabilities with a single line instead of XML or boilerplate. Annotation Purpose Meta annotation bootstrapping a Boot app Auto configures beans from classpath Scans packages for components Bind a group of properties to a POJO Register beans / / Conditional bean creation (auto config internals) + Cron/fixed rate tasks + Run methods on a separate thread pool + / Declarative caching"},{"id":"9-spring-web-rest-annotations","heading":"9. Spring Web / REST Annotations","level":2,"text":"Theory. These annotations map incoming HTTP requests to Java methods and turn return values back into HTTP responses, so you never parse a request or serialize JSON by hand. marks a class as a web handler whose return values become the response body (it's + ); the family binds a URL path + HTTP verb to a method; and the parameter annotations declaratively extract pieces of the request and convert them to the right Java types — from the URL template, from the query string, from the JSON payload, / from headers/cookies. Keep controllers focused on the happy path and centralize error handling in a / class. Annotation Purpose + Base path / method mapping / / / / HTTP verb shortcuts Bind URI template variable Bind query/form param Deserialize request body Serialize return value to body / Bind header / cookie Set HTTP status / Global exception handling CORS config"},{"id":"10-spring-data-jpa-annotations","heading":"10. Spring Data JPA Annotations","level":2,"text":"Theory. JPA (with Hibernate as the usual implementation) is Object Relational Mapping : it maps Java classes to database tables so you work with objects and let the framework generate SQL. These annotations declare that mapping — which class is a table ( / ), which field is the primary key and how it's generated ( / ), how columns and relationships ( , , ) are wired, and whether associated data is fetched eagerly or lazily. The single biggest performance pitfall is the N+1 query problem (looping over a parent and triggering one query per child), which or fetch joins solve. Two more must knows: persist enums with (never , which breaks if you reorder the enum), and use for optimistic locking to detect concurrent updates. Annotation Purpose / Map class to a table / Primary key + generation strategy Column mapping & constraints / / / Relationships / FK / join table mapping Persist enums by name (use STRING, not ORDINAL) Skip persistence Custom JPQL/native query Update/delete query Optimistic locking Fetch plan to fight N+1"},{"id":"11-validation-annotations","heading":"11. Validation Annotations","level":2,"text":"Bean Validation (Jakarta) — triggered by / . Common: , , , , / , / , , , / , . Use at class level for group validation and method level validation."},{"id":"12-transaction-aop-annotations","heading":"12. Transaction & AOP Annotations","level":2,"text":"Theory. A transaction groups several database operations so they either all succeed or all roll back together (the ACID guarantee), keeping data consistent even when something fails midway. lets you declare that boundary on a method instead of hand writing commit/rollback logic — Spring wraps the bean in a proxy that opens a transaction before the method runs and commits (or rolls back on error) when it returns. That proxy is an example of AOP (Aspect Oriented Programming) : weaving cross cutting concerns — transactions, logging, security, metrics — around your business methods without cluttering them. Understanding the proxy explains the classic gotchas below: a self invocation ( ) and non public methods bypass the proxy entirely, and by default only unchecked exceptions trigger a rollback. Annotation Purpose Declarative transaction boundary Turn on (auto in Boot) Declare an AOP aspect / / / / Advice types Reusable pointcut expression gotchas (high value interview points): Only works on public methods called through the proxy — a self invocation ( ) bypasses it. By default rolls back only on unchecked exceptions; use for checked."},{"id":"13-testing-annotations","heading":"13. Testing Annotations","level":2,"text":"Theory. Spring offers a spectrum of test types that trade speed for fidelity. A full boots the entire application context — most realistic but slowest — while slice annotations ( , , ) load only one layer, so tests run fast and stay focused on what they verify. Inside the context, / swap a real collaborator (say, a payment gateway or external client) for a Mockito mock/spy, whereas plain / are for pure, context free unit tests. The rule of thumb: choose the narrowest test that still proves the behavior you care about. Annotation Purpose Full context integration test / / Sliced tests (faster) / Swap a bean with a mock/spy / / / JUnit 5 lifecycle / Data driven tests Activate a profile for the test / (Mockito) Plain unit test mocks"},{"id":"14-interview-qa","heading":"14. Interview Q&A","level":2,"text":"Q1. What is an annotation and how does a framework use it? Metadata on code. Frameworks like Spring keep retention annotations and read them via reflection to wire behavior (beans, transactions, mappings). Q2. The three policies? (dropped at compile, e.g., ), (in bytecode, not at runtime — default), (readable via reflection — Spring/JPA). Q3. What does combine? + + . Q4. vs vs ? All are beans via component scanning. They're semantic specializations; additionally enables persistence exception translation. Q5. Why might not work? Self invocation (calling the method from within the same class) bypasses the proxy; non public methods; or expecting rollback on a checked exception without . Q6. field vs constructor injection? Constructor injection is preferred — immutable, testable, fails fast on missing deps, no reflection needed in tests. Field injection hides dependencies. Q7. How do you create and read a custom annotation? Declare with + / , then read via reflection ( , ), usually combined with AOP or a . Q8. Compile time vs runtime annotation processing? Compile time (APT: Lombok, MapStruct) generates code at build with zero runtime cost and early error detection. Runtime (reflection) is flexible but has overhead and fails later. Q9. vs ? Persist enums as (the name) — stores the index, which breaks if enum order changes. Q10. vs ? (Jakarta) triggers bean validation, including nested objects. (Spring) adds validation groups and enables method level validation on beans."}],"qa":[{"q":"What is an annotation and how does a framework use it?","a":"Metadata on code. Frameworks like Spring keep `RUNTIME`-retention annotations and read them via **reflection** to wire behavior (beans, transactions, mappings)."},{"q":"The three `@Retention` policies?","a":"`SOURCE` (dropped at compile, e.g., `@Override`), `CLASS` (in bytecode, not at runtime — default), `RUNTIME` (readable via reflection — Spring/JPA)."},{"q":"What does `@SpringBootApplication` combine?","a":"`@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`."},{"q":"`@Component` vs `@Service` vs `@Repository`?","a":"All are beans via component scanning. They're semantic specializations; `@Repository` additionally enables persistence exception translation."},{"q":"Why might `@Transactional` not work?","a":"Self-invocation (calling the method from within the same class) bypasses the proxy; non-public methods; or expecting rollback on a checked exception without `rollbackFor`."},{"q":"`@Autowired` field vs constructor injection?","a":"Constructor injection is preferred — immutable, testable, fails fast on missing deps, no reflection needed in tests. Field injection hides dependencies."},{"q":"How do you create and read a custom annotation?","a":"Declare with `@interface` + `@Target`/`@Retention(RUNTIME)`, then read via reflection (`isAnnotationPresent`, `getAnnotation`), usually combined with AOP or a `BeanPostProcessor`."},{"q":"Compile-time vs runtime annotation processing?","a":"Compile-time (APT: Lombok, MapStruct) generates code at build with zero runtime cost and early error detection. Runtime (reflection) is flexible but has overhead and fails later."},{"q":"`@EnumType.STRING` vs `ORDINAL`?","a":"Persist enums as `STRING` (the name) — `ORDINAL` stores the index, which breaks if enum order changes."},{"q":"`@Valid` vs `@Validated`?","a":"`@Valid` (Jakarta) triggers bean validation, including nested objects. `@Validated` (Spring) adds validation **groups** and enables method-level validation on beans."}]},"19_Node_JS_Complete_Guide.md":{"title":"Node.js Complete Guide","trackId":"nodejs","trackTitle":"Backend — Node.js","sections":[{"id":"nodejs-complete-guide","heading":"Node.js — Complete Guide","level":1,"text":""},{"id":"backend-javascript-beginner-pro-interview-guide","heading":"Backend JavaScript | Beginner → Pro Interview Guide","level":3,"text":""},{"id":"1-what-is-nodejs","heading":"1. What is Node.js","level":2,"text":"Node.js is a runtime that runs JavaScript outside the browser, built on Chrome's V8 engine. It's single threaded (for your JS code), event driven , and uses non blocking I/O — making it excellent for I/O bound, high concurrency workloads (APIs, real time apps, proxies). Theory. If you're a Java developer, Node is closer to Servlet + thread pool for I/O than to \"one thread per request.\" Your JavaScript runs on one thread, but while waiting for disk/network/DB, Node registers a callback and serves other requests. That model excels when work is mostly waiting (HTTP, DB queries), not computing. Analogy. A restaurant with one waiter (JS thread) who takes orders, passes cooking to the kitchen (libuv/OS), and serves other tables while food cooks — instead of one waiter standing at the stove until each dish is done ( ). Key ideas: Single threaded event loop handles your callbacks; you never manage threads directly for I/O. libuv provides the event loop and a small thread pool (default 4) for file system, DNS, crypto, and compression. Non blocking : I/O operations are offloaded; the main thread keeps serving other requests while waiting. Great for: REST/GraphQL APIs, real time (WebSocket/chat), streaming, microservices, BFF layers. Poor for: CPU heavy work (video encoding, ML) — it blocks the single thread (use worker threads or another runtime). Interview angle. \"Is Node multi threaded?\" → Your JS runs on one thread; libuv uses a thread pool (default 4) for some blocking OS calls; network I/O is async via the OS. CPU bound loops in a request handler block all clients on that process."},{"id":"2-the-event-loop","heading":"2. The Event Loop","level":2,"text":"The heart of Node's concurrency. It processes callbacks in phases , each with its own queue: How it works. When you call or , Node delegates I/O to libuv/OS and returns immediately. When data is ready, the callback is queued in the appropriate phase . Between phases, Node drains microtasks ( , then Promise callbacks). Only when the call stack is empty does the loop pick the next macrotask. Example — tracing one request: 1. HTTP request arrives → poll phase receives socket data. 2. Your handler runs → starts DB query (non blocking). 3. Handler returns; event loop serves other requests. 4. DB responds → callback queued → runs when poll phase picks it up. 5. sends response. If step 2 were synchronously for 2 seconds, steps 3–5 wait for everyone — that's event loop blocking. Microtasks vs macrotasks (asked constantly): Microtasks ( , resolved Promises) run after each phase / current operation , before the loop continues — even before other microtasks. Macrotasks are the phase callbacks (timers, I/O, ). Pitfall. The exact order of vs depends on context. Inside an I/O callback, runs before . In the main script top level, order can differ. Interviewers want you to know microtasks run before macrotasks and runs before Promise . Blocking the loop = killing throughput. A synchronous CPU loop or in a request handler freezes every connection. Keep handlers async and offload CPU work."},{"id":"3-modules","heading":"3. Modules","level":2,"text":"Theory. Node splits code into files. Each file is a module with its own scope — no global pollution. You export what other files need and import dependencies explicitly. CommonJS (default historically): ES Modules (modern, recommended): set in or use . CommonJS ES Modules Syntax / / Loading Synchronous Asynchronous, static analysis at top Tree shaking No Yes Top level await No Yes caches modules — the same instance is returned on subsequent calls (useful for singletons like a DB pool). Example. First runs and creates the pool. Second in another file returns the same pool object — critical for connection limits. Interview angle. ESM enables static analysis and tree shaking (dead code elimination at build time). Mixed CJS/ESM projects need careful or / extensions."},{"id":"4-npm-packagejson","heading":"4. npm, package.json","level":2,"text":"Theory. is the manifest for your Node project — name, scripts, dependencies. npm (or yarn/pnpm) resolves the dependency tree, downloads packages to , and runs scripts. Lockfiles ensure CI and production install the exact same versions you tested locally. ship to production; are build/test only. Semver allows minor+patch; allows patch only; exact pins . pins the exact dependency tree — commit it for reproducible builds. installs strictly from the lockfile (use in CI). checks vulnerabilities. runs a package binary without installing globally."},{"id":"5-async-javascript","heading":"5. Async JavaScript","level":2,"text":"Theory. Node's strength is non blocking I/O, but JavaScript is still single threaded. Async patterns let you express sequential or parallel I/O without blocking the thread. is syntactic sugar over Promises — it does not create new threads. Callbacks → callback hell: Promises: async/await (preferred — reads like sync, try/catch for errors): Run in parallel (don't serially when independent): Example — serial vs parallel: Pitfall. functions always return a Promise. Forgetting on an async call gives you a Promise object, not the resolved value — a common bug in Express handlers and tests. Common bug: awaiting in a loop serializes independent calls. Use with ."},{"id":"6-core-modules","heading":"6. Core Modules","level":2,"text":"Theory. Node ships built in modules so you can build servers without npm installs. for files, for cross platform paths, for pub/sub, for raw HTTP — Express wraps . Knowing these avoids reinventing wheels and helps in interviews when they ask \"how would you build X without Express?\" Other essentials: / , , , , , , , , , ( )."},{"id":"7-streams-buffers","heading":"7. Streams & Buffers","level":2,"text":"Theory. Loading a 2GB log file into a string ( ) can exhaust memory. Streams read/write data in chunks , processing incrementally — like reading a Java line by line instead of loading the whole file into a . Streams process data in chunks without loading it all into memory — essential for large files / network data. Four stream types: Readable , Writable , Duplex , Transform . (or ) handles backpressure — pausing the source when the destination is slow. How backpressure works. A fast readable produces chunks faster than a slow writable (e.g., disk or network) can consume. Without backpressure, chunks buffer in memory until OOM. Writable signals \"pause\" → readable stops until event → readable resumes. wires this automatically and propagates errors. Example. Upload proxy: (readable) → gzip transform → (writable). Each chunk is compressed and sent before the next is read — constant memory regardless of upload size. Buffers hold raw binary data:"},{"id":"8-express","heading":"8. Express","level":2,"text":"The de facto web framework. Theory. Express sits on Node's module and adds routing, middleware pipeline, and response helpers. A request flows through a stack of middleware functions — each can inspect/modify / or end the response. Alternatives worth naming in interviews: Fastify (faster, schema based), NestJS (opinionated, DI + decorators, great for large apps), Koa , Hapi ."},{"id":"9-middleware-routing","heading":"9. Middleware, Routing","level":2,"text":"Middleware = functions with that run in order; the pipeline is the core mental model of Express. How it works. Think of middleware as a chain of filters/interceptors (similar to Spring's filter chain or Python WSGI middleware). Each function receives , , and . Call to pass to the next layer. Call / to finish. Forget either → request hangs until timeout. Order matters — middleware runs top to bottom. Always call or send a response. For async handlers, forward errors: (or use a wrapper) — thrown async errors aren't caught automatically in Express 4."},{"id":"10-rest-api-design","heading":"10. REST API Design","level":2,"text":"Theory. REST maps resources (nouns) to URLs and uses HTTP verbs for actions. Good APIs are predictable: same status codes, error shape, and naming conventions everywhere — so frontend and other services integrate without guessing. Good practices: Use proper status codes (201 created, 204 no content, 400 bad request, 401/403 auth, 404, 409 conflict, 422 validation, 500). Validate input ( , , ). Version your API ( ). Pagination, filtering, sorting via query params. Consistent error shape: ."},{"id":"11-databases","heading":"11. Databases","level":2,"text":"Theory. Node apps typically talk to databases over the network. Each query has latency; opening a new TCP connection per request is expensive. Use a connection pool (reuse connections) and parameterized queries (prevent SQL injection) — same principles as JDBC or Python DB API with placeholders. MongoDB with Mongoose (ODM): PostgreSQL/MySQL — use a pooled client ( ) or an ORM ( , , ): Always use a connection pool (creating a connection per request kills performance). Always parameterize queries — never string concatenate user input. Prisma gives type safe queries and migrations — popular in TypeScript projects."},{"id":"12-auth-security","heading":"12. Auth & Security","level":2,"text":"Theory. Most Node APIs authenticate via JWT (stateless token) or session cookies (stateful server side session). Passwords must never be stored plaintext — bcrypt/argon2 hash with salt. Auth middleware runs before protected routes and attaches user identity to . JWT auth: Security checklist (OWASP aware): for secure HTTP headers; configured to specific origins. Rate limiting ( ) to prevent brute force/DoS. Validate & sanitize all input; parameterize DB queries. Store secrets in env vars / a secrets manager — never in code. Use HTTPS; set , , cookies. Keep dependencies patched ( )."},{"id":"13-error-handling-process-management","heading":"13. Error Handling & Process Management","level":2,"text":"Theory. Uncaught errors in async code don't crash Node by default — they become unhandled rejections . Sync throws in request handlers may crash the process if uncaught. Production apps need centralized error middleware, global handlers, and graceful shutdown so in flight requests finish before exit. Distinguish operational errors (expected: bad input, 404) from programmer errors (bugs) — crash & restart on the latter. Use a process manager ( PM2 ) or container orchestration ( Kubernetes ) to restart crashed processes."},{"id":"14-performance-clustering-worker-threads","heading":"14. Performance, Clustering & Worker Threads","level":2,"text":"Node is single threaded, so a single process uses one CPU core. Two ways to scale: How it works — cluster vs workers. Cluster forks multiple Node processes , each with its own event loop and memory — good for utilizing all CPU cores for I/O bound HTTP servers (share nothing; use Redis for sessions if needed). Worker threads share the same process but run JS on separate threads — good for CPU heavy tasks without the overhead of full process fork. Cluster (multi process, one per core): In production you usually let PM2 ( ) or Kubernetes replicas handle this. Worker threads (offload CPU bound work): Use worker threads for CPU heavy tasks (image processing, big computation) so the event loop stays responsive. Other perf levers: caching (Redis), compression ( ), avoid sync APIs, stream large payloads, use /clinic.js to profile, set proper DB indexes."},{"id":"15-testing","heading":"15. Testing","level":2,"text":"Theory. Test Node apps at two levels: unit tests for pure logic (services, validators) with mocked I/O, and integration tests that hit real HTTP routes (Supertest) optionally with a test database. Fast, deterministic tests run in CI on every commit. Unit tests : pure functions/services with mocks. Integration tests : routes + DB (Supertest, Testcontainers). Frameworks: Jest , Vitest , Mocha+Chai , plus the built in runner. Mock external calls ( , ). Aim for fast, deterministic tests."},{"id":"16-typescript-with-node","heading":"16. TypeScript with Node","level":2,"text":"Setup: , , , / for dev, compile with (or build with / ). Enable in ."},{"id":"17-production-best-practices","heading":"17. Production Best Practices","level":2,"text":"Config via env vars ( locally; real env in prod). Validate config at startup. Structured logging ( , ) with request IDs; don't in prod. Health checks ( , ) for load balancers/K8s. Graceful shutdown on SIGTERM (drain, close DB). Don't block the event loop ; offload CPU; avoid sync FS in handlers. Connection pooling for DBs; timeouts on all outbound calls. Security : helmet, rate limiting, input validation, secrets management, HTTPS. Observability : metrics (Prometheus), tracing (OpenTelemetry). Run multiple instances (cluster/PM2/K8s) behind a load balancer."},{"id":"18-coding-problems","heading":"18. Coding Problems","level":2,"text":"Theory. These patterns appear in live coding rounds and production code alike: promisify legacy callbacks, limit concurrency (like a semaphore), rate limit, retry with backoff, debounce. Understand why each exists — interviewers follow up with \"what happens under load?\""},{"id":"19-interview-qa","heading":"19. Interview Q&A","level":2,"text":"Q1. Is Node.js single threaded? Your JS executes on a single thread (the event loop), but libuv uses a thread pool for some I/O (fs, dns, crypto) and the OS handles network I/O asynchronously. CPU work still blocks the single JS thread. Q2. Explain the event loop phases. timers → pending callbacks → poll → check (setImmediate) → close. Microtasks ( , Promises) run between phases; before other microtasks. Q3. vs vs ? runs first (before promises), then promise microtasks; runs in the timers phase; in the check phase. Inside an I/O cycle, fires before . Q4. How do you avoid callback hell? Promises and async/await; modularize; for parallelism. Q5. vs vs vs ? rejects on first failure; waits for all and reports each; settles on first settled (resolve or reject); resolves on first fulfilled. Q6. How does Node handle CPU bound tasks? Offload to worker threads, child processes, a queue/another service, or cluster. Don't run heavy computation on the event loop. Q7. CommonJS vs ES Modules? CJS is synchronous / ; ESM is async static / with tree shaking and top level await. Q8. What is middleware in Express? A function in the request pipeline that can read/modify req/res, end the response, or pass control via . Error middleware takes 4 args. Q9. How do you scale a Node app? Cluster/PM2/K8s for multi core & multi instance behind a load balancer; caching; DB pooling/indexes; stateless services + external session store. Q10. How do you secure a Node API? Helmet, CORS allowlist, rate limiting, input validation, parameterized queries, bcrypt password hashing, JWT/short lived tokens, secrets in env/secret manager, HTTPS, . Q11. What causes a memory leak in Node and how do you find it? Unbounded caches, lingering event listeners, global variables, closures holding references. Diagnose with heap snapshots ( , Chrome DevTools), , monitoring RSS over time. Q12. What is backpressure in streams? When a writable destination is slower than the readable source. / handle it by pausing the sourc"}],"qa":[{"q":"Is Node.js single-threaded?","a":"Your JS executes on a single thread (the event loop), but libuv uses a thread pool for some I/O (fs, dns, crypto) and the OS handles network I/O asynchronously. CPU work still blocks the single JS thread."},{"q":"Explain the event loop phases.","a":"timers → pending callbacks → poll → check (setImmediate) → close. Microtasks (`process.nextTick`, Promises) run between phases; `nextTick` before other microtasks."},{"q":"`setImmediate` vs `setTimeout(fn, 0)` vs `process.nextTick`?","a":"`nextTick` runs first (before promises), then promise microtasks; `setTimeout(0)` runs in the timers phase; `setImmediate` in the check phase. Inside an I/O cycle, `setImmediate` fires before `setTimeout(0)`."},{"q":"How do you avoid callback hell?","a":"Promises and async/await; modularize; `Promise.all` for parallelism."},{"q":"`Promise.all` vs `allSettled` vs `race` vs `any`?","a":"`all` rejects on first failure; `allSettled` waits for all and reports each; `race` settles on first settled (resolve or reject); `any` resolves on first fulfilled."},{"q":"How does Node handle CPU-bound tasks?","a":"Offload to worker threads, child processes, a queue/another service, or cluster. Don't run heavy computation on the event loop."},{"q":"CommonJS vs ES Modules?","a":"CJS is synchronous `require`/`module.exports`; ESM is async static `import`/`export` with tree-shaking and top-level await."},{"q":"What is middleware in Express?","a":"A function `(req, res, next)` in the request pipeline that can read/modify req/res, end the response, or pass control via `next()`. Error middleware takes 4 args."},{"q":"How do you scale a Node app?","a":"Cluster/PM2/K8s for multi-core & multi-instance behind a load balancer; caching; DB pooling/indexes; stateless services + external session store."},{"q":"How do you secure a Node API?","a":"Helmet, CORS allowlist, rate limiting, input validation, parameterized queries, bcrypt password hashing, JWT/short-lived tokens, secrets in env/secret manager, HTTPS, `npm audit`."},{"q":"What causes a memory leak in Node and how do you find it?","a":"Unbounded caches, lingering event listeners, global variables, closures holding references. Diagnose with heap snapshots (`--inspect`, Chrome DevTools), `clinic.js`, monitoring RSS over time."},{"q":"What is backpressure in streams?","a":"When a writable destination is slower than the readable source. `pipe`/`pipeline` handle it by pausing the source until the destination drains, preventing memory blowup."}]},"20_Kafka_Deep_Dive.md":{"title":"Apache Kafka Deep Dive","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"apache-kafka-deep-dive","heading":"Apache Kafka — Deep Dive","level":1,"text":""},{"id":"event-streaming-for-backend-engineers-interview-grade-depth","heading":"Event Streaming for Backend Engineers | Interview-Grade Depth","level":3,"text":""},{"id":"1-what-kafka-is","heading":"1. What Kafka Is","level":2,"text":"Theory. Kafka is best understood as a distributed commit log , not a traditional message queue. Producers append records to the end of a log; consumers read from any position and advance their own bookmark (offset). Nothing is deleted when you read — retention is time or size based (e.g., 7 days), so multiple consumers can replay history independently. Analogy. Think of Kafka like a DVR for your business events: producers record episodes (order placed, payment captured); different consumer groups can watch the same episodes on their own schedule, pause, rewind, or catch up later. A traditional queue is more like a pipe — message consumed once and gone. How it works — mental model. Example — when to choose Kafka. An order service publishes . Billing, analytics, and shipping services each consume the same stream in their own consumer group — no point to point fan out wiring required. Pitfall. Don't use Kafka for synchronous request/response (\"call payment and wait for result in 200ms\"). Use HTTP/gRPC for that; use Kafka for async decoupling and event history. Interview angle. \"Kafka vs queue\" — log vs queue, pull vs push, replay vs consume and delete, throughput vs routing flexibility. Kafka is a distributed, append only, durable commit log used as an event streaming platform. Producers append events to topics; consumers read them at their own pace. Unlike a traditional queue, reading does not delete — events are retained by time/size, and many independent consumer groups can read the same data. Use it for: decoupling services, event driven architecture, stream processing, log aggregation, high throughput ingestion, replayable event history. Not for: request/response RPC, tiny low throughput needs, or when you need per message TTL/priority (use a queue like RabbitMQ/SQS). Kafka Traditional MQ (RabbitMQ/SQS) Log (retained, replayable) Queue (consumed = gone) Pull based consumers Often push based Ordered per partition Ordering harder Massive throughput Lower throughput,"},{"id":"2-core-architecture","heading":"2. Core Architecture","level":2,"text":"Theory. A Kafka cluster is 3+ brokers (servers) for fault tolerance. Data is organized into topics (e.g., ), each split into partitions for parallelism. Each partition is an ordered, immutable sequence identified by offset (0, 1, 2, …). How it works — write path step by step. 1. Producer sends record to topic . 2. Partitioner computes → partition 1. 3. Broker 2 is leader for partition 1; it appends to local log segment on disk. 4. Follower replicas on brokers 1 and 3 pull data from leader (replication). 5. When all ISR (in sync replicas) acknowledge, leader responds success to producer. How it works — read path. 1. Consumer in group is assigned partition 1. 2. It tracks committed offset (e.g., 847). 3. fetches records from offset 847 onward. 4. After processing, commits offset 850 → next poll starts at 850. Example — 3 brokers, topic , 3 partitions, RF=3: If Broker 2 dies, a follower in ISR for each affected partition is elected new leader — producers/consumers reconnect automatically. Pitfall. Partition count is fixed at topic creation (increasing partitions is possible but reorders keyed messages). Plan partition count for expected throughput and max consumer parallelism upfront. Interview angle. \"Why partitions?\" — ordering + parallelism unit. \"What is ISR?\" — only caught up replicas can become leader, preventing data loss on failover. Broker — a Kafka server; a cluster is many brokers. Topic — a named stream, split into partitions (the unit of parallelism & ordering). Partition — an ordered, immutable sequence; each record has an offset . Replication factor — copies per partition (e.g., RF=3). One leader handles reads/writes; followers replicate. ISR (In Sync Replicas) — replicas caught up with the leader; only ISR members can become leader. Controller — a broker coordinating leadership/metadata (in modern Kafka, KRaft replaces ZooKeeper). Key insight: parallelism is bounded by partition count — you can't have more active consumers in a group than partitions."},{"id":"3-producers","heading":"3. Producers","level":2,"text":"Theory. The producer's job is to turn your application events into durable log records on the right partition, with configurable trade offs between speed and safety. How it works — batching. Producers don't send one TCP packet per message. They accumulate records in a buffer until is reached or expires, then send one compressed batch — this is how Kafka achieves millions of msgs/sec. Example — acks decision matrix: Setting What happens on send Data loss risk Fire and forget Leader crash before write → lost Leader ack only Leader dies before replication → lost + All ISR ack Safe unless all ISR die simultaneously Pitfall. without on the broker still allows ack if only leader is in ISR — configure both for production durability. (the durability knob): — fire and forget (fastest, can lose data). — leader ack only (loses data if leader dies before replication). — all ISR ack (strongest; pair with ). Partitioning: records with the same key go to the same partition ( ) — this is how you preserve per key ordering. No key → round robin / sticky partitioner. Example — key routing. Interview angle. Hot keys — if one key dominates traffic (e.g., a mega account), one partition becomes a bottleneck. Mitigate with sub keys or accept that hot entity is serialized. Throughput levers: + (batching), , more partitions."},{"id":"4-consumers-consumer-groups","heading":"4. Consumers & Consumer Groups","level":2,"text":"Theory. Consumers pull records by calling . A consumer group ( ) is a load balancing unit — Kafka assigns each partition to exactly one consumer in the group so messages are processed once per group, not once globally. How it works — assignment example. Example — scaling limit. Topic has 6 partitions, billing group has 8 consumers → 2 consumers sit idle (no partition assigned). To scale further, increase partition count (with rebalancing cost) or optimize processing speed. How it works — poll loop. 1. returns batch of records (up to ). 2. Process each record (write to DB, call API, etc.). 3. Commit offsets (auto or manual). 4. Repeat. If processing exceeds , broker assumes consumer dead → rebalance. Pitfall. Long running processing inside loop without pausing or increasing interval → spurious rebalances and duplicate processing. A consumer group is a set of consumers sharing a . Kafka assigns each partition to exactly one consumer in the group → horizontal scaling with no duplicate processing within the group. Scale consumers up to the partition count. Multiple groups = independent fan out of the same stream. loop: fetch batch → process → commit offset."},{"id":"5-rebalancing","heading":"5. Rebalancing","level":2,"text":"Theory. Rebalancing is Kafka redistributing partition ownership when group membership changes. During rebalance, consumers revoke partitions, wait for reassignment, then resume — consumption pauses (stop the world in classic mode). How it works — what triggers rebalance. New consumer joins group (scale up). Consumer crashes or fails heartbeat ( ). Consumer exceeds (processing too slow). Partitions added/removed from topic. Example — slow consumer death spiral. Fixes / modern features: Cooperative/incremental rebalancing ( ) — only moves the partitions that must move, instead of revoking everything. Static membership ( ) — avoids rebalance on transient restarts. Tune / so processing finishes within the interval. Keep / sensible. Interview angle. Explain cooperative sticky assignor vs eager: eager revokes all partitions; cooperative revokes only what must move — much less disruption in rolling deploys."},{"id":"6-offsets-delivery-semantics","heading":"6. Offsets & Delivery Semantics","level":2,"text":"Theory. An offset is a monotonically increasing ID per partition — the consumer's cursor. Committed offsets are stored in Kafka's internal topic (or externally for advanced setups). How it works — crash scenarios. Strategy Order Crash after... Result At most once Commit → Process Commit, before process Lost messages At least once Process → Commit Process, before commit Duplicate messages Exactly once Transactional varies No dupes within Kafka pipeline Example — at least once duplicate. The offset is the consumer's bookmark per partition, stored in the internal topic. Semantic How Risk At most once Commit offset before processing Lose messages on crash At least once (default, most common) Process then commit Duplicates on crash → make consumers idempotent Exactly once Transactions / idempotent processing Complex, throughput cost Pitfall. after batch means one bad record can block the whole batch unless you implement per record error handling or seek/skip strategies. Because at least once is the practical default, idempotent consumers are non negotiable — dedupe by a business key or store processed message IDs."},{"id":"7-exactly-once","heading":"7. Exactly-Once","level":2,"text":"Theory. \"Exactly once\" in Kafka means no duplicate writes to Kafka topics/partitions when failures and retries occur — not automatic exactly once to your PostgreSQL database. End to end EOS still requires idempotent sinks. How it works — idempotent producer. 1. Broker assigns (PID) on first connect. 2. Each partition gets a sequence number per PID. 3. Retry of seq 5 after broker already wrote seq 5 → broker dedupes, returns same offset. 4. Without this, retry after timeout could create duplicate records. How it works — transactions (read process write). If crash before commit, transaction aborts — consumer re reads offset 100, output records marked aborted (invisible to consumers). Two pieces: 1. Idempotent producer ( ) — the broker dedupes producer retries using a producer ID + sequence number, so retries don't create duplicates in a partition. 2. Transactions — atomically write to multiple partitions/topics and commit consumer offsets together (the read process write pattern): Consumers must set to skip aborted records. Kafka Streams gives EOS with . Reality check: true end to end exactly once across external systems still needs idempotency at the sink. Most teams do at least once + idempotent consumers . Interview angle. Most production systems: at least once + idempotent consumer (upsert by event ID). Reserve Kafka transactions for stream processing where atomic multi topic writes matter."},{"id":"8-ordering-guarantees","heading":"8. Ordering Guarantees","level":2,"text":"Theory. Kafka guarantees total order within one partition only . Across partitions, there is no global order — by design, for parallelism. How it works. All events with key hash to partition 1: Example — ordering violation without idempotence. Kafka guarantees order only within a partition , not across partitions. To keep order for an entity, use a stable key (e.g., ) so all its events land in one partition. without idempotence can reorder on retry — enable idempotence to keep ordering with pipelining. Trade off: stronger ordering (fewer partitions / single key) vs more parallelism (more partitions)."},{"id":"9-retry-topics-dlq","heading":"9. Retry Topics & DLQ","level":2,"text":"Theory. When message processing fails, you have three bad options: (1) block the partition forever, (2) skip and lose data, (3) retry inline and stall throughput. Retry topics decouple failure handling from the main consumer loop. How it works — non blocking retry flow. Pitfall. Inline retry (sleep in consumer loop) blocks the partition and can trigger rebalance. Always prefer sidecar retry topics for poison messages. Failed message processing shouldn't block the partition or be lost. Non blocking retry : publish the failed record to a delayed retry topic , ack the original so the partition keeps moving. DLQ : after N retries, route to a dead letter topic for manual inspection/replay. Spring Kafka provides this via / . Interview angle. Distinguish retry topic (transient failures, automatic) from DLQ (permanent failure, human intervention). Mention auto wires this in Spring Kafka."},{"id":"10-outbox-pattern","heading":"10. Outbox Pattern","level":2,"text":"Theory. The dual write problem : you need to UPDATE a database row AND publish a Kafka event. These are two separate systems — if the DB commit succeeds but Kafka publish fails (or vice versa), data is inconsistent. How it works — transactional outbox. CDC (Debezium) watches the DB transaction log instead of polling — lower latency, same guarantee. Example. Order saved but Kafka down — outbox row remains . Relay retries when Kafka recovers. No lost events, no phantom events. Problem: you can't atomically write to your DB and publish to Kafka (dual write → inconsistency if one fails). Solution (Transactional Outbox): 1. In the same DB transaction as your business write, insert the event into an table. 2. A separate process (a poller, or Debezium CDC reading the DB log) reads the outbox and publishes to Kafka, then marks it sent. This guarantees the event is published iff the business data was committed — the standard fix for reliable event publishing in microservices. Interview angle. \"Why not publish in after commit?\" — helps but still fails if Kafka is down after commit. Outbox + relay is the durable pattern."},{"id":"11-backpressure","heading":"11. Backpressure","level":2,"text":"Theory. Backpressure is how a slow downstream signals upstream to slow down. Kafka's pull model makes this natural — a slow consumer simply polls less often; the log retains data (until retention limit). How it works — producer side. When producer buffer ( ) fills because brokers are slow or waits on ISR, blocks up to then throws — this is producer backpressure. How it works — consumer side. Example. Analytics consumer writes to overloaded Elasticsearch → lag grows from 1K to 500K → alert fires → scale consumers or pause non critical partitions. Pitfall. Ignoring lag until disk fills — retention will delete old segments; slow consumers lose ability to catch up. Kafka consumers are pull based , which is natural backpressure — a slow consumer just polls slower; the broker retains data. Manage it with: — cap batch size so processing finishes in time. / partitions when a downstream is overwhelmed. Bounded internal work queues / thread pools in the consumer. Watch consumer lag as the overload signal. Producers feel backpressure via + (send blocks when the buffer is full)."},{"id":"12-lag-monitoring","heading":"12. Lag Monitoring","level":2,"text":"Theory. Consumer lag is the gap between the log end (latest offset) and what your consumer has committed. It is the single most important Kafka health signal. How it works — calculating lag. Example — remediation playbook. 1. Lag spiking on one partition → hot key or slow consumer on that partition. 2. Lag uniform across all partitions → need more consumers (up to partition count) or faster processing. 3. Lag after deploy → check for rebalance storm or new bug in handler. Consumer lag = latest offset − committed offset per partition. Rising lag = consumers can't keep up. Tools: , Burrow , Kafka Exporter → Prometheus → Grafana , Confluent Control Center. Alert on sustained/ growing lag, not absolute value. Remedies: add consumers (≤ partitions), add partitions, speed up processing, batch downstream writes. Other key metrics: under replicated partitions, ISR shrink, request latency, broker disk usage. Interview angle. Alert on lag growth rate , not absolute lag — a consumer 1M messages behind but keeping pace may be fine after a restart."},{"id":"13-schema-management","heading":"13. Schema Management","level":2,"text":"Theory. JSON without a schema leads to \"field renamed in prod, consumer crashes.\" A Schema Registry stores versioned schemas (Avro/Protobuf/JSON Schema); each message carries a 4 byte schema ID instead of the full schema — compact and enforceable. How it works — evolution. Pitfall. Removing a required field or changing field type breaks compatibility — CI should run compatibility checks against registry before deploy. For evolving event contracts use a Schema Registry (Avro/Protobuf/JSON Schema). Producers register schemas; consumers fetch by ID — payloads carry a schema id, not the full schema (compact). Compatibility modes : BACKWARD (new consumer reads old data), FORWARD, FULL. Rules: add fields with defaults; don't remove/rename required fields — this is how you avoid breaking consumers on deploy."},{"id":"14-spring-kafka","heading":"14. Spring Kafka","level":2,"text":"Theory. Spring Kafka wraps the Java client with , , and error handling abstractions so you don't hand write poll loops in a Spring service. How it works — listener container. 1. creates N consumer threads ( ). 2. Each thread runs poll → invoke method → ack per . 3. ack mode: you call after successful processing (at least once). 4. + wire retries/DLT without custom code. Pitfall. with 3 partitions → 7 threads idle. Match concurrency to partition count. Set , configure / for retries+DLT, and for parallelism."},{"id":"15-design-50k-eventssec","heading":"15. Design: 50k Events/Sec","level":2,"text":"Theory. This is a classic system design question — interviewers want structured trade off reasoning, not magic numbers. Worked example — sizing partitions. Worked example — end to end flow. The canonical interview question. Structure your answer: 1. Partition for throughput : 50k/sec ÷ 5 10k per partition ≈ 8 16+ partitions ; size consumers to match. 2. Key by entity (e.g., accountId) for per entity ordering + even spread; watch for hot keys. 3. Producer : , idempotence on, batching ( , ). 4. Consumers : at least once + idempotent processing , batch downstream DB writes, manual commits. 5. Reliability : retry topics + DLQ; outbox for the upstream write. 6. Backpressure : pull model + lag based autoscaling (KEDA scales consumers on lag). 7. Observe : lag, throughput, p99 processing time; alert on lag growth. 8. Scale : add partitions + consumers + brokers; RF=3, ."},{"id":"16-interview-qa","heading":"16. Interview Q&A","level":2,"text":"Q1. How does Kafka achieve high throughput? Sequential disk writes (append only log), zero copy transfer, batching, compression, and partition level parallelism. Q2. Partition vs consumer relationship? Within a group, each partition is consumed by exactly one consumer; max useful consumers = partition count. Different groups read independently. Q3. How do you guarantee ordering? Only within a partition. Use a consistent key so an entity's events share a partition; enable idempotence to keep order with retries. Q4. At least once vs exactly once — what do you use? Default at least once + idempotent consumers (dedupe by business key). Exactly once via idempotent producer + transactions when atomic read process write is required. Q5. What is a rebalance and why is it bad? Reassignment of partitions on membership change; it pauses consumption and can cause duplicates. Mitigate with cooperative rebalancing, static membership, and tuned poll intervals. Q6. Kafka vs RabbitMQ? Kafka = durable, replayable log, huge throughput, ordering per partition. RabbitMQ = flexible routing, per message ack/priority/TTL, consume and delete. Choose by replay/throughput vs routing needs. Q7. How do you handle a poison message? Retry with backoff via retry topics (non blocking), then route to a DLQ after N attempts; alert and replay after a fix. Q8. Why the outbox pattern? To avoid the dual write problem — write business data and the event in one DB transaction, then publish via CDC/poller, guaranteeing consistency. Q9. What is ISR and ? ISR = replicas in sync with the leader. With and , a write needs ≥2 in sync copies, trading some availability for durability. Q10. How do you monitor Kafka health? Consumer lag, under replicated/offline partitions, ISR shrink, request latency, disk usage — via Kafka Exporter + Prometheus/Grafana, alerting on growing lag."}],"qa":[{"q":"How does Kafka achieve high throughput?","a":"Sequential disk writes (append-only log), zero-copy transfer, batching, compression, and partition-level parallelism."},{"q":"Partition vs consumer relationship?","a":"Within a group, each partition is consumed by exactly one consumer; max useful consumers = partition count. Different groups read independently."},{"q":"How do you guarantee ordering?","a":"Only within a partition. Use a consistent key so an entity's events share a partition; enable idempotence to keep order with retries."},{"q":"At-least-once vs exactly-once — what do you use?","a":"Default at-least-once + idempotent consumers (dedupe by business key). Exactly-once via idempotent producer + transactions when atomic read-process-write is required."},{"q":"What is a rebalance and why is it bad?","a":"Reassignment of partitions on membership change; it pauses consumption and can cause duplicates. Mitigate with cooperative rebalancing, static membership, and tuned poll intervals."},{"q":"Kafka vs RabbitMQ?","a":"Kafka = durable, replayable log, huge throughput, ordering per partition. RabbitMQ = flexible routing, per-message ack/priority/TTL, consume-and-delete. Choose by replay/throughput vs routing needs."},{"q":"How do you handle a poison message?","a":"Retry with backoff via retry topics (non-blocking), then route to a DLQ after N attempts; alert and replay after a fix."},{"q":"Why the outbox pattern?","a":"To avoid the dual-write problem — write business data and the event in one DB transaction, then publish via CDC/poller, guaranteeing consistency."},{"q":"What is ISR and `min.insync.replicas`?","a":"ISR = replicas in sync with the leader. With `acks=all` and `min.insync.replicas=2`, a write needs ≥2 in-sync copies, trading some availability for durability."},{"q":"How do you monitor Kafka health?","a":"Consumer lag, under-replicated/offline partitions, ISR shrink, request latency, disk usage — via Kafka Exporter + Prometheus/Grafana, alerting on growing lag."}]},"21_Kubernetes_Docker_CloudNative.md":{"title":"Kubernetes, Docker & Cloud-Native","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"kubernetes-docker-cloud-native-deep-dive","heading":"Kubernetes, Docker & Cloud-Native — Deep Dive","level":1,"text":""},{"id":"containerization-orchestration-aws-backend-engineer-edition","heading":"Containerization → Orchestration → AWS | Backend Engineer Edition","level":3,"text":""},{"id":"1-docker-fundamentals","heading":"1. Docker Fundamentals","level":2,"text":"A container packages your app + dependencies into an isolated, portable unit sharing the host kernel (lighter than a VM). An image is the immutable template; a container is a running instance. What & why. Think of an image as a sealed recipe and a container as one cooked meal from that recipe. VMs give each app its own full operating system (heavy, slow to boot). Containers share the host's Linux kernel but give each process its own isolated view of the filesystem, network, and process tree — you get portability (\"runs the same on my laptop and in prod\") without paying for a guest OS per app. How it works. Isolation comes from two Linux kernel features: Namespaces — each container gets its own PID, network, mount, and UTS namespace so it looks like a mini machine (process 1 inside the container is not process 1 on the host). cgroups (control groups) — cap and account for CPU, memory, and I/O so one container cannot starve others. When you , the container runtime (containerd, runc) unpacks image layers into a writable overlay filesystem, applies namespaces/cgroups, and starts your process as PID 1 inside that sandbox. Images are built from a Dockerfile , made of cached layers . Registries (Docker Hub, ECR, GHCR) store images. Key commands: , , , , . Example. You build on your laptop, push to a registry, and a production host pulls the exact same digest. No \"works on my machine\" drift — the image is the deployment artifact. Pitfall. Containers are not VMs: they share the kernel, so a kernel exploit or misconfigured container can affect the host. Also, PID 1 inside the container must handle signals correctly (Java apps often need an init wrapper or form of ) or graceful shutdown fails."},{"id":"2-production-dockerfiles","heading":"2. Production Dockerfiles","level":2,"text":"Use multi stage builds to ship a small, secure runtime image. What & why. A naive Dockerfile that runs and ships the result still contains Maven, source code, and build tools — a 800 MB image with a larger attack surface. Multi stage builds compile in a \"build\" stage and copy only the artifact into a slim \"runtime\" stage. Smaller images pull faster, start faster, and expose fewer packages to CVE scanners. How it works. Each starts a new stage. Earlier stages can be discarded; only the final stage becomes your published image. Docker caches each instruction as a layer — if hasn't changed, the layer is reused and builds skip re downloading dependencies. Best practices: order layers least→most changing (deps before source) for cache hits; small base ( / ); non root user; pin versions; ; one process per container; so the JVM respects the container memory limit. Example. First build: 3 min (downloads deps). You change one Java file and rebuild: Docker reuses the deps layer, recompiles only — 30 sec. In CI, layer caching (or BuildKit cache mounts) is the difference between 5 minute and 45 second pipelines. Interview angle. \"Why not just and in one stage?\" — acceptable for dev; in prod you ship build tools, leak source, and bloat the registry. Pin base image digests ( ) for reproducible builds."},{"id":"3-docker-networking-volumes","heading":"3. Docker Networking, Volumes","level":2,"text":"Networking: containers on a user defined bridge network resolve each other by name. publishes ports. Volumes: persist data beyond container life ( ); containers are ephemeral by design. How it works — networking. By default, containers on the default bridge cannot resolve each other by name. A user defined bridge network gives each container a DNS name ( , ) via Docker's embedded DNS. Traffic between containers stays on the internal network; only maps host port 8080 → container port 8080 for access from your laptop browser. How it works — volumes. Container filesystems are ephemeral: deletes the writable layer. Named volumes ( ) survive container restarts and are managed by Docker — ideal for database data. Bind mounts ( ) map a host directory for live reload dev configs. Pitfall. in Compose only waits for the container to start , not for Postgres to accept connections. Your app may crash on boot; use a healthcheck or retry logic in the app. docker compose for local multi service dev: Example. From inside the container, JDBC URL is — hostname resolves via Docker DNS. From your laptop, you use for the API and typically don't expose Postgres at all."},{"id":"4-kubernetes-architecture","heading":"4. Kubernetes Architecture","level":2,"text":"Kubernetes orchestrates containers across a cluster: scheduling, self healing, scaling, networking. What & why. Running 50 containers across 10 servers by hand — restarting crashed ones, rolling out new versions, wiring networking — does not scale. Kubernetes is a control plane that continuously compares desired state (your YAML) to actual state (what's running) and acts to close the gap. You describe what you want; K8s figures out how. How it works — the control loop. Every K8s object (Deployment, Service, Pod) is watched by a controller . Example: you apply a Deployment with . The Deployment controller creates a ReplicaSet; the ReplicaSet controller creates 3 Pod objects. The scheduler picks a node for each unscheduled Pod (based on CPU/memory requests, affinity, taints). The kubelet on that node pulls the image via containerd and starts the container. If a Pod dies, the ReplicaSet controller sees \"only 2 running, want 3\" and creates a replacement — self healing without human intervention. How a Pod gets scheduled and networked (end to end): 1. → apiserver validates and stores the Pod spec in etcd . 2. Scheduler binds the Pod to a node (writes in etcd). 3. Kubelet on that node sees the new Pod, pulls the image, starts containers with cgroups/namespaces. 4. Kubelet reports Pod status back to apiserver. 5. If the Pod matches a Service's label selector, an Endpoint object is created; kube proxy on every node updates iptables/IPVS rules so traffic to the Service VIP reaches a ready Pod. Core idea — declarative reconciliation: you declare desired state (YAML); controllers continuously drive actual state toward it. This is why K8s self heals. Interview angle. etcd is the source of truth — if etcd is corrupted or slow, the cluster is in trouble. The apiserver is the only component that talks to etcd directly; everything else goes through the apiserver API."},{"id":"5-pods-replicasets-deployments","heading":"5. Pods, ReplicaSets & Deployments","level":2,"text":"Pod — smallest unit; one or more containers sharing network/storage. Usually one app container (+ optional sidecars). ReplicaSet — keeps N identical pods running. Deployment — manages ReplicaSets, enabling rolling updates & rollbacks (what you actually use). How it works — Pod internals. All containers in a Pod share one IP address and one set of volumes. They communicate via . A common pattern is an app container + a sidecar (e.g., log shipper, service mesh proxy) that augments the main process without changing the app image. How it works — Deployment → ReplicaSet → Pod. When you update a Deployment's image tag, the Deployment controller creates a new ReplicaSet with the new template and gradually scales it up while scaling the old ReplicaSet down. Old ReplicaSets are kept (with ) so can instantly revert. Example — Pod lifecycle states. (scheduled but image not pulled yet) → → / . If a node dies, Pods on it go to ; the ReplicaSet creates replacements on healthy nodes. Pitfall. Never create bare Pods in production — they are not self healing. Always use a Deployment (stateless) or StatefulSet (stateful)."},{"id":"6-services-ingress","heading":"6. Services & Ingress","level":2,"text":"Pods are ephemeral with changing IPs; a Service gives a stable virtual IP + DNS name and load balances across matching pods. How it works. A Service is a stable front door. K8s assigns it a cluster internal IP (ClusterIP) and DNS name ( ). The Service controller watches Pods with matching labels and populates an Endpoints (or EndpointSlice ) object with their IPs. kube proxy on each node programs routing rules (iptables or IPVS) so any Pod in the cluster can reach and traffic is load balanced to a ready backend Pod. Service type Use (default) Internal only access Expose on each node's port (dev) Cloud LB (e.g., AWS ELB) for external traffic Ingress + an ingress controller (NGINX/ALB) routes external HTTP(S) by host/path to services — one entry point, TLS termination, path routing. Example. External request: → cloud LoadBalancer → Ingress controller → Service (port 80) → one of 3 ready Pods (port 8080). If one Pod fails readiness, it is removed from Endpoints automatically — no manual intervention. Pitfall. A Service with no ready endpoints returns connection refused or 503. This is almost always a readiness probe failure, not a Service misconfiguration. Check ."},{"id":"7-configmaps-secrets","heading":"7. ConfigMaps & Secrets","level":2,"text":"Externalize configuration from images (12 factor). What & why. Baking config into an image means rebuilding and redeploying for every env change. ConfigMaps hold non sensitive config; Secrets hold sensitive data. Both decouple \"what to run\" (image) from \"how to configure it\" (env specific values). How it works — injection. You can inject config two ways: Environment variables — loads all keys as env vars at Pod start. Changing the ConfigMap does not restart running Pods unless you use a reloader or rolling restart. Volume mounts — ConfigMap/Secret mounted as files. Some apps watch the file for hot reload; most Java/Spring apps read env at startup only. Inject into pods via or mounted files. Secrets are only base64 encoded → enable encryption at rest, RBAC, and prefer an external manager (AWS Secrets Manager + External Secrets Operator, Vault). Never bake secrets into images. Example. Same image runs in dev (ConfigMap: ) and prod (ConfigMap: , Secret: real DB password) — zero image rebuild. Interview angle. \"Are K8s Secrets encrypted?\" — Only base64 by default. Enable at rest, restrict with RBAC, and prefer syncing from an external secrets manager so rotation doesn't require editing YAML."},{"id":"8-probes","heading":"8. Probes","level":2,"text":"K8s uses probes to know pod health — critical for zero downtime. What & why. Without probes, K8s only knows \"the process started.\" A JVM can be alive but stuck in GC, or alive but unable to reach the database. Probes let the platform distinguish \"running\" from \"healthy\" and \"ready for traffic.\" Probe Question On failure liveness Is it alive? Restart the container readiness Can it serve traffic? Remove from Service endpoints (no restart) startup Has it finished booting? Protects slow starting apps from liveness kills How it works. The kubelet runs probe checks on a schedule ( ). For HTTP probes, it hits the path from inside the cluster network. Three consecutive failures (by default) trigger action. Startup probes disable liveness/readiness until they succeed — essential for apps that take 60+ seconds to boot (large Spring contexts, cache warm up). Example — the classic bug. Readiness checks which calls the payment gateway. Gateway is slow → readiness fails → Pod removed from load balancer → all Pods flap → 503 for everyone. Fix: liveness checks a shallow (JVM up); readiness checks DB connectivity only, not every downstream. Classic bug: readiness pointing at a deep health check that depends on a flaky downstream → pod flaps out of rotation. Keep liveness shallow; readiness reflects ability to serve. Interview angle. \"Failing readiness vs failing liveness?\" — Readiness = temporary \"don't send traffic\" (DB migration in progress). Liveness = \"this process is dead, restart it\" (deadlock). Never put downstream checks on liveness."},{"id":"9-resource-requests-limits-qos","heading":"9. Resource Requests, Limits & QoS","level":2,"text":"What & why. Without limits, one memory leaking Pod can OOM the entire node, killing neighbors. Without requests, the scheduler may pack too many Pods onto one node, causing CPU starvation. Requests and limits map directly to cgroup settings the kubelet applies to each container. Requests drive scheduling; limits cap usage. Exceeding memory limit → OOMKilled . Exceeding CPU limit → throttled (not killed). QoS classes: Guaranteed (requests==limits), Burstable , BestEffort — affects eviction order under node pressure. For JVMs, set heap relative to the memory limit ( ) or you'll get OOMKills. How it works — scheduling math. A node with 4 CPU and 16 Gi RAM can fit at most 16 Pods with (CPU bound: 4000m / 250m = 16). The scheduler will not place a Pod on a node that lacks unrequested capacity. Example — OOMKilled scenario. Pod limit: . JVM defaults heap to 25% of host RAM (8 Gi host → 2 Gi heap attempt). Container cgroup limit is 1 Gi → kernel OOM killer terminates the Java process → . Fix: so heap ≈ 768 Mi, leaving room for metaspace, threads, and native memory. Pitfall. CPU throttling (hitting CPU limit) doesn't kill the Pod but causes latency spikes that look like downstream slowness. Check and compare CPU usage to limits."},{"id":"10-hpa","heading":"10. HPA","level":2,"text":"Horizontal Pod Autoscaler scales replica count on metrics. What & why. Traffic spikes at 2 PM — CPU goes from 30% to 85%. Without HPA, latency climbs until you manually scale. HPA watches metrics and adjusts on your Deployment automatically. How it works. Every 15 seconds, the HPA controller reads metrics (from metrics server for CPU/memory, or Prometheus Adapter for custom metrics), compares current utilization to the target, and computes desired replicas: Example: 3 replicas at 90% CPU, target 70% → = = 4 replicas . Needs the metrics server (or custom/external metrics via Prometheus Adapter). For event driven scaling (e.g., Kafka lag , queue depth) use KEDA . HPA scales pods; Cluster Autoscaler scales nodes when pods can't be scheduled. Pitfall. HPA on CPU alone is laggy — by the time CPU rises, you're already slow. For queue consumers, scale on lag (KEDA), not CPU. Also: if is unset, HPA cannot compute utilization percentage."},{"id":"11-rolling-deploys-rollbacks","heading":"11. Rolling Deploys & Rollbacks","level":2,"text":"Default Deployment strategy is RollingUpdate — gradually replace old pods with new, no downtime. How it works — step by step. With , , : 1. New ReplicaSet created with updated image. 2. One new Pod starts (total: 4 Pods temporarily — surge of 1). 3. New Pod must pass readiness before old Pod is terminated. 4. Repeat until all 3 Pods run the new version; old ReplicaSet scaled to 0. 5. Old ReplicaSet retained for instant rollback. Other strategies (via Argo Rollouts/flags): blue green , canary . Readiness probes make rollouts safe (traffic only to ready pods). Example — bad rollout caught early. You deploy ; new Pods fail readiness (bad config). With , old Pods keep serving — users see no downtime. You and investigate. Interview angle. \"How is this zero downtime?\" — Traffic only goes to ready Pods (Endpoints). New Pods must pass readiness before old ones die; guarantees minimum capacity throughout."},{"id":"12-stateful-workloads-storage","heading":"12. Stateful Workloads & Storage","level":2,"text":"Deployment = stateless. For databases/Kafka use StatefulSet (stable network IDs, ordered start, per pod PersistentVolumeClaim ). PV/PVC : PersistentVolume (actual storage, e.g., AWS EBS) bound to a PersistentVolumeClaim requested by a pod; StorageClass enables dynamic provisioning. General advice: run stateful infra (DB, Kafka) as managed cloud services (RDS, MSK) rather than self managing on K8s unless you must. How it works — StatefulSet identity. Pods get stable names: , , (not random hashes). Each Pod gets its own PVC that persists across restarts — always reattaches to the same disk. Pods start and stop in order (0 before 1 before 2) for cluster bootstrap scenarios. How it works — PV/PVC binding. 1. Pod requests → PVC created ( ). 2. PVC asks StorageClass for a dynamically provisioned PV (e.g., 100 Gi EBS volume). 3. PV binds to PVC 1:1; Pod mounts it at . 4. If Pod is rescheduled to another node, the EBS volume reattaches (same AZ required for block storage). Example. You delete Pod — StatefulSet recreates it with the same name and same PVC. Data survives. A Deployment Pod deleted and recreated gets a new identity and no guaranteed storage. Interview angle. \"Would you run Postgres on K8s?\" — Default answer: use managed RDS unless you have a dedicated platform team. StatefulSets add operational complexity (backups, failover, upgrades) that managed services handle for you."},{"id":"13-aws","heading":"13. AWS","level":2,"text":"Core services backend JDs name: How to think about this table. Backend engineers don't need to know every AWS service — but interviews expect you to map a problem to the right building block: compute (EKS/ECS/Lambda), storage (S3/RDS/DynamoDB), messaging (SQS/SNS/MSK), caching (ElastiCache), and cross cutting concerns (IAM, VPC, CloudWatch). Service Use EKS Managed Kubernetes ECS / Fargate Container orchestration (serverless containers) EC2 VMs S3 Object storage (files, backups, static assets) RDS / Aurora Managed relational DB DynamoDB Managed NoSQL (key value, single digit ms) SQS / SNS Queue / pub sub messaging MSK Managed Kafka ElastiCache Managed Redis Lambda Serverless functions IAM Identity & permissions (least privilege) CloudWatch Metrics, logs, alarms ALB Application load balancer Concepts to articulate: VPC/subnets/security groups, IAM roles (use IRSA for pods, not static keys), multi AZ for HA, autoscaling groups, and cost awareness (right sizing, spot instances). Example — IRSA (IAM Roles for Service Accounts). A Pod needs to read from S3. Instead of mounting AWS access keys as Secrets (rotations nightmare), you attach an IAM role to the Pod's ServiceAccount via OIDC. The Pod gets temporary credentials automatically — no static keys in the cluster. Interview angle. \"EKS vs ECS?\" — EKS when you need K8s portability and ecosystem (Helm, operators, multi cloud). ECS/Fargate when you want simpler AWS native orchestration without managing control plane nodes."},{"id":"14-cicd","heading":"14. CI/CD","level":2,"text":"What & why. Manual + doesn't scale and isn't auditable. CI/CD automates build → test → deploy on every merge, with rollback paths when something breaks. Pipeline stages: build → test → scan (Trivy/Snyk) → push image → deploy → smoke test. GitOps (Argo CD / Flux): git is the source of truth; the cluster syncs to the manifests in the repo — auditable, easy rollback. Tools: GitHub Actions, Jenkins, GitLab CI, Harness. How GitOps differs. Instead of CI pushing to the cluster ( ), CI updates a git repo with the new image tag. Argo CD watches that repo and syncs the cluster to match. Deploy history = git history. Rollback = revert a commit. Example flow. Developer merges PR → CI builds image , pushes to registry, updates image tag in git → Argo CD detects drift, applies → rolling update begins → smoke test hits → Slack notification. Pitfall. Deploying tag — K8s may not pull a new if the image is already present on the node (depends on ). Always tag with immutable identifiers (git SHA, semver)."},{"id":"15-troubleshooting-pods","heading":"15. Troubleshooting Pods","level":2,"text":"Theory. Pod problems fall into a few buckets: can't be scheduled (Pending), can't pull image (ImagePullBackOff), crashes on start (CrashLoopBackOff), or runs but isn't ready (readiness failing). The debugging flow is: → (Events section) → → layer specific checks. Symptom Likely cause App crashes on start / failing liveness Bad image name/tag or registry auth No node with enough resources / unbound PVC Memory limit too low (or JVM heap limit) 503 from service No ready pods (readiness failing) Example — worked debug: Pod won't start. 1. → for 5 minutes. 2. → Events: . 3. Root cause: new deployment raised memory from 512Mi to 2Gi; nodes can't fit new Pods. 4. Fix: reduce requests to realistic values, or add nodes (Cluster Autoscaler), or reduce replicas temporarily. 5. Verify: Pod → , readiness passes, shows Pod IP. Example — CrashLoopBackOff. 1. → — app starts before DB is reachable. 2. Fix: add retry logic in app startup, or use init container to wait for DB, or fix readiness to not mark ready until DB connected (not liveness — that would restart forever)."},{"id":"16-interview-qa","heading":"16. Interview Q&A","level":2,"text":"Q1. Container vs VM? Containers share the host kernel and isolate userspace — lightweight, fast start. VMs run a full guest OS — heavier, stronger isolation. Q2. Liveness vs readiness probe? Liveness restarts a hung container; readiness removes a pod from the load balancer until it can serve. Failing readiness ≠ restart. Q3. How does a rolling update achieve zero downtime? New pods start and pass readiness before old pods are terminated ( , ), so there's always healthy capacity; rollback reverts to the previous ReplicaSet. Q4. requests vs limits, and what is OOMKilled? Requests are guaranteed/scheduled resources; limits are caps. Exceeding the memory limit kills the container (OOMKilled); CPU over limit throttles. Q5. How do you autoscale on Kafka lag? HPA on CPU isn't enough; use KEDA with a Kafka lag scaler to scale consumers by lag, plus Cluster Autoscaler for nodes. Q6. How are Secrets secured in K8s? By default only base64 encoded. Enable encryption at rest, restrict via RBAC, and prefer external managers (AWS Secrets Manager + External Secrets, Vault); never bake into images. Q7. Deployment vs StatefulSet? Deployment for stateless, interchangeable pods. StatefulSet for stable identities + persistent per pod storage (databases, Kafka). Q8. How does a Service find its pods? Via label selectors; kube proxy/endpoints route traffic to matching, ready pods; clients use the stable Service DNS name. Q9. What is GitOps? Declarative deployment where git holds the desired manifests and a controller (Argo CD/Flux) continuously reconciles the cluster to match — auditable, reversible. Q10. JVM in a container — common pitfall? Old JVMs ignored cgroup limits and sized heap to the host. Use a modern JDK + so heap fits the container limit, avoiding OOMKills."}],"qa":[{"q":"Container vs VM?","a":"Containers share the host kernel and isolate userspace — lightweight, fast start. VMs run a full guest OS — heavier, stronger isolation."},{"q":"Liveness vs readiness probe?","a":"Liveness restarts a hung container; readiness removes a pod from the load balancer until it can serve. Failing readiness ≠ restart."},{"q":"How does a rolling update achieve zero downtime?","a":"New pods start and pass readiness before old pods are terminated (`maxUnavailable: 0`, `maxSurge`), so there's always healthy capacity; rollback reverts to the previous ReplicaSet."},{"q":"requests vs limits, and what is OOMKilled?","a":"Requests are guaranteed/scheduled resources; limits are caps. Exceeding the memory limit kills the container (OOMKilled); CPU over-limit throttles."},{"q":"How do you autoscale on Kafka lag?","a":"HPA on CPU isn't enough; use **KEDA** with a Kafka lag scaler to scale consumers by lag, plus Cluster Autoscaler for nodes."},{"q":"How are Secrets secured in K8s?","a":"By default only base64-encoded. Enable encryption-at-rest, restrict via RBAC, and prefer external managers (AWS Secrets Manager + External Secrets, Vault); never bake into images."},{"q":"Deployment vs StatefulSet?","a":"Deployment for stateless, interchangeable pods. StatefulSet for stable identities + persistent per-pod storage (databases, Kafka)."},{"q":"How does a Service find its pods?","a":"Via label selectors; kube-proxy/endpoints route traffic to matching, ready pods; clients use the stable Service DNS name."},{"q":"What is GitOps?","a":"Declarative deployment where git holds the desired manifests and a controller (Argo CD/Flux) continuously reconciles the cluster to match — auditable, reversible."},{"q":"JVM in a container — common pitfall?","a":"Old JVMs ignored cgroup limits and sized heap to the host. Use a modern JDK + `-XX:MaxRAMPercentage` so heap fits the container limit, avoiding OOMKills."}]},"22_Resilience_Distributed_Patterns.md":{"title":"Resilience & Distributed Patterns","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"resilience-distributed-systems-patterns-deep-dive","heading":"Resilience & Distributed Systems Patterns — Deep Dive","level":1,"text":""},{"id":"building-services-that-survive-failure-senior-backend-edition","heading":"Building Services That Survive Failure | Senior Backend Edition","level":3,"text":""},{"id":"1-why-resilience","heading":"1. Why Resilience","level":2,"text":"In a distributed system, everything fails : networks drop, downstreams slow, instances die. Resilience engineering assumes failure and limits its blast radius so one failing dependency doesn't cascade into a full outage. The cascade you're preventing: Service A calls slow Service B → A's threads block waiting → A's thread pool exhausts → A stops serving all requests → callers of A fail too. One slow dependency takes down the chain. The patterns below stop this. Example — the numbers. The order service has a thread pool of 200 threads. The inventory service normally responds in 50 ms. It degrades to 30 seconds (DB lock). Each blocked thread holds a connection for 30s. After 200 concurrent requests, the order service thread pool is exhausted — including requests that never touch inventory. Timeouts would fail those calls at 3s, freeing threads; circuit breakers would fail fast at 0ms once inventory is known to be down. Fallacies of distributed computing (worth naming): the network is not reliable, latency is not zero, bandwidth is not infinite, topology changes . Design accordingly. Interview angle. Resilience is not \"prevent all failures\" — it's \"fail fast, fail small, recover automatically.\" Name the cascade explicitly in interviews; it shows you understand production dynamics, not just pattern names."},{"id":"2-timeouts-deadlines","heading":"2. Timeouts & Deadlines","level":2,"text":"The most important and most forgotten pattern. Every network call needs a timeout — an unbounded call is a latent outage. What & why. Without a timeout, a hung TCP connection or a downstream that stopped responding holds your thread forever. In a synchronous call chain, one stuck call eventually exhausts the entire pool. A timeout is a contract: \"I will wait at most X ms, then I treat this as failure and move on.\" How it works — two timeout layers. Connect timeout — how long to establish the TCP connection (downstream dead / wrong host). Read/request timeout — how long to wait for the response after connection is established (downstream accepted but slow). Set connect and read/request timeouts on HTTP clients, DB calls, and Kafka. Deadline propagation: pass a remaining time budget downstream so the whole call chain respects one overall deadline (don't let B spend 10s when A already timed out at 3s). Timeout should be < the caller's timeout (so retries have room). Example — deadline propagation. Gateway timeout: 5s. Order service timeout to inventory: 3s. Inventory timeout to DB: 1s. Each layer leaves headroom for its caller. If the gateway passed \"remaining: 5s\" and inventory used all 5s on the DB, the order service would timeout at the gateway level with no useful error. Libraries like gRPC propagate deadlines via metadata; for HTTP, pass or use a shared context. Pitfall. Setting all timeouts to 30s \"to be safe\" — under load, 30s × 200 threads = still exhausted. Pick timeouts from SLO (p99 latency × 2–3) and enforce them."},{"id":"3-retries-backoff","heading":"3. Retries & Backoff","level":2,"text":"Retries recover from transient failures (blip, brief overload) — but naive retries amplify outages. What & why. A 503 from a downstream that just restarted is worth retrying. But if 1000 clients all retry immediately when a service comes back, they create a thundering herd that knocks it down again. Exponential backoff spreads retries over time; jitter (random noise) prevents synchronized waves. How it works — backoff math. Attempt 0: wait 100ms + jitter. Attempt 1: 200ms. Attempt 2: 400ms. Attempt 3: 800ms. With jitter ±50ms, clients don't retry in lockstep. Rules: Only retry idempotent operations (GET, or writes guarded by idempotency keys) — never blindly retry a payment POST. Use exponential backoff + jitter to avoid the thundering herd . Cap attempts; combine with a circuit breaker so you stop retrying a dead dependency. Distinguish retryable (5xx, timeout) from non retryable (4xx) errors. Example. Payment gateway returns 503 (overloaded). Retry 3 times with backoff: 200ms, 400ms, 800ms. If still failing, circuit breaker opens — stop retrying entirely and return fallback. Retrying 100 times into a dead service makes recovery slower for everyone. Interview angle. \"Retry a POST?\" — Only with an idempotency key. Otherwise a timeout after the server processed the request but before the response arrived causes a double charge on retry."},{"id":"4-circuit-breaker","heading":"4. Circuit Breaker","level":2,"text":"Stops hammering a failing dependency, giving it time to recover and failing fast for callers. What & why. Retries help with blips; they hurt when a dependency is genuinely down — you waste threads and add load to a sick service. A circuit breaker is like an electrical breaker: after too many failures, it \"trips\" and stops sending traffic until things cool down. CLOSED — calls flow; failures counted. OPEN — calls fail fast (no call made) for a cooldown; return a fallback. HALF OPEN — allow a few trial calls; success → CLOSED, failure → OPEN. This converts slow, cascading failures into fast, contained ones. How it works — sample thresholds (Resilience4j defaults). Sliding window of last 20 calls. If ≥50% fail (10 of 20), transition to OPEN. Wait 10 seconds (cooldown). Allow 3 trial calls in HALF OPEN — if all succeed, CLOSED; if any fail, back to OPEN for another 10s. Example — inventory service outage. 1. Inventory DB dies. First 10 of 20 calls timeout at 3s each → 50% failure rate → OPEN. 2. Order service calls fail immediately (0ms) with fallback \"stock unknown\" — threads freed instantly. 3. After 10s, HALF OPEN sends 3 trial requests. Inventory still down → 3 failures → OPEN again. 4. Inventory recovers. Trial calls succeed → CLOSED. Normal traffic resumes. Pitfall. Circuit breaker counts failures per dependency instance in the library, but if you have 50 pods each with their own breaker, a degraded service still gets 50 trial calls every cooldown from HALF OPEN across the fleet. Consider shared state (Redis) for cluster wide breakers in extreme cases, or accept that per instance breakers are usually sufficient."},{"id":"5-bulkhead","heading":"5. Bulkhead","level":2,"text":"Isolate resources so one slow dependency can't consume all threads/connections (named after ship compartments). What & why. On a ship, flooding one compartment doesn't sink the vessel — watertight bulkheads contain it. In software, if inventory and payments share one thread pool of 200, a slow inventory call blocks threads that payment calls need. Separate pools contain the damage. How it works. Each downstream gets its own bounded thread pool, connection pool, or semaphore. When inventory's pool (10 threads, queue 20) fills up, new inventory calls are rejected immediately — but the payment pool (50 threads) is untouched. Give each downstream its own thread pool / connection pool / semaphore . If dependency B is slow, only B's pool exhausts; calls to C and the rest of the app keep working. In Kubernetes, running separate services is itself a bulkhead. Example. Order service calls inventory (10 thread bulkhead) and recommendations (10 thread bulkhead). Inventory slows to 5s per call. After 30 concurrent inventory requests, the bulkhead rejects new ones with — caller gets fallback. Recommendation calls and order creation (which doesn't need inventory) continue normally. Interview angle. Bulkhead vs circuit breaker: bulkhead limits concurrency (how many calls in flight); circuit breaker limits attempts based on failure rate. Use both — bulkhead prevents resource exhaustion; breaker stops calling a known dead service."},{"id":"6-rate-limiting","heading":"6. Rate Limiting","level":2,"text":"Protect a service (and downstreams) from overload/abuse by capping request rate. What & why. A traffic spike, a misconfigured client loop, or a DDoS can overwhelm your service and its dependencies. Rate limiting is a gate: \"you get N requests per window; excess gets 429.\" How it works — Token Bucket (most common). A bucket holds tokens (capacity = burst size). Tokens refill at a steady rate (e.g., 100/sec). Each request consumes one token. If the bucket is empty, reject with 429. This allows short bursts (bucket full = 100 instant requests) while enforcing average rate over time. Algorithm Behavior Token Bucket Allows bursts up to bucket size; smooth average rate Leaky Bucket Fixed output rate; bursts queue (or drop) Fixed Window Count per clock window; simple but allows 2× burst at window edges Sliding Window Accurate; more memory/compute Algorithms: Token Bucket (allows bursts), Leaky Bucket (smooths), Fixed Window (simple, edge bursts), Sliding Window (accurate). Where: API gateway (global), per client/tenant, and per downstream (don't overwhelm a dependency). Distributed rate limiting: keep counters in Redis so the limit is shared across all instances. Return with a header when limited. Example. Per tenant limit: 1000 req/min. Tenant A sends 1500 req/min → 500 get 429 with . Other tenants unaffected. In Redis: with TTL 60s; if count 1000, reject. Pitfall. Rate limiting at the app only — a misbehaving client hitting 50 Pod instances gets 50× the limit unless counters are shared (Redis) or enforced at the gateway."},{"id":"7-resilience4j","heading":"7. Resilience4j","level":2,"text":"The standard Java library (Hystrix is dead). Compose decorators; order matters. What & why. Hystrix (Netflix, deprecated) pioneered the patterns; Resilience4j is its lightweight successor — no thread per command overhead, native functional composition, Micrometer metrics. In Spring Boot 3, it's the default choice for circuit breakers, retries, and bulkheads. How it works — decorator stacking. Each annotation wraps your method in a decorator chain. When a call enters, it passes through outer decorators first. The recommended order matters because each layer handles a different failure mode: Retry (outermost): retries the whole chain below on transient failure. CircuitBreaker : stops calling if failure rate too high. RateLimiter : rejects if rate exceeded. TimeLimiter : enforces async timeout. Bulkhead (innermost): limits concurrent executions before your code runs. Recommended decorator order (outer→inner): Retry → CircuitBreaker → RateLimiter → TimeLimiter → Bulkhead. Resilience4j exposes metrics to Prometheus. Example — call flow when inventory is down. Retry attempt 1 → CircuitBreaker OPEN → fail fast (0ms) → Retry attempt 2 → fail fast → attempt 3 → fail fast → fallback returns . Total time: 0ms, not 3 × 3s timeout. Pitfall. Putting Retry inside CircuitBreaker means the breaker sees one logical call as multiple attempts, tripping faster — usually you want Retry outside so the breaker evaluates the final outcome."},{"id":"8-idempotency-keys","heading":"8. Idempotency Keys","level":2,"text":"Make a write safe to execute multiple times (essential because retries + at least once delivery cause duplicates). What & why. Networks are unreliable: your client sends , the server charges the card, but the response is lost. Client retries → double charge. An idempotency key lets the server recognize \"I've already processed this exact request\" and return the original result without re executing. How it works — server side flow. 1. Client generates UUID per logical operation (not per HTTP attempt — same key on retry). 2. Server checks store atomically: or Redis . 3. If key exists with status → return cached response body + status code. 4. If key exists with status → return 409 or wait (another request is processing). 5. If absent → mark , execute payment, store result, mark . Client generates a unique key per logical operation; server dedupes. Store keys with a TTL; use a unique constraint as a backstop. This is how payment APIs (Stripe style) guarantee \"charge once\" under retries. Example. Client sends with , amount $50. Server charges, stores . Network drops response. Client retries same key. Server finds , returns — no second charge. Interview angle. Idempotency keys solve client retries. They don't solve duplicate Kafka messages — you still need idempotent consumers (check business key before processing)."},{"id":"9-distributed-locks","heading":"9. Distributed Locks","level":2,"text":"Coordinate exclusive access across instances (e.g., only one node runs a scheduled job). What & why. You have 5 Pod replicas; a job must run once per minute, not 5 times. A distributed lock ensures only one instance executes at a time. Also used for leader election and serializing writes to a shared resource. How it works — Redis lock. is atomic: set only if Not eXists, with expiry in milliseconds. The is a unique token (UUID) so you only release your own lock. Redisson wraps this with lease time + auto renewal (watchdog). Always set a TTL (so a crashed holder doesn't deadlock forever) and fence with a token/version to handle the \"lock expired mid work\" edge case ( fencing tokens ). Alternatives: ZooKeeper/etcd for stronger guarantees. Redlock is the multi node Redis algorithm (debated; for critical correctness prefer a consensus store). Example — the lock expiry race. 1. Instance A acquires lock (TTL 30s), starts a 45s report. 2. Lock expires at 30s. Instance B acquires lock, starts report. 3. Both run concurrently — duplicate report emails. 4. Fix: use Redisson watchdog (auto renews while holder is alive), or make the job idempotent, or use fencing tokens so downstream rejects stale writes. Pitfall. Never use without TTL — a crash before release deadlocks all future runs forever."},{"id":"10-saga-pattern","heading":"10. Saga Pattern","level":2,"text":"Microservices can't share one ACID transaction across databases. A Saga is a sequence of local transactions, each with a compensating action to undo on failure. What & why. In a monolith, is one transaction. In microservices, each step is a separate service with its own DB. You can't hold a 2PC lock across HTTP calls (blocks, single point of failure). Sagas accept eventual consistency: each step commits locally; if a later step fails, you run compensating actions to undo prior steps. Choreography (event driven, no coordinator): Pros: simple, decoupled. Cons: hard to follow flow, risk of cyclic events. Orchestration (a central orchestrator drives steps): Pros: explicit, easier to reason/monitor. Cons: orchestrator is a focal point. Sagas give eventual consistency , not isolation — design for intermediate states and idempotent steps. Pair with the outbox pattern for reliable event publishing. Example — checkout saga (orchestration). 1. → inventory reserves, returns . 2. → fails (card declined). 3. Orchestrator calls — compensation. 4. Order marked . User sees \"payment declined.\" Stock is released. No orphaned reservation. Intermediate state. Between steps 1 and 2, stock is reserved but not paid — other users see reduced availability. Design UIs and APIs to tolerate this (show \"reserved\" not \"sold\"). Pitfall. Compensating actions aren't always perfect inverses — a shipped package can't be \"un shipped\"; you initiate a return flow instead. Design compensations as business operations, not SQL rollbacks."},{"id":"11-snowflake-ids","heading":"11. Snowflake IDs","level":2,"text":"Globally unique, roughly time sortable 64 bit IDs without a central sequence (no DB round trip, no collisions across nodes). What & why. Auto increment IDs ( ) require a single DB writer — bottleneck in distributed systems. UUIDv4 is distributed but random — terrible for B tree index locality (inserts scatter across the index, causing page splits). Snowflake gives you uniqueness + time ordering + no coordination per ID. How it works — bit layout. 41 bit timestamp — milliseconds since epoch; IDs sort chronologically. 10 bit machine ID — assigned at deploy time (1024 machines); must be unique per node. 12 bit sequence — counter 0–4095 within the same millisecond; rolls over if 4096 IDs/ms on one node. Each node generates IDs locally. Same millisecond: increment sequence. New millisecond: reset sequence to 0. No network call. Time ordered → good for DB index locality and pagination. No coordination needed per ID (only a unique machine id). Alternatives: ULID (sortable, URL safe), UUIDv7 (time ordered). Avoid random UUIDv4 as a clustered PK (index fragmentation). Example. At , machine 42, sequence 0 → ID (simplified). Next ID same ms → sequence 1. IDs inserted into a B tree land on the right edge of the index (append only) — fast inserts, unlike random UUIDs that fragment pages. Pitfall. Clock skew between machines can cause duplicate IDs if clocks go backward — use NTP sync and handle backward clock by waiting until caught up (Twitter's original approach)."},{"id":"12-consistency-cap-eventual-consistency","heading":"12. Consistency, CAP & Eventual Consistency","level":2,"text":"Theory — CAP. In a distributed system during a network P artition, you must choose between C onsistency (every read sees the latest write) and A vailability (every request gets a response). You can't have both while partitioned. Most microservice stacks choose AP — stay available, accept temporary staleness, reconcile later. CAP: under a network P artition you choose C onsistency or A vailability. Most microservice stacks pick AP with eventual consistency. Strong consistency — reads always see the latest write (single DB, quorum reads). Costs latency/availability. Eventual consistency — replicas converge over time; you accept temporary staleness for availability/scale (Saga outcomes, caches, read replicas). PACELC extends CAP: even without partitions, there's a latency vs consistency trade off. Practical patterns: read your writes via sticky routing, version vectors, idempotency to tolerate reordering. Example — shopping cart. User adds item (write to primary DB). Immediately reads cart from a read replica that hasn't replicated yet — item missing. Fix: read your writes (route that user's reads to primary for N seconds after write), or accept \"item may take a moment to appear.\" Example — social feed. Post appears on your profile instantly (your write) but friends' feeds update over 1–2 seconds (fan out async). Acceptable staleness for the use case. Interview angle. Don't recite CAP as \"pick 2 of 3\" — explain when partition happens and what your system does. Payment ledger → strong consistency (single DB or consensus). Recommendation feed → eventual consistency."},{"id":"13-graceful-degradation-fallbacks","heading":"13. Graceful Degradation & Fallbacks","level":2,"text":"When a dependency is down, degrade instead of erroring out: What & why. Users prefer a partial experience over a 500 error. Graceful degradation keeps the core path working while non critical features fail silently or with reduced quality. This is the consumer side counterpart to circuit breakers — what you return when the breaker is open. How it works — degradation tiers. 1. Full service — all dependencies healthy. 2. Degraded — non critical dependency down; core response with defaults/stale data. 3. Unavailable — core dependency down; clear error with retry guidance. Serve stale cache when the source is unavailable. Return a default/partial response (e.g., empty recommendations) rather than a 500. Feature flags / kill switches to disable non critical features under load. Load shedding — reject low priority traffic to protect core flows. Make the failure visible (degraded banner) but keep the core working. Example — product page. Inventory service down (circuit open). Instead of 500, return product details with and a banner \"Stock info temporarily unavailable.\" User can still browse and add to cart; checkout validates stock at payment time. Example — load shedding. Black Friday traffic 10× normal. API gateway returns 429 for (low priority) while gets full capacity. Core revenue path protected. Interview angle. Degradation requires product decisions, not just engineering — agree upfront which fields can be empty/stale and which must fail hard (price, inventory at checkout)."},{"id":"14-interview-qa","heading":"14. Interview Q&A","level":2,"text":"Q1. How do you prevent a cascading failure? Timeouts on every call + circuit breakers (fail fast) + bulkheads (isolate pools) + fallbacks. Together they contain a slow dependency. Q2. When should you NOT retry? Non idempotent writes without an idempotency key, and non transient errors (4xx). Always use backoff + jitter and cap attempts. Q3. Explain circuit breaker states. CLOSED (normal), OPEN (fail fast after failure threshold), HALF OPEN (trial calls); success closes it, failure reopens. Q4. How do you make a payment API safe under retries? Idempotency keys: client sends a unique key, server dedupes and returns the stored response so the charge happens once. Q5. How do distributed transactions work without 2PC? Saga pattern — local transactions + compensating actions, via choreography (events) or orchestration (coordinator); gives eventual consistency. Q6. Choreography vs orchestration? Choreography = services react to events, decoupled but harder to trace. Orchestration = central coordinator, explicit and observable but a focal point. Q7. How do you implement a distributed lock and its pitfalls? Redis (or Redisson) with a TTL and a fencing token; pitfalls are lock expiry mid work and releasing another holder's lock — use tokens/Lua and fencing. Q8. Why Snowflake IDs over UUIDv4? Time sortable, index friendly, globally unique without coordination; UUIDv4 is random and fragments clustered indexes. Q9. Strong vs eventual consistency — pick one for a feed/cart? Cart/payment → strong/transactional. Social feed/recommendations/analytics → eventual consistency for scale and availability. Q10. What's the single most overlooked resilience practice? Timeouts on every outbound call (plus deadline propagation). Without them, one slow dependency exhausts threads and takes the service down."}],"qa":[{"q":"How do you prevent a cascading failure?","a":"Timeouts on every call + circuit breakers (fail fast) + bulkheads (isolate pools) + fallbacks. Together they contain a slow dependency."},{"q":"When should you NOT retry?","a":"Non-idempotent writes without an idempotency key, and non-transient errors (4xx). Always use backoff + jitter and cap attempts."},{"q":"Explain circuit breaker states.","a":"CLOSED (normal), OPEN (fail fast after failure threshold), HALF_OPEN (trial calls); success closes it, failure reopens."},{"q":"How do you make a payment API safe under retries?","a":"Idempotency keys: client sends a unique key, server dedupes and returns the stored response so the charge happens once."},{"q":"How do distributed transactions work without 2PC?","a":"Saga pattern — local transactions + compensating actions, via choreography (events) or orchestration (coordinator); gives eventual consistency."},{"q":"Choreography vs orchestration?","a":"Choreography = services react to events, decoupled but harder to trace. Orchestration = central coordinator, explicit and observable but a focal point."},{"q":"How do you implement a distributed lock and its pitfalls?","a":"Redis `SET NX PX` (or Redisson) with a TTL and a fencing token; pitfalls are lock expiry mid-work and releasing another holder's lock — use tokens/Lua and fencing."},{"q":"Why Snowflake IDs over UUIDv4?","a":"Time-sortable, index-friendly, globally unique without coordination; UUIDv4 is random and fragments clustered indexes."},{"q":"Strong vs eventual consistency — pick one for a feed/cart?","a":"Cart/payment → strong/transactional. Social feed/recommendations/analytics → eventual consistency for scale and availability."},{"q":"What's the single most overlooked resilience practice?","a":"Timeouts on every outbound call (plus deadline propagation). Without them, one slow dependency exhausts threads and takes the service down."}]},"23_Observability_Production_Debugging.md":{"title":"Observability & Production Debugging","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"observability-production-debugging-deep-dive","heading":"Observability & Production Debugging — Deep Dive","level":1,"text":""},{"id":"metrics-logs-traces-its-slow-in-prod-senior-signal","heading":"Metrics, Logs, Traces & \"It's Slow in Prod\" | Senior Signal","level":3,"text":""},{"id":"1-observability-vs-monitoring","heading":"1. Observability vs Monitoring","level":2,"text":"Monitoring answers known questions: \"Is CPU high? Is the service up?\" (predefined dashboards/alerts). Observability lets you ask new questions about unknown problems from the system's outputs — \"why are p99 latencies for tenant X spiking only on POST /orders?\" — without shipping new code. You need observability because distributed systems fail in ways you didn't predict. The goal: go from symptom → root cause quickly. How it works. Monitoring is a fixed set of gauges and thresholds you decided ahead of time. Observability means your system emits rich telemetry (metrics with labels, structured logs, traces with context) so you can slice and filter ad hoc during an incident. The difference is like a car dashboard (monitoring: speed, fuel) vs a mechanic's diagnostic port (observability: read any sensor, any time). Example. Monitoring alert: \"error rate 5%.\" Observability follow up: filter errors by , , — discover only one tenant hitting a new API version that sends malformed payloads. No new code deployed; you queried existing structured logs and metrics. Interview angle. Monitoring tells you something broke . Observability tells you why , where , and for whom ."},{"id":"2-three-pillars","heading":"2. Three Pillars","level":2,"text":"Pillar Question it answers Tool examples Metrics What is happening? (aggregates, trends) Prometheus, Micrometer Logs What exactly happened in this event? ELK, Loki Traces Where did the time go across services? OpenTelemetry, Jaeger They're complementary: a metric alert tells you something is wrong → a trace shows which hop is slow → logs for that trace/span tell you why . How they correlate during an incident. 1. Metrics — Grafana alert fires: p99 latency 2s for 5 min. 2. Traces — open Jaeger, filter , sort by duration, pick a slow trace ( ). 3. Logs — query → see on the slow span. Each pillar links via shared identifiers ( , , , ). Without correlation IDs, you're grep ing blindly across gigabytes of logs. Pitfall. Having only metrics — you know latency is high but not whether it's the DB, a downstream, or GC. Having only logs — you can't see the cross service picture. You need all three, wired together."},{"id":"3-metrics-with-prometheus","heading":"3. Metrics with Prometheus","level":2,"text":"Prometheus pulls (scrapes) metrics from a endpoint and stores time series. What & why. Metrics are numeric measurements over time — request count, latency histogram, queue depth. They're cheap to store (aggregated) and fast to query across millions of data points. Prometheus is the de facto standard in cloud native stacks because it integrates with Kubernetes service discovery and has a powerful query language (PromQL). How it works — pull model. Every 15s (configurable), Prometheus HTTP scrapes on each target. If a scrape fails, the target is marked down — built in health detection. Metrics are stored as time series identified by name + labels: . Metric types: Counter (monotonic, e.g., requests total), Gauge (up/down, e.g., queue size), Histogram/Summary (distributions → percentiles). PromQL: Use labels (route, status, instance) for slicing — but avoid high cardinality labels (userId) which explode storage. Example — cardinality explosion. × 1 million users = 1 million time series. Prometheus memory explodes. Fix: aggregate by or , not individual user. Log user level detail in traces/logs instead. Interview angle. Push vs pull: Prometheus pulls (simple, no client side buffer). Pushgateway exists for short lived batch jobs only — not for regular services."},{"id":"4-grafana","heading":"4. Grafana","level":2,"text":"Visualization layer over Prometheus/Loki/Jaeger. What & why. Raw PromQL results are tables of numbers. Grafana turns them into dashboards you stare at during incidents — latency graphs, error rate panels, saturation gauges — with drill down links to traces and logs. How to build useful dashboards. Golden signals overview — one row per service: rate, errors, duration (p50/p95/p99), saturation. Scan all services in 10 seconds. Drill down dashboards — per service detail: latency by endpoint, error breakdown by status code, JVM heap, thread pools, Kafka consumer lag. Template variables — dropdown for , , so one dashboard works everywhere. Deploy annotations — vertical lines on graphs when a deploy happened. A latency spike at 14:32 + deploy annotation at 14:31 = strong correlation. Build dashboards per service: traffic, error rate, latency (p50/p95/p99), saturation (CPU/mem), queue depth, Kafka lag. Use template variables (per service, per env), annotations for deploys (correlate a latency spike with a release). Keep a \"golden signals\" overview dashboard + drill down dashboards. Example. On call opens the overview dashboard — p99 jumped from 200ms to 3s. Clicks through to the orders api drill down — only is slow, not . Clicks a trace link from the slow panel → lands in Jaeger on a 2.8s inventory span. Pitfall. Dashboard sprawl — 50 dashboards nobody maintains. Invest in 1 overview + 1 per critical service, kept current."},{"id":"5-structured-logging","heading":"5. Structured Logging","level":2,"text":"Log JSON , not free text — so logs are queryable. What & why. Plain text logs ( ) require regex to search and can't be aggregated. Structured JSON logs ( ) are fields you filter, group, and join with traces/metrics in Loki or Elasticsearch. How it works. Use a structured logging API that emits key value pairs. In Java, or Logback JSON encoder. Every log line carries consistent fields so queries like work instantly. Use SLF4J + Logback/Log4j2 , or (Node). Output JSON in prod. Levels: ERROR (needs attention), WARN (suspicious), INFO (business events), DEBUG (dev). Don't log at DEBUG in prod by default. Never log secrets/PII (passwords, tokens, card numbers) — mask them. Include , , , and relevant business keys in every line. Example query (Loki). → all inventory errors with timing, last hour. Pitfall. Logging everything at INFO in prod — storage cost and noise. Log business events and errors at INFO; diagnostic detail at DEBUG (enabled temporarily during incidents via config reload)."},{"id":"6-log-aggregation","heading":"6. Log Aggregation","level":2,"text":"Centralize logs from all pods/instances: What & why. With 30 Pods across 10 nodes, one Pod at a time doesn't scale. Log aggregation ships all stdout to a central store where you search, filter, and correlate across the fleet. How it works in Kubernetes. A log agent (Fluent Bit) runs as a DaemonSet — one agent per node. It tails (where container runtime writes stdout), parses JSON, adds K8s metadata (pod name, namespace, labels), and ships to the backend. ELK/EFK : Elasticsearch (store/search) + Logstash/Fluentd/Fluent Bit (ship/parse) + Kibana (UI). Grafana Loki : indexes only labels (not full text) → cheaper; query with LogQL ; integrates tightly with Grafana + traces. In Kubernetes, a log agent (Fluent Bit) runs as a DaemonSet, tails container stdout, and ships centrally. Apps should log to stdout and let the platform handle shipping. ELK vs Loki. ELK indexes full text — powerful search (\"find any log containing \") but expensive at scale. Loki indexes labels only ( , , ) — cheap, but full text search is slower/limited. Loki wins when you're already on Grafana and correlate via labels/traceId. Pitfall. Apps writing to files inside containers — the file is lost when the Pod dies and the agent may not tail it. Always log to stdout/stderr."},{"id":"7-distributed-tracing","heading":"7. Distributed Tracing","level":2,"text":"A trace follows one request across services; each hop is a span with timing + parent/child links. This is the only practical way to find which service is slow in a chain. How it works — context propagation. When the gateway receives a request, the OTel SDK creates a root span and generates a . On outbound HTTP calls, the SDK injects the trace context into headers ( per W3C spec). The downstream service extracts it, creates a child span linked to the same trace, and repeats. The trace backend (Jaeger) assembles all spans by into a waterfall view. OpenTelemetry (OTel) is the vendor neutral standard (SDK + auto instrumentation agent) → export to Jaeger /Tempo/Zipkin. Context propagation : trace context travels in headers ( per W3C) across HTTP/Kafka so spans link into one trace. Java: attach the OTel agent ( ) for auto instrumentation of HTTP, JDBC, Kafka. Use sampling (e.g., tail based) to control volume/cost while keeping interesting traces. Sampling. Head based: sample 1% at trace start (cheap, may miss slow traces). Tail based: collect all spans, decide after completion whether to keep (keeps all errors and slow traces, discards normal ones — higher value, needs a collector like OTel Collector). Example — async gap. HTTP request creates span A. App publishes to Kafka (span B, 5ms). Consumer processes message in a different thread — you must manually propagate trace context into the Kafka consumer (via message headers) or the trace breaks and you lose visibility into the async half. Interview angle. Traces show where time went; they don't show why (that's logs). Always pivot from slow span → logs filtered by ."},{"id":"8-correlation-ids","heading":"8. Correlation IDs","level":2,"text":"A single ID stitching together logs/traces for one request. What & why. A single user request touches gateway → order service → inventory → DB. Without a shared ID, finding all log lines for that one request across four services is impossible. A correlation ID (or traceId in OTel) is the thread that sews the story together. How it works. Generate at the edge (API gateway). Store in SLF4J MDC (Mapped Diagnostic Context) so every log line in that thread automatically includes it. Propagate as an HTTP header on every outbound call. On async boundaries (Kafka consumer, , thread pool), explicitly copy MDC to the worker thread. Generate at the edge (gateway), propagate through every service and async hop (including Kafka headers and thread handoffs — copy MDC into worker threads). With OTel, the traceId serves this role; link logs to traces by including traceId in logs. Example — support ticket. Customer reports failed checkout at 14:32. Support gives you . Query: → see the full path: gateway OK → order service OK → payment service . Root cause in 30 seconds. Pitfall. MDC not copied to async threads — logs after or Kafka consumption have no correlation ID. Use (Spring) or manual MDC copy."},{"id":"9-red-use-methods","heading":"9. RED & USE Methods","level":2,"text":"Frameworks for what to measure: RED — for request driven services (APIs). Signal Metric Example PromQL Rate Requests per second Errors Failed requests per second Duration Latency distribution USE — for resources (hosts, queues, disks). Signal Metric Example Utilization % time resource busy CPU at 80%, disk 60% full Saturation Queued work waiting thread pool queue depth, disk IO wait Errors Error count disk read errors, NIC drops RED (request driven services): R ate (req/s), E rrors (failed req/s), D uration (latency distribution). Great for APIs. USE (resources): U tilization, S aturation, E rrors. Great for hosts/queues/disks. Four Golden Signals (Google SRE): latency, traffic, errors, saturation. Instrument every service with RED at minimum; you can debug 80% of incidents from these. Example — reading RED during an incident. Rate normal (1000 req/s), errors jumped from 0.1% to 8%, duration p99 from 200ms to 4s. Pattern: something is failing and slow — likely a downstream timeout (errors + high duration together), not a simple bug (errors alone). Interview angle. RED tells you about the service from the caller's perspective. USE tells you about the infrastructure the service runs on. Check RED first (user impact), then USE (why — CPU throttled? pool saturated?)."},{"id":"10-sli-slo-sla","heading":"10. SLI, SLO, SLA","level":2,"text":"SLI (Indicator) — a measured number, e.g., % of requests < 300ms. SLO (Objective) — your internal target, e.g., 99.9% of requests < 300ms over 30 days. SLA (Agreement) — the contractual promise to customers (with penalties), usually looser than the SLO. Error budget = 100% − SLO (e.g., 0.1% may fail). If you're burning budget fast, freeze risky deploys; if you have budget, ship faster. This balances reliability vs velocity. How it works — error budget math. SLO: 99.9% availability over 30 days. Total minutes: 43,200. Allowed downtime: 0.1% = 43.2 minutes per month. If you've used 40 minutes in the first week, you're burning budget 4× too fast → freeze deploys, focus on reliability. If you've used 5 minutes all month, you have budget to spare → ship that risky refactor. Example — SLI definition. SLI: measured over a rolling 30 day window. SLO: SLI ≥ 99.9%. SLA (customer facing): 99.5% with service credits if breached — intentionally looser than SLO so you get warned (SLO breach) before customers are affected (SLA breach). Interview angle. SLOs are a team agreement , not a monitoring threshold. They drive decisions: \"We have 10 minutes of error budget left — do we ship today or wait?\""},{"id":"11-alerting","heading":"11. Alerting","level":2,"text":"Alert on symptoms, not causes — alert on \"error rate / latency SLO breach\" (user impact), not \"CPU 80%\" (which may be fine). Make alerts actionable — every alert needs a runbook; otherwise it's noise. Avoid alert fatigue: use multi window burn rate alerts, dedupe, and severities (page vs ticket). Page on user facing impact (SLO burn), ticket on slow burn/capacity. How it works — multi window burn rate (Google SRE). Instead of alerting when error rate 1% (noisy), alert when error budget is burning too fast: Fast burn (14.4× normal): 2% errors for 5 min → page immediately (budget gone in 3 hours). Slow burn (6× normal): 5% errors for 30 min → ticket (budget gone in 6 days if sustained). This catches real outages fast without paging on every blip. Example — bad alert vs good alert. Bad: — fires every afternoon during batch jobs; on call ignores it. Good: — only fires on real user impact with enough traffic to matter. Pitfall. Alerting on causes (pod restarted, GC pause) creates noise. The pod restarted and errors spiked? Alert on errors. Pod restarted and nothing changed? Log it, don't page."},{"id":"12-debugging-slow-service","heading":"12. Debugging: Slow Service","level":2,"text":"The headline interview question — answer as a method , not a guess: Worked example — p99 went from 200ms to 3s. 1. Confirm & scope: Grafana overview → only affected, all 6 instances, started 14:30. Deploy annotation at 14:28 — suspicious but not confirmed yet. 2. Trace it: Jaeger → filter → pick trace . Waterfall: gateway 10ms → orders api 2.8s → inventory call 2.6s → DB query inside inventory 2.5s. Slow hop: inventory's DB query. 3. Narrow by layer: DB : check inventory service DB metrics — query time p99 up. on the slow query → missing index on . Not a deploy issue; a new query path from today's feature flag. Downstream : inventory p99 up but root cause is its DB, not network. JVM : orders api GC normal, thread dump clean. Saturation : HikariCP pending threads = 0 on orders api; inventory DB connection pool at 95% — related but secondary. 4. Logs for trace id : → . 5. Fix: add index on . Deploy. Dashboard p99 back to 200ms by 15:00. Confirm with traces. General method: 1. Confirm & scope: check the dashboard — is latency up for all routes or one? All instances or one? Since when (correlate with a deploy/traffic change via annotations)? 2. Trace it: open a slow request's distributed trace → find the span consuming the time (DB? downstream? GC pause?). 3. Narrow by layer: DB : slow query? check query metrics, , N+1, missing index, lock waits, connection pool exhaustion (HikariCP threads). Downstream : a dependency's p99 up → circuit breaker/timeout firing? JVM : GC pauses (GC logs), thread dump ( ) for blocked/locked threads, heap pressure. Saturation : CPU throttling (K8s limits), thread pool/queue full, Kafka lag. 4. Logs for the trace id to see errors/retries on the slow span. 5. Hypothesize → verify with data → fix → confirm the dashboard recovers. Strong answers are systematic (metrics → traces → logs) and mention concrete tools. Weak answers jump to \"restart it\" or \"add a bigger server.\""},{"id":"13-debugging-errors-memory-cpu","heading":"13. Debugging: Errors / Memory / CPU","level":2,"text":"Sudden error spike: correlate with deploy (rollback first if so) → group errors by type/endpoint in logs → check a failing trace → check downstream health/circuit breakers. Worked example — error spike after deploy. 1. Alert: 5xx rate 0.1% → 12% at 09:05. Deploy at 09:03. 2. Rollback immediately ( ) — errors drop to 0.2% by 09:08. Confirms deploy cause. 3. Logs: → all errors: . New API version sends a field the old code can't parse. 4. Fix forward with compatible deserialization, redeploy, monitor. High memory / OOM: capture a heap dump ( or ), analyze in MAT/Eclipse for dominator tree → usual culprits: unbounded caches, lingering collections, classloader/ThreadLocal leaks. In K8s, confirm it's a real leak vs heap container limit (OOMKilled). Worked example — OOMKilled in K8s. 1. Pod status: , restart count 47. 2. — memory at limit (1 Gi) before each kill. 3. Check: real leak or misconfigured JVM? not set → heap sized for host (8 Gi), container limit 1 Gi → OOM. 4. Fix: set , redeploy. Memory stable at 750 Mi. Not a leak — misconfiguration. High CPU: thread dump several times ( ) → find hot/runnable threads; use async profiler /flame graphs → usual culprits: tight loops, excessive GC (check GC logs), regex/serialization hotspots, lock contention. Worked example — high CPU. 1. CPU at 95% sustained, latency normal (CPU bound background work, not request path). 2. Three snapshots 5s apart — same 50 threads in at . 3. async profiler flame graph — 70% CPU in Jackson serialization of a 5 MB response object. 4. Fix: paginate response, cache serialized output. CPU drops to 30%. Tooling: (threads), (heap), , Arthas (live diagnostics without restart), async profiler, Java Flight Recorder + Mission Control."},{"id":"14-interview-qa","heading":"14. Interview Q&A","level":2,"text":"Q1. Difference between monitoring and observability? Monitoring tracks predefined health (known unknowns); observability lets you investigate novel problems from metrics/logs/traces (unknown unknowns). Q2. The three pillars and how they work together? Metrics (aggregate trends) alert you; traces localize the slow/failing hop; logs explain why — pivot between them via trace/correlation IDs. Q3. Push vs pull metrics; why Prometheus pulls? Prometheus scrapes targets — simpler service discovery, easy health detection (scrape fails = down), no client side buffering. Push (via Pushgateway) is for short lived jobs. Q4. How do you trace a request across microservices? Propagate trace context in headers (W3C ) via OpenTelemetry; each service emits spans to a backend (Jaeger) that assembles the full trace by traceId. Q5. Walk me through debugging a slow endpoint in prod. Scope it on dashboards → open a distributed trace to find the slow span → drill into that layer (DB/downstream/JVM/saturation) → read logs by trace id → fix and verify. (See §12.) Q6. How do you find a memory leak in Java? Heap dump ( /auto on OOM) → analyze dominator tree in MAT → look for unbounded caches, ThreadLocals, listeners; watch RSS/heap trend over time. Q7. SLI vs SLO vs SLA and error budget? SLI = measured metric, SLO = internal target, SLA = customer contract. Error budget = allowed unreliability (100−SLO); spend it on velocity, protect it when burning fast. Q8. What should you alert on? User impacting symptoms (SLO/error rate/latency burn), actionable with runbooks — not raw resource metrics that don't imply impact. Q9. RED vs USE? RED (Rate, Errors, Duration) for request services; USE (Utilization, Saturation, Errors) for resources/queues. Q10. Why log to stdout in containers? The platform (Fluent Bit/K8s) collects stdout centrally; apps shouldn't manage log files/rotation. Pair with structured JSON + trace ids."}],"qa":[{"q":"Difference between monitoring and observability?","a":"Monitoring tracks predefined health (known unknowns); observability lets you investigate novel problems from metrics/logs/traces (unknown unknowns)."},{"q":"The three pillars and how they work together?","a":"Metrics (aggregate trends) alert you; traces localize the slow/failing hop; logs explain why — pivot between them via trace/correlation IDs."},{"q":"Push vs pull metrics; why Prometheus pulls?","a":"Prometheus scrapes targets — simpler service discovery, easy health detection (scrape fails = down), no client-side buffering. Push (via Pushgateway) is for short-lived jobs."},{"q":"How do you trace a request across microservices?","a":"Propagate trace context in headers (W3C `traceparent`) via OpenTelemetry; each service emits spans to a backend (Jaeger) that assembles the full trace by traceId."},{"q":"Walk me through debugging a slow endpoint in prod.","a":"Scope it on dashboards → open a distributed trace to find the slow span → drill into that layer (DB/downstream/JVM/saturation) → read logs by trace id → fix and verify. (See §12.)"},{"q":"How do you find a memory leak in Java?","a":"Heap dump (`jmap`/auto-on-OOM) → analyze dominator tree in MAT → look for unbounded caches, ThreadLocals, listeners; watch RSS/heap trend over time."},{"q":"SLI vs SLO vs SLA and error budget?","a":"SLI = measured metric, SLO = internal target, SLA = customer contract. Error budget = allowed unreliability (100−SLO); spend it on velocity, protect it when burning fast."},{"q":"What should you alert on?","a":"User-impacting symptoms (SLO/error-rate/latency burn), actionable with runbooks — not raw resource metrics that don't imply impact."},{"q":"RED vs USE?","a":"RED (Rate, Errors, Duration) for request services; USE (Utilization, Saturation, Errors) for resources/queues."},{"q":"Why log to stdout in containers?","a":"The platform (Fluent Bit/K8s) collects stdout centrally; apps shouldn't manage log files/rotation. Pair with structured JSON + trace ids."}]},"24_JVM_Performance_Advanced_Concurrency.md":{"title":"JVM Performance & Advanced Concurrency","trackId":"java-core","trackTitle":"Java","sections":[{"id":"jvm-performance-advanced-concurrency-deep-dive","heading":"JVM Performance & Advanced Concurrency — Deep Dive","level":1,"text":""},{"id":"gc-tuning-diagnostics-concurrency-beyond-synchronized","heading":"GC Tuning, Diagnostics & Concurrency Beyond synchronized","level":3,"text":""},{"id":"1-jvm-memory-areas","heading":"1. JVM Memory Areas","level":2,"text":"Theory. When debugging OOM or leaks, you need to know where memory lives. The heap holds your objects; stacks hold method call context; Metaspace holds class definitions. A \"Java heap OOM\" and \"Metaspace OOM\" have different causes and fixes. How it works. Heap: All objects from . GC manages this. caps it. Two generations: Young (Eden + Survivors) for new objects, Old for long lived. Stack (per thread): Each method call = one frame with local variables and references. Default 1MB per platform thread ( ). Deep recursion → . References point to heap objects; primitives live directly on the stack. Metaspace: Class metadata, method bytecode, static fields. Grows in native memory (outside ). Classloader leaks (redeploy without GC) fill Metaspace. Code Cache: JIT compiled native code. Rarely tuned unless \"CodeCache is full\" errors appear. Example. — the object is on the heap; the reference variable is in the current stack frame. When the method returns, the frame is popped; if nothing else references that , it becomes GC eligible. Pitfall. Container OOMKilled with \"heap only 60% used\" → total process memory (heap + Metaspace + thread stacks × count + direct buffers) exceeded the cgroup limit. Heap is where GC works and where leaks live. Metaspace grows in native memory — leaks here come from classloader churn (e.g., redeploys, dynamic proxies). flavors: , , , ."},{"id":"2-generational-gc","heading":"2. Generational GC","level":2,"text":"Theory. Allocating and collecting every object in one big heap would be slow. Generational hypothesis: most objects die within milliseconds (temp strings, request wrappers). Collect the young area often (cheap); touch old area rarely (expensive). The weak generational hypothesis : most objects die young. So the heap is split: How it works — Minor GC step by step: 1. Application allocates in Eden until full. 2. Minor GC (Stop The World, but short): GC roots (stack refs, static fields) mark reachable objects in Eden + active Survivor. 3. Live objects copied to the other Survivor space; age incremented. Dead objects in Eden discarded — no compaction needed in Eden (copying collector). 4. Survivors that exceed MaxTenuringThreshold (default 15) move to Old Gen . 5. When Old Gen fills → Full GC — marks entire heap, compacts, long pause. Example. A REST handler creates 50 short lived objects per request. After the response, all 50 are unreachable. Minor GC reclaims them without touching Old Gen — pause might be 5–20ms vs 500ms+ for Full GC. New objects allocate in Eden ; a minor GC copies survivors between survivor spaces, aging them. Objects surviving enough cycles are promoted to Old gen. Minor GC = clean Young (fast). Full GC = whole heap (stop the world, expensive — minimize these)."},{"id":"3-garbage-collectors","heading":"3. Garbage Collectors","level":2,"text":"Theory. A GC collector decides when and how to reclaim unreachable objects. Trade off: throughput (total work done) vs latency (pause length). Batch jobs want throughput; payment APIs want low tail latency. GC Best for Trait Serial Tiny heaps, single core One thread, STW Parallel (Throughput) Batch jobs Max throughput, longer pauses CMS (removed) Legacy Concurrent, deprecated G1 (default since 9) General server apps Region based, predictable pauses ZGC Large heaps, low latency Sub millisecond pauses, scales to TBs Shenandoah Low latency Concurrent compaction (Red Hat) Modern default = G1 . For latency critical / large heap services, ZGC ( , generational ZGC in Java 21+). Interview angle. Serial = one GC thread, small heaps. Parallel = multiple GC threads, longer pauses acceptable. G1 = region based, targets pause time. ZGC/Shenandoah = concurrent relocation, sub ms pauses at cost of some throughput overhead."},{"id":"4-g1-vs-zgc","heading":"4. G1 vs ZGC","level":2,"text":"Theory. G1 is the balanced default — good for most Spring services with 2–16GB heaps. ZGC is for when p99/p999 latency SLAs matter or heaps are huge (100GB+), because it keeps pauses under 1ms regardless of heap size. G1 (Garbage First): Divides heap into equal regions (1–32MB each); regions can be Eden, Survivor, or Old dynamically. Maintains a remembered set of cross region references so it doesn't scan the entire heap. Collects regions with the most garbage first (Garbage First) to meet target. Pauses typically 10–200ms depending on heap and live set. ZGC: Uses colored pointers (metadata bits in address) and load barriers — when your code reads a reference, the barrier may fix it if the object was moved. Concurrent relocation: moves objects while application threads run; only brief pauses for root scanning. Pauses < 1ms even on terabyte heaps. Trade off: 15% throughput overhead vs G1 on some workloads. How to choose: Scenario Pick Typical microservice, 4GB heap, p99 < 500ms OK G1 (default) Large heap (64GB+), strict p99 < 50ms ZGC Batch ETL, maximize throughput Parallel GC G1 (Garbage First): Divides heap into equal regions ; collects regions with the most garbage first. Targets a pause goal ( ). Great all rounder; pauses typically tens–low hundreds of ms. ZGC: Concurrent marking + relocation using colored pointers / load barriers → pauses < 1ms regardless of heap size (GBs–TBs). Choose it when tail latency (p99/p999) matters or heaps are very large. Rule: don't tune blindly — measure pauses/throughput first, change one thing, re measure."},{"id":"5-reading-gc-logs","heading":"5. Reading GC Logs","level":2,"text":"Theory. GC logs tell you whether performance problems are allocation pressure (too many short lived objects), undersized heap (frequent Full GC), or leaks (heap after GC keeps growing). Without logs you're guessing. How to read a typical G1 line: — 42nd GC event — Minor GC (Young gen only) — heap used before → after (512M live before, 128M after — 384M reclaimed) — current heap capacity — pause duration (application threads stopped) Example — leak signature: Over 24 hours, goes 1.2GB → 1.5GB → 1.8GB → 2.1GB while traffic is flat. GC can't reclaim — something is retaining objects. What to look for: Pause times — are they within your latency budget? Spikes? Frequency — frequent Full GCs = heap too small or a leak. Promotion / allocation rate — high churn means lots of short lived garbage. Heap after GC trending up over time → memory leak (GC can't reclaim). Tools: GCeasy.io , GCViewer to visualize. Symptom mapping: long STW pauses → tune collector / pause goal; rising post GC heap → leak; frequent Full GC → undersized Old gen or leak."},{"id":"6-jvm-flags","heading":"6. JVM Flags","level":2,"text":"Theory. JVM flags control heap size, GC algorithm, and diagnostic behavior. Wrong flags cause resize pauses ( ≠ ), OOM in containers (fixed ignoring cgroup limits), or missing artifacts when debugging production incidents. In containers, prefer over fixed so the JVM respects cgroup limits (modern JDKs are container aware). Always enable HeapDumpOnOutOfMemoryError in prod — a leak is unfixable without the dump."},{"id":"7-heap-dumps-leaks","heading":"7. Heap Dumps & Leaks","level":2,"text":"Theory. A heap dump is a photograph of every object in memory at one instant — who references whom. It's the primary artifact for answering \"why is my heap 8GB?\" Leak hunting compares dumps over time or analyzes dominators after OOM. A heap dump is a snapshot of all objects — the primary leak hunting artifact. How it works — dominator tree: Object A dominates B if every path from GC roots to B goes through A. The dominator tree shows the retention hierarchy — the biggest retained set at the top is usually your leak (e.g., a cache holding millions of entries). Worked example — ThreadLocal leak: Analyze in Eclipse MAT : Dominator tree — what retains the most memory. Leak Suspects report. Compare two dumps over time to see what grows. Common Java leaks: Unbounded caches / collections that only ever grow. s not removed (esp. in pooled threads) → also classloader leaks. Listeners/callbacks never deregistered. Large object graphs held by long lived references; keys with broken ."},{"id":"8-thread-dumps-cpu-profiling","heading":"8. Thread Dumps & CPU Profiling","level":2,"text":"Theory. When a service is slow but not crashed , thread dumps show what every thread is doing right now. Multiple dumps seconds apart distinguish transient waits from permanent bottlenecks. CPU profiling shows where time is spent, not just that threads are busy. A thread dump shows every thread's stack + state — for hangs, deadlocks, high CPU. How to read a thread entry: BLOCKED waiting for a lock held by exec 3 → lock contention at Same method in many RUNNABLE threads across 3 dumps → CPU hotspot (tight loop or heavy computation) Example workflow for \"CPU at 100%\": 1. Take 3 thread dumps 5 seconds apart. 2. Find threads with high time stuck in the same method. 3. Run async profiler on that method for a flame graph. 4. Check GC logs — sometimes high CPU is GC thrashing, not application code. Read it: Many BLOCKED threads on the same lock → contention bottleneck. The JVM prints \"Found one Java level deadlock\" with the cycle. RUNNABLE threads repeatedly in the same method across dumps → CPU hotspot. CPU profiling: async profiler (flame graphs, low overhead) or Java Flight Recorder ( , view in JDK Mission Control) to find where CPU/allocations go."},{"id":"9-diagnostic-tooling","heading":"9. Diagnostic Tooling","level":2,"text":"Theory. Production JVM problems fall into three buckets: memory (heap/Metaspace), threads (deadlock/contention), and CPU (hot methods). Different tools attach to each without redeploying — critical when you can't restart prod to add debug logging. Tool Use List JVMs Thread dump (deadlock, hang, CPU) Heap dump / histogram ( ) Swiss army: GC, threads, flags, JFR Live GC stats Arthas Attach to a running JVM — trace methods, watch args, decompile, profile — no restart async profiler Flame graphs (CPU, alloc, lock) JFR + JMC Low overhead always on profiling MAT Heap dump analysis Arthas is a senior favorite: , , , on a live prod JVM without redeploying."},{"id":"10-threadpoolexecutor-tuning","heading":"10. ThreadPoolExecutor Tuning","level":2,"text":"Theory. Thread pool tuning is backpressure design : how does your system behave when overloaded? A bounded queue + rejection policy answers that — grow threads, block the caller, or fail fast — instead of silently consuming all memory. Sizing: CPU bound ≈ cores; I/O bound ≈ cores × (1 + wait/compute). Never use / in prod (unbounded queue/threads → OOM). Task flow: < core → new thread; else enqueue; queue full & < max → new thread; full & at max → reject. (Unbounded queue ⇒ max never reached.) Always + ; isolate subsystems with separate pools ( bulkhead )."},{"id":"11-completablefuture","heading":"11. CompletableFuture","level":2,"text":"Always pass an explicit executor to — the default is shared JVM wide and blocking on it starves parallel streams and other tasks."},{"id":"12-virtual-threads","heading":"12. Virtual Threads","level":2,"text":"Theory. Virtual threads let you scale I/O bound services without reactive programming complexity. The JVM multiplexes many virtual threads onto few OS threads, unmounting on blocking I/O. Java 21 (Loom) — millions of cheap threads mounted onto a few carrier threads. How it works. Carrier threads (≈ ) run virtual thread code. On blocking I/O, the virtual thread yields its carrier. Pinning happens when code holds a monitor during blocking — the carrier can't switch. doesn't pin. Interview angle. \"Should I replace my thread pool with virtual threads?\" → For I/O bound request handling, yes (one virtual thread per task). For CPU bound work or strict pool sizing for backpressure, keep bounded platform pools. Best for I/O bound services: write simple blocking code that scales massively; a blocked virtual thread unmounts its carrier. Not for CPU bound work (still bounded by cores). Pinning pitfall: a virtual thread blocking inside a block can't unmount → it pins the carrier. Use instead of around blocking calls. Don't pool virtual threads (one per task); be careful with heavy ThreadLocals at scale."},{"id":"13-lock-free-atomics","heading":"13. Lock-Free & Atomics","level":2,"text":"Theory. Locks block threads — context switches and priority inversion under contention. Lock free algorithms use CAS to retry until success, so at least one thread always makes progress. You trade lock overhead for retry loops under heavy contention. How it works — CAS at the CPU level: If another thread changed between read and CAS, the CAS fails and the loop retries with the fresh value. Example — why LongAdder beats AtomicLong under contention: — all threads CAS the same memory word → cache line bouncing. — each thread updates its own cell , summed on read → spreads contention. ABA problem: Thread A reads A, Thread B changes A→B→A, Thread A's CAS(A, new) succeeds even though the structure changed. Fix: with a version counter. CAS (Compare And Swap) = lock free atomic update; spins on failure rather than blocking. / spread contention across cells — use for hot counters. ABA problem: value goes A→B→A and CAS wrongly succeeds → use . Concurrent collections ( ) use fine grained locking/CAS; prefer atomic compound ops ( , ) over check then act."},{"id":"14-jit-escape-analysis-warmup","heading":"14. JIT, Escape Analysis & Warmup","level":2,"text":"Theory. Java starts interpreted (slow, fast startup) and gets faster as the JIT compiles hot methods to native machine code. This is why the first 1000 requests in a service are slower than the next 10,000 — and why micro benchmarks without warmup lie. How it works — tiered compilation: 1. Interpreter runs everything initially. 2. C1 (client) compiler — quick, less optimized native code for warm methods. 3. C2 (server) compiler — aggressive optimizations (inlining, loop unrolling) for the hottest methods. 4. Methods that aren't hot stay interpreted — cold code paths don't pay compilation cost. Escape analysis: If the JIT proves an object never escapes a method (no return, no store to field, no pass to other threads), it may: Stack allocate instead of heap allocating (eliminates GC pressure). Scalar replace — break object fields into separate local variables. Lock elision — remove on objects that can't be shared. Example: Pitfall. Benchmarking with (interpret only) or before warmup gives misleading numbers. Use JMH with warmup iterations. For serverless/cold start, consider CDS/AppCDS (class data sharing) or GraalVM native image. The JVM interprets bytecode first, then the JIT compiles hot methods to native code (C1/C2 tiered compilation) → why Java gets faster after warmup . Escape analysis can stack allocate or scalar replace objects that don't escape a method, eliminating heap allocation/locks (lock elision). Implications: micro benchmarks must warm up (use JMH ); cold start latency matters for serverless (consider AOT/GraalVM native image, or CDS/AppCDS to speed startup)."},{"id":"15-interview-qa","heading":"15. Interview Q&A","level":2,"text":"Q1. Walk through what happens in a minor GC. Live objects in Eden + active survivor are copied to the other survivor space and aged; dead objects are reclaimed; long lived ones get promoted to Old gen. It's fast and stop the world but short. Q2. G1 vs ZGC — when do you pick ZGC? ZGC for very large heaps and strict tail latency (sub ms pauses); G1 is the balanced default for typical server apps with a pause goal. Q3. How do you diagnose a memory leak? Enable HeapDumpOnOutOfMemoryError, take/compare heap dumps, analyze the dominator tree in MAT; watch post GC heap trending up. Usual causes: unbounded caches, ThreadLocals, listeners. Q4. High CPU in prod — your steps? Multiple thread dumps to find hot RUNNABLE threads, async profiler/JFR flame graph; check GC logs for GC driven CPU; look for tight loops, lock contention, serialization hotspots. Q5. Why avoid Executors.newFixedThreadPool in prod? Unbounded grows until OOM under overload. Build a ThreadPoolExecutor with a bounded queue + rejection policy. Q6. When do virtual threads help and what's the pinning issue? They help I/O bound concurrency (cheap, unmount on blocking). Pinning: blocking inside keeps the carrier busy — use ReentrantLock instead. Q7. What is CAS and the ABA problem? Lock free compare and swap; ABA is when a value changes away and back, fooling CAS — fix with a version stamp (AtomicStampedReference). Q8. Why is Java \"slow then fast\"? JIT compiles hot paths to native code after profiling at runtime (tiered compilation) — performance improves after warmup; benchmark with JMH. Q9. Container OOMKilled but heap looks fine — why? Total process memory (heap + Metaspace + thread stacks + direct buffers + native) exceeded the container limit. Size with MaxRAMPercentage and account for off heap. Q10. How do you debug a deadlock? Take a thread dump ( / ); the JVM reports the deadlock cycle and the locks involved; fix via consistent lock ordering or tryLock with timeout."}],"qa":[{"q":"Walk through what happens in a minor GC.","a":"Live objects in Eden + active survivor are copied to the other survivor space and aged; dead objects are reclaimed; long-lived ones get promoted to Old gen. It's fast and stop-the-world but short."},{"q":"G1 vs ZGC — when do you pick ZGC?","a":"ZGC for very large heaps and strict tail-latency (sub-ms pauses); G1 is the balanced default for typical server apps with a pause goal."},{"q":"How do you diagnose a memory leak?","a":"Enable HeapDumpOnOutOfMemoryError, take/compare heap dumps, analyze the dominator tree in MAT; watch post-GC heap trending up. Usual causes: unbounded caches, ThreadLocals, listeners."},{"q":"High CPU in prod — your steps?","a":"Multiple thread dumps to find hot RUNNABLE threads, async-profiler/JFR flame graph; check GC logs for GC-driven CPU; look for tight loops, lock contention, serialization hotspots."},{"q":"Why avoid Executors.newFixedThreadPool in prod?","a":"Unbounded `LinkedBlockingQueue` grows until OOM under overload. Build a ThreadPoolExecutor with a bounded queue + rejection policy."},{"q":"When do virtual threads help and what's the pinning issue?","a":"They help I/O-bound concurrency (cheap, unmount on blocking). Pinning: blocking inside `synchronized` keeps the carrier busy — use ReentrantLock instead."},{"q":"What is CAS and the ABA problem?","a":"Lock-free compare-and-swap; ABA is when a value changes away and back, fooling CAS — fix with a version stamp (AtomicStampedReference)."},{"q":"Why is Java \"slow then fast\"?","a":"JIT compiles hot paths to native code after profiling at runtime (tiered compilation) — performance improves after warmup; benchmark with JMH."},{"q":"Container OOMKilled but heap looks fine — why?","a":"Total process memory (heap + Metaspace + thread stacks + direct buffers + native) exceeded the container limit. Size with MaxRAMPercentage and account for off-heap."},{"q":"How do you debug a deadlock?","a":"Take a thread dump (`jstack`/`jcmd`); the JVM reports the deadlock cycle and the locks involved; fix via consistent lock ordering or tryLock with timeout."}]},"25_Redis_Caching_Strategy.md":{"title":"Redis & Caching Strategy","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"redis-caching-strategy-deep-dive","heading":"Redis & Caching Strategy — Deep Dive","level":1,"text":""},{"id":"caching-patterns-pitfalls-redis-as-more-than-a-cache","heading":"Caching Patterns, Pitfalls & Redis as More Than a Cache","level":3,"text":""},{"id":"1-why-cache","heading":"1. Why Cache","level":2,"text":"A cache stores hot data in fast memory to cut latency and offload the database. Redis is an in memory, single threaded, key value data structure store with sub millisecond latency. Theory. Without a cache, every read hits disk backed storage. A typical PostgreSQL query on indexed data costs 1–10 ms ; under load that climbs fast. Redis reads from RAM in 0.1–1 ms (same datacenter). At 10,000 reads/sec, moving 80% of traffic from DB to cache can drop DB load from 10K to 2K QPS — often the difference between a stable system and one that falls over during a traffic spike. What problem it solves. Caching is a read amplifier reducer : you pay memory to avoid repeating expensive work (SQL joins, aggregation, external API calls). It does not make writes faster by itself — that requires different patterns (write behind, queues). Cache when: reads ≫ writes, data is expensive to compute/fetch, slight staleness is acceptable. Don't cache: strongly consistent financial balances at read time, rarely read data, or when staleness is dangerous. Example. A product catalog service serves 50K product detail reads/sec but only 200 updates/sec. Each DB read is a 3 table join ( 8 ms). Caching product JSON in Redis with a 5 minute TTL cuts p99 latency from 40 ms to 2 ms and removes 98% of DB reads. The 200 writes/sec still go to DB; cache entries are invalidated on update. Why single threaded is fine: Redis avoids lock overhead and context switches; it's network/memory bound, and command execution is atomic. (Redis 6+ has multi threaded I/O for the network layer.) How it works. One Redis thread executes commands from a queue. Because each command is atomic, you get race free , , and Lua scripts without application level locks. Bottleneck is usually network RTT or memory bandwidth , not CPU — pipelining multiple commands per round trip is the main throughput lever. Pitfall. Caching everything \"just in case\" wastes RAM and creates consistency headaches. Cache only data with proven read heavy "},{"id":"2-data-structures","heading":"2. Data Structures","level":2,"text":"Redis is \"data structures over the network\" — pick the right one: Type Use case String Simple KV, counters ( ), serialized objects Hash Object fields ( ) List Queues, recent items ( / ) Set Unique membership, tags Sorted Set (ZSet) Leaderboards, rate limiters, time ordered ( ) Bitmap / HyperLogLog Presence flags, approx unique counts Stream Append only log / lightweight messaging Geo Location queries Choosing the right structure (e.g., ZSet for a sliding window rate limiter) is the difference between elegant and hacky. How it works (pick the right tool). String — simplest; stores serialized JSON blobs. / are atomic — ideal for counters without read modify write races. Hash — one key, many fields. Updating does not rewrite the whole object (unlike replacing a JSON string). Good for partial updates on profiles. List — doubly linked list; + gives a simple queue. Not durable by default for critical jobs (use a message broker for that), but fine for lightweight work queues. Set / Sorted Set — Set = O(1) membership. ZSet = Set + score for ordering. Rate limiters store timestamps as scores; leaderboards store points as scores. Bitmap / HyperLogLog — Bitmap: one bit per user for \"logged in today\" with minimal memory. HyperLogLog: 12 KB per key for millions of unique visitors with 0.81% error — trade exact count for 100× less RAM. Stream — append only log with consumer groups (like a lightweight Kafka partition). Useful for event fan out when you don't need full Kafka durability. Example. Tracking daily active users for 10M users: a Bitmap uses 1.25 MB ( ). Storing per user would need gigabytes of key overhead. Interview angle. \"How would you implement a leaderboard?\" → ZSet: , top 10 = . O(log N) insert, O(log N + M) range query."},{"id":"3-caching-patterns","heading":"3. Caching Patterns","level":2,"text":"Two axes: who reads/writes the DB and when . Cache aside — app manages cache (most common). Read through — cache library loads from DB on miss. Write through — write cache + DB synchronously. Write behind (write back) — write cache now, DB async later (fast but risk loss). Theory. These patterns differ in who owns consistency and when the DB is touched . Cache aside puts logic in your app; read/write through puts it in a cache library or sidecar. Pattern Read path Write path Best when Cache aside App → cache → DB on miss App → DB → invalidate cache General purpose; most Spring apps Read through Cache loads from DB on miss App → DB (cache not updated on write unless paired) You want simpler read code Write through Same as cache aside for reads App → cache + DB sync Writes must be immediately readable from cache Write behind Cache aside for reads App → cache; async DB flush Write spikes; loss tolerable Interview angle. Interviewers often ask \"which pattern for an e commerce product page?\" → Cache aside: reads dominate, stale price for 30s may be OK with TTL + invalidation on price change. Not write through (every inventory tick would double write)."},{"id":"4-cache-aside","heading":"4. Cache-Aside","level":2,"text":"The default pattern. The application code owns cache logic. Theory. Also called lazy loading : the cache is populated only when someone asks for the data. The application is the orchestrator — Redis does not call your DB for you. How it works (read path). 1. App checks cache. 2. Hit → return immediately (no DB). 3. Miss → load from DB, write to cache with TTL, return. 4. If the key is absent in DB, optionally cache a null marker (see §9) to avoid repeated misses. How it works (write path). 1. App writes to DB (source of truth). 2. App deletes the cache key (invalidation). 3. Next read repopulates from DB. Example. 1M users, 10K DAU, each reads profile 5×/day = 50K reads/day. Without cache: 50K DB queries. With cache (10 min TTL, 90% hit rate after warm up): 5K DB queries/day. Cold start after deploy: hit rate starts at 0% and climbs over one TTL window. Pitfall. Two concurrent misses for the same key both hit the DB (no coalescing by default). Under expiry of a hot key this becomes a stampede (§8). Pros: simple, resilient (cache down ⇒ DB still served), only caches what's used. Cons: first read is a miss; risk of stale data until TTL/invalidation. Interview angle. \"What if Redis is down?\" → Cache aside degrades gracefully: every read goes to DB (higher latency, DB load spike — but no outage). Write through tied to cache availability is riskier."},{"id":"5-write-through-write-behind","heading":"5. Write-Through & Write-Behind","level":2,"text":"Write through: every write goes to cache and DB synchronously. Pro: cache always fresh. Con: write latency = cache + DB; caches data that may never be read. Write behind: write to cache immediately, flush to DB asynchronously (batched). Pro: very fast writes, absorbs spikes. Con: data loss risk if cache dies before flush; eventual consistency. Use only when some loss is tolerable (metrics, counters). How it works (write through). On , the app writes to Redis and DB in sequence (or via a cache library that wraps both). The next read always hits a warm, fresh cache entry — no invalidation step. Write latency = ( 1 ms + 5 ms = 6 ms vs 5 ms DB only). How it works (write behind). On , the app increments a counter in Redis and returns. A background worker batches flushes to DB every N seconds or M records. If Redis crashes before flush, those increments are lost. Example (write behind). Analytics: 100K page view writes/sec. Writing each to PostgreSQL would saturate the DB. Buffer in Redis ( ), flush aggregated counts every 10s → 100K Redis ops/sec but only 6 DB writes/min per article. Pitfall (write through). Admin updates a config flag once/day; 1M users never read it. Write through caches it anyway — wasted RAM. Cache aside would never populate it. Pitfall (write behind). Using write behind for order payment status → Redis dies → payment marked \"pending\" in cache but never persisted → financial inconsistency. Never use write behind for authoritative business state. Interview angle. \"When write behind?\" → Metrics, click counts, IoT sensor aggregates where approximate counts or small loss windows are acceptable and write volume is extreme."},{"id":"6-eviction-policies","heading":"6. Eviction Policies","level":2,"text":"When Redis hits , it evicts per policy ( ): Policy Behavior Reject writes (errors) — safe default for a DB like use Evict least recently used across all keys — typical cache choice Evict least frequently used — better when popularity is stable Evict only keys with a TTL Random For a pure cache, (or ) + a limit is standard. Set to 75% of available RAM. How it works. Redis tracks access patterns per key. When exceeds : LRU (Least Recently Used) — evicts keys not accessed recently. Redis approximates LRU with a sampling pool (checks a random subset, evicts oldest) — not perfect LRU but fast and good enough. LFU (Least Frequently Used, Redis 4.0+) — evicts keys with lowest access frequency. Better when popularity is stable (e.g., top 100 products stay hot for days). volatile policies — only consider keys that have a TTL set. Use when some keys (session locks) must never be evicted but cache entries have TTL. noeviction — returns errors on write when full. Use when Redis is a primary store (job queue, session store) and losing keys is unacceptable. Example. Redis with 4 GB RAM, , . You cache 500K product objects ( 6 KB each ≈ 3 GB). A new product launch adds 50K entries → Redis evicts least recently used products until under limit. Cold products fall out; hot products stay. Pitfall. No limit → Redis grows until the OS OOM killer terminates the process → total cache loss → avalanche on DB (§9). Interview angle. \"LRU vs LFU?\" → LRU adapts quickly to shifting hot sets (news site during breaking story). LFU resists one time spikes evicting long term hot keys."},{"id":"7-ttl-design","heading":"7. TTL Design","level":2,"text":"Every cache entry should usually have a TTL (expiry) so stale data self heals and memory is bounded. Theory. TTL is your staleness budget : how long can readers see old data without an explicit invalidation? It also bounds memory — expired keys are deleted (lazy on access + active background scanning). How it works. Redis expires keys two ways: (1) passive — key accessed after expiry → deleted on read; (2) active — background thread samples keys with TTL and removes expired ones. A key can live slightly past its TTL until accessed or sampled — don't rely on sub second precision for correctness. Short TTL = fresher data, more DB load; long TTL = less load, more staleness. Tune per data volatility. Add jitter to TTLs (e.g., 600 ± random 60s) so many keys don't expire simultaneously (→ avalanche, §9). Use logical expiry (store an field, refresh in background) for hot keys you never want to hard miss. Example (jitter). 10,000 product keys all cached at deploy with . Without jitter they expire at T+600s together → 10K simultaneous DB misses. With jitter : expirations spread over 2 minutes → peak DB load 1/15th as high. Example (logical expiry). Store in the cached value. On read: if , return data. If but data still present, return stale data and trigger async refresh (one thread rebuilds). User never waits for DB on a hot key. Pitfall. TTL alone without invalidation on write → user updates profile, other users see old data until TTL expires. Always pair TTL with delete on write for mutable data. Interview angle. \"How do you pick TTL?\" → Measure data change frequency. Config that changes hourly → TTL 5–10 min + invalidation on admin update. User session → TTL 30 min sliding. Static product description → TTL 1 hour+."},{"id":"8-cache-stampede-hot-keys","heading":"8. Cache Stampede & Hot Keys","level":2,"text":"Cache stampede (dogpile): a popular key expires → thousands of concurrent requests miss and all hit the DB at once → DB overload. How it works (stampede timeline). 1. T=0: Key expires (TTL reached). 2. T=0+1ms: 500 concurrent requests for product 42 — all cache miss. 3. All 500 threads query DB for the same product (500 identical heavy queries). 4. DB connection pool exhausted; p99 latency spikes from 10 ms to 2 s; other endpoints suffer. Fixes: Mutex/lock : first requester rebuilds; others wait or serve stale (e.g., Redis rebuild lock). Logical expiry + async refresh : serve slightly stale data while one worker refreshes. Request coalescing (single flight) at the app layer. Hot key: one key gets disproportionate traffic, overloading a single shard. How it works (hot key). In Redis Cluster, key maps to one hash slot on one node. 1M reads/sec on that key → one node handles all traffic while other nodes sit idle. Fixes: local in process cache (L1) in front of Redis (L2), key replication/sharding of the hot value, or client side caching. Example (hot key mitigation). Split into … with identical value; clients pick random suffix. Reads spread across 10 slots/nodes. Writes update all 10 keys. Example (L1 + L2). Caffeine cache in JVM (1s TTL, 10K entries) → Redis (5 min TTL) → DB. A viral post hit 100K times/sec: 99% served from in process memory, 0.9% from Redis, 0.1% from DB. Interview angle. \"How do you detect hot keys?\" → Monitor per key QPS in Redis ( in dev; or vendor metrics in prod). Sudden single key spike + high CPU on one cluster node = hot key."},{"id":"9-penetration-avalanche","heading":"9. Penetration & Avalanche","level":2,"text":"Cache penetration: queries for keys that don't exist (often malicious) always miss and hit the DB. Fix: cache the null/empty result (short TTL) and/or a Bloom filter to reject non existent keys fast. Cache avalanche: many keys expire at the same time (or Redis restarts) → mass DB load. Fix: TTL jitter , multi level cache, and Redis HA so it doesn't all vanish. How it works (penetration). Attacker sends , , … for IDs that don't exist. Cache aside: every request misses (nothing to cache), DB runs every time. 10K bogus IDs/sec = 10K DB queries/sec that return empty. Fix (cache nulls). Fix (Bloom filter). Pre load all valid user IDs into a Bloom filter ( 10 bits per ID). On read: if Bloom says \"definitely not in set\" → return 404 without touching DB or Redis. False positives ( 1%) still hit DB — tunable via filter size. How it works (avalanche). Scenario A: 50K keys share TTL=3600, all set at same deploy → expire together. Scenario B: Redis primary fails, replica promoted with cold cache → every request misses until cache warms. DB sees thundering herd similar to stampede but across many keys. Example. E commerce flash sale: 20K product keys cached with identical 5 min TTL. At expiry, checkout service fires 20K DB reads in 1 second. Fix: jitter TTLs + pre warm cache before expiry via background refresh at T 30s. Interview angle. Penetration = non existent keys . Avalanche = mass expiry or cache loss . Different root causes; both end at DB overload but fixes differ (Bloom/null cache vs jitter/HA/pre warm)."},{"id":"10-cache-consistency","heading":"10. Cache Consistency","level":2,"text":"The cache and DB can drift. Invalidate, don't update the cache, and order operations carefully. Theory. The DB is the source of truth; the cache is a derived copy . Updating the cache directly on write risks writing stale data if the DB write fails or if two writers race. Recommended: update DB → delete cache key (cache aside repopulates on next read). Deleting is safer than writing a possibly stale value. Race: reader loads old DB value and writes cache after a concurrent invalidate. Mitigate with short TTL (bounds staleness), delayed double delete, or versioned keys. For cross service freshness, publish an invalidation event (Kafka) so all caches drop the key. Accept that caches are eventually consistent — don't cache data that must be strictly correct on every read. How it works (the classic race). 1. Thread A: read miss → starts DB read for user 1. 2. Thread B: update user 1 in DB → deletes cache key. 3. Thread A: DB returns old value (read started before B's commit, or READ COMMITTED sees pre update row). 4. Thread A: writes old value into cache. 5. Next 10 minutes (TTL): all reads get stale data until TTL expires or another invalidation. Fix (delayed double delete). After DB update + cache delete, schedule a second delete after 500 ms — catches the late write from step 4. Fix (versioned keys). Cache key = . On update, increment version in DB to v6. Old cache key is orphaned and expires via TTL; new reads use . Example (cross service). Order service updates order status in DB, publishes event. Inventory and notification services consume event and delete their local cache keys — no stale reads across services. Interview angle. \"Can you guarantee strong consistency with Redis cache?\" → Not without distributed transactions or reading through DB every time. Practical answer: invalidation + short TTL + version keys; accept bounded staleness."},{"id":"11-distributed-lock","heading":"11. Distributed Lock","level":2,"text":"Theory. When multiple app instances run the same cron job or compete to rebuild a cache key, you need mutual exclusion across JVMs . Redis (set if not exists) gives a cheap distributed lock — but it has caveats. How it works. is atomic in Redis: only one client succeeds if the key is absent. Others get . The unique (UUID) identifies the lock holder. Release must be conditional — otherwise you might delete a lock re acquired by another process after yours expired. Always set a TTL (crashed holder won't deadlock). Fence with a token/version to survive \"lock expired mid work\". Redisson provides with auto renewal (watchdog) and reentrancy. For correctness critical locking prefer a consensus store (ZooKeeper/etcd); Redlock (multi node) is debated. Example. Cache rebuild: 500 threads miss . First thread succeeds → rebuilds. Others fail SET → wait 100 ms and retry cache GET (now populated) or return stale. Pitfall (lock expiry). Thread acquires lock (30s TTL), GC pause lasts 35s, work resumes, thread deletes lock — but another thread already acquired it. Token check in Lua prevents deleting the wrong holder's lock. Pitfall (Redlock debate). Martin Kleppmann showed edge cases with clock drift and multi master failover. For payment idempotency or inventory, prefer DB row locks or etcd — Redis lock is fine for cache rebuild coordination. (Covered in depth in the Resilience guide §9.) Interview angle. \"Is Redis lock enough for payment processing?\" → No for sole correctness mechanism. Use DB or unique constraint + idempotent insert. Redis lock OK as optimization layer with TTL + token."},{"id":"12-rate-limiter","heading":"12. Rate Limiter","level":2,"text":"Redis is ideal for distributed rate limiting (shared counter across instances). Theory. Each API server has its own in memory counter — useless behind a load balancer (user hits server A 50 times + server B 50 times = 100 total, limit is 60). Redis holds the shared state all instances read/write atomically. How it works (fixed window). Window = calendar minute. User sends 100 requests at 00:00:59 and 100 at 00:01:00 → 200 in 2 seconds despite \"100/minute\" limit. Cheap (one key per window) but unfair at boundaries. How it works (sliding window). Store each request timestamp in a ZSet. Count entries in . Accurate rolling 60 second window. Cost: O(log N) per request + memory for each request ID in the window (fine up to few hundred req/min per user). Token bucket can be implemented atomically with a Lua script. Atomicity (single threaded Redis / Lua) avoids race conditions across instances. Example. Free tier: 100 req/min per API key. Key . Sliding window with ZSet; at request 101 in the last 60s return HTTP 429 with (seconds until oldest request falls out of window). Interview angle. \"Fixed vs sliding window?\" → Fixed = simpler, boundary burst problem. Sliding = accurate, more memory. Token bucket = allows controlled bursts (good for APIs that tolerate short spikes)."},{"id":"13-persistence-ha-cluster","heading":"13. Persistence, HA & Cluster","level":2,"text":"Persistence: RDB (point in time snapshots, compact, some loss window) vs AOF (append every write, more durable, larger). Many use both . HA: primary–replica replication + Redis Sentinel for automatic failover. Scale out: Redis Cluster shards keys across nodes via hash slots (16384 slots); multi key ops must share a slot (use hash tags ). Managed: a managed cloud Redis service that handles replication, failover, and patching. How it works (RDB). Fork child process → write memory snapshot to disk ( ). Parent continues serving. Snapshot every N minutes or M writes. Recovery: load RDB file on restart. Data loss window: everything since last snapshot (e.g., up to 5 min). How it works (AOF). Append every write command to a log file. policy trades durability vs speed: (safest, slowest), (default — at most 1s loss), (OS buffer). Recovery: replay log. AOF rewrite compacts log periodically. How it works (replication). Primary streams writes to replicas asynchronously. Replica serves reads (with lag). Sentinel monitors primary; on failure, promotes replica ( 30s failover typical). How it works (Cluster). 16384 hash slots partitioned across master nodes. → slot → node. and share slot — required for multi key transactions on same user. Example. 6 GB dataset, 100K ops/sec. Single node maxes at 80K ops/sec → Redis Cluster with 3 masters (2 GB each) + 3 replicas for HA. Client uses cluster aware driver (Lettuce, JedisCluster). Pitfall. Using Redis as only copy of critical data without AOF/fsync → restart loses everything. Cache use case: acceptable. Session store: enable AOF + replicas. Interview angle. \"When RDB only?\" → Pure cache where cold start + DB repopulate is OK. \"When AOF?\" → Session store, rate limit state you can't rebuild from DB."},{"id":"14-spring-cache","heading":"14. Spring Cache","level":2,"text":"Theory. Spring Cache abstracts cache aside behind annotations. AOP intercepts method calls — you don't write manually. Under the hood it still uses cache aside semantics with a . How it works. → interceptor checks Redis before method body. Miss → executes method → serializes return value to Redis. → deletes key (preferred over on updates to avoid stale writes if DB transaction rolls back). Pitfall. on update writes cache inside the same transaction as DB — if transaction rolls back, cache has new data DB doesn't. Prefer + let next read repopulate. Pitfall. Default serialization (JDK serialization) breaks across deployments. Configure JSON serializer (Jackson) in . gives cache aside with one annotation; configure a for per cache TTLs and serialization (JSON). Example. Per cache TTL: cache 10 min, cache 1 hour — configure separate entries in bean. Interview angle. \"How does handle stampede?\" → It doesn't by default — same as manual cache aside. Add + custom with locking, or use Caffeine L1 + Redis L2."},{"id":"15-interview-qa","heading":"15. Interview Q&A","level":2,"text":"Q1. Cache aside vs write through? Cache aside: app loads on miss and writes DB then invalidates cache (lazy, resilient). Write through: writes go to cache and DB synchronously (always fresh, higher write latency). Q2. How do you keep cache and DB consistent? Update DB then delete the cache key (repopulate on next read); use short TTLs and invalidation events. Caches are eventually consistent — don't cache strict correctness data. Q3. What is a cache stampede and how do you prevent it? A popular key expires and many requests hit the DB at once. Prevent with a rebuild mutex, logical expiry + async refresh, request coalescing, and TTL jitter. Q4. Penetration vs avalanche? Penetration: misses for non existent keys hitting the DB → cache nulls / Bloom filter. Avalanche: mass simultaneous expiry → TTL jitter + HA + multi level cache. Q5. Which eviction policy for a cache? (or ) with a limit. if Redis is used as a primary store and you can't drop data. Q6. Why is Redis fast despite being single threaded? In memory, no lock contention, atomic commands, efficient data structures, and (in 6+) multi threaded network I/O. It's memory/network bound, not CPU bound. Q7. How do you handle a hot key? Add a local L1 cache in front of Redis, replicate/shard the hot value, or use client side caching; identify via monitoring. Q8. RDB vs AOF? RDB = periodic snapshots (compact, possible data loss between snapshots). AOF = log every write (more durable, larger, slower replay). Often enable both. Q9. How does Redis Cluster shard data? 16384 hash slots distributed across nodes; key → CRC16 → slot → node. Multi key ops need same slot via hash tags. Q10. Implement a distributed rate limiter in Redis. Fixed window with + , or accurate sliding window with a sorted set of timestamps ( / / ), made atomic via Lua so it's correct across instances."}],"qa":[{"q":"Cache-aside vs write-through?","a":"Cache-aside: app loads on miss and writes DB then invalidates cache (lazy, resilient). Write-through: writes go to cache and DB synchronously (always fresh, higher write latency)."},{"q":"How do you keep cache and DB consistent?","a":"Update DB then delete the cache key (repopulate on next read); use short TTLs and invalidation events. Caches are eventually consistent — don't cache strict-correctness data."},{"q":"What is a cache stampede and how do you prevent it?","a":"A popular key expires and many requests hit the DB at once. Prevent with a rebuild mutex, logical expiry + async refresh, request coalescing, and TTL jitter."},{"q":"Penetration vs avalanche?","a":"Penetration: misses for non-existent keys hitting the DB → cache nulls / Bloom filter. Avalanche: mass simultaneous expiry → TTL jitter + HA + multi-level cache."},{"q":"Which eviction policy for a cache?","a":"`allkeys-lru` (or `lfu`) with a `maxmemory` limit. `noeviction` if Redis is used as a primary store and you can't drop data."},{"q":"Why is Redis fast despite being single-threaded?","a":"In-memory, no lock contention, atomic commands, efficient data structures, and (in 6+) multi-threaded network I/O. It's memory/network-bound, not CPU-bound."},{"q":"How do you handle a hot key?","a":"Add a local L1 cache in front of Redis, replicate/shard the hot value, or use client-side caching; identify via monitoring."},{"q":"RDB vs AOF?","a":"RDB = periodic snapshots (compact, possible data loss between snapshots). AOF = log every write (more durable, larger, slower replay). Often enable both."},{"q":"How does Redis Cluster shard data?","a":"16384 hash slots distributed across nodes; key → CRC16 → slot → node. Multi-key ops need same slot via hash tags."},{"q":"Implement a distributed rate limiter in Redis.","a":"Fixed window with `INCR`+`EXPIRE`, or accurate sliding window with a sorted set of timestamps (`ZADD`/`ZREMRANGEBYSCORE`/`ZCARD`), made atomic via Lua so it's correct across instances."}]},"26_SQL_at_Scale.md":{"title":"SQL at Scale","trackId":"cloud-native-distributed","trackTitle":"Cloud-Native & Distributed Systems","sections":[{"id":"sql-at-scale-deep-dive","heading":"SQL at Scale — Deep Dive","level":1,"text":""},{"id":"query-tuning-indexing-replication-sharding-locking","heading":"Query Tuning, Indexing, Replication, Sharding & Locking","level":3,"text":""},{"id":"1-how-a-query-executes","heading":"1. How a Query Executes","level":2,"text":"Theory. You write declarative SQL (\"give me orders for customer 42\"); the engine decides how to fetch it. Understanding this pipeline is the foundation of all tuning — you're not making queries faster by rewriting Java, you're helping the optimizer pick a cheaper plan. How it works (each stage). 1. Parser — checks syntax, builds a parse tree. → error here. 2. Optimizer — generates candidate plans (index seek vs full scan, join order, join algorithm). Estimates cost using statistics : row counts, index selectivity, column histograms. Picks lowest estimated cost plan. 3. Executor — runs the plan: requests pages from storage, applies filters, joins, sorts, aggregates. 4. Storage engine (InnoDB, PostgreSQL heap) — B tree index lookups, buffer pool (RAM cache for disk pages), WAL for durability. The optimizer uses table statistics (row counts, value distribution) to choose indexes and join order. Stale stats → bad plans (run ). The executor runs the chosen plan: index seek vs full scan, join algorithm (nested loop / hash / merge), sort, aggregate. Your job: give the optimizer good indexes and stats, and write queries it can optimize. Example. Without index: Seq Scan on 10M rows → filter 42 → sort all matches → take 20. Cost: seconds. With : Index Seek to customer 42's rows (already sorted by date) → read first 20. Cost: milliseconds. Pitfall. The optimizer is a cost based guess . A plan that's optimal at 1K rows may be catastrophic at 10M rows after data growth — re check plans after major data changes. Interview angle. \"Who decides to use an index?\" → Not you explicitly (usually). The optimizer compares estimated cost of index seek vs sequential scan based on statistics. exists but is a last resort."},{"id":"2-indexing","heading":"2. Indexing","level":2,"text":"An index is a sorted data structure (usually a B tree ) that turns an O(n) scan into an O(log n) seek. Theory. Think of a book index vs reading every page. Without an index, the DB reads every row (full table scan). With a B tree index on , it jumps to the right leaf pages in 3 4 disk reads for a 10M row table (log base 1000 fanout). How it works (B tree). Balanced tree of pages. Root → internal nodes → leaf nodes containing . Range queries ( ) walk consecutive leaf pages efficiently. Inserts/updates maintain balance — that's why writes on indexed columns cost extra I/O. Key concepts: Clustered index (PK in InnoDB) — table rows stored in PK order on disk; one per table. Random PKs (UUIDv4) fragment it — prefer sequential/UUIDv7/Snowflake. Secondary index — separate structure pointing back to the row (PK). Lookup = index seek + bookmark lookup (fetch full row from clustered index). Composite index — multi column; leftmost prefix rule : helps , , — not alone. Covering index — index includes all columns the query needs → answered from the index alone (no table lookup). Cardinality — high cardinality columns (email) index well; low cardinality (boolean) often don't. Example (covering index). Query: . Index includes all columns → Index Only Scan — no table access. 10× faster on wide tables. Example (leftmost prefix failure). Index . Query without → index unused (can't skip the leading column in the B tree ordering). Costs: indexes slow writes (every insert/update maintains them) and use space. Index for your read patterns , don't over index. Interview angle. \"Why not index every column?\" → Each index adds write amplification (5 indexes = 5 B tree updates per INSERT) and storage. A 10 column index table can double disk usage."},{"id":"3-explain-plans","heading":"3. EXPLAIN Plans","level":2,"text":"(and for actual timings) shows the optimizer's plan — the single most important tuning tool. Theory. EXPLAIN is a dry run — it shows what the DB would do without always executing (standard EXPLAIN) or actually runs and reports real timings ( in PostgreSQL). Example (reading output — PostgreSQL style). Good: Index Scan , 20 rows, 0.2 ms total. Bad pattern you'd see: Problems: full table scan (9.5M rows filtered), sort spilled to disk. What to look for: Seq Scan / Full Table Scan on a big table in a filtered query → missing index. Index Scan / Index Seek → good. Rows estimated vs actual wildly off → stale statistics. Sort / Hash spilling to disk → memory or missing index for . Join type: Nested Loop (good for small), Hash Join (large unsorted), Merge Join (sorted). Pitfall. EXPLAIN in dev with 1K rows shows Seq Scan (fast enough). Same query in prod with 10M rows takes 30s. Always test with production scale data or at least realistic row counts. Interview angle. Walk through a bad plan out loud: \"Seq Scan means no usable index; 9.5M rows removed by filter means we're reading the whole table. Fix: composite index on (customer id, created at).\""},{"id":"4-the-n1-problem","heading":"4. The N+1 Problem","level":2,"text":"The most common ORM performance bug: 1 query to fetch a list + N queries to fetch each item's relation. Theory. ORMs lazy load associations by default. You fetch 100 orders in one query, then touching triggers a separate SQL query per order — 1 + 100 = 101 round trips. Each round trip adds 1 ms (same DC) → 100 ms wasted on network alone, plus DB parse/plan overhead. Example. 50 orders on a page, each with 5 items. N+1 = 51 queries. With JOIN FETCH = 1 query returning 250 rows (some duplication of order columns). Page load: 51 × 2 ms = 102 ms vs 1 × 5 ms = 5 ms. Fixes: JOIN FETCH (JPQL): . to declare a fetch plan on the repository method. Batch fetching ( / ) → loads relations in batches. Detect with SQL logging / Hibernate statistics / an APM. This is a top interview \"find the bug\" item. How batch fetching works. With : first access to order 1's items triggers — loads items for 25 orders at once. 50 orders → 2 batch queries instead of 50. Pitfall. JOIN FETCH with OneToMany on a large collection → cartesian product explosion (100 orders × 50 items each = 5000 rows in one result). Use batch fetch or a separate query with . Interview angle. \"How do you find N+1 in production?\" → Enable Hibernate / SQL logging in staging, or APM tooling showing query count per endpoint. An endpoint with 1 + N query pattern is the smoking gun."},{"id":"5-query-optimization","heading":"5. Query Optimization","level":2,"text":"Theory. Query tuning is about reducing rows examined , round trips , and lock duration . Small rewrites often beat hardware upgrades. Select only needed columns ( , not ) — enables covering indexes, less I/O. Avoid functions on indexed columns in ( can't use the index) — rewrite as a range. Keyset pagination instead of large ( ) — scans and discards 100k rows. Prefer over for large sets; avoid correlated subqueries when a join works. Batch inserts/updates instead of row by row. Watch implicit type casts (string vs int column) that disable indexes. Keep transactions short to reduce lock time. Example (sargable rewrite). Bad: — function on column → index ignored. Good: — range scan on index. Example (keyset pagination). Page 5000 of a feed with : DB reads 100,020 rows, returns 20. With keyset: — reads 20 rows regardless of page depth. Example (batch insert). 10,000 rows: 10,000 individual INSERTs = 10,000 round trips. One in batches of 500 = 20 round trips. Interview angle. \"Why is OFFSET slow?\" → The DB must scan and skip all preceding rows. Page 1 costs 20 row reads; page 5000 costs 100,020 row reads."},{"id":"6-transactions-isolation","heading":"6. Transactions & Isolation","level":2,"text":"ACID: Atomicity, Consistency, Isolation, Durability. Theory. Isolation levels control what anomalies concurrent transactions can see . Higher isolation = fewer anomalies but more locking/retries and lower throughput. Isolation levels trade consistency for concurrency: Level Dirty read Non repeatable read Phantom read READ UNCOMMITTED ✅ possible ✅ ✅ READ COMMITTED (PG default) ❌ ✅ ✅ REPEATABLE READ (MySQL default) ❌ ❌ ✅ (PG/InnoDB largely prevent) SERIALIZABLE ❌ ❌ ❌ (as if serial, costliest) Dirty read : see uncommitted data. Non repeatable : same row changes between reads. Phantom : new rows match a re run query. Most apps run READ COMMITTED ; bump to SERIALIZABLE only for critical invariants. MVCC (PG/InnoDB) gives readers consistent snapshots without blocking writers. How it works (MVCC). Each transaction sees a snapshot of the database at a point in time. Writers create new row versions; readers see old versions until commit. No shared read locks needed — readers don't block writers (unlike old school 2PL). Example (non repeatable read at READ COMMITTED). 1. Tx A: → 100. 2. Tx B: ; COMMIT. 3. Tx A: → 50 (different from step 1 — non repeatable). At REPEATABLE READ, Tx A would still see 100 throughout its lifetime. Pitfall. Long running transactions at REPEATABLE READ hold old row versions → bloat (PostgreSQL) or undo log growth (InnoDB) → slower queries and vacuum pressure. Interview angle. \"Default isolation in MySQL vs PostgreSQL?\" → MySQL InnoDB: REPEATABLE READ. PostgreSQL: READ COMMITTED. Know what your stack defaults to."},{"id":"7-optimistic-vs-pessimistic-locking","heading":"7. Optimistic vs Pessimistic Locking","level":2,"text":"Theory. Two strategies for concurrent updates to the same row. Choice depends on contention frequency and cost of a conflict . Optimistic — assume no conflict; detect at commit via a version column. Best for low contention , read heavy data; no DB locks held; conflicts → retry. How it works (optimistic). Read row with . Modify locally. On save: . If another tx already bumped version to 4, UPDATE affects 0 rows → → app retries. Pessimistic — lock the row up front. Best for high contention , must not conflict (e.g., decrementing inventory/balance). Risk: lock waits/deadlocks; keep the transaction short. Example (inventory). 2 users buy last item simultaneously. Optimistic: both read . Both try update. One succeeds (version→6), other fails → show \"sold out\" on retry. Pessimistic: second user's blocks until first commits → sees → reject immediately. Example (atomic SQL — often best for counters). — single atomic statement, no explicit lock in app code. means sold out. Rule: optimistic for \"rarely collides\", pessimistic for \"must serialize\". For hot counters consider atomic SQL ( ). Pitfall (pessimistic). Holding while calling an external payment API (2–5 s) blocks all other purchases of that item. Lock only around the DB update, not external I/O. Interview angle. \"Lost update problem?\" → Two reads, two writes, last write wins. Fix: optimistic version, pessimistic lock, or atomic ."},{"id":"8-connection-pooling-hikaricp","heading":"8. Connection Pooling & HikariCP","level":2,"text":"Opening a DB connection is expensive; a pool reuses a fixed set. HikariCP is the Spring Boot default. Theory. Creating a TCP connection + TLS + DB auth + session setup costs 10–50 ms . At 1000 req/sec, opening a new connection per request would spend all time on handshakes. A pool keeps N connections warm and lends them to threads for the duration of a query/transaction. How it works. Request thread calls → Hikari returns an idle pooled connection (or waits up to ). Thread runs queries, calls → connection returns to pool (not actually closed). Pool sizing myth: bigger isn't better. A good starting point is ; often 10 20 beats 100. Too many connections thrash the DB. Pool exhaustion symptom: requests block waiting for a connection (latency spike) — check Hikari metric. The DB's must exceed total pool size across all instances — a hidden scaling limit. Example (scaling limit). 20 app instances × pool size 50 = 1000 connections. PostgreSQL → 900 requests fail or hang. Fix: reduce pool to 5 per instance (100 total) or use PgBouncer connection multiplexer. Example (Little's Law intuition). If each query takes 10 ms and you have 10 connections, max throughput ≈ 10 / 0.01 = 1000 queries/sec from that pool. More connections don't help if DB CPU is already saturated. Pitfall. Connection leak (forgetting in non Spring code) → pool drains → all threads block at . catches this in dev. Interview angle. \"Why not pool size = number of threads?\" → DB server has limited CPU; 200 connections mostly wait on locks and I/O, wasting memory on both sides."},{"id":"9-read-replicas","heading":"9. Read Replicas","level":2,"text":"Scale reads by replicating the primary to read only replicas. Theory. Write scaling hits a ceiling on one primary (single node WAL flush rate, 5K–20K writes/sec depending on hardware). Reads often dominate (10:1 or 100:1 ratio) — replicas copy the primary's WAL/binlog and serve read queries, multiplying read capacity. Route writes to primary , reads to replicas (read/write splitting via + a routing datasource, or a proxy like ProxySQL). Replication lag : replicas are slightly behind → a user may not \"read their own write\". Mitigate by reading from primary right after a write (read your writes) or sticky routing. Replicas help read scaling and HA (promote on primary failure); they do not scale writes. How it works. Primary commits write → records change in WAL → ships to replicas → replicas replay WAL → data available for reads (typically 10 ms–few seconds behind under load). Example (read your writes). User updates profile → POST goes to primary. Immediately GET profile → route to primary (or same session sticky replica). Other users' reads can hit replicas (eventual consistency OK). Example (lag under load). Bulk backfill updates 10M rows → replica lag spikes to 60s. Users on replica see stale data for a minute. Pause backfill or throttle; route critical reads to primary during backfill. Pitfall. on a method that accidentally triggers a lazy load write (some ORMs) → routed to replica → error or silent failure. Audit read only transaction boundaries. Interview angle. \"Can replicas scale writes?\" → No — all writes still go to one primary. Replicas only multiply read throughput."},{"id":"10-partitioning","heading":"10. Partitioning","level":2,"text":"Split one big table into smaller physical pieces within one database (same schema). Theory. A 500M row table has a 500M row index — inserts slow down, scans get expensive, maintenance (VACUUM, index rebuild) takes hours. Partitioning splits it into manageable chunks the optimizer can prune . Range (by date — great for time series/log data; drop old partitions cheaply), List , Hash . Benefits: smaller indexes per partition, partition pruning (scan only relevant partitions), easy archival. Different from sharding: partitioning is one DB; sharding spans multiple DBs/servers. How it works (pruning). Query: . Optimizer scans only — ignores other monthly partitions. 500M total rows but only 15M in June partition examined. Example (archival). Retention policy: keep 12 months. — instant metadata operation vs locking millions of rows for hours. Pitfall. Partition key must appear in WHERE clause for pruning. Query without → scans all partitions → worse than no partitioning. Interview angle. \"Partitioning vs sharding?\" → Partitioning = one database, one connection string, SQL joins still work across partitions. Sharding = multiple DB servers, app level routing."},{"id":"11-sharding","heading":"11. Sharding","level":2,"text":"Split data across multiple databases/servers to scale writes and storage beyond one machine. Theory. When one primary + replicas + caching still can't handle write volume or disk size (typically few TB or 10K sustained writes/sec depending on workload), you split data horizontally across independent DB instances. Each shard holds a subset of rows. Shard key choice is everything — pick high cardinality, evenly distributed (e.g., hashed). A bad key → hot shards . Strategies: hash based (even spread, hard to range scan), range based (range queries easy, risk hotspots), directory/lookup (flexible, extra hop). Hard parts: cross shard joins/transactions (avoid — denormalize or app side join), rebalancing when adding shards ( consistent hashing helps), global unique IDs ( Snowflake ), cross shard aggregation. Shard only when a single primary + replicas + caching is genuinely maxed — it adds major complexity. Example (hash sharding). 4 shards, shard key = . . Customer 42 always maps to shard 2. Order queries by customer id hit one shard. Query all orders in date range → fan out to all 4 shards + merge in app — expensive. Example (hot shard). Shard by . 90% of users in one country → one shard handles 90% of traffic while others idle. Fix: hash on instead. How rebalancing works. Adding shard 5 to a 4 shard setup remaps 80% of keys. Consistent hashing limits movement to 1/N of keys when adding node N+1. Interview angle. \"When to shard?\" → Last resort after exhausting vertical scale, read replicas, caching, connection pooling, query tuning, and partitioning. First question should always be \"can we avoid sharding?\""},{"id":"12-sql-vs-nosql-denormalization","heading":"12. SQL vs NoSQL & Denormalization","level":2,"text":"SQL (Postgres/MySQL) NoSQL (document/KV/wide column stores) Model Relational, schema, joins KV/document/wide column Consistency Strong, ACID Often eventual, tunable Scale Vertical + replicas + sharding Horizontal by design Best for Transactions, complex queries Huge scale, simple access patterns Default to SQL for transactional systems; reach for NoSQL for massive scale with known access patterns (e.g., a key value store designed around by partition key + sort key). Denormalize (duplicate data to avoid joins) for read performance at scale — trading write complexity/consistency for read speed. Materialized views / read models (CQRS) are structured denormalization. How to decide (decision flow). 1. Need multi row ACID transactions across entities? → SQL. 2. Access pattern fixed and key based at massive scale (millions of writes/sec)? → Consider wide column or key value NoSQL. 3. Schema changes weekly in early startup? → Document store offers flexibility; SQL with migrations also works if team prefers constraints. Example (denormalization). Order listing page needs customer name. Normalized: on every list query. Denormalized: store on row at order creation. Write path copies name once; read path is single table scan. Trade off: if customer renames, historical orders show old name (usually acceptable). Pitfall. Denormalizing without an update strategy → stale duplicated data. Pair denormalization with events ( → update order snapshots) or accept immutability (order snapshot at purchase time). Interview angle. \"SQL vs NoSQL for a social feed?\" → Hybrid: SQL for user accounts/transactions; Cassandra/Redis for feed timelines (write heavy, simple key based reads, eventual consistency OK)."},{"id":"13-migrations-at-scale","heading":"13. Migrations at Scale","level":2,"text":"Schema changes on big tables can lock/break things: Theory. On a table with 100M rows, or can lock writes for minutes to hours. Zero downtime migration requires backward compatible, phased changes — never big bang renames/drops with live traffic. Use Flyway/Liquibase for versioned, repeatable migrations. Expand–contract (backward compatible) pattern for zero downtime: add nullable column → backfill in batches → start writing both → switch reads → drop old. Never rename/drop in one step with live traffic. Adding an index on a large table can lock it — use (Postgres) / online DDL (MySQL ). Backfill in batches to avoid long transactions and replication lag. Example (expand–contract: rename → ). 1. Expand: — deploy code that writes both columns. 2. Backfill: in batches of 10K — off peak, throttled. 3. Switch reads: Deploy code reading from (fallback to if null). 4. Contract: — only after all rows backfilled and code stable. How online index creation works (PostgreSQL). builds index in background without blocking writes (two pass scan). Slower than regular CREATE INDEX but no production outage. Pitfall. Backfill in one transaction: → huge WAL, replica lag hours, disk fill. Always batch with between batches. Interview angle. \"Can you rename a column in production?\" → Not directly. Use expand–contract over days/weeks with multiple deploys."},{"id":"14-interview-qa","heading":"14. Interview Q&A","level":2,"text":"Q1. How do you find and fix a slow query? → look for full scans / bad row estimates / disk sorts → add the right index, rewrite to be sargable (no functions on indexed cols), select fewer columns, update stats. Q2. Explain the leftmost prefix rule. A composite index serves predicates on , , (in order) but not or alone — because the index is sorted by first. Q3. What is the N+1 problem and how do you fix it? One query for parents + one per child. Fix with JOIN FETCH, , or batch fetching; detect via SQL logs. Q4. Optimistic vs pessimistic locking — when each? Optimistic (version column) for low contention read heavy data, conflicts retried. Pessimistic ( ) for high contention must not conflict updates like balances. Q5. READ COMMITTED vs REPEATABLE READ? READ COMMITTED sees each statement's latest committed data (non repeatable reads possible). REPEATABLE READ gives a stable snapshot for the transaction (prevents non repeatable reads). Q6. Read replica caveat? Asynchronous replication lag means replicas can be stale — handle read your writes by routing post write reads to the primary. Q7. Partitioning vs sharding? Partitioning splits a table within one DB (pruning, archival); sharding spreads data across multiple DBs/servers to scale writes — far more complex. Q8. How do you pick a shard key? High cardinality and even distribution to avoid hot shards; align with common query patterns; combine with consistent hashing for rebalancing and Snowflake IDs for uniqueness. Q9. Why not set HikariCP pool size to 100? More connections thrash the DB (context switching, lock contention); a small pool (10 20) usually gives better throughput. The DB's max connections is a hard ceiling across instances. Q10. How do you add a column to a huge table with zero downtime? Expand–contract: add nullable column, backfill in batches, dual write, switch reads, then drop the old — using online DDL / concurrent index creation."}],"qa":[{"q":"How do you find and fix a slow query?","a":"`EXPLAIN ANALYZE` → look for full scans / bad row estimates / disk sorts → add the right index, rewrite to be sargable (no functions on indexed cols), select fewer columns, update stats."},{"q":"Explain the leftmost-prefix rule.","a":"A composite index `(a,b,c)` serves predicates on `a`, `a,b`, `a,b,c` (in order) but not `b` or `c` alone — because the index is sorted by `a` first."},{"q":"What is the N+1 problem and how do you fix it?","a":"One query for parents + one per child. Fix with JOIN FETCH, `@EntityGraph`, or batch fetching; detect via SQL logs."},{"q":"Optimistic vs pessimistic locking — when each?","a":"Optimistic (version column) for low-contention read-heavy data, conflicts retried. Pessimistic (`SELECT ... FOR UPDATE`) for high-contention must-not-conflict updates like balances."},{"q":"READ COMMITTED vs REPEATABLE READ?","a":"READ COMMITTED sees each statement's latest committed data (non-repeatable reads possible). REPEATABLE READ gives a stable snapshot for the transaction (prevents non-repeatable reads)."},{"q":"Read replica caveat?","a":"Asynchronous replication lag means replicas can be stale — handle read-your-writes by routing post-write reads to the primary."},{"q":"Partitioning vs sharding?","a":"Partitioning splits a table within one DB (pruning, archival); sharding spreads data across multiple DBs/servers to scale writes — far more complex."},{"q":"How do you pick a shard key?","a":"High cardinality and even distribution to avoid hot shards; align with common query patterns; combine with consistent hashing for rebalancing and Snowflake IDs for uniqueness."},{"q":"Why not set HikariCP pool size to 100?","a":"More connections thrash the DB (context switching, lock contention); a small pool (10-20) usually gives better throughput. The DB's max_connections is a hard ceiling across instances."},{"q":"How do you add a column to a huge table with zero downtime?","a":"Expand–contract: add nullable column, backfill in batches, dual-write, switch reads, then drop the old — using online DDL / concurrent index creation."}]},"27_Design_Patterns_Java.md":{"title":"Design Patterns in Java","trackId":"design-patterns","trackTitle":"Design Patterns","sections":[{"id":"design-patterns-in-java-deep-dive-with-real-corporate-examples","heading":"Design Patterns in Java — Deep Dive with Real Corporate Examples","level":1,"text":"Patterns are named, reusable solutions to recurring design problems . They are not libraries you import — they are vocabulary and structure . In an interview, the value is showing when and why you'd reach for one (and the trade off), not just reciting the UML. Every example below uses a realistic backend scenario (payments, provisioning, notifications, billing) — not toy \"animal/pizza\" examples."},{"id":"1-why-design-patterns-matter","heading":"1. Why Design Patterns Matter","level":2,"text":"Theory. A design pattern captures the experience of solving a class of problems so you don't reinvent (often badly) a structure that's already been refined. Their real benefits: Shared vocabulary. Saying \"use a Strategy here\" communicates an entire design in two words during a review. Decoupling. Most patterns exist to reduce coupling — between what varies and what stays stable — so change is local. Testability. Patterns that inject collaborators (Strategy, Factory, Observer) make seams for mocking. The cost. Patterns add indirection. A premature pattern is accidental complexity . The senior signal is knowing the trade off: introduce a pattern when you have two or more real variations or a clear axis of change — not \"just in case\". Mental model. Almost every pattern is an application of one of two principles: \"program to an interface, not an implementation\" and \"favour composition over inheritance.\""},{"id":"2-solid-principles-recap","heading":"2. SOLID Principles Recap","level":2,"text":"Patterns are how SOLID shows up in code. Quick recap with the \"why\": S — Single Responsibility. A class should have one reason to change. A that also formats currency and writes to DB has three reasons to change. O — Open/Closed. Open for extension, closed for modification. Strategy/Decorator let you add behaviour without editing tested code. L — Liskov Substitution. A subtype must be usable wherever its base is, without surprises (the classic violation breaks ). I — Interface Segregation. Many small role interfaces beat one fat interface; clients shouldn't depend on methods they don't use. D — Dependency Inversion. Depend on abstractions. High level policy shouldn't import low level details — this is what makes DI containers (Spring) possible. Interview line: \"Patterns are the tactics ; SOLID is the strategy . I reach for a pattern when a SOLID principle is being violated by a concrete change request.\""},{"id":"3-pattern-categories-how-to-choose","heading":"3. Pattern Categories & How to Choose","level":2,"text":"Creational (Singleton, Factory Method, Abstract Factory, Builder, Prototype) — control how objects are created , hiding construction complexity and concrete types. Structural (Adapter, Bridge, Composite, Decorator, Facade, Flyweight, Proxy) — control how objects are composed into larger structures. Behavioural (Chain, Command, Iterator, Mediator, Memento, Observer, State, Strategy, Template Method, Visitor, Interpreter) — control how objects collaborate and distribute responsibility . Choosing heuristic: \"I need to swap an algorithm at runtime\" → Strategy . \"Behaviour depends on internal state and transitions\" → State . \"I must add responsibilities without subclass explosion\" → Decorator . \"I must integrate an incompatible third party API\" → Adapter . \"Object creation depends on config/environment\" → Factory / Abstract Factory . \"Notify many parts on an event\" → Observer ."},{"id":"4-singleton","heading":"4. Singleton","level":2,"text":"Intent. Guarantee a class has exactly one instance and provide a global access point. Problem it solves. Some resources are inherently single: a connection pool, a metrics registry, an in memory feature flag cache. Multiple copies waste resources or cause inconsistency. Trade offs. Global state hurts testability and hides dependencies; in Spring you rarely hand roll one (the container manages singleton scoped beans). Know the thread safety variants. Corporate example — feature flag cache. Loaded once at startup, read by every request. Variants to mention: eager ( ), enum singleton (serialization/reflection safe — Joshua Bloch's preferred), and double checked locking with for lazy init with parameters."},{"id":"5-factory-method","heading":"5. Factory Method","level":2,"text":"Intent. Define an interface for creating an object but let subclasses/implementations decide which concrete class to instantiate. Problem it solves. Callers shouldn't hard code when the concrete type depends on input or configuration — that scatters and breaks Open/Closed. Corporate example — payment processor selection. A gateway router returns the right processor per payment method. Why it's better than a bare inline: creation logic lives in one place; adding touches only the factory."},{"id":"6-abstract-factory","heading":"6. Abstract Factory","level":2,"text":"Intent. Provide an interface for creating families of related objects without specifying concrete classes. Problem it solves. When a whole set of products must be consistent (same vendor, same cloud, same region), you don't want a client mixing a vendor A queue with a vendor B blob store. Corporate example — cloud provider abstraction. Switch the entire infra stack (queue + object store + secrets) by swapping one factory. Factory Method vs Abstract Factory: Factory Method creates one product via inheritance; Abstract Factory creates a family via composition."},{"id":"7-builder","heading":"7. Builder","level":2,"text":"Intent. Separate the construction of a complex object from its representation so the same process can build different representations; build step by step. Problem it solves. Telescoping constructors ( ) are unreadable and error prone; many optional fields need a fluent, validated assembly. Corporate example — outbound HTTP/API request or a complex . Real world: Lombok's , , , Stream collectors. Builders shine for immutable objects with many optional fields."},{"id":"8-prototype","heading":"8. Prototype","level":2,"text":"Intent. Create new objects by cloning a prototypical instance instead of constructing from scratch. Problem it solves. When object creation is expensive (heavy initialization, deep config) or you need many near identical variants, cloning a pre built template is cheaper and avoids re running setup. Corporate example — per tenant config templates. Start from a vetted base config, then tweak per customer. Gotcha to mention: does a shallow copy — you must deep copy mutable members or callers will share state. A copy constructor or factory is often clearer than ."},{"id":"9-adapter","heading":"9. Adapter","level":2,"text":"Intent. Convert the interface of a class into another interface clients expect; let incompatible types work together. Problem it solves. You can't change a third party SDK or legacy system, but your code depends on a clean interface. Wrap the foreign API. Corporate example — integrating a legacy/3rd party payment gateway. Real world: , (byte→char), Spring's . Adapter vs Facade: Adapter changes an interface to match a contract; Facade simplifies a complex subsystem behind a new interface."},{"id":"10-bridge","heading":"10. Bridge","level":2,"text":"Intent. Decouple an abstraction from its implementation so the two can vary independently (avoids a combinatorial subclass explosion). Problem it solves. Two independent dimensions of change. Without Bridge you get , , , … (M×N classes). With it, M + N. Corporate example — notifications: message type × delivery channel ."},{"id":"11-composite","heading":"11. Composite","level":2,"text":"Intent. Compose objects into tree structures and let clients treat individual objects and compositions uniformly . Problem it solves. Hierarchies (org charts, file systems, nested UI, bill of materials) where a \"group\" and a \"leaf\" must support the same operations. Corporate example — organizational hierarchy headcount/cost roll up. Real world: the entire DOM, Swing/JavaFX component trees, listings."},{"id":"12-decorator","heading":"12. Decorator","level":2,"text":"Intent. Attach additional responsibilities to an object dynamically by wrapping it — a flexible alternative to subclassing. Problem it solves. You need optional, stackable behaviours (logging, caching, retry, compression) without a class per combination. Corporate example — wrapping a repository/service with cross cutting concerns. Real world: streams ( ), , Spring's transactional/cache proxies conceptually decorate beans."},{"id":"13-facade","heading":"13. Facade","level":2,"text":"Intent. Provide a single, simplified interface to a complex subsystem. Problem it solves. Clients shouldn't orchestrate ten low level services to do one business action; hide the choreography behind one method. Corporate example — customer onboarding orchestrates many services. Facade vs Mediator: Facade is a one way simplification (clients → subsystem); Mediator coordinates peer objects talking to each other."},{"id":"14-flyweight","heading":"14. Flyweight","level":2,"text":"Intent. Minimize memory by sharing as much state as possible between many fine grained objects; split state into intrinsic (shared) and extrinsic (passed in). Problem it solves. Millions of objects that mostly duplicate immutable data (currency metadata, glyphs, tariff definitions) blow up heap. Corporate example — sharing immutable currency/tariff objects across millions of line items. Real world: caches −128..127; the JVM's String pool. Extrinsic state (e.g., the amount) is stored per item, not in the flyweight."},{"id":"15-proxy","heading":"15. Proxy","level":2,"text":"Intent. Provide a surrogate that controls access to another object — for lazy loading, access control, caching, or remote calls. Problem it solves. You want to add control around an object without changing it or the client (vs Decorator which adds behaviour ; Proxy controls access/lifecycle ). Corporate example — protection + lazy proxy for an expensive report service. Real world: Spring AOP ( , ) is built on dynamic proxies (JDK/CGLIB); Hibernate lazy loading proxies; RMI/remote stubs."},{"id":"16-chain-of-responsibility","heading":"16. Chain of Responsibility","level":2,"text":"Intent. Pass a request along a chain of handlers ; each either handles it or forwards it. Decouples sender from receiver. Problem it solves. A request must pass through ordered, independent steps (validation, auth, enrichment) and you want to add/reorder steps without touching others. Corporate example — request processing pipeline for an incoming order. Real world: Servlet chains, Spring Security filter chain, logging handlers, interceptor stacks."},{"id":"17-command","heading":"17. Command","level":2,"text":"Intent. Encapsulate a request as an object , letting you parameterize, queue, log, and undo operations. Problem it solves. You need to decouple the invoker from the receiver , schedule work, retry it, or record it for audit/undo. Corporate example — async job queue with retry + audit. Real world: / submitted to an , undo stacks in editors, transactional outbox commands, CQRS command handlers."},{"id":"18-iterator","heading":"18. Iterator","level":2,"text":"Intent. Provide sequential access to elements of an aggregate without exposing its internal representation . Problem it solves. Clients shouldn't know if data is backed by an array, tree, or a paged remote API; traversal logic lives with the collection. Corporate example — transparently paginating a remote API as one stream. Real world: the entire / , , ."},{"id":"19-mediator","heading":"19. Mediator","level":2,"text":"Intent. Define an object that encapsulates how a set of objects interact , so they don't refer to each other directly (reduces many to many coupling to many to one). Problem it solves. When N components each know about the others, every change ripples. A mediator centralizes coordination. Corporate example — an order saga coordinator mediating inventory, payment, shipping. Real world: Spring's (loosely), the MVC controller coordinating view/model, chat room servers, orchestration style Sagas."},{"id":"20-memento","heading":"20. Memento","level":2,"text":"Intent. Capture and externalize an object's internal state so it can be restored later , without violating encapsulation. Problem it solves. Undo/redo, checkpoints, or rollback to a prior known good state. Corporate example — workflow snapshot/rollback in a multi step approval. Real world: editor undo, database savepoints conceptually, transactional rollback."},{"id":"21-observer","heading":"21. Observer","level":2,"text":"Intent. Define a one to many dependency so that when one object changes state, all its dependents are notified automatically (publish/subscribe). Problem it solves. Multiple parts must react to an event (order placed → email, analytics, inventory) without the publisher knowing the subscribers. Corporate example — domain events on order placement. Real world: Spring / , , reactive streams, Kafka consumers (distributed observer). Use to avoid during notification."},{"id":"22-state","heading":"22. State","level":2,"text":"Intent. Allow an object to alter its behaviour when its internal state changes — it appears to change its class. Replaces sprawling state with polymorphic state objects. Problem it solves. Entities with a lifecycle (order, ticket, subscription) where allowed actions depend on the current state, and transitions must be explicit. Corporate example — order lifecycle. State vs Strategy: identical structure, different intent — Strategy is chosen by the client ; State transitions are driven internally by the object itself."},{"id":"23-strategy","heading":"23. Strategy","level":2,"text":"Intent. Define a family of interchangeable algorithms, encapsulate each, and make them swappable at runtime. Problem it solves. A single behaviour has multiple implementations selected by context (pricing rules, routing, retry policy) and you want to avoid and enable Open/Closed. Corporate example — shipping cost / discount strategy per customer tier. Real world: passed to , in thread pools, Spring's pluggable . In modern Java, a Strategy is often just a lambda — ."},{"id":"24-template-method","heading":"24. Template Method","level":2,"text":"Intent. Define the skeleton of an algorithm in a base method, deferring specific steps to subclasses — subclasses change steps, not the structure. Problem it solves. Several flows share the same ordered steps but differ in a few; you want to avoid duplicating the invariant skeleton (and keep the order fixed). Corporate example — an ETL / file import pipeline. Template Method (inheritance, compile time) vs Strategy (composition, runtime): Template fixes the structure and varies steps via subclassing; Strategy varies the whole algorithm via injection. Real world: Spring's / (you supply the callback steps), , servlet ."},{"id":"25-visitor","heading":"25. Visitor","level":2,"text":"Intent. Represent an operation to be performed on elements of an object structure , letting you add new operations without changing the element classes (double dispatch). Problem it solves. A stable set of element types but many operations that would otherwise pollute each element class (taxes, validation, export, pricing on different product types). Corporate example — tax / pricing computation over heterogeneous billing items. Trade off: great when operations change often but element types are stable; painful when you frequently add element types (every visitor must change)."},{"id":"26-interpreter","heading":"26. Interpreter","level":2,"text":"Intent. Given a language, define a representation for its grammar and an interpreter to evaluate sentences in it. Problem it solves. You have a small, recurring DSL — eligibility rules, query filters, pricing expressions — that business users tweak. Modelling the grammar as objects keeps evaluation clean. Corporate example — a rules engine for promotion eligibility ( ). Reality check: for anything non trivial, prefer a real parser/expression library (e.g., SpEL, ANTLR) — Interpreter is mostly asked to test grammar as objects thinking."},{"id":"27-enterprise-spring-patterns","heading":"27. Enterprise & Spring Patterns","level":2,"text":"Patterns you actually use daily in Spring backends: Dependency Injection / IoC — the container creates and wires beans; your classes depend on abstractions ( /constructor injection). This is Dependency Inversion at framework scale. Repository / DAO — abstracts persistence; the rest of the app depends on the interface. DTO + Mapper — decouple API/transport shape from domain entities (MapStruct); never expose JPA entities directly. Proxy/AOP — , , , are dynamic proxies wrapping your beans (this is why self invocation bypasses them). Template Method — , / , . Factory — / , methods, . Singleton — default bean scope (one instance per container). Observer — + for in process domain events. Strategy — inject a and pick by type; great for pluggable algorithms. Builder — , , Lombok . Interview gold: explain why doesn't work on a private method or self invocation — because it's a proxy (Proxy pattern), and the proxy is only consulted on external calls."},{"id":"28-anti-patterns-to-avoid","heading":"28. Anti-Patterns to Avoid","level":2,"text":"God Object / God Service — one class that does everything; violates SRP, untestable. Singleton abuse — global mutable state masquerading as design; hides dependencies, breaks tests. Anemic Domain Model — entities are just getters/setters with all logic in services (sometimes acceptable, but know the trade off vs rich domain). Pattern overuse / \"patternitis\" — adding Strategy/Factory/Abstract Factory where a simple method or would do; accidental complexity. Service Locator (as DI replacement) — hides dependencies vs constructor injection which makes them explicit. Premature abstraction — building flexibility for variations that never come (YAGNI). The senior signal: knowing when not to use a pattern is as valuable as knowing the pattern."},{"id":"29-interview-qa","heading":"29. Interview Q&A","level":2,"text":"Q1. Strategy vs State — they look identical, what's the difference? Same structure (polymorphic object holding behaviour). Difference is intent and who drives change : with Strategy the client picks the algorithm and it usually doesn't change itself; with State the object transitions between states internally based on events, and states often know the next state. Strategy answers \"how do I do X\"; State answers \"what can I do given where I am\". Q2. Factory Method vs Abstract Factory? Factory Method creates one product and typically relies on inheritance/overriding a creator method. Abstract Factory creates a family of related products and relies on composition — you pass around a factory object that produces a consistent set. Q3. Decorator vs Proxy — both wrap an object. Decorator adds behaviour/responsibilities (and you can stack many). Proxy controls access/lifecycle (lazy load, security, remote, caching) and usually mirrors the subject one to one. Intent differs even though structure is similar. Q4. How is Spring related to a pattern, and why can self invocation break it? It's the Proxy pattern (JDK dynamic proxy or CGLIB). The transactional logic lives in the proxy that wraps your bean. When method A calls method B on , the call bypasses the proxy, so B's is ignored. Fix: call via the injected bean, or restructure. Q5. Why prefer composition over inheritance, and which patterns embody it? Inheritance is tight, compile time coupling and a single axis of extension; composition lets behaviour be combined and swapped at runtime. Strategy, Decorator, Bridge, and State all favour composition; Template Method is the notable inheritance based one. Q6. When would you NOT introduce a pattern? When there's a single implementation and no real axis of change — a pattern then adds indirection without payoff (YAGNI). Introduce it when a second real variation appears or a SOLID principle is being violated by an incoming change. Q7. Give a real Decorator you've used from the JDK."}],"qa":[{"q":"Strategy vs State — they look identical, what's the difference?","a":"Same structure (polymorphic object holding behaviour). Difference is *intent and who drives change*: with **Strategy** the client picks the algorithm and it usually doesn't change itself; with **State** the object transitions between states internally based on events, and states often know the next state. Strategy answers \"how do I do X\"; State answers \"what can I do given where I am\"."},{"q":"Factory Method vs Abstract Factory?","a":"Factory Method creates **one product** and typically relies on inheritance/overriding a creator method. Abstract Factory creates a **family of related products** and relies on composition — you pass around a factory object that produces a consistent set."},{"q":"Decorator vs Proxy — both wrap an object.","a":"**Decorator** adds *behaviour/responsibilities* (and you can stack many). **Proxy** controls *access/lifecycle* (lazy load, security, remote, caching) and usually mirrors the subject one-to-one. Intent differs even though structure is similar."},{"q":"How is Spring `@Transactional` related to a pattern, and why can self-invocation break it?","a":"It's the **Proxy** pattern (JDK dynamic proxy or CGLIB). The transactional logic lives in the proxy that wraps your bean. When method A calls method B on `this`, the call bypasses the proxy, so B's `@Transactional` is ignored. Fix: call via the injected bean, or restructure."},{"q":"Why prefer composition over inheritance, and which patterns embody it?","a":"Inheritance is tight, compile-time coupling and a single axis of extension; composition lets behaviour be combined and swapped at runtime. Strategy, Decorator, Bridge, and State all favour composition; Template Method is the notable inheritance-based one."},{"q":"When would you NOT introduce a pattern?","a":"When there's a single implementation and no real axis of change — a pattern then adds indirection without payoff (YAGNI). Introduce it when a second real variation appears or a SOLID principle is being violated by an incoming change."},{"q":"Give a real Decorator you've used from the JDK.","a":"`new BufferedInputStream(new GZIPInputStream(new FileInputStream(file)))` — each wrapper adds a capability (buffering, decompression) over the same `InputStream` contract."},{"q":"How do lambdas change classic patterns in modern Java?","a":"Many single-method patterns collapse to lambdas: Strategy (`Comparator`, `Function`), Command (`Runnable`), Observer (functional listeners), Template Method callbacks (`JdbcTemplate`). The pattern *intent* remains, but the boilerplate (a class per strategy) disappears."},{"q":"How would you implement undo in an editor — which pattern(s)?","a":"**Command** (each edit is a command with `execute`/`undo`) plus optionally **Memento** (snapshot state to restore). Command stacks give fine-grained redo; Memento is better for coarse checkpoints."},{"q":"Which pattern fixes a subclass explosion across two dimensions?","a":"**Bridge** — separate the abstraction hierarchy from the implementation hierarchy so you get M+N classes instead of M×N."}]},"28_Design_Patterns_Python.md":{"title":"Design Patterns in Python","trackId":"design-patterns","trackTitle":"Design Patterns","sections":[{"id":"design-patterns-in-python-the-pythonic-way-advanced-examples","heading":"Design Patterns in Python — The Pythonic Way (Advanced Examples)","level":1,"text":"The GoF patterns were written for C++/Java. In Python, first class functions, duck typing, decorators, generators, and modules let you express many patterns far more simply — and some patterns disappear entirely. This guide shows each pattern the Pythonic way, with the theory of what changes vs Java and realistic backend examples. Interview signal: knowing which patterns Python makes trivial (Strategy, Command, Iterator) and which still matter (State, Observer, Adapter)."},{"id":"1-design-patterns-the-pythonic-way","heading":"1. Design Patterns the Pythonic Way","level":2,"text":"Theory. A design pattern is a named solution to a recurring design problem. The problems are language agnostic, but the solutions depend on what the language gives you for free. Python provides: Functions are objects — you can pass, store, and return them. Many patterns whose whole point is \"wrap a behaviour in an object\" become \"just pass the function\". Duck typing — you don't need a shared interface/ ; if it has the method, it works. This dissolves a lot of Adapter/Strategy boilerplate. Decorators, generators, context managers, descriptors — built in language features that are patterns. So the Pythonic rule is: reach for a class heavy GoF structure only when you genuinely need state, polymorphism over types, or a stable extension point — otherwise use the lighter idiom."},{"id":"2-python-idioms-that-replace-patterns","heading":"2. Python Idioms That Replace Patterns","level":2,"text":"GoF Pattern Pythonic replacement Strategy a plain function / passed as an argument Command a closure or Iterator a generator ( ) Singleton a module (modules are singletons) Template Method a higher order function taking step callbacks Decorator (structural) the syntax Factory a dict dispatch Observer callbacks list, or /signals Adapter duck typing / a small wrapper function Key interview point: \"In Python I'd first ask whether the pattern is even needed — e.g., Strategy is usually just a function. I use the full class structure when I need shared state, registration, or to switch on object type .\""},{"id":"3-creational-patterns-overview","heading":"3. Creational Patterns Overview","level":2,"text":"Creational patterns hide how objects are made. In Python, construction is cheap and dynamic ( , metaclasses, , factories), so heavy creational machinery is rarer. The common Pythonic creational tools: named constructors ( ) — far more common than Factory classes. dict dispatch for selecting a concrete type. for boilerplate free value objects."},{"id":"4-singleton","heading":"4. Singleton","level":2,"text":"Intent. Exactly one instance, globally accessible. Pythonic theory. A module is already a singleton — imported once and cached in . So the cleanest Singleton in Python is \"put state/functions in a module.\" When you must have a class, use a metaclass or . Corporate example — a shared config/connection registry. Gotcha: the metaclass approach is not thread safe by default — guard with a if instances may be created concurrently."},{"id":"5-factory-method","heading":"5. Factory Method","level":2,"text":"Intent. Decide which concrete class to instantiate without hard coding it at the call site. Pythonic theory. No need for a creator class hierarchy — use a dict dispatch or a . Classes are first class, so you map keys → classes. Corporate example — notifier selection. Adding a is one registry line — Open/Closed without a class hierarchy."},{"id":"6-abstract-factory","heading":"6. Abstract Factory","level":2,"text":"Intent. Create families of related objects that must stay consistent. Pythonic theory. Often a single object (or module) exposing factory functions. Duck typing means the \"abstract\" part is implicit — any object with the right methods qualifies. Corporate example — pluggable cloud backend (queue + blob + secrets). (PEP 544) gives static checking without forcing runtime inheritance — very Pythonic."},{"id":"7-builder","heading":"7. Builder","level":2,"text":"Intent. Assemble a complex object step by step. Pythonic theory. Python has keyword arguments and defaults , so telescoping constructors aren't a problem — Builder is far less needed. Use it for fluent APIs or when assembly has real steps/validation. cover most \"value object\" cases. Corporate example — fluent query/report builder. For simple objects, prefer just — kwargs are the builder."},{"id":"8-prototype","heading":"8. Prototype","level":2,"text":"Intent. Create new objects by cloning an existing one. Pythonic theory. Python has cloning built in : (shallow) and (deep). You rarely write a Prototype class — you call or implement / . Corporate example — per tenant config derived from a base template. Gotcha: is shallow — nested mutable objects stay shared. Use when nested state must be independent."},{"id":"9-adapter","heading":"9. Adapter","level":2,"text":"Intent. Make an incompatible interface match the one your code expects. Pythonic theory. Duck typing means you often just need a thin wrapper (function or class) exposing the expected method names. Still genuinely useful for wrapping third party SDKs. Corporate example — adapting a legacy payment SDK to our port. Now the app depends on and never sees the vendor's shape."},{"id":"10-bridge","heading":"10. Bridge","level":2,"text":"Intent. Separate an abstraction from its implementation so both vary independently (avoid M×N subclasses). Pythonic theory. Implemented with composition — inject the \"implementor\" object. Duck typing keeps it light. Corporate example — alert type × delivery channel ."},{"id":"11-composite","heading":"11. Composite","level":2,"text":"Intent. Treat individual objects and groups uniformly via a tree. Pythonic theory. Same as Java but lighter — a common base or just duck typing on a / method. Recursion + generators read cleanly. Corporate example — folder/asset hierarchy size roll up."},{"id":"12-decorator","heading":"12. Decorator","level":2,"text":"Intent. Add responsibilities to an object dynamically. Pythonic theory. Python has two things called \"decorator\": (a) the GoF structural pattern (wrap an object), and (b) the syntax (wrap a function/class). The syntax version is the everyday tool for cross cutting concerns. Always use to preserve metadata. Corporate example — a retry with backoff decorator for flaky external calls (a classic senior interview ask). Structural decorator (wrapping an object), for stacking concerns:"},{"id":"13-facade","heading":"13. Facade","level":2,"text":"Intent. One simple entry point over a complex subsystem. Pythonic theory. Often just a module of high level functions, or one class delegating to several. Modules make excellent facades. Corporate example — onboarding facade."},{"id":"14-flyweight","heading":"14. Flyweight","level":2,"text":"Intent. Share immutable, fine grained objects to save memory. Pythonic theory. Python already interns small ints (−5..256) and some strings. You implement Flyweight with a cache (often via on a factory) and to shrink per object memory. Corporate example — shared currency objects across millions of line items. is itself a key memory optimization interview topic — mention it."},{"id":"15-proxy","heading":"15. Proxy","level":2,"text":"Intent. A surrogate that controls access (lazy, security, caching, remote). Pythonic theory. Implement with a wrapper, or dynamically via to forward calls. is a built in lazy proxy for attributes. Corporate example — lazy + access controlled report proxy. Dynamic forwarding variant: proxies everything else automatically."},{"id":"16-chain-of-responsibility","heading":"16. Chain of Responsibility","level":2,"text":"Intent. Pass a request through ordered handlers until one handles it. Pythonic theory. Can be a list of callables you iterate, or linked handler objects. The callable list version is very Pythonic. Corporate example — request validation pipeline. This is exactly how WSGI/ASGI middleware and Django middleware work."},{"id":"17-command","heading":"17. Command","level":2,"text":"Intent. Encapsulate a request as an object for queuing, logging, undo. Pythonic theory. A command is usually just a closure or — you don't need a class with . Corporate example — a deferred job queue. Use a class only if you need or to persist/serialize the command."},{"id":"18-iterator","heading":"18. Iterator","level":2,"text":"Intent. Sequential access without exposing internals. Pythonic theory. This pattern is built into the language — / , and most powerfully generators ( ). Generators give lazy, memory efficient iteration for free; you almost never write the explicit class. Corporate example — transparently streaming a paginated API. Generators are lazy: nothing is fetched until iterated, and only one page is in memory at a time."},{"id":"19-mediator","heading":"19. Mediator","level":2,"text":"Intent. Centralize complex interactions so peers don't reference each other. Pythonic theory. Same as Java — a coordinator class. Often combined with callbacks/events. Corporate example — order saga coordinator."},{"id":"20-memento","heading":"20. Memento","level":2,"text":"Intent. Capture and restore an object's state without breaking encapsulation. Pythonic theory. Often implemented with , , or / (the pickle protocol). Corporate example — undo for a document/workflow. For dataclasses: then restore by reassignment."},{"id":"21-observer","heading":"21. Observer","level":2,"text":"Intent. One to many notification (pub/sub). Pythonic theory. A list of callbacks is the idiomatic form; libraries like or framework signals (Django signals) formalize it. Corporate example — domain events on order placed. For async fan out, subscribers can be coroutines awaited via ."},{"id":"22-state","heading":"22. State","level":2,"text":"Intent. Behaviour changes with internal state; replace big with state objects. Pythonic theory. Still valuable — Python's dynamism doesn't remove the need for explicit state machines. Use classes or + transition table. Corporate example — order lifecycle as state objects. State vs Strategy: identical shape; State transitions itself based on events, Strategy is chosen by the client."},{"id":"23-strategy","heading":"23. Strategy","level":2,"text":"Intent. Swap interchangeable algorithms at runtime. Pythonic theory. The poster child for \"just use a function.\" Since functions are first class, a Strategy is usually a passed in — no interface, no class. Corporate example — pluggable discount policy. Compare to Java's interface + multiple classes — Python collapses it to functions."},{"id":"24-template-method","heading":"24. Template Method","level":2,"text":"Intent. Fixed algorithm skeleton with overridable steps. Pythonic theory. Use either classic inheritance (abstract base with ) or a higher order function that takes step callbacks (often more Pythonic). Corporate example — ETL pipeline skeleton. Functional alternative: ."},{"id":"25-visitor","heading":"25. Visitor","level":2,"text":"Intent. Add operations over a fixed set of element types without modifying them. Pythonic theory. Classic double dispatch works, but Python often uses to dispatch on type — much cleaner than . Corporate example — tax computation over heterogeneous bill items via . is the Pythonic Visitor — add a new operation as a new generic function."},{"id":"26-interpreter","heading":"26. Interpreter","level":2,"text":"Intent. Model a small grammar/DSL and evaluate it. Pythonic theory. Build expression objects (or use / carefully). For real DSLs use / ; the pattern is mostly for interview/teaching. Corporate example — a promotion eligibility rule engine. Never use bare on user supplied strings — it's a code execution vulnerability."},{"id":"27-interview-qa","heading":"27. Interview Q&A","level":2,"text":"Q1. Which design patterns become unnecessary or trivial in Python, and why? Strategy and Command (functions/closures are first class), Iterator (generators), Singleton (modules are singletons), Template Method (higher order functions), and parts of Factory (dict dispatch / ). Python's dynamic features replace the boilerplate classes these patterns need in Java. Q2. There are two \"decorators\" in Python — explain the difference. The GoF Decorator is a structural pattern: wrap an object to add behaviour, stackable, same interface. The syntax is a language feature that wraps a function or class . They share a name and spirit (wrapping) but the syntax version is for cross cutting concerns on callables. Q3. How would you write a retry decorator with exponential backoff? A parameterized decorator (decorator factory) returning a wrapper that loops up to N times, catches specified exceptions, sleeps with jitter, and re raises on the final attempt. Use to preserve metadata. (See section 12.) Q4. What's the Pythonic way to implement Strategy? Pass a . Since functions are objects, you inject the algorithm directly instead of defining an interface and concrete classes. Lambdas or module level functions act as strategies. Q5. How does relate to the Visitor pattern? It provides type based dispatch: register a different implementation per argument type. This achieves Visitor's \"operation over many types\" goal without double dispatch — you add a new operation as a new generic function with registrations. Q6. Why is a module a good Singleton? Python imports a module once and caches it in ; subsequent imports return the same object. Module level state and functions are therefore inherently single instance and globally accessible — no metaclass needed. Q7. How do you make a class memory efficient for the Flyweight pattern? Use to eliminate the per instance , and cache shared instances (e.g., on a factory) so identical intrinsic state isn't duplicated. Q8. Iterator vs generator in Pytho"}],"qa":[{"q":"Which design patterns become unnecessary or trivial in Python, and why?","a":"Strategy and Command (functions/closures are first-class), Iterator (generators), Singleton (modules are singletons), Template Method (higher-order functions), and parts of Factory (dict dispatch / `@classmethod`). Python's dynamic features replace the boilerplate classes these patterns need in Java."},{"q":"There are two \"decorators\" in Python — explain the difference.","a":"The **GoF Decorator** is a structural pattern: wrap an object to add behaviour, stackable, same interface. The **`@decorator` syntax** is a language feature that wraps a *function or class*. They share a name and spirit (wrapping) but the syntax version is for cross-cutting concerns on callables."},{"q":"How would you write a retry decorator with exponential backoff?","a":"A parameterized decorator (decorator factory) returning a wrapper that loops up to N times, catches specified exceptions, sleeps `base * 2**attempt` with jitter, and re-raises on the final attempt. Use `functools.wraps` to preserve metadata. (See section 12.)"},{"q":"What's the Pythonic way to implement Strategy?","a":"Pass a `Callable`. Since functions are objects, you inject the algorithm directly instead of defining an interface and concrete classes. Lambdas or module-level functions act as strategies."},{"q":"How does `functools.singledispatch` relate to the Visitor pattern?","a":"It provides type-based dispatch: register a different implementation per argument type. This achieves Visitor's \"operation over many types\" goal without `accept()/visit()` double dispatch — you add a new operation as a new generic function with registrations."},{"q":"Why is a module a good Singleton?","a":"Python imports a module once and caches it in `sys.modules`; subsequent imports return the same object. Module-level state and functions are therefore inherently single-instance and globally accessible — no metaclass needed."},{"q":"How do you make a class memory-efficient for the Flyweight pattern?","a":"Use `__slots__` to eliminate the per-instance `__dict__`, and cache shared instances (e.g., `functools.lru_cache` on a factory) so identical intrinsic state isn't duplicated."},{"q":"Iterator vs generator in Python?","a":"An iterator implements `__iter__`/`__next__` and raises `StopIteration`. A generator is a function with `yield` that *produces* an iterator automatically, maintaining state between calls — lazy and memory-efficient. Generators are the idiomatic Iterator."},{"q":"When would you still write a full class-based pattern in Python?","a":"When you need (a) shared mutable state, (b) polymorphism dispatched on object *type*, (c) an explicit, registrable extension point, or (d) lifecycle/transition logic (State machines). Otherwise prefer functions/idioms."},{"q":"What's the danger of the Interpreter pattern with `eval`?","a":"`eval`/`exec` on untrusted input executes arbitrary code (RCE). Build an explicit expression tree or use a safe parsing library (`ast.literal_eval`, `lark`) instead."}]},"29_Python_Interview_Questions.md":{"title":"Python Interview Questions (2026)","trackId":"python-stack","trackTitle":"Python Stack","sections":[{"id":"python-interview-questions-2026-edition-with-deep-answers","heading":"Python Interview Questions — 2026 Edition (with Deep Answers)","level":1,"text":"Curated from current (2026) hiring trends. Modern Python interviews have moved past syntax — at mid/senior level they probe interpreter internals (the GIL, memory model, descriptors), concurrency trade offs, async discipline, and production patterns. The follow up that filters candidates is always \"why\" — why the GIL exists, why mutable defaults bite, why is unreliable. Each answer below gives the mechanism , not just the symptom. Every question is in flashcard form ( ) so it appears in Practice mode."},{"id":"1-core-language-data-model","heading":"1. Core Language & Data Model","level":2,"text":"Q1. What is the difference between and ? calls and compares values ; compares identity (same object in memory, i.e. same ). Use only for singletons like , , . A classic gotcha: is because CPython caches small ints (−5..256), but may be . Never rely on for value equality. Q2. Mutable vs immutable types — and why does it matter? Immutable: , , , , , . Mutable: , , , most objects. It matters because (a) only immutable, hashable objects can be dict keys / set members; (b) immutables are safe to share across threads; (c) \"modifying\" an immutable creates a new object. A tuple is immutable but can contain mutable items, so it's only hashable if all its elements are hashable. Q3. How does Python pass arguments — by value or by reference? Neither classically — it's \"pass by object reference\" (call by sharing). The function receives a reference to the same object. Rebinding the parameter ( ) doesn't affect the caller, but mutating a mutable argument ( ) does. So passing a list and appending to it changes the caller's list. Q4. Why are mutable default arguments dangerous? Default values are evaluated once at function definition time, not per call. So reuses the same list across calls, accumulating state. Fix: . This is one of the most common Python \"gotcha\" interview questions. Q5. What's the difference between shallow and deep copy? (shallow) copies the outer object but shares references to nested objects — mutating a nested list affects both. recursively copies everything, producing fully independent objects (and handles cycles). Deepcopy is slower; use it only when nested independence is required. Q6. Explain truthiness in Python. Objects are truthy unless they define returning or returning . Falsy values: , , , , , , , , . This enables idioms like instead of ."},{"id":"2-data-structures-complexity","heading":"2. Data Structures & Complexity","level":2,"text":"Q7. What are the time complexities of common list/dict/set operations? : index/append O(1), insert/pop at front O(n), O(n). / : insert/lookup/delete average O(1) (hash table), worst case O(n) on pathological collisions. preserves insertion order since 3.7 (a language guarantee, not just CPython). Use a / for membership tests, never a list, when n is large. Q8. When would you use a tuple over a list? Tuples are immutable → hashable (usable as dict keys / set members), slightly more memory efficient, and signal \"fixed structure\" (e.g., a coordinate or a record). Lists are for homogeneous, growing/mutable sequences. Use / for readable fixed records. Q9. What is and used for? auto creates a default value for missing keys, eliminating boilerplate — great for grouping. counts hashables and offers . These are interview favourites: e.g., word frequency is . Q10. vs — when to use which? gives O(1) appends/pops at both ends, making it ideal for queues, BFS, and sliding windows. A is O(n) for front operations. Use whenever you push/pop from the left. Q11. How does a Python ensure uniqueness, and what's required of members? A set is a hash table; membership uses then . Members must be hashable (immutable contract). Two objects that are must have the same hash, or the set breaks — that's why if you override you must override ."},{"id":"3-oop-mro-dunder-methods","heading":"3. OOP, MRO & Dunder Methods","level":2,"text":"Q12. Explain the Method Resolution Order (MRO) and C3 linearization. MRO is the order Python searches base classes for an attribute/method. Python uses the C3 linearization algorithm, which guarantees a consistent order that respects (a) a class before its parents and (b) the order parents are listed, while handling diamond inheritance. Inspect with or . follows the MRO, not just the immediate parent. Q13. What does actually do? returns a proxy that dispatches method calls to the next class in the MRO of the instance's type — not necessarily the literal parent. In multiple inheritance, this enables cooperative method chaining (each class calls and every base runs exactly once). Q14. What are dunder (magic) methods? Name important ones. Special methods Python calls implicitly to integrate with language syntax/protocols: / (construction), / (representation), / (equality), etc. (ordering), / / (containers), / (context managers), (callable objects), / (attribute access). Q15. vs ? is the static method that actually creates and returns the instance (used for immutable types, singletons, metaclasses). initializes the already created instance and returns . runs first; if it doesn't return an instance of the class, is skipped. Q16. What is a metaclass and when would you use one? A metaclass is \"the class of a class\" — is the default. It controls class creation ( / at class definition time). Real uses: enforcing API contracts, auto registering subclasses (plugin systems), ORMs (Django models), and frameworks. Rule of thumb: \"if you're not sure you need a metaclass, you don't\" — a class decorator or is usually simpler. Q17. What are descriptors? Objects defining / / that control attribute access when used as class attributes. They power , , , and ORM fields. A data descriptor ( / ) takes precedence over instance ; a non data descriptor (only ) does not. This is a senior level \"internals\" question. Q18. vs vs instance method? Instance method receives . receives (used for alter"},{"id":"4-decorators","heading":"4. Decorators","level":2,"text":"Q19. What is a decorator and how does it work? A decorator is a callable that takes a function (or class) and returns a replacement. above is sugar for . They implement cross cutting concerns (logging, caching, auth, retry, timing) without editing the wrapped function's body. They rely on closures and first class functions. Q20. Why must you use ? Without it, the wrapper replaces the original function's metadata ( , , , signature), breaking introspection, debugging, and tools. on the wrapper copies that metadata across. Forgetting it is a common red flag in code review. Q21. Write a decorator that retries with exponential backoff (a frequent senior ask). A parameterized decorator (decorator factory) returns a wrapper that loops up to N attempts, catches the given exceptions, sleeps plus random jitter, and re raises on the final attempt: Adding a parameter and configurable exceptions is what signals \"I've built real services that call real services.\" Q22. What's a stateful / class based decorator? Use a class with to keep state across invocations (e.g., a call counter, rate limiter, or memo cache). receives the function (or config); runs the wrapped logic. Class decorators are handy when the decorator itself needs attributes/methods. Q23. What does do? It memoizes a function's return values keyed by its arguments (args must be hashable), bounded by with LRU eviction. Huge for expensive pure functions / recursion (e.g., it turns naive recursive Fibonacci from exponential to linear). (3.9+) is ."},{"id":"5-generators-iterators","heading":"5. Generators & Iterators","level":2,"text":"Q24. What is a generator and why use one? A function with that produces values lazily — each resumes after the last , keeping local state. It doesn't materialize the whole sequence, so it's memory efficient for large/infinite streams and pipelines. Example: streaming a multi GB file line by line instead of . Q25. Iterator vs iterable vs generator? An iterable has returning an iterator (lists, dicts). An iterator has and , and is consumed once. A generator is the easiest way to make an iterator (a function with , or a generator expression ). Q26. What does do? It delegates to a sub iterator: yields all of 's values, transparently forwards / / , and captures the sub generator's return value. It flattens nested generators without a manual loop and is the foundation of generator based coroutines. Q27. How are generators related to coroutines? Generators can receive values via , making them two way (the historical basis for before ). Today coroutines are the dedicated construct, but they share the suspend/resume model: execution pauses at / and resumes later, preserving state. Q28. What's the difference between a list comprehension and a generator expression? builds the whole list in memory eagerly; returns a generator that yields lazily. For large data or when you only iterate once (e.g., ), the generator expression saves memory."},{"id":"6-the-gil-concurrency","heading":"6. The GIL & Concurrency","level":2,"text":"Q29. What is the GIL and why does it exist? The Global Interpreter Lock is a mutex in CPython that allows only one thread to execute Python bytecode at a time . It exists because CPython's memory management (reference counting) is not thread safe — without the GIL, concurrent ref count updates could corrupt memory and crash the interpreter. The GIL was the simplest way to make the interpreter safe and single threaded fast. Q30. How does the GIL affect performance, and how do you work around it? CPU bound multithreading doesn't scale across cores (threads serialize on the GIL). Workarounds: use multiprocessing (separate processes, each with its own GIL) for CPU bound work; use threading/asyncio for I/O bound work (the GIL is released during blocking I/O and in many C extensions like NumPy). Mention free threaded Python (PEP 703, experimental in 3.13+) which can disable the GIL using per object locks — a strong signal of current knowledge. Q31. threading vs multiprocessing vs asyncio — when to use each? asyncio : thousands of concurrent I/O bound tasks on one thread via an event loop (web servers, API clients). threading : I/O bound work, especially legacy/blocking libraries (GIL released during I/O). multiprocessing : CPU bound work needing true parallelism across cores (data crunching, ML inference), at the cost of process overhead and inter process communication. Q32. Is the GIL released during I/O? Yes — when a thread blocks on I/O (socket read, file read, ) it releases the GIL so other threads can run. That's why threading still helps I/O bound workloads despite the GIL. C extensions can also explicitly release it around long computations."},{"id":"7-async-await-asyncio","heading":"7. Async / Await (asyncio)","level":2,"text":"Q33. What are and ? defines a coroutine . Calling it returns a coroutine object that does nothing until awaited or scheduled on the event loop. suspends the current coroutine until the awaited awaitable completes, yielding control back to the loop so other coroutines run. starts the loop. Q34. How do you run coroutines concurrently? schedules them concurrently and waits for all (returning results in order). schedules a coroutine to run in the background and returns a you can await later. (3.11+) gives structured concurrency with automatic cancellation on failure. Q35. What's the danger of calling a blocking function inside async code? A blocking call (CPU work, , blocking DB driver) freezes the entire event loop — all coroutines stall because there's one thread. Fixes: use async libraries, or offload blocking work via / . This \"don't block the loop\" discipline is a key senior signal. Q36. Why is useful? It yields control to the event loop without delay, letting other ready tasks run. It's used to prevent a tight loop or long running coroutine from starving others — a cooperative scheduling courtesy point. Q37. How do you implement timeouts and cancellation? (3.11+) or cancel a coroutine that exceeds a deadline by raising / . Coroutines should handle to clean up (close connections) and then re raise. Proper cancellation handling is essential for resilient async services."},{"id":"8-memory-management-garbage-collection","heading":"8. Memory Management & Garbage Collection","level":2,"text":"Q38. How does Python manage memory? Primarily via reference counting : every object tracks how many references point to it; when the count hits zero the memory is freed immediately (deterministic). A supplementary generational cyclic garbage collector ( module) detects and frees reference cycles that ref counting alone can't. Small objects use pymalloc arenas/pools for efficiency. Q39. Why is a cyclic garbage collector needed on top of reference counting? Reference counting cannot reclaim reference cycles ( ) — their counts never reach zero even when unreachable. The cyclic GC periodically scans for unreachable cycles and collects them. It's generational (objects surviving collections are promoted) to amortize cost. Q40. Why is unreliable for cleanup? runs when the ref count hits zero, but timing is non deterministic (especially with cycles, where the GC decides when), it may not run at interpreter shutdown, and exceptions inside it are ignored. For resource cleanup use context managers ( ) or , not . Q41. What is and why use it? declares a fixed set of attributes, removing each instance's . Benefits: lower memory (significant when you have millions of instances) and slightly faster attribute access. Trade offs: no dynamic attributes and some complications with multiple inheritance. A common memory optimization interview answer. Q42. What's the difference between and garbage collection? removes the name binding and decrements the object's ref count; the object is freed only when its count reaches zero. It does not force collection by itself. To force a cyclic collection you call ."},{"id":"9-performance-optimization","heading":"9. Performance & Optimization","level":2,"text":"Q43. How do you profile and optimize Python code? Measure first: / for function level hotspots, for micro benchmarks, / for memory, line profilers for line level. Then optimize the proven hotspot: better algorithm/data structure, vectorize with NumPy, cache ( ), use generators to cut memory, move CPU bound work to multiprocessing or C extensions. Never optimize without profiling. Q44. Why are list comprehensions faster than equivalent for loops? They're optimized at the bytecode level (the list building loop runs in C, avoiding repeated / Python level calls). They're also more readable. For pure transformation/filtering they're both faster and clearer — but don't sacrifice readability for nested comprehensions. Q45. What are some idiomatic ways to speed up CPU bound Python? Use libraries that release the GIL and run in C (NumPy, pandas), use / , compile hotspots with Cython/Numba, cache results, and choose better algorithms/data structures. For truly parallel pure Python, watch free threaded (no GIL) builds."},{"id":"10-type-hints-dataclasses-pydantic","heading":"10. Type Hints, Dataclasses & Pydantic","level":2,"text":"Q46. What are type hints and what benefits do they bring? Optional annotations ( ) that don't affect runtime behaviour but enable static checking (mypy/pyright), better IDE autocomplete, and self documentation. Modern syntax: (3.10+), (3.9+), / for generics, and for structural (duck) typing with checking. Q47. What are dataclasses and why use them? auto generates , , (and /ordering with options) from annotated fields, removing boilerplate. Options: (immutable + hashable), (3.10+, memory), for mutable defaults. Ideal for value objects and DTOs. Q48. Dataclass vs vs Pydantic ? is an immutable, tuple backed lightweight record. is a mutable by default class with rich options but no runtime validation . Pydantic validates and coerces data at runtime against the types (raising ) — the go to for parsing external input (FastAPI request bodies, config). Choose Pydantic when data crosses a trust boundary. Q49. What is (structural typing)? (PEP 544) defines an interface by shape : any object with the required methods/attributes satisfies it, no explicit inheritance needed (static duck typing). It lets you type check duck typed code — e.g., \"anything with a method\" — which is far more Pythonic than forcing ABC inheritance."},{"id":"11-standard-library-idioms","heading":"11. Standard Library Idioms","level":2,"text":"Q50. Which types should every Python dev know? (counting/most common), (auto default values for grouping), (O(1) both ends queue), (mostly historical now), (lightweight record), (layered dict lookup, e.g., config overrides). Q51. What does give you? Memory efficient iterator building blocks: (concatenate), (slice an iterator), , / / , , , , / . They let you build lazy data pipelines without loading everything into memory. Q52. What's in ? / (memoization), (preserve metadata in decorators), (pre bind arguments → a new callable), , (type based function overloading), (compute once per instance). Senior favourites: for command objects and as a Pythonic Visitor. Q53. How do you read a large file without exhausting memory? Iterate the file object directly ( ) which yields lines lazily, or read in chunks ( ). Avoid / on huge files. For structured data, use streaming parsers (e.g., over the file iterator)."},{"id":"12-modern-python-310-313","heading":"12. Modern Python (3.10 - 3.13)","level":2,"text":"Q54. What is structural pattern matching ( / )? Introduced in 3.10, it matches a value against structural patterns — literals, sequences, mappings, class patterns with capture, and guards ( ). It's more powerful than a : it can destructure and bind. Great for parsing ASTs, command dispatch, and handling tagged unions. Q55. What is the walrus operator ? Assignment expressions assign and return a value inline. Useful to avoid recomputation/duplication: or . Use sparingly for readability. Q56. What is free threaded Python (PEP 703)? An experimental build (3.13+) that removes the GIL , using per object locking and atomic reference counting so multiple threads can execute Python bytecode in parallel. It enables true multithreaded CPU parallelism but has overhead and ecosystem caveats. Mentioning it shows you track Python's direction. Q57. Name a few other recent improvements worth knowing. union syntax (3.10), for TOML config (3.11), exception groups + and much faster interpreter (3.11), / (3.11), (3.10), and per interpreter GIL / improved error messages with carets pointing at the exact expression."},{"id":"13-exceptions-context-managers","heading":"13. Exceptions & Context Managers","level":2,"text":"Q58. How do context managers work? Objects implementing / used with to guarantee setup/teardown even on exceptions (files, locks, DB transactions). receives exception info and can suppress it by returning . The simpler form is on a generator that s once between setup and cleanup. Q59. How do you write an async context manager? Implement / and use , or decorate a generator with . Used for async resources like DB connection pools and HTTP sessions that must be awaited on acquire/release. Q60. What are exception groups and ? Added in 3.11, bundles multiple exceptions raised together (e.g., several failing tasks in a ), and selectively handles subsets by type. Essential for structured concurrency where many coroutines can fail simultaneously. Q61. EAFP vs LBYL — what's the Pythonic style? EAFP (\"Easier to Ask Forgiveness than Permission\") — try the operation and catch the exception ( ) — is idiomatic Python, avoids race conditions, and is often faster on the happy path. LBYL (\"Look Before You Leap\") checks first ( ) and can race in concurrent code."},{"id":"14-testing-tooling","heading":"14. Testing & Tooling","level":2,"text":"Q62. How do you test Python code? What's the ecosystem? is the de facto standard (fixtures, parametrize, plain ). is built in. Use / for mocking, / for coverage, for property based testing. Structure tests by behaviour and isolate external systems with fakes/mocks. Q63. What modern tooling improves Python code quality? (extremely fast linter + formatter, replacing flake8/isort and often black), (formatter), / (static typing), / / (dependency & env management), (run checks on commit). Mentioning / signals current ecosystem awareness. Q64. How do you manage dependencies and virtual environments? Isolate per project with / . Pin dependencies with (+ hashes) or a lockfile via / / . is the modern standard for project + tool config. Reproducible builds matter for production parity."},{"id":"15-tricky-output-gotchas","heading":"15. Tricky Output & Gotchas","level":2,"text":"Q65. What does return and why? . Floats are IEEE 754 binary and can't represent 0.1/0.2/0.3 exactly, so the sum is . Compare floats with or use for money. This is a classic \"do you understand floating point\" check. Q66. What does produce, and what's the trap? A list of three references to the same inner list , so sets column 0 in every row ( ). The replicates references, not copies. Correct: . Q67. What does this print: ? It raises . Because assigns to , Python treats as local to throughout, so the before assignment references an unbound local. Fix with (to use the enclosing scope) — tests understanding of scoping/closures. Q68. Why might capture the wrong value? Closures capture the variable , not its value at creation time. After the loop, all lambdas see . Fix by binding via default arg: , or use a factory function. Late binding of closures is a frequent gotcha. Q69. What's the difference between and , and what does do for lists? adds one element; adds each element. is in place (mutates ), whereas creates a new list. The in place behaviour matters when is shared/aliased."},{"id":"16-coding-problems-pythonic","heading":"16. Coding Problems (Pythonic)","level":2,"text":"Q70. Reverse words / count word frequency idiomatically. Frequency: . Reverse words: . Interviewers want idiomatic stdlib use ( , slicing, comprehensions) over manual loops. Q71. Flatten a nested list. For one level: . For arbitrary depth, recurse or use a generator: . Generators keep it memory efficient. Q72. Deduplicate a list while preserving order. — dicts preserve insertion order (3.7+) and keys are unique, so this is the cleanest O(n) approach. A deduplicates but loses order. Q73. Merge two dictionaries. 3.9+: (right wins on conflicts); in place . Pre 3.9: . For deep/recursive merges you write a helper or use a library. Q74. Group a list of records by a key. . Or after sorting by the key (groupby only groups consecutive equal keys). is usually clearer for unsorted data. Q75. Implement an LRU cache. Easiest: decorate with . To implement manually, use (move accessed keys to the end with , to evict the oldest) — a common system design flavoured coding question."}],"qa":[{"q":"What is the difference between `is` and `==`?","a":"`==` calls `__eq__` and compares *values*; `is` compares *identity* (same object in memory, i.e. same `id()`). Use `is` only for singletons like `None`, `True`, `False`. A classic gotcha: `a = 256; b = 256; a is b` is `True` because CPython caches small ints (−5..256), but `a = 257; b = 257; a is b` may be `False`. Never rely on `is` for value equality."},{"q":"Mutable vs immutable types — and why does it matter?","a":"Immutable: `int`, `float`, `str`, `tuple`, `frozenset`, `bytes`. Mutable: `list`, `dict`, `set`, most objects. It matters because (a) only immutable, hashable objects can be dict keys / set members; (b) immutables are safe to share across threads; (c) \"modifying\" an immutable creates a new object. A tuple is immutable but can *contain* mutable items, so it's only hashable if all its elements are hashable."},{"q":"How does Python pass arguments — by value or by reference?","a":"Neither classically — it's **\"pass by object reference\"** (call by sharing). The function receives a reference to the same object. Rebinding the parameter (`x = ...`) doesn't affect the caller, but mutating a mutable argument (`lst.append(...)`) does. So passing a list and appending to it changes the caller's list."},{"q":"Why are mutable default arguments dangerous?","a":"Default values are evaluated **once** at function definition time, not per call. So `def f(x, acc=[])` reuses the *same* list across calls, accumulating state. Fix: `def f(x, acc=None): acc = [] if acc is None else acc`. This is one of the most common Python \"gotcha\" interview questions."},{"q":"What's the difference between shallow and deep copy?","a":"`copy.copy` (shallow) copies the outer object but shares references to nested objects — mutating a nested list affects both. `copy.deepcopy` recursively copies everything, producing fully independent objects (and handles cycles). Deepcopy is slower; use it only when nested independence is required."},{"q":"Explain truthiness in Python.","a":"Objects are truthy unless they define `__bool__` returning `False` or `__len__` returning `0`. Falsy values: `None`, `False`, `0`, `0.0`, `\"\"`, `[]`, `{}`, `set()`, `range(0)`. This enables idioms like `if items:` instead of `if len(items) > 0:`."},{"q":"What are the time complexities of common list/dict/set operations?","a":"`list`: index/append O(1), insert/pop at front O(n), `in` O(n). `dict`/`set`: insert/lookup/delete average O(1) (hash table), worst case O(n) on pathological collisions. `dict` preserves insertion order since 3.7 (a language guarantee, not just CPython). Use a `set`/`dict` for membership tests, never a list, when n is large."},{"q":"When would you use a tuple over a list?","a":"Tuples are immutable → hashable (usable as dict keys / set members), slightly more memory-efficient, and signal \"fixed structure\" (e.g., a coordinate or a record). Lists are for homogeneous, growing/mutable sequences. Use `namedtuple`/`dataclass` for readable fixed records."},{"q":"What is `collections.defaultdict` and `Counter` used for?","a":"`defaultdict(list)` auto-creates a default value for missing keys, eliminating `if key not in d` boilerplate — great for grouping. `Counter` counts hashables and offers `.most_common(n)`. These are interview favourites: e.g., word frequency is `Counter(text.split()).most_common(3)`."},{"q":"`deque` vs `list` — when to use which?","a":"`collections.deque` gives O(1) appends/pops at *both* ends, making it ideal for queues, BFS, and sliding windows. A `list` is O(n) for front operations. Use `deque` whenever you push/pop from the left."},{"q":"How does a Python `set` ensure uniqueness, and what's required of members?","a":"A set is a hash table; membership uses `__hash__` then `__eq__`. Members must be **hashable** (immutable contract). Two objects that are `==` must have the same hash, or the set breaks — that's why if you override `__eq__` you must override `__hash__`."},{"q":"Explain the Method Resolution Order (MRO) and C3 linearization.","a":"MRO is the order Python searches base classes for an attribute/method. Python uses the **C3 linearization** algorithm, which guarantees a consistent order that respects (a) a class before its parents and (b) the order parents are listed, while handling diamond inheritance. Inspect with `Cls.__mro__` or `Cls.mro()`. `super()` follows the MRO, not just the immediate parent."},{"q":"What does `super()` actually do?","a":"`super()` returns a proxy that dispatches method calls to the **next class in the MRO** of the instance's type — not necessarily the literal parent. In multiple inheritance, this enables cooperative method chaining (each class calls `super().__init__()` and every base runs exactly once)."},{"q":"What are dunder (magic) methods? Name important ones.","a":"Special methods Python calls implicitly to integrate with language syntax/protocols: `__init__`/`__new__` (construction), `__repr__`/`__str__` (representation), `__eq__`/`__hash__` (equality), `__lt__` etc. (ordering), `__len__`/`__getitem__`/`__iter__` (containers), `__enter__`/`__exit__` (context managers), `__call__` (callable objects), `__getattr__`/`__setattr__` (attribute access)."},{"q":"`__new__` vs `__init__`?","a":"`__new__` is the static method that actually *creates and returns* the instance (used for immutable types, singletons, metaclasses). `__init__` *initializes* the already-created instance and returns `None`. `__new__` runs first; if it doesn't return an instance of the class, `__init__` is skipped."},{"q":"What is a metaclass and when would you use one?","a":"A metaclass is \"the class of a class\" — `type` is the default. It controls class *creation* (`__new__`/`__init__` at class-definition time). Real uses: enforcing API contracts, auto-registering subclasses (plugin systems), ORMs (Django models), and frameworks. Rule of thumb: \"if you're not sure you need a metaclass, you don't\" — a class decorator or `__init_subclass__` is usually simpler."},{"q":"What are descriptors?","a":"Objects defining `__get__`/`__set__`/`__delete__` that control attribute access when used as class attributes. They power `property`, `classmethod`, `staticmethod`, and ORM fields. A data descriptor (`__set__`/`__delete__`) takes precedence over instance `__dict__`; a non-data descriptor (only `__get__`) does not. This is a senior-level \"internals\" question."},{"q":"`@classmethod` vs `@staticmethod` vs instance method?","a":"Instance method receives `self`. `@classmethod` receives `cls` (used for alternative constructors like `User.from_json` and for subclass-aware behaviour). `@staticmethod` receives neither — it's a namespaced plain function with no access to instance/class state."},{"q":"What is a decorator and how does it work?","a":"A decorator is a callable that takes a function (or class) and returns a replacement. `@deco` above `def f` is sugar for `f = deco(f)`. They implement cross-cutting concerns (logging, caching, auth, retry, timing) without editing the wrapped function's body. They rely on closures and first-class functions."},{"q":"Why must you use `functools.wraps`?","a":"Without it, the wrapper replaces the original function's metadata (`__name__`, `__doc__`, `__module__`, signature), breaking introspection, debugging, and tools. `@functools.wraps(fn)` on the wrapper copies that metadata across. Forgetting it is a common red flag in code review."},{"q":"Write a decorator that retries with exponential backoff (a frequent senior ask).","a":"A parameterized decorator (decorator factory) returns a wrapper that loops up to N attempts, catches the given exceptions, sleeps `base * 2**(attempt-1)` plus random jitter, and re-raises on the final attempt:\n```python\nimport functools, random, time\ndef retry(times=3, base=0.2, exc=(Exception,)):\n    def deco(fn):\n        @functools.wraps(fn)\n        def wrap(*a, **k):\n            for i in range(1, times + 1):\n                try: return fn(*a, **k)\n                except exc:\n                    if i == times: raise\n                    d = base * 2 ** (i - 1); time.sleep(d + random.uniform(0, d))\n        return wrap\n    return deco\n```\nAdding a `jitter` parameter and configurable exceptions is what signals \"I've built real services that call real services.\""},{"q":"What's a stateful / class-based decorator?","a":"Use a class with `__call__` to keep state across invocations (e.g., a call counter, rate limiter, or memo cache). `__init__` receives the function (or config); `__call__` runs the wrapped logic. Class decorators are handy when the decorator itself needs attributes/methods."},{"q":"What does `functools.lru_cache` do?","a":"It memoizes a function's return values keyed by its arguments (args must be hashable), bounded by `maxsize` with LRU eviction. Huge for expensive pure functions / recursion (e.g., it turns naive recursive Fibonacci from exponential to linear). `functools.cache` (3.9+) is `lru_cache(maxsize=None)`."},{"q":"What is a generator and why use one?","a":"A function with `yield` that produces values **lazily** — each `next()` resumes after the last `yield`, keeping local state. It doesn't materialize the whole sequence, so it's memory-efficient for large/infinite streams and pipelines. Example: streaming a multi-GB file line by line instead of `f.readlines()`."},{"q":"Iterator vs iterable vs generator?","a":"An **iterable** has `__iter__` returning an iterator (lists, dicts). An **iterator** has `__iter__` *and* `__next__`, and is consumed once. A **generator** is the easiest way to make an iterator (a function with `yield`, or a generator expression `(x*x for x in it)`)."},{"q":"What does `yield from` do?","a":"It delegates to a sub-iterator: `yield from sub` yields all of `sub`'s values, transparently forwards `.send()`/`.throw()`/`.close()`, and captures the sub-generator's return value. It flattens nested generators without a manual loop and is the foundation of generator-based coroutines."},{"q":"How are generators related to coroutines?","a":"Generators can receive values via `.send()`, making them two-way (the historical basis for `asyncio` before `async def`). Today `async def` coroutines are the dedicated construct, but they share the suspend/resume model: execution pauses at `await`/`yield` and resumes later, preserving state."},{"q":"What's the difference between a list comprehension and a generator expression?","a":"`[x for x in it]` builds the whole list in memory eagerly; `(x for x in it)` returns a generator that yields lazily. For large data or when you only iterate once (e.g., `sum(x*x for x in nums)`), the generator expression saves memory."},{"q":"What is the GIL and why does it exist?","a":"The Global Interpreter Lock is a mutex in CPython that allows only **one thread to execute Python bytecode at a time**. It exists because CPython's memory management (reference counting) is **not thread-safe** — without the GIL, concurrent ref-count updates could corrupt memory and crash the interpreter. The GIL was the simplest way to make the interpreter safe and single-threaded-fast."},{"q":"How does the GIL affect performance, and how do you work around it?","a":"CPU-bound multithreading **doesn't scale** across cores (threads serialize on the GIL). Workarounds: use **multiprocessing** (separate processes, each with its own GIL) for CPU-bound work; use **threading/asyncio** for I/O-bound work (the GIL is released during blocking I/O and in many C extensions like NumPy). Mention **free-threaded Python (PEP 703, experimental in 3.13+)** which can disable the GIL using per-object locks — a strong signal of current knowledge."},{"q":"threading vs multiprocessing vs asyncio — when to use each?","a":"**asyncio**: thousands of concurrent **I/O-bound** tasks on one thread via an event loop (web servers, API clients). **threading**: I/O-bound work, especially legacy/blocking libraries (GIL released during I/O). **multiprocessing**: **CPU-bound** work needing true parallelism across cores (data crunching, ML inference), at the cost of process overhead and inter-process communication."},{"q":"Is the GIL released during I/O?","a":"Yes — when a thread blocks on I/O (socket read, file read, `time.sleep`) it releases the GIL so other threads can run. That's why threading still helps I/O-bound workloads despite the GIL. C extensions can also explicitly release it around long computations."},{"q":"What are `async def` and `await`?","a":"`async def` defines a **coroutine**. Calling it returns a coroutine object that does nothing until awaited or scheduled on the event loop. `await` suspends the current coroutine until the awaited awaitable completes, yielding control back to the loop so other coroutines run. `asyncio.run(main())` starts the loop."},{"q":"How do you run coroutines concurrently?","a":"`await asyncio.gather(coro1(), coro2(), coro3())` schedules them concurrently and waits for all (returning results in order). `asyncio.create_task(coro)` schedules a coroutine to run in the background and returns a `Task` you can await later. `asyncio.TaskGroup` (3.11+) gives structured concurrency with automatic cancellation on failure."},{"q":"What's the danger of calling a blocking function inside async code?","a":"A blocking call (CPU work, `time.sleep`, blocking DB driver) freezes the **entire event loop** — all coroutines stall because there's one thread. Fixes: use async libraries, or offload blocking work via `await asyncio.to_thread(fn, ...)` / `loop.run_in_executor`. This \"don't block the loop\" discipline is a key senior signal."},{"q":"Why is `await asyncio.sleep(0)` useful?","a":"It yields control to the event loop without delay, letting other ready tasks run. It's used to prevent a tight loop or long-running coroutine from starving others — a cooperative-scheduling courtesy point."},{"q":"How do you implement timeouts and cancellation?","a":"`asyncio.timeout()` (3.11+) or `asyncio.wait_for(coro, timeout)` cancel a coroutine that exceeds a deadline by raising `TimeoutError`/`CancelledError`. Coroutines should handle `CancelledError` to clean up (close connections) and then re-raise. Proper cancellation handling is essential for resilient async services."},{"q":"How does Python manage memory?","a":"Primarily via **reference counting**: every object tracks how many references point to it; when the count hits zero the memory is freed immediately (deterministic). A supplementary **generational cyclic garbage collector** (`gc` module) detects and frees reference cycles that ref-counting alone can't. Small objects use **pymalloc** arenas/pools for efficiency."},{"q":"Why is a cyclic garbage collector needed on top of reference counting?","a":"Reference counting cannot reclaim **reference cycles** (`a.ref = b; b.ref = a`) — their counts never reach zero even when unreachable. The cyclic GC periodically scans for unreachable cycles and collects them. It's generational (objects surviving collections are promoted) to amortize cost."},{"q":"Why is `__del__` unreliable for cleanup?","a":"`__del__` runs when the ref count hits zero, but timing is non-deterministic (especially with cycles, where the GC decides when), it may not run at interpreter shutdown, and exceptions inside it are ignored. For resource cleanup use **context managers (`with`)** or `try/finally`, not `__del__`."},{"q":"What is `__slots__` and why use it?","a":"`__slots__` declares a fixed set of attributes, removing each instance's `__dict__`. Benefits: **lower memory** (significant when you have millions of instances) and slightly faster attribute access. Trade-offs: no dynamic attributes and some complications with multiple inheritance. A common memory-optimization interview answer."},{"q":"What's the difference between `del x` and garbage collection?","a":"`del x` removes the *name* binding and decrements the object's ref count; the object is freed only when its count reaches zero. It does **not** force collection by itself. To force a cyclic collection you call `gc.collect()`."},{"q":"How do you profile and optimize Python code?","a":"Measure first: `cProfile`/`profile` for function-level hotspots, `timeit` for micro-benchmarks, `tracemalloc`/`memory_profiler` for memory, line-profilers for line-level. Then optimize the proven hotspot: better algorithm/data structure, vectorize with NumPy, cache (`lru_cache`), use generators to cut memory, move CPU-bound work to multiprocessing or C extensions. Never optimize without profiling."},{"q":"Why are list comprehensions faster than equivalent for-loops?","a":"They're optimized at the bytecode level (the list building loop runs in C, avoiding repeated `LOAD_METHOD`/`append` Python-level calls). They're also more readable. For pure transformation/filtering they're both faster and clearer — but don't sacrifice readability for nested comprehensions."},{"q":"What are some idiomatic ways to speed up CPU-bound Python?","a":"Use libraries that release the GIL and run in C (NumPy, pandas), use `multiprocessing`/`concurrent.futures.ProcessPoolExecutor`, compile hotspots with Cython/Numba, cache results, and choose better algorithms/data structures. For truly parallel pure-Python, watch free-threaded (no-GIL) builds."},{"q":"What are type hints and what benefits do they bring?","a":"Optional annotations (`def f(x: int) -> str`) that don't affect runtime behaviour but enable **static checking** (mypy/pyright), better IDE autocomplete, and self-documentation. Modern syntax: `str | None` (3.10+), `list[int]` (3.9+), `TypeVar`/`Generic` for generics, and `Protocol` for structural (duck) typing with checking."},{"q":"What are dataclasses and why use them?","a":"`@dataclass` auto-generates `__init__`, `__repr__`, `__eq__` (and `__hash__`/ordering with options) from annotated fields, removing boilerplate. Options: `frozen=True` (immutable + hashable), `slots=True` (3.10+, memory), `field(default_factory=...)` for mutable defaults. Ideal for value objects and DTOs."},{"q":"Dataclass vs `namedtuple` vs Pydantic `BaseModel`?","a":"`namedtuple` is an immutable, tuple-backed lightweight record. `@dataclass` is a mutable-by-default class with rich options but **no runtime validation**. **Pydantic** validates and coerces data at runtime against the types (raising `ValidationError`) — the go-to for parsing external input (FastAPI request bodies, config). Choose Pydantic when data crosses a trust boundary."},{"q":"What is `Protocol` (structural typing)?","a":"`typing.Protocol` (PEP 544) defines an interface by *shape*: any object with the required methods/attributes satisfies it, no explicit inheritance needed (static duck typing). It lets you type-check duck-typed code — e.g., \"anything with a `.read()` method\" — which is far more Pythonic than forcing ABC inheritance."},{"q":"Which `collections` types should every Python dev know?","a":"`Counter` (counting/most_common), `defaultdict` (auto-default values for grouping), `deque` (O(1) both-ends queue), `OrderedDict` (mostly historical now), `namedtuple` (lightweight record), `ChainMap` (layered dict lookup, e.g., config overrides)."},{"q":"What does `itertools` give you?","a":"Memory-efficient iterator building blocks: `chain` (concatenate), `islice` (slice an iterator), `groupby`, `product`/`permutations`/`combinations`, `cycle`, `count`, `accumulate`, `takewhile`/`dropwhile`. They let you build lazy data pipelines without loading everything into memory."},{"q":"What's in `functools`?","a":"`lru_cache`/`cache` (memoization), `wraps` (preserve metadata in decorators), `partial` (pre-bind arguments → a new callable), `reduce`, `singledispatch` (type-based function overloading), `cached_property` (compute once per instance). Senior favourites: `partial` for command objects and `singledispatch` as a Pythonic Visitor."},{"q":"How do you read a large file without exhausting memory?","a":"Iterate the file object directly (`for line in f:`) which yields lines lazily, or read in chunks (`while chunk := f.read(8192):`). Avoid `f.readlines()`/`f.read()` on huge files. For structured data, use streaming parsers (e.g., `csv.reader` over the file iterator)."},{"q":"What is structural pattern matching (`match`/`case`)?","a":"Introduced in 3.10, it matches a value against structural patterns — literals, sequences, mappings, class patterns with capture, and guards (`case Point(x, y) if x == y:`). It's more powerful than a `switch`: it can destructure and bind. Great for parsing ASTs, command dispatch, and handling tagged unions."},{"q":"What is the walrus operator `:=`?","a":"Assignment expressions assign *and* return a value inline. Useful to avoid recomputation/duplication: `if (n := len(data)) > 10: print(n)` or `while (chunk := f.read(1024)):`. Use sparingly for readability."},{"q":"What is free-threaded Python (PEP 703)?","a":"An experimental build (3.13+) that **removes the GIL**, using per-object locking and atomic reference counting so multiple threads can execute Python bytecode in parallel. It enables true multithreaded CPU parallelism but has overhead and ecosystem caveats. Mentioning it shows you track Python's direction."},{"q":"Name a few other recent improvements worth knowing.","a":"`str | None` union syntax (3.10), `tomllib` for TOML config (3.11), exception groups + `except*` and much faster interpreter (3.11), `asyncio.TaskGroup`/`timeout` (3.11), `@dataclass(slots=True)` (3.10), and per-interpreter GIL / improved error messages with carets pointing at the exact expression."},{"q":"How do context managers work?","a":"Objects implementing `__enter__`/`__exit__` used with `with` to guarantee setup/teardown even on exceptions (files, locks, DB transactions). `__exit__` receives exception info and can suppress it by returning `True`. The simpler form is `@contextlib.contextmanager` on a generator that `yield`s once between setup and cleanup."},{"q":"How do you write an async context manager?","a":"Implement `__aenter__`/`__aexit__` and use `async with`, or decorate a generator with `@contextlib.asynccontextmanager`. Used for async resources like DB connection pools and HTTP sessions that must be awaited on acquire/release."},{"q":"What are exception groups and `except*`?","a":"Added in 3.11, `ExceptionGroup` bundles multiple exceptions raised together (e.g., several failing tasks in a `TaskGroup`), and `except*` selectively handles subsets by type. Essential for structured concurrency where many coroutines can fail simultaneously."},{"q":"EAFP vs LBYL — what's the Pythonic style?","a":"EAFP (\"Easier to Ask Forgiveness than Permission\") — try the operation and catch the exception (`try: d[k] except KeyError:`) — is idiomatic Python, avoids race conditions, and is often faster on the happy path. LBYL (\"Look Before You Leap\") checks first (`if k in d:`) and can race in concurrent code."},{"q":"How do you test Python code? What's the ecosystem?","a":"`pytest` is the de facto standard (fixtures, parametrize, plain `assert`). `unittest` is built-in. Use `unittest.mock`/`pytest-mock` for mocking, `coverage`/`pytest-cov` for coverage, `hypothesis` for property-based testing. Structure tests by behaviour and isolate external systems with fakes/mocks."},{"q":"What modern tooling improves Python code quality?","a":"`ruff` (extremely fast linter + formatter, replacing flake8/isort and often black), `black` (formatter), `mypy`/`pyright` (static typing), `uv`/`poetry`/`pip-tools` (dependency & env management), `pre-commit` (run checks on commit). Mentioning `ruff`/`uv` signals current ecosystem awareness."},{"q":"How do you manage dependencies and virtual environments?","a":"Isolate per-project with `venv`/`virtualenv`. Pin dependencies with `requirements.txt` (+ hashes) or a lockfile via `poetry`/`pip-tools`/`uv`. `pyproject.toml` is the modern standard for project + tool config. Reproducible builds matter for production parity."},{"q":"What does `0.1 + 0.2 == 0.3` return and why?","a":"`False`. Floats are IEEE-754 binary and can't represent 0.1/0.2/0.3 exactly, so the sum is `0.30000000000000004`. Compare floats with `math.isclose(a, b)` or use `decimal.Decimal` for money. This is a classic \"do you understand floating point\" check."},{"q":"What does `[[0]*3]*3` produce, and what's the trap?","a":"A list of **three references to the same inner list**, so `m[0][0] = 1` sets column 0 in *every* row (`[[1,0,0],[1,0,0],[1,0,0]]`). The `*` replicates references, not copies. Correct: `[[0]*3 for _ in range(3)]`."},{"q":"What does this print: `def f(): x = 10; def g(): print(x); x = 20; return g; f()()`?","a":"It raises `UnboundLocalError`. Because `g` assigns to `x`, Python treats `x` as local to `g` throughout, so the `print(x)` before assignment references an unbound local. Fix with `nonlocal x` (to use the enclosing scope) — tests understanding of scoping/closures."},{"q":"Why might `for i in range(3): funcs.append(lambda: i)` capture the wrong value?","a":"Closures capture the **variable**, not its value at creation time. After the loop, all lambdas see `i == 2`. Fix by binding via default arg: `lambda i=i: i`, or use a factory function. Late binding of closures is a frequent gotcha."},{"q":"What's the difference between `append` and `extend`, and what does `a += b` do for lists?","a":"`append(x)` adds one element; `extend(iterable)` adds each element. `a += b` is in-place `extend` (mutates `a`), whereas `a = a + b` creates a new list. The in-place behaviour matters when `a` is shared/aliased."},{"q":"Reverse words / count word frequency idiomatically.","a":"Frequency: `from collections import Counter; Counter(text.lower().split()).most_common(3)`. Reverse words: `\" \".join(reversed(text.split()))`. Interviewers want idiomatic stdlib use (`Counter`, slicing, comprehensions) over manual loops."},{"q":"Flatten a nested list.","a":"For one level: `[x for sub in nested for x in sub]`. For arbitrary depth, recurse or use a generator: `def flat(xs): for x in xs: yield from flat(x) if isinstance(x, list) else [x]`. Generators keep it memory-efficient."},{"q":"Deduplicate a list while preserving order.","a":"`list(dict.fromkeys(items))` — dicts preserve insertion order (3.7+) and keys are unique, so this is the cleanest O(n) approach. A `set` deduplicates but loses order."},{"q":"Merge two dictionaries.","a":"3.9+: `merged = d1 | d2` (right wins on conflicts); in-place `d1 |= d2`. Pre-3.9: `{**d1, **d2}`. For deep/recursive merges you write a helper or use a library."},{"q":"Group a list of records by a key.","a":"`from collections import defaultdict; g = defaultdict(list); for r in records: g[r.dept].append(r)`. Or `itertools.groupby` **after sorting** by the key (groupby only groups consecutive equal keys). `defaultdict` is usually clearer for unsorted data."},{"q":"Implement an LRU cache.","a":"Easiest: decorate with `@functools.lru_cache(maxsize=128)`. To implement manually, use `collections.OrderedDict` (move accessed keys to the end with `move_to_end`, `popitem(last=False)` to evict the oldest) — a common system-design-flavoured coding question."}]},"30_Git_and_GitHub.md":{"title":"Git & GitHub Complete Guide","trackId":"git-github","trackTitle":"Git & GitHub","sections":[{"id":"git-github-complete-guide","heading":"Git & GitHub — Complete Guide","level":1,"text":""},{"id":"version-control-from-first-principles-to-production-workflows-interview-grade-depth","heading":"Version Control from First Principles to Production Workflows | Interview-Grade Depth","level":3,"text":""},{"id":"1-what-is-git-github","heading":"1. What Is Git & GitHub","level":2,"text":"Theory. Git is a distributed version control system (DVCS): a tool that records snapshots of your project over time so you can review history, go back to any past state, and merge work from many people. \"Distributed\" is the key word — every clone is a full copy of the entire repository including its complete history, not just the latest files. GitHub is a hosting platform built on top of Git that adds collaboration features (pull requests, issues, code review, CI/CD, access control). Git is the engine; GitHub is one of several cars built around it (GitLab and Bitbucket are alternatives). Analogy. Think of Git as the \"save game\" system for code, but instead of one save slot it keeps an unlimited, branching timeline of every save, who made it, and why. GitHub is the shared cloud where teammates' timelines meet. Why distributed matters. In older centralized systems (SVN), there's one server holding history; if it's down you can't commit or view history. With Git, you commit, branch, diff, and view full history offline because it's all local. You only need the network to share (push/pull). This makes Git fast (most operations are local disk reads) and resilient (every clone is a backup). Centralized vs Distributed at a glance: Centralized (SVN) Distributed (Git) History location Only on the server On every clone Commit offline No Yes Branching cost Expensive, discouraged Cheap, encouraged Single point of failure The server None (any clone restores it) Interview angle. A classic opener is \"Git vs GitHub?\" — Git is the version control tool you run locally; GitHub is a web service that hosts Git repositories and layers on collaboration. Saying \"they're the same thing\" is an instant red flag."},{"id":"2-how-git-works-internally","heading":"2. How Git Works Internally","level":2,"text":"Theory. Understanding Git's internals turns its commands from magic spells into obvious operations. At its core, Git is a content addressable key value store : you give it content, it stores it and hands you a key (a SHA 1/SHA 256 hash of the content). Everything in Git is one of four object types : Blob — the contents of a file (no filename, no metadata, just bytes). Tree — a directory listing: maps names → blobs (files) and other trees (subfolders). Commit — a snapshot pointer: references one top level tree, plus author, message, timestamp, and parent commit(s) . Tag — a named, often annotated pointer to a specific commit (used for releases). How it works. A commit is not a diff — it points to a complete tree (a full snapshot of the project at that moment). Git stays efficient because unchanged files reuse the same blob hash across commits (deduplication), and storage is later compressed into packfiles. Because each commit records its parent(s), the history forms a DAG (Directed Acyclic Graph) — a chain (or web, when branches merge) of commits pointing backward in time. The hash is the integrity guarantee. A commit's hash is computed from its content including its parent's hash . Change anything in history and every downstream hash changes — which is why you can't silently alter a past commit, and why force pushing rewritten history is so disruptive to others. The three areas every Git user must know: Working directory — the actual files you edit on disk. Staging area / index — a \"draft\" of your next commit; moves changes here. This lets you commit some changes and leave others, crafting clean commits. Repository — the folder holding all objects and refs (the permanent history). Refs and HEAD. A branch is simply a movable pointer (a 41 byte file) to a commit — that's why branching is instant and cheap. HEAD is a pointer to \"where you are now\" (usually the current branch). When you commit, the branch pointer advances to the new commit. Example. You commit . Git sto"},{"id":"3-the-core-workflow","heading":"3. The Core Workflow","level":2,"text":"Theory. Ninety percent of daily Git is a tight loop: change files → stage what you want → commit with a message → push to share. The staging step is what trips up newcomers, so internalize it: doesn't save anything permanently; it selects what your next commit will contain. How it works — file states. A file is in one of these states, and tells you which: Untracked — Git has never seen it (new file). Needs to start tracking. Modified — tracked and changed but not staged. Staged — marked to go into the next commit. Committed — safely stored in history. Example — crafting a clean commit. You fixed a bug and reformatted a file. Stage only the bug fix ( lets you pick individual hunks), commit it as \"Fix null check in parser\", then stage and commit the formatting separately. Reviewers thank you because each commit does exactly one thing. Commit message convention (widely expected): a short imperative summary ≤ 50 chars, a blank line, then a body explaining why . Example: — not . Pitfall. only stages already tracked files — brand new files are silently left out. After adding new files, always before assuming everything is in. Interview angle. \"Difference between and ?\" — stages a change into the index (the draft of the next snapshot); permanently records the staged snapshot into history. The two step design is intentional: it lets you build precise, reviewable commits."},{"id":"4-branching-merging","heading":"4. Branching & Merging","level":2,"text":"Theory. A branch is an independent line of development — a movable pointer to a commit. You create one to work on a feature without disturbing the stable branch, then merge it back when done. Because branches are just pointers, creating and switching them is near instant. How it works — two kinds of merge. 1. Fast forward merge. If hasn't moved since you branched, merging just slides the pointer forward to your feature's latest commit. No new commit is created — history stays linear. 2. Three way merge. If both branches advanced (commits on and on your feature), Git can't just slide the pointer. It finds the common ancestor , compares both tips against it, combines the changes, and creates a new merge commit with two parents . Example. You branch off at commit B. A teammate merges a hotfix into (commit E). When you merge your cart work back, Git performs a three way merge of B→D (your work) and B→E (the hotfix), producing merge commit M containing both. Pitfall. Long lived branches drift far from and create painful \"big bang\" merges. Keep branches short lived (hours to a few days) and frequently update them from . Interview angle. \"Fast forward vs three way merge?\" — fast forward when the target branch hasn't diverged (just moves the pointer, linear history); three way when both diverged (creates a merge commit with two parents). Some teams force to always create a merge commit so the feature's boundary is visible in history."},{"id":"5-merge-vs-rebase","heading":"5. Merge vs Rebase","level":2,"text":"Theory. Both and integrate changes from one branch into another, but they tell different historical stories . Merge preserves exactly what happened (including the branch shape) by creating a merge commit. Rebase rewrites your branch's commits so they appear to have been built on top of the latest target — producing a clean, linear history as if you'd started from the newest code. How it works — rebase mechanics. Rebase takes each of your branch's commits, sets them aside, moves your branch pointer to the target tip, and re applies your commits one by one on top. Because each replayed commit has a new parent, it gets a new hash — these are new commits, not the originals. That's why rebasing shared history is dangerous: everyone else still has the old commits. The golden rule of rebase: Never rebase commits that others have already pulled. Rebase your local, unpushed work to tidy it up; use merge for anything already shared. Interactive rebase is a power tool for cleaning up local history before sharing: Common use: squash five \"wip\" commits into one meaningful commit before opening a pull request. Example. You have messy local commits: \"wip\", \"fix typo\", \"actually fix\", \"tests\". Before pushing, , mark the last three as , and you ship one clean commit: \"Add discount calculation with tests\". Pitfall. Rebasing a branch others are using, then force pushing, makes their history diverge and causes duplicate commits/conflicts for the whole team. If you must force push a rebased branch, use (it refuses if someone else pushed in the meantime). Merge Rebase History Preserved, non linear (merge commits) Rewritten, linear Commit hashes Unchanged Changed (new commits) Safe on shared branches Yes No Best for Integrating finished work Cleaning local history before sharing Interview angle. Expect \"When would you rebase vs merge?\" — Rebase local feature work onto the latest to keep a clean, linear history and avoid noisy merge commits; merge when integrating a completed feature or an"},{"id":"6-working-with-remotes","heading":"6. Working With Remotes","level":2,"text":"Theory. A remote is a named reference to another copy of the repository, usually on a server like GitHub (the default name is ). Your local branches and the remote's branches are separate ; Git tracks the remote's state via remote tracking branches like , which update only when you or . How it works — fetch vs pull (a key distinction). downloads new commits and updates , but leaves your working files and local untouched . It's the safe \"show me what's new\" command. is plus an immediate (default) or ( ) of those changes into your current branch — it changes your files. Example — the safe update habit. Before integrating, run then to see exactly what's incoming. If it looks fine, (or just ). This avoids surprise conflicts in the middle of your work. Push rejected (\"fetch first\"). If the remote has commits you don't have locally, Git rejects your push to prevent overwriting others' work. The fix is to integrate first: Pitfall. overwrites the remote branch and can erase teammates' commits. Prefer , which only forces if no one else has pushed since your last fetch. Never force push shared branches like unless the whole team agrees. Interview angle. \"Difference between fetch and pull?\" — only downloads and updates remote tracking refs; downloads and integrates into your branch (it's fetch + merge/rebase). \"Why was my push rejected?\" — the remote advanced; pull/rebase, resolve, then push."},{"id":"7-undoing-changes","heading":"7. Undoing Changes","level":2,"text":"Theory. Git rarely loses data — almost any \"mistake\" is recoverable. The trick is matching the right undo command to where the change lives (working dir, staging, or committed) and whether it's been shared. The big four are , , , and . How it works — reset vs revert (the most important distinction). moves the branch pointer backward , effectively rewriting history . It's perfect for local, unshared commits but dangerous on shared branches because it removes commits others may have. The flags control what happens to your files : soft keeps them staged, mixed keeps them as unstaged edits, hard throws them away. leaves history intact and instead adds a new commit that applies the inverse of a previous commit. This is the safe way to undo something already pushed, because it doesn't rewrite anyone's history. Example — flags illustrated. You committed too early. puts you back to just before the commit with your changes still staged, ready to amend and re commit. If instead you wanted to also unstage them, use . If you wanted to nuke them entirely, (but be sure). Example — undo a bad deploy. A commit on broke production and it's already pushed. Don't reset (that rewrites shared history). Run to create a clean \"undo\" commit, push it, and you're rolled back without disturbing anyone's clone. The reflog safety net. Even after a or a botched rebase, shows every position HEAD held (e.g., ). You can to jump back to before the mistake. Reflog is local and expires (default 90 days), but it has saved countless engineers. Pitfall. permanently discards uncommitted work in the working directory — and that isn't in reflog. Commit or stash before doing anything destructive. Interview angle. \"How do you undo a pushed commit?\" — (adds an inverse commit, safe). \"Undo a local commit but keep the code?\" — . \"Recover after ?\" — to find the lost commit, then reset back to it."},{"id":"8-stash-cherry-pick-tags","heading":"8. Stash, Cherry-pick & Tags","level":2,"text":"Theory. These three are everyday \"surgical\" tools: stash parks unfinished work temporarily, cherry pick copies a single commit from one branch to another, and tags mark specific commits (typically releases). How it works. Stash saves your changes onto a separate stack (stored as special commits) and reverts your working directory to a clean state, so you can switch branches or pull without committing half done work. Popping replays them back. Cherry pick takes the diff a commit introduced and re applies it as a new commit (new hash) on your current branch — useful for backporting a fix to a release branch without merging the whole feature. Tags are permanent pointers (unlike branches, they don't move). Annotated tags store metadata and are what you use for releases; lightweight tags are just a name on a commit. Example — stash to handle an urgent interruption. You're mid feature when a production bug arrives. , switch to a hotfix branch, fix and ship it, switch back, — your in progress work returns exactly as you left it. Example — cherry pick a hotfix. A critical fix lands on as commit . Your branch needs it but not the rest of . then copies just that fix over. Pitfall. Cherry picking the same change that later gets merged can create duplicate commits and conflicts. Prefer cherry pick for isolated backports; for ongoing integration, merge. Also remember: tags must be pushed explicitly — many people wonder why their release tag isn't on GitHub. Interview angle. \"What's for?\" — temporarily shelving uncommitted changes to get a clean working tree. \"Lightweight vs annotated tag?\" — annotated tags carry author, date, and message (and are stored as full objects), so they're preferred for releases."},{"id":"9-resolving-merge-conflicts","heading":"9. Resolving Merge Conflicts","level":2,"text":"Theory. A merge conflict happens when two branches change the same lines of the same file (or one edits a file the other deletes) and Git can't decide which version wins. Git is conservative: rather than guess, it pauses the merge and asks you to resolve it. Conflicts are normal, not errors — knowing how to resolve them calmly is a core skill. How it works — conflict markers. Git rewrites the conflicted region with markers showing both sides: Everything between and is your side. Everything between and is the incoming side. Your job: edit the file to the correct final result, delete all three markers , then stage and continue. Example. Two developers both changed the default timeout. After , you open the file, see both and , decide the correct value is , remove the markers leaving , then and . Done. Reducing conflicts. Pull/rebase frequently so branches don't drift; keep changes small and focused; agree on formatting (a shared formatter prevents whitespace only conflicts); avoid two people rewriting the same file simultaneously. Pitfall. Accidentally committing a leftover marker ( ) breaks the build. Before committing a resolution, search the file for conflict markers. Also, blindly choosing \"accept theirs/mine\" in an editor can silently drop the other side's logic — read both sides. Interview angle. \"How do you resolve a merge conflict?\" — Git marks the conflicting regions; you edit to the intended result, remove the markers, the file, and complete the merge (or to start over). Mention prevention: small, frequent merges and shared formatting."},{"id":"10-gitignore-tracking-files","heading":"10. .gitignore & Tracking Files","level":2,"text":"Theory. Not everything belongs in version control. Build artifacts, dependencies, secrets, and machine specific files should be ignored . A file lists patterns of paths Git should not track. This keeps the repo small, avoids leaking secrets, and prevents noisy diffs. How it works. only affects untracked files. If a file is already tracked , adding it to does nothing — Git keeps tracking it. To stop tracking a file you previously committed, remove it from the index but keep it on disk: Critical — secrets. If you accidentally commit a secret (API key, password), adding it to later is not enough — it still lives in history and anyone with the repo can read it. You must (1) immediately rotate/revoke the secret, and (2) purge it from history (e.g., with or BFG) and force push. Treat any committed secret as compromised. Large files. Git is built for text/source, not large binaries (videos, datasets, big assets). Committing them bloats every clone forever. Use Git LFS (Large File Storage) , which stores big files outside the main history and keeps lightweight pointers in the repo. Pitfall. A committed or folder makes the repo huge and clones slow. Set up before the first commit; if it's already in, and commit. Interview angle. \"You committed a password — what now?\" — rotate the secret immediately (assume it's leaked), then rewrite history to remove it and force push. Ignoring it afterward is insufficient because history retains it."},{"id":"11-github-essentials","heading":"11. GitHub Essentials","level":2,"text":"Theory. GitHub wraps Git with a web UI and collaboration features. The building blocks: a repository (a hosted Git repo plus issues/PRs/settings), a fork (your personal server side copy of someone else's repo), a clone (a local copy), pull requests (proposals to merge changes with review), and branch protection (rules that guard important branches). How it works — clone vs fork. Clone : copy a repo to your machine. You can push back only if you have write access. Fork : GitHub makes a copy of the repo under your account . You clone that , push to it freely, and open a pull request back to the original. This is the standard model for contributing to projects you don't have write access to (open source). Authentication. GitHub no longer accepts account passwords over HTTPS. Use a Personal Access Token (PAT) in place of a password, or set up SSH keys for password less push/pull. SSH is convenient for your own machine; PATs are common in scripts and CI. Example. To fix a typo in an open source library: fork it, clone your fork, branch, commit the fix, push to your fork, and open a PR to the upstream repo. Maintainers review and merge. Interview angle. \"Fork vs clone?\" — a clone is a local copy; a fork is a server side copy under your account that enables contributing without write access (you PR back to the source). \"Why did ask for a token, not my password?\" — GitHub requires PATs or SSH keys instead of account passwords."},{"id":"12-pull-requests-code-review","heading":"12. Pull Requests & Code Review","level":2,"text":"Theory. A Pull Request (PR) is a request to merge one branch into another, wrapped in a review and discussion workflow. It's where code quality is enforced: teammates read the diff, comment, request changes, and approve before the code reaches . PRs also run automated checks (CI: tests, linters, builds) so broken code is caught before merge. How it works — the lifecycle. 1. Branch off , commit your work, push the branch. 2. Open a PR ( ← ) with a clear title and description (what changed and why , plus how to test). 3. CI runs automatically; reviewers comment and approve or request changes. 4. You address feedback by pushing more commits to the same branch (the PR updates live). 5. Once approved and green, merge — then delete the branch. Three ways to merge a PR (know the difference): Strategy What it does History result Merge commit Keeps all commits + adds a merge commit Full, non linear history Squash and merge Combines all PR commits into one Clean, one commit per PR (very popular) Rebase and merge Replays commits onto base, no merge commit Linear, preserves individual commits Example — a good PR. Title: \"Add rate limiting to login endpoint\". Description: explains the abuse it prevents, the algorithm chosen, and how to test it; links the related issue ( ). It's 200 lines, focused, with tests. Reviewers can understand and approve it quickly — small, well described PRs get merged fast. Pitfall. Giant PRs (2,000+ lines touching everything) are nearly impossible to review well, so bugs slip through. Keep PRs small and single purpose. Another pitfall: pushing directly to and bypassing review — that's what branch protection (require PR + passing checks + approvals) prevents. Interview angle. \"Walk me through your PR workflow\" and \"squash vs merge commit vs rebase merge?\" are common. Emphasize small focused PRs, descriptive context, required reviews + CI, and that squash merge keeps history clean (one commit per feature)."},{"id":"13-branching-strategies","heading":"13. Branching Strategies","level":2,"text":"Theory. A branching strategy is the team's agreed convention for how branches are created, named, and merged. It balances stability (don't break ) against velocity (ship often). The two dominant models are Git Flow and trunk based development . Git Flow. Multiple long lived branches with strict roles: — production ready, tagged releases only. — integration branch for the next release. — branch off , merge back to . — stabilize a release before it goes to . — urgent fixes branched off . Good for scheduled releases and versioned software (e.g., desktop apps), but heavy — lots of branches and merging overhead. Trunk based development. Everyone integrates into a single trunk ( ) via short lived feature branches merged within a day or two, behind feature flags if needed. Favored by teams doing continuous delivery because it minimizes merge pain and keeps everyone close to the latest code. This is the modern default for most web/services teams. GitHub Flow is the lightweight middle ground (and what most GitHub teams use): branch off , open a PR, get review + CI, merge to , deploy. Simple and effective. Naming conventions (pick one and be consistent): , , , . Example. A SaaS team uses trunk based: a dev branches , ships it in a day behind a feature flag, merges to , and CI auto deploys. A company shipping quarterly desktop releases might prefer Git Flow to stabilize each version on a branch. Pitfall. Adopting heavyweight Git Flow on a fast moving web team creates needless merge overhead and stale branches; conversely, trunk based without tests/feature flags risks shipping half done work. Match the strategy to your release cadence. Interview angle. \"Which branching strategy and why?\" — there's no single right answer; explain the trade off (Git Flow for versioned/scheduled releases, trunk based/GitHub Flow for continuous delivery) and tie it to release cadence and team size."},{"id":"14-github-actions-cicd","heading":"14. GitHub Actions (CI/CD)","level":2,"text":"Theory. GitHub Actions is GitHub's built in automation/CI CD system. You define workflows in YAML under , and GitHub runs them automatically on events (push, pull request, schedule, manual trigger). This is how teams automatically test every PR, build artifacts, and deploy — without a separate CI server. CI (Continuous Integration) = automatically build + test every change; CD (Continuous Delivery/Deployment) = automatically release it. How it works — the hierarchy. Workflow — a YAML file triggered by an event. Job — a set of steps that run on a fresh virtual machine ( runner ); jobs run in parallel by default. Step — a single command or a reusable action (e.g., ). How it gates quality. When a workflow runs on , its result appears as a check on the PR. Combined with branch protection (\"require status checks to pass\"), a PR with failing tests simply cannot be merged. That's the automated safety net replacing \"it works on my machine\". Example — a real pipeline. On every PR: checkout → install deps (cached) → lint → run unit + integration tests → build a Docker image. On merge to : additionally push the image to a registry and deploy to staging. Secrets (registry passwords, deploy tokens) are stored in GitHub Secrets , never in the YAML. Pitfall. Hardcoding credentials in workflow files leaks them in history — always use . Another: not caching dependencies makes every run slow and expensive. And using Docker tags makes deploys non reproducible — pin versions. Interview angle. \"How would you set up CI/CD?\" — describe triggering a workflow on PRs to run tests/lint as a required check (so broken code can't merge), then on merge to build and deploy, with secrets in the platform's secret store and dependency caching for speed."},{"id":"15-collaboration-project-management","heading":"15. Collaboration & Project Management","level":2,"text":"Theory. Beyond code, GitHub coordinates how teams work : Issues track bugs and tasks, labels/milestones/Projects organize them, branch protection rules enforce process, CODEOWNERS auto assigns reviewers, and Releases publish versioned builds. Knowing these signals that you can operate inside a real team, not just push code. Key features. Issues — trackable units of work (bug reports, feature requests). Link them to PRs; writing in a PR auto closes the issue on merge. Labels & Milestones — categorize ( , ) and group issues toward a release. Projects (boards) — Kanban style tracking of issues/PRs across columns (To do → In progress → Done). Branch protection rules — require PRs, passing checks, and a minimum number of approvals before merging into ; optionally forbid force pushes. CODEOWNERS — a file mapping paths to owners so the right people are auto requested for review (e.g., ). Releases & tags — package a tagged commit with release notes and downloadable artifacts. Example — issue to release flow. A bug is filed as Issue 42 with label and added to the current milestone. A dev opens PR \"Fix off by one in pagination (Closes 42)\". CODEOWNERS auto requests the backend reviewer; CI passes; it's approved and squash merged — Issue 42 closes automatically. At release time, a tag and release notes are published. Pitfall. No branch protection on means anyone can force push or merge unreviewed code, risking outages and lost history. For shared repos, protect : require PRs, reviews, and green CI. Interview angle. Teams probe whether you understand process: \"How do you prevent bad code reaching main?\" — branch protection (required reviews + passing CI), CODEOWNERS for the right reviewers, and small reviewed PRs. \"How do issues and PRs connect?\" — link them; auto closes on merge for traceability."},{"id":"16-best-practices-common-mistakes","heading":"16. Best Practices & Common Mistakes","level":2,"text":"Commit hygiene. Make small, focused commits that each do one thing — easier to review, revert, and bisect. Write meaningful messages : imperative summary (\"Add retry to payment client\"), with a body explaining why when it's non obvious. Don't commit commented out code, debug prints, or unrelated formatting churn. Branch & merge hygiene. Keep branches short lived ; integrate with frequently to avoid giant conflicts. Use clear branch names ( , , ). Delete branches after merge to keep the repo tidy. Safety. Never commit secrets ( , keys, tokens). If you do, rotate immediately and purge history. Avoid on shared branches; use when force is truly needed. Pull/rebase before pushing to avoid rejected pushes and surprise conflicts. Collaboration. Keep PRs small and described ; link issues; respond to review comments by pushing follow up commits. Require CI + reviews on protected branches so quality isn't optional. Common mistakes (and the fix): Mistake Why it hurts Fix Committing , build output Bloats repo, slow clones before first commit; Committing secrets Leaks credentials forever Rotate the secret; purge history; ignore going forward Giant, unfocused PRs Unreviewable; bugs slip in Split into small, single purpose PRs on shared history Erases others' commits Use for shared branches Force pushing shared branches Breaks teammates' clones ; coordinate; avoid on Vague messages (\"fix\", \"update\") History is useless later Imperative summary + the \"why\" Interview angle. Expect \"What are your Git best practices?\" — small focused commits with good messages, short lived branches, never commit secrets, prefer over on shared history, small reviewed PRs, and CI as a required gate. Concrete examples beat buzzwords."},{"id":"17-interview-qa","heading":"17. Interview Q&A","level":2,"text":"Q1. Git vs GitHub? Git is a distributed version control tool you run locally; GitHub is a hosting platform that stores Git repos and adds collaboration (PRs, issues, reviews, CI/CD, access control). Git works fully offline; GitHub is where teams share. Q2. Is a commit a diff or a snapshot? A snapshot. Each commit points to a complete tree (the full project state) plus parent commit(s); Git computes diffs on demand by comparing two snapshots. Unchanged files reuse the same blob hash for efficiency. Q3. Why is branching cheap in Git? A branch is just a movable pointer (a tiny file) to a commit — creating one doesn't copy files, it writes one ref. Switching branches just moves HEAD. Q4. vs ? downloads remote commits and updates remote tracking refs (e.g., ) without touching your files. is + (or ) into your current branch — it changes your working tree. Q5. Merge vs rebase? Merge preserves history and creates a merge commit (non linear). Rebase rewrites your commits onto the target tip for a linear history (new hashes). Golden rule: never rebase commits others have already pulled. Q6. How do you undo a commit that's already pushed? — it adds a new commit that inverts the change, keeping shared history intact. Don't shared branches. Q7. Difference between , , ? All move the branch pointer back. keeps changes staged, (default) keeps them unstaged, discards them entirely (dangerous). Q8. How do you resolve a merge conflict? Git marks conflicting regions with , , . Edit the file to the intended result, remove all markers, it, and commit (or to back out). Q9. You committed a secret. What do you do? Assume it's compromised: rotate/revoke it immediately, then rewrite history to remove it ( /BFG) and force push. Adding it to alone doesn't remove it from history. Q10. What recovers a commit after ? lists every position HEAD held; find the lost commit and . (Only committed work is recoverable this way — not uncommitted edits.) Q11. / squash merge — why use it? It collapses all of"}],"qa":[{"q":"Git vs GitHub?","a":"Git is a distributed version control *tool* you run locally; GitHub is a *hosting platform* that stores Git repos and adds collaboration (PRs, issues, reviews, CI/CD, access control). Git works fully offline; GitHub is where teams share."},{"q":"Is a commit a diff or a snapshot?","a":"A snapshot. Each commit points to a complete tree (the full project state) plus parent commit(s); Git computes diffs on demand by comparing two snapshots. Unchanged files reuse the same blob hash for efficiency."},{"q":"Why is branching cheap in Git?","a":"A branch is just a movable pointer (a tiny file) to a commit — creating one doesn't copy files, it writes one ref. Switching branches just moves HEAD."},{"q":"`git fetch` vs `git pull`?","a":"`fetch` downloads remote commits and updates remote-tracking refs (e.g., `origin/main`) without touching your files. `pull` is `fetch` + `merge` (or `rebase`) into your current branch — it changes your working tree."},{"q":"Merge vs rebase?","a":"Merge preserves history and creates a merge commit (non-linear). Rebase rewrites your commits onto the target tip for a linear history (new hashes). Golden rule: never rebase commits others have already pulled."},{"q":"How do you undo a commit that's already pushed?","a":"`git revert <sha>` — it adds a new commit that inverts the change, keeping shared history intact. Don't `reset` shared branches."},{"q":"Difference between `reset --soft`, `--mixed`, `--hard`?","a":"All move the branch pointer back. `--soft` keeps changes staged, `--mixed` (default) keeps them unstaged, `--hard` discards them entirely (dangerous)."},{"q":"How do you resolve a merge conflict?","a":"Git marks conflicting regions with `<<<<<<<`, `=======`, `>>>>>>>`. Edit the file to the intended result, remove all markers, `git add` it, and commit (or `git merge --abort` to back out)."},{"q":"You committed a secret. What do you do?","a":"Assume it's compromised: rotate/revoke it immediately, then rewrite history to remove it (`git filter-repo`/BFG) and force-push. Adding it to `.gitignore` alone doesn't remove it from history."},{"q":"What recovers a commit after `git reset --hard`?","a":"`git reflog` lists every position HEAD held; find the lost commit and `git reset --hard HEAD@{n}`. (Only committed work is recoverable this way — not uncommitted edits.)"},{"q":"`git merge --squash` / squash merge — why use it?","a":"It collapses all of a branch's commits into a single commit on the target, keeping `main` history clean (one commit per feature/PR) and hiding noisy WIP commits."},{"q":"What is HEAD? What's a \"detached HEAD\"?","a":"HEAD points to your current location (usually the current branch). Detached HEAD means HEAD points directly at a commit, not a branch — commits made there aren't on any branch and can be lost unless you create a branch."},{"q":"`git cherry-pick` use case?","a":"Copy a specific commit (e.g., a hotfix) from one branch onto another without merging the whole branch — common for backporting a fix to a release branch."},{"q":"How do you keep a fork in sync with the original?","a":"Add the original as `upstream`, then `git fetch upstream` and `git merge upstream/main` (or rebase) into your branch."},{"q":"How do you enforce code quality on `main`?","a":"Branch protection: require pull requests, a minimum number of approvals, and passing CI status checks; use CODEOWNERS to auto-request the right reviewers; forbid force-pushes to `main`."},{"q":"What's `--force-with-lease` and why prefer it over `--force`?","a":"`--force-with-lease` only overwrites the remote branch if no one else has pushed since your last fetch, preventing you from clobbering teammates' commits. Plain `--force` overwrites unconditionally."}]},"31_Angular_Complete_Guide.md":{"title":"Angular Complete Guide","trackId":"frontend","trackTitle":"Frontend — React & Angular","sections":[{"id":"angular-complete-guide","heading":"Angular — Complete Guide","level":1,"text":""},{"id":"beginner-pro-components-rxjs-di-signals-interview-qa","heading":"Beginner → Pro | Components, RxJS, DI, Signals, Interview Q&A","level":3,"text":""},{"id":"1-what-is-angular-how-it-works","heading":"1. What Is Angular & How It Works","level":2,"text":"Theory. Angular is a complete, opinionated framework (not just a library) for building large single page web applications, maintained by Google. Unlike React — which is a UI library you assemble with other tools — Angular ships everything in the box : a component system, routing, an HTTP client, forms, dependency injection, testing utilities, and a powerful CLI. It uses TypeScript as its primary language, so your app is strongly typed end to end. (Note: modern \"Angular\" means Angular 2+; \"AngularJS\" is the unrelated, retired 1.x version — don't confuse them in an interview.) Analogy. React is a high quality engine you bolt into a chassis you build yourself; Angular is the whole car, pre assembled — fewer decisions, more consistency across teams, at the cost of a steeper initial learning curve. How it works — the big picture. The UI is a tree of components . Each component is a TypeScript class (logic) + an HTML template (view) + CSS (styles), tied together by the decorator. Data binding keeps the template and the class in sync — change a class field and the view updates; a user event updates the class. Dependency Injection (DI) supplies components with shared services (data access, business logic) instead of components creating those themselves. A change detection mechanism figures out what changed and updates only the affected DOM. What problem it solves. For large apps with many teams, Angular's opinionated structure (a standard project layout, DI, typed everything, a single CLI) means any developer can move between projects and feel at home. That consistency is exactly why enterprises pick it. Interview angle. \"Angular vs React vs AngularJS?\" — Angular is a full framework (batteries included, TypeScript, DI); React is a UI library ; AngularJS (1.x) is the legacy, fundamentally different predecessor. \"Why a framework over a library?\" — consistency, built in solutions, and less decision fatigue for big teams."},{"id":"2-typescript-essentials-for-angular","heading":"2. TypeScript Essentials for Angular","level":2,"text":"Theory. Angular is written in and for TypeScript — JavaScript plus a static type system. Types are checked at compile time and erased at runtime, catching a whole class of bugs (wrong shapes, typos, null misuse) before the app runs. You can't be productive in Angular without being comfortable here. The pieces you'll use constantly: How it works — decorators. Angular relies heavily on decorators ( , , ). A decorator is a function that attaches metadata to a class, method, or property; Angular's compiler reads that metadata to know \"this class is a component,\" \"inject this dependency here,\" \"this property is an input.\" They're the glue between your code and the framework. Pitfall. Using everywhere defeats the purpose — you lose all type safety. Prefer real interfaces for your data; reserve for genuinely dynamic cases (and prefer when you must, since it forces a check before use). Interview angle. \"Why TypeScript for Angular?\" — compile time safety, better tooling/autocomplete, self documenting interfaces, and decorators that the framework depends on. Expect to explain vs (interface = compile time shape only, no runtime code; class = real runtime construct with logic)."},{"id":"3-components-templates","heading":"3. Components & Templates","level":2,"text":"Theory. A component is the fundamental UI building block: a class decorated with that controls a patch of screen (its template). Every Angular app is a tree of components rooted at a single root component. The decorator's metadata wires the class to its view. How it works — view encapsulation. By default, the CSS you write in a component is scoped to that component only (Angular adds unique attributes to the elements and rewrites your selectors). So two components can both define styles without clashing — a big advantage over global CSS in large apps. Templates are HTML enriched with Angular syntax: interpolation, property binding, binding, and structural directives ( , ). They're declarative — you describe what the UI should show for the current state, and Angular keeps the DOM in sync. Interview angle. \"What's in a component?\" — a decorator (with selector, template, styles) plus a class holding state and methods. \"How is component CSS scoped?\" — view encapsulation isolates styles to the component by default."},{"id":"4-data-binding-directives","heading":"4. Data Binding & Directives","level":2,"text":"Theory. Data binding is how the template and the component class stay synchronized. Angular has four binding forms — knowing the syntax and direction of each is a guaranteed interview question. Binding Syntax Direction Use Interpolation class → view Display a value as text Property binding class → view Set an element/component property Event binding view → class Respond to user actions Two way binding both Form inputs (sync field ⇄ input) How it works — two way binding is not magic. is just syntactic sugar for a property binding plus an event binding: (class→view) combined with (view→class). Understanding this demystifies it and explains why two way binding is really one way binding in both directions. Directives are instructions that change the DOM. Three kinds: Structural ( , , ) — add/remove elements. The means they restructure the DOM. Attribute ( , ) — change appearance/behavior of an existing element. Custom — your own (e.g., a directive). Modern Angular (17+) also offers built in control flow: , , — cleaner and faster than the / directives. Pitfall. In , always provide for lists that change. Without it, Angular re renders the entire list on any change (it can't tell which items are the same); lets it reuse DOM nodes by a stable id — a major performance fix (the Angular equivalent of React's ). Interview angle. \"Explain the four data bindings.\" \"What does desugar to?\" \"Structural vs attribute directives?\" — structural add/remove elements ( ), attribute modify existing ones. \"Why ?\" — to avoid full list re rendering."},{"id":"5-component-communication","heading":"5. Component Communication","level":2,"text":"Theory. Components form a tree, so they need ways to talk. The core mechanisms: (parent → child data), with (child → parent events), and a shared service for components that aren't directly related. How it works. marks a property the parent can bind to; when the parent's bound value changes, Angular updates the child's property (and you can react in ). exposes an the child calls ( ); the parent listens with event binding syntax and receives the value as . For unrelated components (e.g., a header and a sidebar), share a service holding the state (often a ) and inject it into both — both read and write the same source of truth. Pitfall. Don't build long / chains through many intermediate components (\"prop drilling\"). When data needs to reach far apart components, a shared service (or a state library) is cleaner. Interview angle. \"How do components communicate?\" — down, / up, shared service for siblings/distant components. Be ready to write a child that takes an and emits an ."},{"id":"6-services-dependency-injection","heading":"6. Services & Dependency Injection","level":2,"text":"Theory. A service is a plain class (decorated ) that holds reusable logic or state — data fetching, business rules, logging — kept out of components so components stay focused on the view. Dependency Injection (DI) is the mechanism Angular uses to create and supply those services to whoever needs them, instead of each class doing itself. DI is arguably Angular's most important architectural feature. How it works. Angular maintains an injector — a registry of how to create each dependency (its \"providers\"). When it builds a component, it reads the constructor's parameter types and looks up or creates each one from the injector, then passes them in. registers a service as an app wide singleton (one instance shared everywhere). You can also provide a service at the component level to get a fresh instance per component. Why DI matters (the payoff): Testability — in tests you inject a mock service instead of the real one, with no code change to the component. Loose coupling — components depend on an abstraction, not a concrete construction. Single source of truth — a root singleton service is the standard way to share state across the app. Pitfall. Misunderstanding scope: = one shared instance (state persists app wide); providing a service in a component's array = a new instance per component (state is isolated). Choosing the wrong one causes \"my data resets\" or \"my data leaks between components\" bugs. Interview angle. \"What is DI and why use it?\" — the framework creates and supplies dependencies, giving testability and loose coupling. \"How do you make a service a singleton?\" — . \"Service vs component responsibility?\" — services hold logic/state, components handle the view."},{"id":"7-rxjs-observables","heading":"7. RxJS & Observables","level":2,"text":"Theory. Angular is built on RxJS — reactive programming with Observables . An Observable is a stream of values over time that you subscribe to: HTTP responses, user input events, route changes, timers — all are streams. Instead of callbacks or one shot Promises, you compose streams with operators ( , , , ). This is the part of Angular newcomers find hardest, so understanding it sets you apart. Observable vs Promise (a key comparison): Promise Observable Values One (resolves once) Many (a stream over time) Lazy? No (runs immediately) Yes (runs on subscribe) Cancelable No Yes (unsubscribe) Operators No Rich (map, filter, switchMap…) How it works — why operators matter. Without RxJS, a search box would fire an API call on every keystroke and you'd get out of order responses. The pipeline above: waits for the user to stop typing, skips duplicate terms, and cancels the in flight request when a new term arrives — so you always show results for the latest query. That's a real bug class solved declaratively in four lines. Subjects. A is an Observable you can also push values into (it's both producer and consumer). A additionally remembers the last value and emits it to new subscribers — the standard tool for sharing state in a service. Pitfall — memory leaks. A subscription stays active until you unsubscribe; forgetting to do so leaks memory and can cause callbacks on destroyed components. Fixes: the pipe in templates (auto subscribes and unsubscribes), , or manual unsubscribe in . The pipe is the cleanest and most recommended. Interview angle. \"Observable vs Promise?\" — many vs one, lazy, cancelable, composable. \" vs ?\" — cancels the previous inner stream (perfect for search/autocomplete); runs them concurrently. \"How do you avoid subscription leaks?\" — pipe / / unsubscribe in ."},{"id":"8-http-client-apis","heading":"8. HTTP Client & APIs","level":2,"text":"Theory. Angular's is the built in service for talking to REST APIs. It returns Observables (not Promises), so responses plug straight into RxJS pipelines, and it integrates with interceptors for cross cutting concerns like auth headers and error handling. How it works — interceptors. An HTTP interceptor sits in the request/response pipeline and can modify every request or response — the standard place to attach a JWT auth header, log calls, show a loading spinner, or centrally handle 401/500 errors. This keeps that logic out of every individual service. Pitfall. calls are lazy — if you forget to (and don't use the pipe), the request never fires . This trips up developers coming from /Promises. Also, requests are immutable; you must to modify them in an interceptor. Interview angle. \"How do you call an API in Angular?\" — inject , return a typed , subscribe (or async pipe). \"What's an interceptor for?\" — cross cutting request/response logic like auth headers and global error handling. \"Why didn't my request fire?\" — you didn't subscribe."},{"id":"9-routing-navigation","heading":"9. Routing & Navigation","level":2,"text":"Theory. The Angular Router maps URL paths to components, enabling a single page app to have multiple \"pages\" without full reloads. You declare a route table (path → component), render the active component in a , and navigate with links or programmatically. How it works. Route parameters ( ) are read via (e.g., to load the right user). Prefer subscribing to so navigation between and (which reuses the component) still updates. Guards ( , ) run before a route activates/deactivates — the standard way to block unauthenticated users or warn about unsaved changes. Lazy loading ( / ) loads a feature's code only when its route is first visited, shrinking the initial bundle. Pitfall. Reading a route param once in breaks when the user navigates to another id of the same component (Angular reuses the instance, so doesn't re run). Subscribe to instead. Interview angle. \"How does routing work?\" — route table maps paths to components rendered in . \"What are guards?\" — pre navigation checks (auth, unsaved changes). \"How do you reduce initial load?\" — lazy load feature routes."},{"id":"10-forms-template-driven-reactive","heading":"10. Forms — Template-Driven & Reactive","level":2,"text":"Theory. Angular offers two form approaches. Template driven forms put the logic in the HTML (with ) — simple and quick for small forms. Reactive forms define the form model in the TypeScript class ( / ) — more powerful, testable, and the right choice for anything non trivial. How it works. A reactive form is a tree of s (each tracks value + validity + touched/dirty state) grouped in s. Because the model is in code, you can subscribe to (an Observable!), add dynamic controls, and unit test validation without rendering the DOM. Template driven vs Reactive: Template driven Reactive Where logic lives HTML template Component class Best for Simple forms Complex/dynamic forms Testability Harder Easy (model in code) Validation Directives in template in code Pitfall. Showing validation errors immediately (before the user interacts) is jarring. Gate error messages on / . Also, build custom validators as functions returning (valid) or an error object — don't cram complex rules into the template. Interview angle. \"Template driven vs reactive — when to use which?\" — reactive for anything complex/dynamic/testable; template driven for trivial forms. \"How do you do cross field validation (e.g., password match)?\" — a validator on the itself."},{"id":"11-pipes","heading":"11. Pipes","level":2,"text":"Theory. A pipe transforms a value in the template for display — formatting dates, currency, text, or filtering — without changing the underlying data. They keep templates clean and reusable. How it works — pure vs impure pipes. By default pipes are pure : Angular only re runs them when the input reference changes, which makes them fast. An impure pipe re runs on every change detection cycle (needed for things like filtering a mutating array) — flexible but a performance risk if overused. The pipe is a special built in impure pipe that subscribes to an Observable/Promise and auto unsubscribes. Custom pipe: Pitfall. Don't use an impure pipe to filter/sort large lists on every cycle — it kills performance. Do the filtering in the component (or with an RxJS pipeline) and bind the result. Interview angle. \"What's a pipe?\" — a template value transformer. \"Pure vs impure pipe?\" — pure re runs only on reference change (fast); impure runs every cycle. \"What does the pipe do?\" — subscribes to an Observable and manages unsubscription automatically."},{"id":"12-lifecycle-hooks","heading":"12. Lifecycle Hooks","level":2,"text":"Theory. Angular calls specific methods on a component at well defined moments in its life — creation, change, destruction. Implementing these lifecycle hooks lets you run code at the right time (fetch data, react to input changes, clean up subscriptions). The ones you must know: Hook When it runs Typical use Once, after first inputs set Initial data fetch, setup Whenever an changes React to new input values Just before the component is removed Unsubscribe , cleanup After the view & child views render Access DOM/ How it works. During change detection, Angular invokes these hooks in a fixed order ( → → ... → , and at teardown). Putting code in the wrong hook causes bugs — e.g., touching a in fails because the view isn't rendered yet (use ). Pitfall. The most common real world bug: forgetting cleanup, leaking subscriptions. Prefer the pipe or so you don't have to manage this manually. Also, don't put heavy work in (it fires often). Interview angle. \"Why instead of the constructor for data fetching?\" — the constructor is for DI/cheap setup; runs after Angular has set the inputs, so it's the correct place for initialization logic. \"How do you prevent memory leaks?\" — clean up in (or use async pipe)."},{"id":"13-change-detection-performance","heading":"13. Change Detection & Performance","level":2,"text":"Theory. Change detection (CD) is how Angular keeps the DOM in sync with your data: after any event (click, HTTP response, timer), Angular walks the component tree and updates the DOM where bound values changed. Understanding CD is the key to Angular performance. How it works. Angular uses a library called Zone.js that patches async APIs (events, , XHR). When any async task completes, Zone tells Angular \"something might have changed,\" and Angular runs change detection from the root down the tree, comparing each binding's current value to its previous value and updating the DOM on a difference. By default this checks every component on every cycle — usually fast, but wasteful in large trees. The big optimization — . Setting a component's tells Angular: \"only re check me when my reference changes, an event fires inside me, or an pipe emits.\" This skips entire subtrees that can't have changed. Why needs immutability. Because compares input references , you must replace objects/arrays rather than mutating them ( , not ). Mutating in place keeps the same reference, so won't detect the change and the view goes stale. Other performance levers: in (reuse DOM nodes). Lazy load feature routes (smaller initial bundle). pipe instead of manual subscribe (fewer manual CD triggers, no leaks). The modern Signals model (next section) gives fine grained updates without checking the whole tree. Interview angle. \"How does change detection work?\" — Zone.js notices async events and Angular re checks the tree, updating changed bindings. \"What does do and what does it require?\" — checks only on input reference change/events/async; requires immutable data. \"Performance tips?\" — + immutability, , lazy loading, signals."},{"id":"14-modules-vs-standalone-components","heading":"14. Modules vs Standalone Components","level":2,"text":"Theory. Historically, Angular grouped components, directives, and pipes into NgModules ( ) that declared what belonged together and what was imported. Since Angular 14+ (and the default in 17+), standalone components remove the need for NgModules: a component declares its own dependencies in its array and is used directly. This is now the recommended approach. How it works & why it changed. NgModules added boilerplate and indirection — you had to register every component in a module's and manage what each module imported/exported. Standalone components make each component self describing (it lists its own imports), which is simpler to reason about, easier to lazy load, and friendlier to newcomers. Pitfall. Mixed codebases: you'll encounter both styles. A standalone component is (in another component's ), while an NgModule declared component is (in a module). Mixing them up (\"component is part of the declaration of two modules\" errors) is a classic beginner trap. Interview angle. \"NgModule vs standalone components?\" — standalone (the modern default) removes NgModule boilerplate; each component declares its own imports, simplifying structure and lazy loading. Be aware NgModules still exist in older apps."},{"id":"15-signals-modern-angular","heading":"15. Signals (Modern Angular)","level":2,"text":"Theory. Signals (stable since Angular 17) are a new reactive primitive for state: a signal holds a value, and anything that reads it automatically tracks it, so when the value changes, only the things that depend on it update. This brings fine grained reactivity — updates without relying on Zone.js scanning the whole component tree — making apps faster and the data flow easier to follow. How it works — the three pieces. — a container you read by calling it ( ) and write with / . — a derived signal that automatically recalculates only when the signals it reads change (and caches otherwise). — runs a side effect whenever any signal it reads changes (logging, syncing). Because reads are tracked precisely, Angular updates exactly the DOM bindings that depend on a changed signal — no tree wide check. This is the future direction of Angular (eventually reducing the Zone.js dependency). Signals vs RxJS (a likely 2026 interview question). Signals are great for synchronous component state (simple, no subscriptions, no leaks). RxJS still shines for async streams and event composition (HTTP, debounced search, websockets). They coexist — use signals for local state, RxJS for streams, with interop helpers ( / ) bridging them. Interview angle. \"What are signals and why were they added?\" — a fine grained reactivity primitive for state that updates only dependents, improving performance and simplicity over whole tree change detection. \"Signals vs RxJS?\" — signals for sync state, RxJS for async streams; they interoperate."},{"id":"16-state-management-ngrx","heading":"16. State Management (NgRx)","level":2,"text":"Theory. As apps grow, sharing and coordinating state across many components gets hard. For simple needs, a shared service with a (or signals) is enough. For large, complex apps, NgRx provides a Redux style centralized store: a single immutable state tree updated through a strict, predictable, debuggable flow. How it works — the NgRx data flow: Store — the single source of truth (one immutable state object). Action — a plain event describing \"something happened\" ( , ). Reducer — a pure function that produces the next state immutably (never mutates). Selector — a memoized query to read a slice of state efficiently. Effect — handles side effects (API calls): listens for an action, does async work, dispatches a result action. Example flow. A component dispatches . An effect catches it, calls the API, and on success dispatches . The reducer for that action returns new state with the users. A selector feeds those users back into any component that subscribes — all unidirectional and traceable. Pitfall — over engineering. NgRx adds significant boilerplate. Don't reach for it on a small app; a service with signals/ is simpler and sufficient. Use NgRx when many components share complex state, you need time travel debugging, or strict predictability across a large team. Interview angle. \"When do you use NgRx vs a service?\" — services for simple shared state; NgRx for large apps needing a predictable, centralized, debuggable store. \"Explain the NgRx flow\" — action → reducer (pure) → store → selector → component, with effects for async. \"Why must reducers be pure?\" — predictability, testability, and so the store can track every state change."},{"id":"17-testing-angular","heading":"17. Testing Angular","level":2,"text":"Theory. Angular ships with first class testing support: Jasmine (assertion/spec framework) + Karma (test runner), plus the utility to create components with their dependencies injected (often mocked). Testability is a core reason DI exists — you swap real services for fakes effortlessly. How it works. builds a miniature Angular environment: it configures a testing module, instantiates the component, and wires DI — letting you provide mock services via . runs change detection so lifecycle hooks fire and the template renders, which you can then query and assert on. The testing pyramid for Angular: Unit tests — services and component logic in isolation (fast, most numerous). Component/integration tests — component + template via . E2E tests — the full app in a browser (Cypress/Playwright), fewest. Pitfall. Forgetting means the template never renders and may not run — your assertions test nothing. Also, prefer mocking (via ) over hitting real endpoints. Interview angle. \"How do you test an Angular component?\" — to create it with mocked dependencies, to render, then assert. \"How does DI help testing?\" — you inject mocks in place of real services without touching the component."},{"id":"18-build-tools-angular-cli","heading":"18. Build Tools & Angular CLI","level":2,"text":"Theory. The Angular CLI is the official command line tool that scaffolds, builds, serves, tests, and optimizes Angular apps — a major productivity boost and a reason teams choose Angular. It enforces a consistent project structure and handles the complex build pipeline for you. How it works — the production build. does AOT (Ahead of Time) compilation (compiles templates to JS at build time, not in the browser), tree shaking (drops unused code), minification , and bundling — producing small, fast assets. AOT also catches template errors at build time instead of at runtime. JIT vs AOT (interview point): JIT (Just in Time) — compiles templates in the browser at runtime (old dev default; slower startup, bigger bundle). AOT (Ahead of Time) — compiles at build time (the default now; faster, smaller, errors caught early). Production always uses AOT. Pitfall. Shipping a dev build to production (no AOT/minification) gives slow load times and a huge bundle. Always build with the production configuration. Watch bundle size — lazy load routes and check the size report. Interview angle. \"What does the Angular CLI do?\" — scaffold, serve, build, test, and optimize. \"AOT vs JIT?\" — AOT compiles at build time (faster, smaller, errors caught early) and is the production default; JIT compiles in the browser."},{"id":"19-angular-vs-react","heading":"19. Angular vs React","level":2,"text":"Theory. A near guaranteed question if the role touches both. Frame it as framework vs library and trade offs, not \"which is better.\" Aspect Angular React Type Full framework (batteries included) UI library (assemble your own stack) Language TypeScript (first class) JavaScript or TypeScript Structure Opinionated, consistent Flexible, you decide DI Built in, powerful Not built in (use context/props) Data flow Two way binding available One way by default Reactivity RxJS + Signals + Zone.js CD Hooks + virtual DOM diffing Routing/HTTP/Forms Built in Separate libraries Learning curve Steeper (more concepts) Gentler core, choices add up Best fit Large enterprise apps, big teams Anything from widgets to large apps How to answer well. \"Angular gives you a complete, consistent framework with DI, routing, forms, and HTTP built in and TypeScript throughout — great for large teams that value uniformity. React is a focused UI library you compose with your chosen tools — more flexibility, lighter core. Angular has a steeper learning curve but less decision making; React is quicker to start but you assemble the ecosystem yourself.\" Interview angle. Don't say one is \"better.\" Show you understand the trade off (consistency/built ins vs flexibility/ecosystem) and can pick based on team size, app scale, and need for structure."},{"id":"20-coding-problems-patterns","heading":"20. Coding Problems & Patterns","level":2,"text":"1. Debounced search (RxJS) — the canonical Angular task: 2. Reusable confirm dialog via a service — encapsulate shared UI/logic in an injectable service, return an Observable the caller subscribes to for the result. 3. Custom structural directive (e.g., ) — show/hide content based on a condition; demonstrates understanding of / . 4. + immutable update — given a list component, make it and update the list with (not ) so change detection fires. 5. Parent–child with / — a list (parent) renders rows (child); each row emits a event the parent handles. Classic whiteboard component task. Tips: narrate your reasoning, use the pipe to avoid leaks, prefer reactive forms, and mention / for performance — interviewers love unprompted performance awareness."},{"id":"21-interview-qa","heading":"21. Interview Q&A","level":2,"text":"Q1. Angular vs AngularJS vs React? Angular (2+) is a modern TypeScript framework; AngularJS (1.x) is the retired, architecturally different predecessor; React is a UI library, not a full framework. Q2. Explain the four types of data binding. Interpolation and property (class→view), event (view→class), two way (both — sugar for property + event binding). Q3. What is dependency injection and why does Angular use it? The framework creates and supplies dependencies (services) to classes instead of them constructing their own — giving testability, loose coupling, and easy singletons ( ). Q4. Observable vs Promise? Observable = lazy, multiple values over time, cancelable, composable with operators; Promise = eager, single value, not cancelable. Angular APIs (HTTP, router, forms) return Observables. Q5. What is and when do you use it? It maps to an inner Observable and cancels the previous one when a new value arrives — ideal for type ahead search so only the latest request's results are used. Q6. How do you prevent subscription memory leaks? Use the pipe, , or unsubscribe in . Q7. How does change detection work, and what is ? Zone.js notifies Angular of async events; Angular re checks bindings down the tree and updates the DOM. limits checks to input reference changes/events/async emissions and requires immutable data. Q8. Template driven vs reactive forms? Template driven (logic in HTML, simple forms) vs reactive (model in code, complex/dynamic, testable). Prefer reactive for non trivial forms. Q9. vs constructor? Constructor is for DI and cheap setup; runs after inputs are set — the right place for initialization like data loading. Q10. NgModules vs standalone components? Standalone (modern default) removes NgModule boilerplate — each component declares its own imports; simpler and better for lazy loading. Q11. What are signals? A fine grained reactivity primitive for state ( , , ) that updates only dependents — faster and simpler than whole tree change detection; compl"}],"qa":[{"q":"Angular vs AngularJS vs React?","a":"Angular (2+) is a modern TypeScript framework; AngularJS (1.x) is the retired, architecturally different predecessor; React is a UI library, not a full framework."},{"q":"Explain the four types of data binding.","a":"Interpolation `{{ }}` and property `[ ]` (class→view), event `( )` (view→class), two-way `[( )]` (both — sugar for property + event binding)."},{"q":"What is dependency injection and why does Angular use it?","a":"The framework creates and supplies dependencies (services) to classes instead of them constructing their own — giving testability, loose coupling, and easy singletons (`providedIn: 'root'`)."},{"q":"Observable vs Promise?","a":"Observable = lazy, multiple values over time, cancelable, composable with operators; Promise = eager, single value, not cancelable. Angular APIs (HTTP, router, forms) return Observables."},{"q":"What is `switchMap` and when do you use it?","a":"It maps to an inner Observable and **cancels the previous** one when a new value arrives — ideal for type-ahead search so only the latest request's results are used."},{"q":"How do you prevent subscription memory leaks?","a":"Use the `async` pipe, `takeUntilDestroyed()`, or unsubscribe in `ngOnDestroy`."},{"q":"How does change detection work, and what is `OnPush`?","a":"Zone.js notifies Angular of async events; Angular re-checks bindings down the tree and updates the DOM. `OnPush` limits checks to input reference changes/events/async emissions and requires immutable data."},{"q":"Template-driven vs reactive forms?","a":"Template-driven (logic in HTML, simple forms) vs reactive (model in code, complex/dynamic, testable). Prefer reactive for non-trivial forms."},{"q":"`ngOnInit` vs constructor?","a":"Constructor is for DI and cheap setup; `ngOnInit` runs after inputs are set — the right place for initialization like data loading."},{"q":"NgModules vs standalone components?","a":"Standalone (modern default) removes NgModule boilerplate — each component declares its own imports; simpler and better for lazy loading."},{"q":"What are signals?","a":"A fine-grained reactivity primitive for state (`signal`, `computed`, `effect`) that updates only dependents — faster and simpler than whole-tree change detection; complements RxJS (sync state vs async streams)."},{"q":"What is an HTTP interceptor?","a":"A function in the request/response pipeline used for cross-cutting concerns — attaching auth headers, logging, and global error handling."},{"q":"When would you use NgRx?","a":"For large apps with complex shared state needing a predictable, centralized, debuggable store; for simple cases a service with `BehaviorSubject`/signals is enough."},{"q":"AOT vs JIT?","a":"AOT compiles templates at build time (smaller, faster, errors caught early — production default); JIT compiles in the browser at runtime."},{"q":"What does `trackBy` do in `*ngFor`?","a":"Gives Angular a stable identity per item so it reuses DOM nodes instead of re-rendering the whole list — a key performance optimization (like React's `key`)."}]},"32_Real_Interview_Questions.md":{"title":"Real Interview Questions","trackId":"utility","trackTitle":"Planning","sections":[{"id":"real-interview-questions-company-wise-qa","heading":"Real Interview Questions — Company-wise Q&A","level":1,"text":""},{"id":"actual-questions-from-real-technical-rounds-with-full-answers","heading":"Actual questions from real technical rounds, with full answers","level":3,"text":""},{"id":"1-how-to-use-this-guide","heading":"1. How to use this guide","level":2,"text":"This page is your real interview bank — not generic theory, but questions that were actually asked in technical rounds, with answers you can revise before your next interview. How it's organized: Each company + round gets its own section below. Questions are grouped by topic (OOP, Java 8, coding, etc.). Answers are written for spoken interview style — short opening line, then depth if they probe. Tip: Read the Fast answer first (30 seconds), then the details if the interviewer asks \"tell me more\" or \"give an example.\""},{"id":"2-product-company-java-backend-round-1","heading":"2. Product Company — Java Backend, Round 1","level":2,"text":"Role: Software Engineer — Java Experience: 3 years Round: First technical (coding + core Java + OOP) Location: Pune"},{"id":"q1-inheritance-vs-composition","heading":"Q1. Inheritance vs Composition","level":3,"text":"Fast answer Inheritance and composition are two ways to build relationships between classes. Rule of thumb: Prefer composition over inheritance unless there is a clear \"is a\" relationship. Inheritance Composition Models is a (Dog is an Animal) has a (Car has an Engine) Mechanism Child inherits from parent Object contains other objects Coupling Tight — parent changes affect children Looser — swap parts easily Flexibility Fixed hierarchy Change behavior at runtime Risk Deep trees, fragile base class Slightly more boilerplate Inheritance — when it fits Use when the subclass truly is a specialized version of the parent and the hierarchy is stable. Pros: simple reuse, natural polymorphism. Cons: tight coupling; parent changes can break children; deep trees are hard to maintain. Composition — when it fits Use when one object has another and you want to swap or combine behavior. Pros: flexible, easy to test, follows Single Responsibility, swap implementations. Cons: a bit more code (delegation). Real world example — Strategy with composition Game design example (why composition scales) Inheritance explosion: Composition: 30 second interview close \"Inheritance models is a; composition models has a. I default to composition for flexibility and loose coupling, and use inheritance only when the relationship is stable and truly is a.\""},{"id":"q2-coding-invert-manager-employee-map-streams","heading":"Q2. Coding — Invert Manager → Employee map (Streams)","level":3,"text":"Problem Given where key = manager ID and value = set of employee IDs, produce where key = employee ID and value = set of manager IDs. Example Input: Output: Correct solution Why the common mistake happens Correct mental model → manager ID ( ) → of employees ( , ) For each employee, pair: → Full solution (as written in the interview) Sample input from the round flatMap() — step by step walkthrough Input: Iteration 1 — receives : 1. → , 2. → , Iteration 2 — receives : 1. Stream → , 2. Map → , After flattening , the stream contains: After + + : Hierarchy example (manager is also an employee) Input: After (before grouping): Final output: 30 second explanation (say this in the interview) \"Inside flatMap, is the set of employees and is the manager. For each employee I create pairs, then groupingBy collects all managers per employee into a Set.\""},{"id":"q3-what-test-cases-would-you-write-for-the-invert-problem","heading":"Q3. What test cases would you write for the invert problem?","level":3,"text":"Fast answer Cover happy path, multiple managers per employee, empty/null inputs, and hierarchy edge cases. Scenario Input Expected output 1 Normal 2 Multiple managers ⭐ 3 Empty map 4 Null input or throw (document contract) 5 Manager, no reportees 6 Single pair 7 Manager is also employee ⭐ 8 Duplicates in set Same as (Set dedupes) 9 Null reportee set NPE without filter — handle with or Interview one liner \"I test normal input, employee with multiple managers, empty and null maps, empty reportee lists, manager as employee hierarchies, and null values in the map — plus duplicate employees which Set handles automatically.\""},{"id":"q4-important-edge-cases-beyond-basic-tests","heading":"Q4. Important edge cases (beyond basic tests)","level":3,"text":"Must mention in interview: 1. Empty input → 2. Null input → return empty map or (state your choice) 3. Manager with no reportees → no employees in output 4. Employee with multiple managers — validates + + 5. Manager is also an employee — common in org charts ( , ) 6. Null reportee set — throws NPE without a null check 7. Circular hierarchy — fine for this invert problem (no recursion); matters if you later traverse the tree 8. Duplicate keys in input — impossible in a (keys unique) Spoken answer \"For this inversion I considered empty and null inputs, managers with no reportees, employees reporting to multiple managers, managers who are also employees, null reportee sets, and circular reporting. Cycles don't break inversion, but they'd matter if we walked the hierarchy recursively.\""},{"id":"q5-when-to-use-an-abstract-class","heading":"Q5. When to use an Abstract Class?","level":3,"text":"Fast answer Use an abstract class when subclasses share common code and you want to force certain methods to be implemented — but you never want anyone to instantiate the base type directly. Use an abstract class when: Situation Why abstract class Common implementation for multiple subclasses Put shared logic in the base; subclasses inherit it Force subclasses to implement specific methods Declare methods — compiler enforces override Base object should never be created directly class cannot be instantiated with Abstract class vs Interface (quick contrast) Abstract class Interface State (fields) Can have instance fields Only constants (until Java 8+ default methods) Constructor Yes No Multiple inheritance No (single extends) Yes (implements many) When to pick Shared code + forced overrides Pure contract / capability 30 second interview close \"I use an abstract class when several subclasses share real implementation and I need to force certain methods while preventing direct instantiation of the base type.\""},{"id":"q6-hashmap-vs-concurrenthashmap","heading":"Q6. HashMap vs ConcurrentHashMap","level":3,"text":"Fast answer Both store key value pairs . is for single threaded use; is thread safe for concurrent read/write. HashMap ConcurrentHashMap Thread safe ❌ No ✅ Yes Null key ✅ One null key allowed ❌ Not allowed Null values ✅ Multiple null values ❌ Not allowed Performance O(1) average for get/put/remove O(1) average; scales under concurrency Use when Single threaded apps Multi threaded / concurrent apps Internal working — HashMap 1. Stores entries in an array of buckets . 2. Uses to pick the bucket index. 3. Uses to resolve key equality inside a bucket. 4. From Java 8 : long collision chains convert to a Red Black Tree (when bucket size 8) for better worst case performance. Internal working — ConcurrentHashMap 1. Also uses buckets and hashing. 2. Uses fine grained synchronization — locks at bucket/segment level, not the whole map. 3. Uses CAS (Compare And Swap) atomic operations so different threads can work on different buckets concurrently. 4. Much better than wrapping a in or . Why CHM disallows null keys/values In concurrent code, is ambiguous — does it mean \"key not present\" or \"value is null\"? That ambiguity causes bugs under races, so CHM rejects nulls outright. 30 second interview close \"HashMap is fast but not thread safe and allows one null key. ConcurrentHashMap is for multi threaded apps — fine grained locking and CAS per bucket, no null keys or values, and much better throughput than synchronizing the entire map.\""},{"id":"q7-checked-vs-unchecked-exceptions","heading":"Q7. Checked vs Unchecked Exceptions","level":3,"text":"Fast answer Checked (Compile time) Unchecked (Runtime) Checked by compiler ✅ Yes — must handle or declare ❌ No — optional to handle When thrown Compile time enforcement; thrown at runtime if triggered Only at runtime Extends (not ) Handle with or Optional Checked exception examples Unchecked (runtime) exception examples Compile time error vs runtime exception — don't confuse them A compile time error happens when cannot produce a file — the JVM never runs. Since compilation fails → is not created → JVM never starts . Correct — declare with throws Correct — handle with try catch (compiles; exception may still throw at runtime) Code compiles because the checked exception is handled. At runtime : if exists → no exception; if missing → thrown and caught. The FileNotFoundException trap (interview favorite) What you write When error appears with no try/catch or throws Compile time error try catch around Compiles fine; exception thrown at runtime only if file missing Easy way to remember Compile time: Java checks whether you've handled the possibility of a checked exception. Runtime: Java actually tries to open the file — if it's not there, it throws . 30 second interview close \"Checked exceptions like FileNotFoundException must be handled or declared at compile time. Unchecked exceptions like NullPointerException are not enforced by the compiler. A compile time error means javac failed — the program never runs. Runtime exceptions happen while the JVM is executing.\""},{"id":"q8-what-is-a-compile-time-error-full-interview-answer","heading":"Q8. What is a compile-time error? (Full interview answer)","level":3,"text":"Fast answer A compile time error occurs during compilation when the Java compiler ( ) detects invalid code. The file is never created, so the program never runs . Common causes Cause Example Syntax errors Missing , unmatched Undeclared variables when doesn't exist Type mismatch — String assigned to int Unhandled checked exception without try catch or throws Wrong method signature Calling method with wrong argument types Type mismatch example Spoken interview answer \"A compile time error happens when javac analyzes the source and finds something invalid — syntax mistakes, undeclared variables, type mismatches, or unhandled checked exceptions. For example, fails at compile time because String and int are incompatible. Since compilation fails, no .class file is produced and the JVM never executes the program.\" Checked exception — what the compiler actually does When the compiler sees : 1. It knows constructor can throw (checked). 2. It checks whether catches it or declares . 3. If neither → stops compilation with . 4. No file → JVM never starts → program never executes. This is why interviewers ask: \"Is FileNotFoundException compile time or runtime?\" — both are involved, in different ways : Compile time: compiler forces you to handle the possibility . Runtime: JVM throws it only if the file actually doesn't exist (after you've handled the compile time requirement)."},{"id":"3-more-companies-coming-soon","heading":"3. More companies coming soon","level":2,"text":"New company rounds will be added here as you share them. Each gets its own section with full Q&A. Planned format per entry: Company / role / round metadata Questions grouped by topic Fast answer + deep answer + code where needed Test cases / edge cases for coding problems Send your next interview questions and we'll add the next section."}],"qa":[]},"Complete_PeerReview_Report.md":{"title":"Peer Review Report","trackId":"utility","trackTitle":"Planning","sections":[{"id":"complete-peer-review-report","heading":"Complete Peer Review Report","level":1,"text":"Reviewed By: Prabhat Singh Rajput Date: 10 April 2026 Total Files Reviewed: 3 Total Issues Found: 41"},{"id":"1-customerrequestjava","heading":"1. CustomerRequest.java","level":1,"text":"Package: com.jpmc.payments Total Issues: 15"},{"id":"issue-1-thread-safety-issued-is-not-atomic","heading":"Issue 1 — Thread Safety: issued++ is Not Atomic","level":3,"text":"Line: 18, 48 Severity: CRITICAL Problem: only guarantees visibility across threads but does NOT make the operation atomic. is a read modify write operation — in a multi threaded environment, two threads can read the same value simultaneously, both increment, and write back the same result, causing a lost update (race condition). Problematic Code (Line 18): And Line 48: Fix:"},{"id":"issue-2-thread-safety-arraylist-is-not-thread-safe","heading":"Issue 2 — Thread Safety: ArrayList is Not Thread-Safe","level":3,"text":"Line: 17 Severity: CRITICAL Problem: is not synchronized. Concurrent additions or iterations from multiple threads can cause or data corruption. Problematic Code (Line 17): Fix:"},{"id":"issue-3-invalid-requests-still-added-to-list-and-counted","heading":"Issue 3 — Invalid Requests Still Added to List and Counted","level":3,"text":"Lines: 47–49 Severity: HIGH Problem: is called and result stored in , but regardless of the result, is incremented and the request is added to . This means invalid/corrupt requests pollute the list and inflate the counter. Problematic Code (Lines 47–49): Fix:"},{"id":"issue-4-urgentonly-filter-applied-after-http-call-logic-bug","heading":"Issue 4 — urgentOnly Filter Applied AFTER HTTP Call (Logic Bug)","level":3,"text":"Lines: 86–88 Severity: HIGH Problem: The check is placed AFTER an expensive HTTP API call is already made. Non urgent requests are fetched and then skipped — wasting network calls and time. The filter must be applied BEFORE the API call. Problematic Code (Lines 86–88): (This appears after the HTTP connection on Lines 80–84) Fix: Move the filter check BEFORE the HTTP call:"},{"id":"issue-5-totalvalue-makes-n-api-calls-in-a-loop-performance-issue","heading":"Issue 5 — totalValue() Makes N API Calls in a Loop (Performance Issue)","level":3,"text":"Lines: 77–93 Severity: HIGH Problem: For every request in , a separate HTTP call is made to fetch the product price. This results in N network calls for N requests — extremely inefficient and slow, especially under load. Problematic Code (Lines 77–81): Fix: Collect all unique values first, fetch prices in batch (or cache them), then calculate values:"},{"id":"issue-6-http-connection-never-closed-resource-leak","heading":"Issue 6 — HTTP Connection Never Closed (Resource Leak)","level":3,"text":"Lines: 63–69 and 80–84 Severity: HIGH Problem: is opened but is never called in either or . This causes connection/resource leaks over time. Problematic Code (Lines 63–69): Fix:"},{"id":"issue-7-submitrequest-sends-empty-post-body","heading":"Issue 7 — submitRequest() Sends Empty POST Body","level":3,"text":"Lines: 65–66 Severity: HIGH Problem: signals that data will be written to the connection, but no data is ever actually written to the output stream. The POST body is completely empty. Also, no header is set. Problematic Code (Lines 65–66): Fix:"},{"id":"issue-8-no-null-check-for-environment-parameter","heading":"Issue 8 — No Null Check for environment Parameter","level":3,"text":"Lines: 60–61 Severity: HIGH Problem: returns if the environment key is not found in the map. The next line immediately uses to build the endpoint — causing a . Problematic Code (Lines 60–61): Fix:"},{"id":"issue-9-api_servers-is-public-and-mutable","heading":"Issue 9 — API_SERVERS is Public and Mutable","level":3,"text":"Lines: 10–15 Severity: MEDIUM Problem: The map is and backed by a , meaning any external class can modify it (e.g., override PROD URL). This is a serious security and stability concern. Problematic Code (Lines 10–15): Fix:"},{"id":"issue-10-req_key-not-guaranteed-unique","heading":"Issue 10 — req_key Not Guaranteed Unique","level":3,"text":"Line: 46 Severity: MEDIUM Problem: is built using . Two requests created within the same millisecond for the same product and customer will get identical values — causing key collision. Problematic Code (Line 46): Fix:"},{"id":"issue-11-totalvalue-hardcodes-prod-environment","heading":"Issue 11 — totalValue() Hardcodes \"PROD\" Environment","level":3,"text":"Line: 79 Severity: MEDIUM Problem: The environment is hardcoded as inside . This makes the method untestable in non production environments (DEV, LOCAL, QAT) and violates the open/closed principle. Problematic Code (Line 79): Fix:"},{"id":"issue-12-price-is-always-00-incomplete-implementation","heading":"Issue 12 — price is Always 0.0 (Incomplete Implementation)","level":3,"text":"Lines: 83–84 Severity: MEDIUM Problem: is declared as with a comment saying it's a placeholder. The XML/JSON parsing from the API response is never implemented, meaning will always return regardless of actual data. Problematic Code (Lines 83–84): Fix: Implement the actual response parsing logic using the already imported and (or use a JSON parser if response is JSON)."},{"id":"issue-13-unused-imports","heading":"Issue 13 — Unused Imports","level":3,"text":"Lines: 5–6 Severity: LOW Problem: and are imported but never used anywhere in the code. This adds unnecessary clutter and confusion. Problematic Code (Lines 5–6): Fix: Remove both unused imports, or implement the XML parsing logic they were intended for (see Issue 12)."},{"id":"issue-14-non-standard-field-naming-java-convention-violation","heading":"Issue 14 — Non-Standard Field Naming (Java Convention Violation)","level":3,"text":"Lines: 20–21, 25–26 Severity: LOW Problem: Java naming conventions require for field names. Using (underscores) is a violation of standard Java coding style and reduces readability. Problematic Code (Lines 20–21, 25–26): Fix:"},{"id":"issue-15-static-list-grows-forever-memory-leak","heading":"Issue 15 — Static List Grows Forever (Memory Leak)","level":3,"text":"Line: 17 Severity: MEDIUM Problem: is a static with no mechanism to remove old or processed entries. It accumulates every request created for the entire lifetime of the application — this is a memory leak that worsens over time in long running applications. Problematic Code (Line 17): Fix:"},{"id":"customerrequestjava-summary-table","heading":"CustomerRequest.java — Summary Table","level":3,"text":"Line(s) Issue Severity 1 18, 48 not atomic (race condition) CRITICAL 2 17 not thread safe CRITICAL 3 47–49 Invalid requests added to list and counted HIGH 4 86–88 filter after API call HIGH 5 77–81 N API calls in loop — performance issue HIGH 6 63–69, 80–84 HTTP connection never closed — resource leak HIGH 7 65–66 Empty POST body sent HIGH 8 60–61 No null check for environment — NullPointerException HIGH 9 10–15 public and mutable MEDIUM 10 46 not unique MEDIUM 11 79 hardcodes \"PROD\" MEDIUM 12 83–84 always 0.0 — incomplete implementation MEDIUM 13 5–6 Unused imports LOW 14 20–21, 25–26 snake case field names — Java convention violation LOW 15 17 Static list grows forever — memory leak MEDIUM"},{"id":"2-paymentgatewayservicejava","heading":"2. PaymentGatewayService.java","level":1,"text":"Package: com.jpmc.payments.gateway Total Issues: 14"},{"id":"issue-1-critical-security-hardcoded-encryption-key-in-source-code","heading":"Issue 1 — CRITICAL SECURITY: Hardcoded Encryption Key in Source Code","level":3,"text":"Line: 12 Severity: CRITICAL Problem: The encryption key (which appears to be a live production API/secret key) is hardcoded directly in the source code. Anyone with access to the repository, build artifacts, or decompiled JAR can see this key. This is an immediate security breach. Problematic Code (Line 12): Fix:"},{"id":"issue-2-critical-security-pci-dss-violation-logging-card-number-and-cvv","heading":"Issue 2 — CRITICAL SECURITY / PCI DSS VIOLATION: Logging Card Number and CVV","level":3,"text":"Lines: 17–18 Severity: CRITICAL Problem: Card number and CVV are being printed in plain text to the application log. This is a direct PCI DSS (Payment Card Industry Data Security Standard) violation. CVV must NEVER be logged, stored, or transmitted after the authorization step. Exposing card numbers in logs is also strictly prohibited. Problematic Code (Lines 17–18): Fix:"},{"id":"issue-3-critical-security-hardcoded-database-credentials","heading":"Issue 3 — CRITICAL SECURITY: Hardcoded Database Credentials","level":3,"text":"Lines: 27–29 Severity: CRITICAL Problem: Database URL, username, and password are hardcoded in plain text in the source code. This exposes production credentials to anyone who can read the code, making the database vulnerable to unauthorized access. Problematic Code (Lines 27–29): Fix:"},{"id":"issue-4-critical-security-pci-dss-storing-cvv-in-database","heading":"Issue 4 — CRITICAL SECURITY / PCI DSS: Storing CVV in Database","level":3,"text":"Lines: 30–32 Severity: CRITICAL Problem: PCI DSS (Requirement 3.2) explicitly prohibits storing CVV/CVC/CAV2 data after authorization — even in encrypted form. The code inserts CVV directly into the table, which is a severe compliance violation that can result in loss of payment processing ability and heavy fines. Problematic Code (Lines 30–32): Fix:"},{"id":"issue-5-critical-security-sql-injection-in-all-queries","heading":"Issue 5 — CRITICAL SECURITY: SQL Injection in All Queries","level":3,"text":"Lines: 30–36, 51–52 Severity: CRITICAL Problem: All SQL queries are built by directly concatenating user supplied input (cardNumber, cvv, expiry, amount, currency, transactionId). This is a classic SQL Injection vulnerability — an attacker can input values like to manipulate or destroy the database. Problematic Code (Lines 30–32): Fix — Use PreparedStatement for ALL queries:"},{"id":"issue-6-no-transaction-management-data-integrity-risk","heading":"Issue 6 — No Transaction Management (Data Integrity Risk)","level":3,"text":"Lines: 30–37 Severity: HIGH Problem: Two INSERT statements are executed separately with no transaction wrapping. If the first INSERT (payment tokens) succeeds but the second INSERT (charges) fails, you end up with an orphaned token record and no charge record. The database is left in an inconsistent state. Problematic Code (Lines 30–37): Fix:"},{"id":"issue-7-database-connection-never-closed-resource-leak","heading":"Issue 7 — Database Connection Never Closed (Resource Leak)","level":3,"text":"Lines: 22, 26–29 Severity: HIGH Problem: is declared outside the try block but is never closed in a block or try with resources. If an exception occurs, the connection leaks, eventually exhausting the database connection pool and crashing the service. Problematic Code (Lines 22, 26–29): Fix:"},{"id":"issue-8-processrefund-connection-also-never-closed","heading":"Issue 8 — processRefund() Connection Also Never Closed","level":3,"text":"Lines: 55–59 Severity: HIGH Problem: Same resource leak issue in . A new connection is opened inside the try block but never closed. Also repeats the hardcoded credentials problem (Issue 3). Problematic Code (Lines 55–57): Fix:"},{"id":"issue-9-token_id-exposes-actual-card-data","heading":"Issue 9 — token_id Exposes Actual Card Data","level":3,"text":"Line: 40 Severity: HIGH Problem: returns the last 4 digits of the card number and returns it as . This is NOT a token — it's actual card data. A real tokenization system should generate an opaque random ID that has no mathematical relationship to the original card number. Problematic Code (Line 40): Fix:"},{"id":"issue-10-using-double-for-monetary-values","heading":"Issue 10 — Using double for Monetary Values","level":3,"text":"Line: 14 (method signature), 35, 51 Severity: HIGH Problem: uses floating point arithmetic which cannot precisely represent many decimal values (e.g., ). For financial calculations, this can result in incorrect amounts — overcharging or undercharging customers. Problematic Code (Line 14): Fix:"},{"id":"issue-11-deprecated-classforname-for-jdbc-driver","heading":"Issue 11 — Deprecated Class.forName() for JDBC Driver","level":3,"text":"Line: 25 Severity: MEDIUM Problem: is a legacy approach from JDBC 3.0. Since JDBC 4.0 (Java 6+), drivers are automatically discovered via the Service Provider Interface (SPI). This call is unnecessary and adds confusion. Problematic Code (Line 25): Fix: Remove this line entirely. Spring Boot's auto configuration handles driver registration automatically."},{"id":"issue-12-inconsistent-error-handling-between-methods","heading":"Issue 12 — Inconsistent Error Handling Between Methods","level":3,"text":"Lines: 42–44 vs 62 Severity: MEDIUM Problem: uses the proper to log errors (Line 43), but uses (Line 62). This is inconsistent and means refund errors won't appear in application logs or monitoring systems. Problematic Code (Line 62): Fix:"},{"id":"issue-13-processrefund-has-no-return-value-or-status","heading":"Issue 13 — processRefund() Has No Return Value or Status","level":3,"text":"Lines: 50–64 Severity: MEDIUM Problem: returns , so the caller has no way to know whether the refund succeeded or failed. All failures are silently swallowed. This means the system could tell a customer their refund was processed when it actually failed. Problematic Code (Line 50): Fix:"},{"id":"issue-14-logger-not-declared-as-static-final","heading":"Issue 14 — Logger Not Declared as static final","level":3,"text":"Line: 11 Severity: LOW Problem: The Logger is declared as an instance variable ( ) instead of a class level constant. This creates a new Logger reference per instance. Java best practice is to use . Problematic Code (Line 11): Fix:"},{"id":"paymentgatewayservicejava-summary-table","heading":"PaymentGatewayService.java — Summary Table","level":3,"text":"Line(s) Issue Severity 1 12 Hardcoded encryption/API key in source CRITICAL 2 17–18 Logging card number and CVV — PCI DSS violation CRITICAL 3 27–29 Hardcoded database credentials CRITICAL 4 30–32 Storing CVV in database — PCI DSS violation CRITICAL 5 30–36, 51–52 SQL Injection in all queries CRITICAL 6 30–37 No transaction management — data inconsistency risk HIGH 7 22, 26–29 DB connection never closed — resource leak HIGH 8 55–59 processRefund() connection never closed HIGH 9 40 exposes actual card digits — not a real token HIGH 10 14, 35, 51 Using for monetary values HIGH 11 25 Deprecated for JDBC driver MEDIUM 12 43 vs 62 Inconsistent error handling (Logger vs System.out) MEDIUM 13 50 returns void — no status reported MEDIUM 14 11 Logger not declared as LOW"},{"id":"3-inventoryalertservicejava","heading":"3. InventoryAlertService.java","level":1,"text":"Package: com.jpmc.inventory Total Issues: 12"},{"id":"issue-1-critical-security-hardcoded-slack-webhook-url","heading":"Issue 1 — CRITICAL SECURITY: Hardcoded Slack Webhook URL","level":3,"text":"Line: 14 Severity: CRITICAL Problem: The Slack webhook URL is hardcoded directly in source code. This is a live credential — anyone with access to the repository can use it to send arbitrary messages to your Slack channel or abuse the webhook. Webhook URLs must never be committed to source control. Problematic Code (Line 14): Fix: And in (loaded from environment/secrets manager):"},{"id":"issue-2-non-constant-fields-named-as-constants-naming-convention-violation","heading":"Issue 2 — Non-Constant Fields Named as Constants (Naming Convention Violation)","level":3,"text":"Lines: 12–14 Severity: MEDIUM Problem: , , and use UPPER SNAKE CASE which is the Java convention for constants. However, these fields are neither nor — they are mutable instance variables. This misleads developers into thinking these values cannot change. Problematic Code (Lines 12–14): Fix — Either make them true constants: Or rename them to camelCase if they need to be configurable:"},{"id":"issue-3-insecure-http-instead-of-https-for-internal-api","heading":"Issue 3 — Insecure HTTP Instead of HTTPS for Internal API","level":3,"text":"Line: 13 Severity: HIGH Problem: uses (unencrypted) to communicate with the inventory service. In a financial/enterprise environment, all internal service communication should use HTTPS/TLS to prevent man in the middle attacks and eavesdropping, even within an internal network. Problematic Code (Line 13): Fix:"},{"id":"issue-4-manual-json-parsing-is-extremely-fragile","heading":"Issue 4 — Manual JSON Parsing is Extremely Fragile","level":3,"text":"Lines: 29–34 Severity: HIGH Problem: The code manually parses JSON by removing brackets and braces with and splitting on commas. This is extremely brittle and will break for: Values containing commas (e.g., ) Nested JSON objects Arrays within the response Different key ordering Extra whitespace Any valid JSON the parser doesn't anticipate Problematic Code (Lines 29–34): Fix — Use a proper JSON library:"},{"id":"issue-5-silent-exception-swallowing-both-catch-blocks-are-empty","heading":"Issue 5 — Silent Exception Swallowing (Both Catch Blocks are Empty)","level":3,"text":"Lines: 38–39, 52–53 Severity: HIGH Problem: Both blocks are completely empty with only a comment. If the inventory API is down, if the response cannot be parsed, or if Slack fails, the error is completely hidden — no logging, no alerting, no monitoring. This makes debugging impossible and means failures go undetected silently. Problematic Code (Lines 38–39): And Lines 52–53: Fix:"},{"id":"issue-6-no-http-response-status-code-check","heading":"Issue 6 — No HTTP Response Status Code Check","level":3,"text":"Lines: 23–27 Severity: HIGH Problem: The code calls the inventory API and directly processes the response body without ever checking if the HTTP status code indicates success (2xx). If the server returns a 404, 500, or 503, the body might contain an error message which the parser will attempt to process and either silently fail or throw an exception. Problematic Code (Lines 23–27): Fix:"},{"id":"issue-7-unlimited-slack-alerts-no-deduplication-or-throttling","heading":"Issue 7 — Unlimited Slack Alerts — No Deduplication or Throttling","level":3,"text":"Lines: 35–37 Severity: HIGH Problem: Every time the scheduler runs (every 5 minutes), a Slack alert is sent for EVERY low stock item. If a product stays low stock for hours, the Slack channel will be flooded with hundreds of duplicate alerts. There is no tracking of which alerts have already been sent. Problematic Code (Lines 35–37): Fix — Track already alerted products:"},{"id":"issue-8-arrayindexoutofboundsexception-risk","heading":"Issue 8 — ArrayIndexOutOfBoundsException Risk","level":3,"text":"Line: 33 Severity: HIGH Problem: is accessed assuming items always come in pairs. If the parsed array has an odd number of elements (malformed response, trailing comma, etc.), will throw . Since the catch block is empty (Issue 5), this failure will be completely silent. Problematic Code (Line 33): Fix: Use a proper JSON parser (see Issue 4 fix) which eliminates this risk entirely."},{"id":"issue-9-json-payload-built-by-string-concatenation-injection-risk","heading":"Issue 9 — JSON Payload Built by String Concatenation (Injection Risk)","level":3,"text":"Lines: 44–45 Severity: MEDIUM Problem: The Slack message payload is built by string concatenation. If contains double quotes, backslashes, or special JSON characters, the resulting JSON will be malformed and the Slack API will reject it. Problematic Code (Lines 44–45): Fix — Use ObjectMapper to build JSON safely:"},{"id":"issue-10-no-null-or-empty-check-on-response-body","heading":"Issue 10 — No Null or Empty Check on Response Body","level":3,"text":"Line: 28 Severity: MEDIUM Problem: could return or an empty string. Calling on a null body throws . Neither case is handled. Problematic Code (Line 28): Fix:"},{"id":"issue-11-httpclient-field-not-declared-as-final","heading":"Issue 11 — HttpClient Field Not Declared as final","level":3,"text":"Line: 16 Severity: LOW Problem: is initialized once and never reassigned, but it is not declared . Making it communicates intent clearly, prevents accidental reassignment, and is a Java best practice. Problematic Code (Line 16): Fix:"},{"id":"issue-12-no-logger-defined-zero-observability","heading":"Issue 12 — No Logger Defined — Zero Observability","level":3,"text":"Entire Class Severity: HIGH Problem: The entire class has no logger defined. Combined with empty catch blocks (Issue 5), there is absolutely no visibility into what this service is doing — no record of when inventory checks ran, how many items were checked, which alerts were sent, or any errors. In production, this service is essentially a black box. Fix — Add a logger:"},{"id":"inventoryalertservicejava-summary-table","heading":"InventoryAlertService.java — Summary Table","level":3,"text":"Line(s) Issue Severity 1 14 Hardcoded Slack webhook URL — live credential in source CRITICAL 2 12–14 Non constant fields named as constants — misleading MEDIUM 3 13 HTTP instead of HTTPS for internal API HIGH 4 29–34 Manual JSON parsing — extremely fragile HIGH 5 38–39, 52–53 Both catch blocks empty — silent failure HIGH 6 23–27 HTTP response status never checked HIGH 7 35–37 Unlimited Slack alerts — no deduplication HIGH 8 33 ArrayIndexOutOfBoundsException risk HIGH 9 44–45 JSON payload built by string concatenation MEDIUM 10 28 No null/empty check on response body MEDIUM 11 16 not declared as LOW 12 Entire class No logger defined — zero observability HIGH"},{"id":"4-overall-summary","heading":"4. Overall Summary","level":1,"text":""},{"id":"issues-by-file","heading":"Issues by File","level":2,"text":"File CRITICAL HIGH MEDIUM LOW TOTAL CustomerRequest.java 2 6 5 2 15 PaymentGatewayService.java 5 5 3 1 14 InventoryAlertService.java 1 7 3 1 12 TOTAL 8 18 11 4 41"},{"id":"issues-by-severity-all-files","heading":"Issues by Severity (All Files)","level":2,"text":""},{"id":"critical-8-issues-fix-immediately","heading":"CRITICAL (8 Issues — Fix Immediately)","level":3,"text":"File Line(s) Issue CustomerRequest.java 18, 48 not atomic — race condition CustomerRequest.java 17 not thread safe PaymentGatewayService.java 12 Hardcoded encryption/API key in source code PaymentGatewayService.java 17–18 Logging card number and CVV — PCI DSS violation PaymentGatewayService.java 27–29 Hardcoded database credentials PaymentGatewayService.java 30–32 Storing CVV in database — PCI DSS violation PaymentGatewayService.java 30–52 SQL Injection in all queries InventoryAlertService.java 14 Hardcoded Slack webhook URL — live credential"},{"id":"high-18-issues","heading":"HIGH (18 Issues)","level":3,"text":"File Line(s) Issue CustomerRequest.java 47–49 Invalid requests added to list and counted CustomerRequest.java 86–88 filter applied after API call CustomerRequest.java 77–81 N API calls in loop — performance CustomerRequest.java 63–84 HTTP connection never closed CustomerRequest.java 65–66 Empty POST body sent CustomerRequest.java 60–61 No null check for environment — NPE PaymentGatewayService.java 30–37 No transaction management PaymentGatewayService.java 22–29 DB connection never closed PaymentGatewayService.java 55–59 processRefund() connection never closed PaymentGatewayService.java 40 exposes actual card digits PaymentGatewayService.java 14, 35 Using for monetary values InventoryAlertService.java 13 HTTP instead of HTTPS InventoryAlertService.java 29–34 Manual JSON parsing — fragile InventoryAlertService.java 38–53 Both catch blocks empty — silent failure InventoryAlertService.java 23–27 HTTP response status never checked InventoryAlertService.java 35–37 Unlimited Slack alerts — no deduplication InventoryAlertService.java 33 ArrayIndexOutOfBoundsException risk InventoryAlertService.java Whole class No logger — zero observability"},{"id":"medium-11-issues","heading":"MEDIUM (11 Issues)","level":3,"text":"File Line(s) Issue CustomerRequest.java 10–15 public and mutable CustomerRequest.java 46 not unique CustomerRequest.java 79 hardcodes \"PROD\" CustomerRequest.java 83–84 always 0.0 — incomplete CustomerRequest.java 17 Static list grows forever — memory leak PaymentGatewayService.java 25 Deprecated PaymentGatewayService.java 42–62 Inconsistent error handling PaymentGatewayService.java 50 returns void InventoryAlertService.java 12–14 Non constant fields named as constants InventoryAlertService.java 44–45 JSON payload by string concatenation InventoryAlertService.java 28 No null/empty check on response body"},{"id":"low-4-issues","heading":"LOW (4 Issues)","level":3,"text":"File Line(s) Issue CustomerRequest.java 5–6 Unused imports CustomerRequest.java 20–26 snake case field names PaymentGatewayService.java 11 Logger not InventoryAlertService.java 16 not declared"},{"id":"top-recommendations","heading":"Top Recommendations","level":2,"text":"1. Immediately revoke and rotate all hardcoded credentials and API keys found in source code 2. Replace all SQL string concatenation with to eliminate SQL Injection 3. Never log or store CVV — this is a PCI DSS violation with severe legal consequences 4. Use for all monetary calculations — never 5. Externalize all configuration (DB credentials, API keys, URLs) to environment variables or a secrets manager 6. Add proper logging to all services and handle all exceptions — never swallow silently 7. Use thread safe collections and atomic operations wherever shared state is accessed concurrently"}],"qa":[]},"README.md":{"title":"README","trackId":"other","trackTitle":"Other","sections":[{"id":"stackready","heading":"StackReady","level":1,"text":"Full stack interview preparation — Java, Spring Boot, React, System Design, DSA, Python, AWS, AI Agents. Author: Prabhat Singh Rajput 🌐 Live site: https://jerryd24.github.io/stack ready/"},{"id":"whats-inside","heading":"What's inside","level":2,"text":"Track Guides Java Core Java, Multithreading, Streams, JVM Performance, CP Tricks Spring Boot Spring Boot, Annotations deep dive Cloud Native & Distributed Kafka, Kubernetes & Docker, Resilience patterns, Observability, Redis, SQL at scale Design Patterns Java patterns, Python patterns System Design HLD, LLD (Java & Python) DSA Algorithms & patterns, Python CP Python Core, FastAPI, Python interview questions Frontend React.js complete guide Backend Node.js complete guide Cloud & Languages AWS, Go AI Agents & MCP"},{"id":"local-development","heading":"Local development","level":2,"text":"Open http://localhost:8080"},{"id":"update-content-website-auto-syncs","heading":"Update content → website auto-syncs","level":2,"text":"1. Edit any file in this repo root 2. Push to 3. GitHub Action runs and syncs 4. GitHub Pages serves the updated site (1–2 min) Manual sync (optional):"},{"id":"github-pages-setup-one-time","heading":"GitHub Pages setup (one-time)","level":2,"text":"1. Wait for Deploy StackReady to GitHub Pages to succeed (Actions tab) 2. Settings → Pages → Source: Deploy from a branch 3. Branch: → Folder: → Save Live URL: https://jerryd24.github.io/stack ready/"},{"id":"progress-tracking","heading":"Progress tracking","level":2,"text":"Your read status and roadmap checkboxes are saved in browser cookies (same browser, 1 year expiry). No account required."},{"id":"license","heading":"License","level":2,"text":"Personal study material — use and share freely."}],"qa":[]},"RESUME_STACKREADY.md":{"title":"RESUME_STACKREADY","trackId":"other","trackTitle":"Other","sections":[{"id":"stackready-resume-bullets","heading":"StackReady — Resume Bullets","level":1,"text":"Author: Prabhat Singh Rajput Live: https://jerryd24.github.io/stack ready/ GitHub: https://github.com/JerryD24/stack ready"},{"id":"project-title-for-resume","heading":"Project title for resume","level":2,"text":"StackReady JavaScript, HTML/CSS, GitHub Actions, GitHub Pages, AI/MCP Personal Project Author: Prabhat Singh Rajput"},{"id":"resume-bullets-copy-paste-ready","heading":"Resume bullets (copy-paste ready)","level":2,"text":"Use 3–4 bullets on your resume, similar to your Answer Fission format: • Designed and developed StackReady , a responsive interview preparation platform with 7 structured learning tracks (Java, Spring Boot, React, System Design, DSA, Python, AWS), modular markdown based content, and a searchable web UI deployed on GitHub Pages. • Built a cookie based progress system to persist read status, roadmap checklist completion, and reader preferences per browser — enabling personalized study tracking without login or backend database. • Implemented CI/CD with GitHub Actions — automated content sync from source guides to the live site and deployment to — so content updates on reflect on the production website within minutes. • Authored 20+ in depth technical guides (2,500+ lines each for core topics) covering multithreading, JPA/Hibernate, system design, and AI Agents & MCP — designed as structured knowledge for both human learners and future AI tool integration."},{"id":"ai-mcp-bullets-use-honestly-pick-what-fits","heading":"AI / MCP bullets (use honestly — pick what fits)","level":2,"text":"What you actually built today: Educational content on MCP, AI agents, RAG, prompt engineering ( ) Architecture notes for exposing guides as MCP resources (documented, not live server yet) Safe resume bullets: • Documented Model Context Protocol (MCP) integration patterns for developer tooling — including MCP server design, resource exposure, and RAG based Q&A over study material — as part of an AI ready interview prep knowledge base. • Structured interview content for AI agent consumption — consistent markdown sections, topic indexing, and search metadata — enabling future integration with Cursor, Claude Desktop, and custom MCP servers for context aware interview coaching. Stronger bullet (only after you build MCP server): • Built an MCP server exposing StackReady study guides as queryable resources, enabling AI assistants (Cursor / Claude) to retrieve Java, Spring, and System Design context during coding and interview prep sessions."},{"id":"skills-to-list-under-this-project","heading":"Skills to list under this project","level":2,"text":"· · · · · · · · · · · (concepts)"},{"id":"one-line-summary-for-linkedin-cover-letter","heading":"One-line summary (for LinkedIn / cover letter)","level":2,"text":"StackReady is a personal open source interview prep platform I built and authored, combining structured full stack study guides, a responsive web UI, automated GitHub Pages deployment, and AI/MCP ready content architecture."},{"id":"interview-talking-points-if-interviewer-asks","heading":"Interview talking points (if interviewer asks)","level":2,"text":"1. Why built it? — Consolidate scattered interview notes into one searchable, trackable platform for 3+ YOE → senior role prep. 2. Architecture? — Static site (no backend cost), markdown as source of truth, GitHub Actions for sync + deploy. 3. MCP / AI? — Content structured for future MCP resource server; guide covers how agents use tools, RAG, and context engineering. 4. Impact? — Reusable personal knowledge base; public repo helps other developers; demonstrates full ownership from content → UI → DevOps."},{"id":"what-not-to-claim-yet","heading":"What NOT to claim (yet)","level":2,"text":"Don't say Unless you \"Built production MCP server\" Actually deploy server \"Integrated ChatGPT API\" Added live API calls \"Microservices architecture\" This is a static site, not microservices Keep resume truthful — the project is already strong as a personal platform + technical writing + DevOps project."}],"qa":[]},"System_Design_Notes.md":{"title":"System Design Notes","trackId":"system-design","trackTitle":"System Design","sections":[{"id":"system-design-notes","heading":"System Design Notes","level":1,"text":""},{"id":"1-url-shortener-like-bitly-tinyurl","heading":"1. URL Shortener (like bit.ly, tinyurl)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Converts a long URL into a short, unique alias (e.g., ) that redirects users to the original URL. ● Read heavy system — for every 1 URL shortened, there are 100 redirect requests."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Given a long URL, generate a unique short URL. ● When a user hits the short URL, redirect them to the original long URL. ● Short links should expire after a configurable time (optional). ● Users can optionally pick a custom short alias. ● Track click analytics (click count, timestamp, location)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● Low latency — redirect should happen in milliseconds. ● High availability — the service should be up 99.99%. ● Short codes should not be predictable (for security). ● System should handle billions of URLs."},{"id":"capacity-estimation","heading":"Capacity Estimation","level":3,"text":"● New URLs created per day: 100 Million (writes) ● Redirects per day: 10 Billion (reads) ● Read:Write ratio: 100:1 ● QPS (writes): 100M / 86400 ≈ 1,200/sec ● QPS (reads): 10B / 86400 ≈ 120,000/sec ● Storage per record: 500 bytes (short code + long URL + metadata) ● Storage for 5 years: 100M × 365 × 5 × 500B ≈ 90 TB ● Short code length: 7 characters (base62) → 62^7 = 3.5 trillion unique codes"},{"id":"hld-high-level-design","heading":"HLD (High-Level Design)","level":3,"text":"● Load Balancer — Distributes traffic across multiple app server instances. ● App Servers — Stateless Spring Boot services handling shorten and redirect APIs. ● Cache (Redis) — Stores frequently accessed short code to URL mappings. Since reads are 100x writes, caching is critical. ● Database (MySQL/Cassandra) — Persistent storage for all URL mappings. ● Key Generation Service (KGS) — (Optional) Pre generates unique short codes in batches. App servers request a code from KGS instead of computing one at runtime. Eliminates collision handling. Architecture Diagram"},{"id":"lld-low-level-design","heading":"LLD (Low-Level Design)","level":3,"text":"API Design Database Schema Short Code Generation Approaches Approach How It Works Pros Cons Base62 Encoding Convert auto increment ID to base62 (a z, A Z, 0 9) Simple, zero collisions Predictable/sequential MD5/SHA256 Hashing Hash the long URL, take first 7 chars Deterministic (same URL = same code) Collisions possible Pre generated Keys (KGS) Generate random keys in advance, assign on demand Fast, no collision at runtime Needs separate key service UUID based Generate UUID, encode to short string Globally unique Longer codes Collision Handling (if using hashing) ● Append an incrementing salt to the URL, re hash until a unique code is found. ● In practice, collisions are rare with 7 char base62 (3.5 trillion combinations). Class Diagram Design Patterns Used ● Strategy Pattern — interface with swappable implementations (Base62, Hash, UUID). ● Repository Pattern — abstracts database access from service logic. ● Cache Aside Pattern — Check Redis first, fallback to DB on cache miss, populate cache after DB read. ● Builder Pattern — For constructing objects with optional fields (expiry, custom alias). Core Service Logic 301 vs 302 Redirect ● 301 (Moved Permanently) — Browser caches the redirect. Faster for users, but you lose analytics on repeat visits because the browser won't hit your server again. ● 302 (Found/Temporary) — Browser always hits your server first. Better for tracking click counts and analytics. Most URL shorteners use 302."},{"id":"scaling-considerations","heading":"Scaling Considerations","level":3,"text":"● Database Sharding — Shard by first character of short code or hash based sharding. Distributes data across multiple DB instances. ● Cache Eviction — Use LRU (Least Recently Used) policy in Redis. Most URLs follow a power law distribution — a few URLs get most of the traffic. ● Read Replicas — Since reads writes, use MySQL read replicas for redirect queries. ● CDN — For global users, put redirect endpoints behind a CDN for geographic caching. ● Cleanup Job — Run a scheduled batch job to delete expired URLs and reclaim storage."},{"id":"common-interview-follow-ups","heading":"Common Interview Follow-ups","level":3,"text":"● How to prevent abuse? — Rate limiting per user/IP (see Rate Limiter section below). ● How to handle custom aliases? — Check uniqueness before saving; reject if already taken. ● How to track analytics? — Publish click events to Kafka asynchronously, aggregate with a batch job. ● How to ensure uniqueness across distributed servers? — Use a Key Generation Service (KGS) or Twitter Snowflake for distributed ID generation. ● How to handle expiry? — Check on read; run a cron job to purge expired rows."},{"id":"2-rate-limiter","heading":"2. Rate Limiter","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Controls the number of requests a client can make to an API within a given time window. ● Protects backend services from being overwhelmed by too many requests (DDoS attacks, bots, misbehaving clients). ● Returns HTTP when the limit is exceeded."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Limit the number of API requests per client (identified by user ID, API key, or IP address). ● Support different rate limits for different APIs or user tiers (e.g., free: 100 req/min, premium: 1000 req/min). ● Return a clear response (429) with headers indicating remaining quota and retry after time. ● The limiter should work in a distributed environment (multiple app server instances)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● Low latency — rate check must add minimal overhead (< 1ms). ● High availability — if the rate limiter goes down, it should fail open (allow requests) rather than block everything. ● Accurate — should not allow significantly more requests than the configured limit. ● Scalable — should handle millions of users and billions of requests."},{"id":"where-to-place-the-rate-limiter","heading":"Where to Place the Rate Limiter","level":3,"text":"● Most common in interviews: Server side middleware or API Gateway with a Redis backend ."},{"id":"rate-limiting-algorithms","heading":"Rate Limiting Algorithms","level":3,"text":"Algorithm 1: Fixed Window Counter Algorithm 2: Sliding Window Log Algorithm 3: Sliding Window Counter (Hybrid) Algorithm 4: Token Bucket (Most Popular) Algorithm 5: Leaky Bucket Algorithm Comparison Algorithm Accuracy Memory Burst Handling Complexity Fixed Window Low (boundary burst) Very Low Poor Simple Sliding Window Log Very High High Good Medium Sliding Window Counter High Low Good Medium Token Bucket High Low Good (controlled) Medium Leaky Bucket High Low No bursts allowed Medium ● Interview recommendation: Token Bucket or Sliding Window Counter — both are practical and commonly used in production."},{"id":"hld-high-level-design","heading":"HLD (High-Level Design)","level":3,"text":"● Redis is the central store for rate limit counters/tokens across all app server instances. ● Rate limiter runs as a filter/interceptor before the request reaches the controller. ● If limit exceeded, return immediately without hitting the backend. Architecture Diagram"},{"id":"lld-low-level-design","heading":"LLD (Low-Level Design)","level":3,"text":"API Response Headers Database/Redis Schema Rate Limit Rules (MySQL — for configuration) Redis Keys (for runtime counters) Class Diagram Design Patterns Used ● Strategy Pattern — interface with multiple algorithm implementations. Swap Token Bucket for Sliding Window without changing any other code. ● Filter/Interceptor Pattern — intercepts requests before they reach controllers. ● Factory Pattern — creates the correct strategy based on configuration. ● Cache Aside Pattern — Rate limit rules loaded from DB and cached in memory with periodic refresh. Token Bucket Implementation (Redis + Lua) ● Why Lua script? — Redis executes Lua atomically. Without it, a race condition can occur: two requests read the same token count simultaneously, both think there's 1 token left, and both pass — exceeding the limit. Spring Boot Filter"},{"id":"scaling-considerations","heading":"Scaling Considerations","level":3,"text":"● Redis Cluster — For millions of clients, use Redis Cluster to shard rate limit keys across nodes. ● Local + Global Limiting — Use an in memory counter per app server (local) + Redis (global). Local counter acts as a fast first check, reducing Redis calls. ● Fail Open Policy — If Redis is unreachable, allow requests through rather than blocking all traffic. Availability strict rate enforcement. ● Race Conditions — Always use Redis Lua scripts or MULTI/EXEC for atomic read check update operations. ● Distributed Sync — In a multi datacenter setup, each datacenter can maintain its own Redis and sync periodically, accepting slight over limit tolerance."},{"id":"common-interview-follow-ups","heading":"Common Interview Follow-ups","level":3,"text":"● How to identify the client? — API key (best), JWT/user ID (authenticated), IP address (fallback, unreliable behind NAT). ● What if Redis goes down? — Fail open (allow all) or use local in memory fallback with degraded accuracy. ● How to handle different limits for different APIs? — Store rules in DB per API path and user tier, cache in memory. ● How does an API Gateway handle it? — Kong, Nginx, AWS API Gateway have built in rate limiting with Redis backends. ● Token Bucket vs Leaky Bucket? — Token Bucket allows bursts (good for real traffic patterns), Leaky Bucket enforces constant rate (good for protecting downstream services). ● How to rate limit by IP when users are behind a proxy? — Use header, but be careful — it can be spoofed."},{"id":"3-system-design-foundations-must-know-concepts","heading":"3. System Design Foundations (Must-Know Concepts)","level":2,"text":""},{"id":"31-cap-theorem","heading":"3.1 CAP Theorem","level":3,"text":"● In a distributed system , you can only guarantee two out of three properties simultaneously: ● Partition Tolerance is NOT optional in real distributed systems — networks WILL fail. So the real choice is between CP and AP . Choice Behavior During Network Partition Example Systems CP (Consistency + Partition Tolerance) System may reject requests to maintain consistency. Returns error rather than stale data. MongoDB, HBase, Redis (single master), ZooKeeper, Bank transaction systems AP (Availability + Partition Tolerance) System always responds, but may return stale/inconsistent data. Cassandra, DynamoDB, CouchDB, DNS CA (Consistency + Availability) Only possible if there are NO network partitions (single node). Not practical in distributed systems. Traditional single node RDBMS (MySQL on one server) ● JP Morgan context: Financial systems almost always choose CP — you cannot show a wrong account balance or process a duplicate transaction. Availability is handled through redundancy, not by relaxing consistency."},{"id":"32-acid-vs-base","heading":"3.2 ACID vs BASE","level":3,"text":"ACID (Traditional RDBMS — Strong Consistency) BASE (NoSQL / Distributed Systems — Eventual Consistency) Property ACID BASE Consistency Strong (immediate) Eventual Use Case Banking, payments, orders Social media feeds, analytics, caching Performance Slower (locks, coordination) Faster (no coordination overhead) Examples MySQL, PostgreSQL, Oracle Cassandra, DynamoDB, MongoDB ● JP Morgan context: Core banking, payments, and trading systems use ACID. Analytics, logging, and notification systems can use BASE."},{"id":"33-consistent-hashing","heading":"3.3 Consistent Hashing","level":3,"text":"● Problem: In a distributed cache/DB with N nodes, if you use to route requests, adding or removing a node causes almost ALL keys to be remapped — massive cache miss storm. ● Solution: Consistent hashing maps both keys and nodes onto a virtual ring (0 to 2^32). A key is assigned to the first node clockwise from its position. ● When a node is added/removed , only keys between the removed node and its predecessor are remapped — typically only keys (where K = total keys, N = total nodes), not all keys. ● Virtual Nodes: Each physical node gets multiple positions on the ring (e.g., Node A → A1, A2, A3). This ensures even distribution even with few nodes. ● Used in: Amazon DynamoDB, Apache Cassandra, Memcached, Content Delivery Networks (CDNs)."},{"id":"34-database-sharding","heading":"3.4 Database Sharding","level":3,"text":"● Sharding = splitting a large database across multiple servers (shards), each holding a subset of the data. Sharding Strategies Strategy How It Works Pros Cons Range based Shard by value range (e.g., user id 1 1M → Shard 1, 1M 2M → Shard 2) Simple, range queries efficient Hotspot risk (one shard gets more traffic) Hash based Even distribution Range queries require hitting all shards Directory based Lookup table maps each key to its shard Flexible Lookup table is a single point of failure Geographic Shard by region (US → Shard 1, EU → Shard 2) Low latency for regional users Cross region queries are slow Problems with Sharding ● Joins across shards — Not possible. Must denormalize data or do application level joins. ● Resharding — Adding a new shard requires data migration. Consistent hashing helps. ● Celebrity/Hotspot problem — One shard gets disproportionate traffic. Solution: further split that shard or use consistent hashing with virtual nodes. ● Distributed transactions — ACID across shards requires 2 Phase Commit (2PC) or Saga pattern."},{"id":"35-database-replication","heading":"3.5 Database Replication","level":3,"text":"● Sync replication — Master waits for at least one replica to confirm write. Slower but consistent. ● Async replication — Master doesn't wait. Faster but risk of data loss if master crashes before replication. Type Consistency Latency Use Case Synchronous Strong Higher Banking, payments Asynchronous Eventual Lower Analytics, read heavy apps Semi synchronous Middle ground Medium Most production systems"},{"id":"36-load-balancing-algorithms","heading":"3.6 Load Balancing Algorithms","level":3,"text":"Algorithm How It Works Best For Round Robin Distribute requests sequentially (1→2→3→1→2→3) All servers have equal capacity Weighted Round Robin Higher capacity servers get more requests Servers with different specs Least Connections Route to the server with fewest active connections Long lived connections (WebSocket) IP Hash Hash client IP to determine server (sticky sessions) Stateful applications Random Pick a random server Simple, surprisingly effective Least Response Time Route to the server with lowest avg response time Latency sensitive applications ● Layer 4 (Transport) — Routes based on IP/TCP (fast, no content inspection). Example: AWS NLB. ● Layer 7 (Application) — Routes based on HTTP headers, URL path, cookies (smarter, slower). Example: AWS ALB, Nginx."},{"id":"37-caching-strategies","heading":"3.7 Caching Strategies","level":3,"text":"Strategy Flow Use Case Cache Aside (Lazy Loading) App checks cache → miss → read DB → populate cache General purpose, most common Read Through Cache itself fetches from DB on miss Simplifies app code Write Through Write to cache AND DB simultaneously Strong consistency, slower writes Write Behind (Write Back) Write to cache first, async flush to DB Fast writes, risk of data loss Write Around Write directly to DB, skip cache Infrequent reads after write Cache Eviction Policies ● LRU (Least Recently Used) — Evict the item that hasn't been accessed for the longest time. Most common. ● LFU (Least Frequently Used) — Evict the item with the fewest access counts. ● FIFO (First In First Out) — Evict the oldest item. ● TTL (Time To Live) — Evict after a fixed time, regardless of access pattern. Cache Problems ● Cache Stampede — Many requests for the same expired key hit DB simultaneously. Solution: lock/mutex, probabilistic early expiry. ● Cache Penetration — Requests for non existent keys always miss cache and hit DB. Solution: cache null results, Bloom filter. ● Cache Avalanche — Many keys expire at the same time, overwhelming DB. Solution: add random jitter to TTL."},{"id":"38-sql-vs-nosql-when-to-use-what","heading":"3.8 SQL vs NoSQL — When to Use What","level":3,"text":"Criteria SQL (Relational) NoSQL Data model Tables with fixed schema Document, Key Value, Column, Graph Schema Rigid (predefined) Flexible (schema less) Transactions ACID BASE (eventual consistency) Scaling Vertical (scale up) Horizontal (scale out) Joins Efficient Expensive or unsupported Query language SQL (standardized) Varies per DB Best for Banking, ERP, relationships Big data, real time, flexible schema NoSQL Type Examples Use Case Key Value Redis, DynamoDB Caching, session store, rate limiting Document MongoDB, Couchbase CMS, catalogs, user profiles Column Family Cassandra, HBase Time series, analytics, IoT Graph Neo4j, Amazon Neptune Social networks, fraud detection, recommendations"},{"id":"39-proxy-types","heading":"3.9 Proxy Types","level":3,"text":"● Forward Proxy — Sits between client and internet. Client sends requests through proxy. Hides client identity. Example: VPN, corporate proxy. ● Reverse Proxy — Sits between internet and servers. Routes client requests to backend servers. Hides server identity. Example: Nginx, HAProxy, AWS ALB."},{"id":"310-communication-protocols","heading":"3.10 Communication Protocols","level":3,"text":"Protocol Type Use Case HTTP/REST Request Response Standard APIs, CRUD operations WebSocket Full duplex persistent Chat, live trading dashboards, notifications gRPC Binary RPC (HTTP/2) Inter microservice communication (low latency) GraphQL Query language over HTTP Flexible client queries, mobile apps Server Sent Events (SSE) Server → Client stream Live feeds, stock tickers Long Polling Client polls, server holds Fallback when WebSocket unavailable ● JP Morgan context: REST for external APIs, gRPC for internal microservice communication, WebSocket for real time trading dashboards, Kafka for event driven async messaging."},{"id":"4-notification-system-push-email-sms","heading":"4. Notification System (Push / Email / SMS)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Sends notifications to users through multiple channels — push notifications (mobile/web), email, SMS. ● Must be reliable (no duplicate/missing notifications), scalable (millions of users), and extensible (easy to add new channels)."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Send notifications via push (iOS/Android/Web), email, and SMS. ● Support scheduled notifications (send at a specific time). ● Support templated messages with dynamic variables. ● Allow users to manage notification preferences (opt in/opt out per channel). ● Track delivery status (sent, delivered, read, failed)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● At least once delivery — better to send a duplicate than miss a notification. ● High throughput — handle millions of notifications per minute. ● Low latency for real time notifications (< 1 second for push). ● Retry on failure with exponential backoff."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":"● Notification Service — Receives notification requests, validates, applies user preferences, routes to the correct channel queue. ● Message Queue — Separate queues per channel for isolation (email slowness doesn't block push). ● Workers — Consume from queues, call third party providers (APNs for iOS, FCM for Android, SendGrid for email, Twilio for SMS). ● User Preference DB — Stores opt in/opt out settings per user per channel. ● Template Service — Stores and renders notification templates with dynamic variables."},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Database Schema Design Patterns ● Strategy Pattern — interface with , , implementations. ● Observer Pattern — Events trigger notifications (order placed → send confirmation to user). ● Template Method Pattern — Common send flow (validate → render template → dispatch → log) with channel specific rendering. ● Retry with Exponential Backoff — On failure, retry after 1s, 2s, 4s, 8s... up to max retries. Key Interview Points ● Idempotency — Use a unique to deduplicate. If the worker crashes after sending but before ACK, the retry should not send a duplicate. ● Priority Queue — OTP/security notifications should have higher priority than marketing emails. ● Rate Limiting per Provider — APNs, Twilio have rate limits. Workers must respect them. ● Delivery Tracking — Push: delivery receipt from FCM/APNs. Email: webhook from SendGrid (open/click tracking). SMS: delivery report from Twilio."},{"id":"5-payment-transaction-system-jp-morgan-favorite","heading":"5. Payment / Transaction System (JP Morgan Favorite)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Processes financial transactions — money transfers, bill payments, merchant payments. ● Must guarantee correctness (no double charging, no lost money), consistency, and auditability."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Process payments (debit from sender, credit to receiver). ● Support multiple payment methods (bank account, credit card, wallet). ● Handle refunds and chargebacks. ● Maintain a complete audit trail / ledger of all transactions. ● Detect and prevent duplicate payments (idempotency)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● Exactly once processing — A payment must never be processed twice. ● Strong consistency — Account balances must always be accurate. ● High availability — Payment service must be up 99.99%. ● Low latency — Payment processing < 500ms. ● Compliance — PCI DSS, SOX audit trails."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Double Entry Ledger (Core Concept) ● Every transaction creates TWO entries — one debit, one credit. The sum of all entries is always zero. Idempotency (Preventing Double Payment) Transaction State Machine Payment Flow (Step by Step) Key Interview Points ● Why double entry ledger? — Self balancing. If debits ≠ credits, you know something went wrong. Used by every bank in the world. ● Distributed transactions across services? — Use the Saga Pattern (see Section 10) instead of 2PC. If payment provider fails, trigger compensating transactions (refund the debit). ● Reconciliation — Periodically compare your ledger with the payment provider's records to detect discrepancies. ● Eventual consistency — External bank transfers are async. Status may be PENDING until the bank confirms. ● PCI Compliance — Never store raw credit card numbers. Use tokenization (Stripe/Vault generates a token for the card)."},{"id":"6-stock-trading-platform-order-book-jp-morgan-favorite","heading":"6. Stock Trading Platform / Order Book (JP Morgan Favorite)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Allows users to place buy/sell orders for stocks. Matches buyers with sellers. Maintains a real time order book. ● Must be extremely low latency (microsecond matching), fair (FIFO ordering), and reliable."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Users can place market orders (execute immediately at best price) and limit orders (execute only at specified price or better). ● Match buy orders with sell orders in real time. ● Maintain an order book per stock (list of pending buy/sell orders sorted by price). ● Provide real time market data feed (price updates, trade history). ● Support order cancellation and modification."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● Ultra low latency — order matching in microseconds. ● Strong consistency — no double fills, no phantom trades. ● High throughput — millions of orders per second. ● Fairness — orders at the same price are matched FIFO (first come, first served)."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Order Book Data Structure Matching Logic (Price Time Priority) Market Order vs Limit Order ● Market Order — \"Buy 100 shares at ANY price.\" Executes immediately at best available price. No price guarantee. ● Limit Order — \"Buy 100 shares at $150.50 or less.\" Only executes if the price condition is met. May sit in the order book indefinitely. Key Interview Points ● In memory order book — The matching engine keeps the order book in memory (TreeMap/Red Black Tree by price, LinkedList per price level for FIFO). ● Single threaded matching — To avoid locks and ensure fairness, the matching engine is often single threaded per stock. LMAX Disruptor pattern. ● Event Sourcing — Every order and trade is an immutable event. You can rebuild the order book by replaying events. ● Market Data Fan out — Price updates pushed to thousands of clients via WebSocket. Use a pub sub system (Kafka → WebSocket servers → clients). ● Risk checks — Before accepting an order: Does the user have enough balance? Is the order size within limits? Is the account flagged?"},{"id":"7-distributed-cache-like-redis-memcached","heading":"7. Distributed Cache (like Redis / Memcached)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● An in memory key value store that sits between the application and the database to reduce latency and database load."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Data Structures Supported (Redis) Sharding in Cache Cluster ● Use consistent hashing to distribute keys across nodes. ● Each key hashes to a position on the ring → assigned to the next clockwise node. ● Adding/removing a node only remaps K/N keys (not all keys). Replication for High Availability Cache Invalidation Strategies ● TTL (Time to Live) — Key auto expires after N seconds. Simple but may serve stale data within TTL. ● Event driven invalidation — When DB is updated, publish an event to invalidate/update the cache entry. ● Write through — Every DB write also writes to cache. Always consistent but slower writes. Key Interview Points ● Cache vs DB — Cache is fast (microseconds) but volatile (data lost on restart). DB is slow (milliseconds) but durable. ● When NOT to cache — Write heavy data, rarely accessed data, data that must be real time consistent. ● Thundering Herd — Multiple requests for the same expired key. Solution: use a distributed lock — only one request fetches from DB, others wait. ● Hot Key Problem — One key gets millions of requests (e.g., celebrity's profile). Solution: replicate hot keys to all nodes, or add random suffix to spread reads. ● Redis vs Memcached — Redis: richer data structures, persistence, replication, Lua scripting. Memcached: simpler, multi threaded (faster for simple key value at scale)."},{"id":"8-message-queue-event-streaming-kafka","heading":"8. Message Queue / Event Streaming (Kafka)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Decouples producers (services that generate events) from consumers (services that process events). ● Enables asynchronous communication, event driven architecture, and data streaming."},{"id":"why-use-a-message-queue","heading":"Why Use a Message Queue","level":3,"text":""},{"id":"hld-architecture-kafka","heading":"HLD Architecture (Kafka)","level":3,"text":""},{"id":"lld-key-concepts","heading":"LLD Key Concepts","level":3,"text":"Topic, Partition, Offset Consumer Groups ● Within a consumer group, each partition is consumed by exactly one consumer (no duplicates). ● Different consumer groups independently consume the same messages (pub sub model). Delivery Guarantees Guarantee Meaning How At most once Message may be lost, never duplicated Consumer commits offset before processing At least once Message never lost, may be duplicated Consumer commits offset after processing Exactly once Message processed exactly once Idempotent producer + transactional consumer Kafka vs RabbitMQ vs SQS Feature Kafka RabbitMQ AWS SQS Model Log based streaming Traditional message queue Cloud managed queue Ordering Per partition Per queue FIFO queue only Retention Configurable (days/weeks) Until consumed 14 days max Throughput Very high (millions/sec) Moderate Moderate Replay Yes (re read old messages) No (once consumed, gone) No Use case Event streaming, CDC, analytics Task queues, RPC Simple async tasks Key Interview Points ● How Kafka achieves high throughput — Sequential disk I/O (appending to log), zero copy transfer, batching, compression, partitioning for parallelism. ● Ordering guarantee — Only within a partition. Use a partition key (e.g., ) so all messages for the same entity go to the same partition. ● Replication — Each partition has one leader and N 1 followers. Writes go to leader, replicated to followers. If leader dies, a follower becomes the new leader. ● Backpressure — If consumer is slow, messages accumulate in the topic. Kafka handles this naturally (consumers pull at their own pace, unlike push based systems)."},{"id":"9-api-gateway","heading":"9. API Gateway","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Single entry point for all client requests to a microservices backend. ● Handles cross cutting concerns: authentication, rate limiting, routing, load balancing, caching, logging."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"responsibilities","heading":"Responsibilities","level":3,"text":"Feature What It Does Routing → User Service, → Order Service Authentication Validate JWT/API key before forwarding to backend Rate Limiting Throttle requests per client/API (see Section 2) Load Balancing Distribute requests across service instances SSL Termination Handle HTTPS at the gateway, backend uses plain HTTP Request Transform Add headers, modify paths, aggregate responses Circuit Breaker If a backend is failing, stop forwarding requests (fail fast) Caching Cache responses for GET requests Logging & Monitoring Centralized request logging, metrics collection API Gateway vs Load Balancer ● Load Balancer — Layer 4/7 traffic distribution. Dumb routing (round robin, least connections). ● API Gateway — Application level intelligence. Auth, rate limiting, request transformation, protocol translation. A superset of load balancer functionality. Key Interview Points ● Single point of failure? — Deploy multiple gateway instances behind a load balancer. Use health checks and auto scaling. ● Backend for Frontend (BFF) — Different gateways for different clients (mobile gateway returns less data than web gateway). ● Service Mesh vs API Gateway — API Gateway handles north south traffic (client → services). Service Mesh (Istio/Envoy) handles east west traffic (service → service)."},{"id":"10-task-job-scheduler-like-cron","heading":"10. Task / Job Scheduler (like cron)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Executes tasks at scheduled times or intervals (send daily report at 8 AM, retry failed payments every 5 minutes). ● Must be reliable (never miss a job), scalable, and support millions of scheduled tasks."},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":"Key Concepts ● Job Store — Database table holding job definitions (cron expression, callback URL, payload, next run at). ● Scheduler — Polls the job store every second for jobs where . Pushes them to the job queue. ● Workers — Consume from the queue and execute the job (HTTP callback, function invocation, etc.). ● Distributed lock — Only one scheduler instance should poll at a time to avoid duplicate execution. Use Redis lock or DB level . Database Schema Key Interview Points ● Exactly once execution — Use distributed locking + idempotency key per job run. ● Missed jobs — If scheduler was down, on restart scan for all jobs where and execute them. ● Scaling — Partition jobs across multiple scheduler instances by job id range or hash. ● Dead letter queue — Jobs that fail after max retries go to a DLQ for manual inspection."},{"id":"11-distributed-system-patterns-must-know-for-jp-morgan","heading":"11. Distributed System Patterns (Must-Know for JP Morgan)","level":2,"text":""},{"id":"111-circuit-breaker-pattern","heading":"11.1 Circuit Breaker Pattern","level":3,"text":"● Prevents a service from repeatedly calling a failing downstream service (which wastes resources and increases latency). ● Library: Resilience4j (Java), Hystrix (deprecated). ● JP Morgan context: If the payment provider is down, stop hammering it. Return a fast failure to the client and retry later."},{"id":"112-saga-pattern-distributed-transactions","heading":"11.2 Saga Pattern (Distributed Transactions)","level":3,"text":"● When a business transaction spans multiple microservices, you can't use a single DB transaction. Saga breaks it into a sequence of local transactions with compensating actions for rollback. Choreography (Event Driven) Orchestration (Central Coordinator) Type Pros Cons Choreography Decoupled, no single point of failure Hard to track, complex event chains Orchestration Easy to understand, centralized control Orchestrator is a single point of failure ● JP Morgan context: Payment processing across multiple services (debit account → call bank → update ledger → send notification). If bank call fails, compensate by reversing the debit."},{"id":"113-event-sourcing","heading":"11.3 Event Sourcing","level":3,"text":"● Instead of storing the current state, store every event (state change) as an immutable record. Current state is derived by replaying all events. ● Advantages: Full audit trail, can rebuild state at any point in time, enables temporal queries (\"what was the balance at 3 PM?\"). ● JP Morgan context: Perfect for financial systems — regulators require complete audit trails. Every transaction is an immutable event."},{"id":"114-cqrs-command-query-responsibility-segregation","heading":"11.4 CQRS (Command Query Responsibility Segregation)","level":3,"text":"● Separate the write model (commands) from the read model (queries). Different databases optimized for each. ● Why? In most systems, reads writes. Optimizing one DB for both is a compromise. Separate them and optimize each independently. ● Often paired with Event Sourcing — Write side stores events, read side maintains materialized views."},{"id":"115-idempotency","heading":"11.5 Idempotency","level":3,"text":"● An operation is idempotent if performing it multiple times produces the same result as performing it once. ● Critical for distributed systems where network failures cause retries. ● Implementation: Store in Redis with a TTL. On retry, return cached response without re executing."},{"id":"116-two-phase-commit-2pc-vs-saga","heading":"11.6 Two-Phase Commit (2PC) vs Saga","level":3,"text":"Property 2PC Saga Consistency Strong (ACID) Eventual Performance Slow (blocking locks) Fast (async) Availability Low (coordinator is SPOF) High Complexity Simple logic, complex infra Complex logic, simple infra Use case Single DB across shards Microservices with separate DBs ● JP Morgan context: 2PC within a single database (cross shard transactions). Saga across microservices."},{"id":"117-bloom-filter","heading":"11.7 Bloom Filter","level":3,"text":"● A space efficient probabilistic data structure that tells you whether an element is \"definitely NOT in the set\" or \"possibly in the set.\" ● Uses: Cache penetration prevention, duplicate detection, spell checking. ● No false negatives. If it says \"not present,\" trust it. Saves unnecessary DB lookups."},{"id":"12-quick-reference-interview-checklist","heading":"12. Quick Reference — Interview Checklist","level":2,"text":""},{"id":"system-design-template-use-this-structure-in-every-interview","heading":"System Design Template (Use This Structure in Every Interview)","level":3,"text":""},{"id":"jp-morgan-specific-focus-areas","heading":"JP Morgan Specific Focus Areas","level":3,"text":""},{"id":"13-design-netflix-video-streaming-platform","heading":"13. Design Netflix (Video Streaming Platform)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Allows users to browse, search, and stream video content (movies, series) on demand. ● Handles massive scale — 200M+ subscribers, thousands of concurrent streams, content available globally."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● User registration, login, and profile management (multiple profiles per account). ● Browse and search content catalog (by genre, trending, recommendations). ● Stream video in adaptive quality based on user's bandwidth. ● Resume playback from where the user left off. ● Support multiple devices (mobile, TV, web, tablet) simultaneously. ● Content upload and encoding pipeline (admin/studio side)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● High availability — 99.99% uptime (users should never see \"service unavailable\"). ● Low latency — video should start playing within 2 3 seconds. ● Scalable — support millions of concurrent streams globally. ● Adaptive streaming — adjust video quality dynamically based on network conditions. ● Content should be cached close to users (CDN) for minimal buffering."},{"id":"capacity-estimation","heading":"Capacity Estimation","level":3,"text":"● Daily active users: 100M ● Concurrent streams at peak: 10M ● Average video size: 3 GB (multiple resolutions) ● Total content library: 15,000 titles × 3 GB × 5 resolutions = 225 TB ● Bandwidth per stream: 5 Mbps (HD) ● Peak bandwidth: 10M × 5 Mbps = 50 Tbps (served via CDN)"},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":"Additional Components"},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Video Upload & Processing Pipeline ● Videos are broken into small chunks (2 10 seconds each) at each resolution. ● Each chunk is independently downloadable — enables adaptive bitrate streaming. Adaptive Bitrate Streaming (ABR) CDN (Content Delivery Network) Recommendation Engine Database Schema (Simplified) Key Interview Points ● Why chunks? — Enables adaptive streaming, parallel downloads, and seeking without downloading entire file. ● CDN is the backbone — 95%+ of Netflix traffic is served from CDN, not origin servers. ● Microservices at Netflix — 700+ microservices. Each team owns a service (auth, billing, recommendations, streaming, etc.). ● Chaos Engineering — Netflix invented Chaos Monkey (randomly kills production instances) to ensure resilience. ● Data pipeline — Kafka + Apache Spark for processing billions of streaming events daily for analytics and recommendations."},{"id":"14-design-facebook-instagram-social-media-platform","heading":"14. Design Facebook / Instagram (Social Media Platform)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Allows users to create profiles, post content (text/images/videos), follow other users, like/comment, and see a personalized news feed. ● Handles billions of users, posts, and interactions daily."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● User registration, login, profile management. ● Create posts (text, images, videos). ● Follow/unfollow other users. ● News feed — personalized timeline showing posts from people you follow. ● Like, comment, and share posts. ● Search for users and content. ● Push notifications for likes, comments, follows. ● Direct messaging (1:1 and group)."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● High availability — platform must be up 99.99%. ● News feed latency < 200ms. ● Support billions of users and trillions of posts. ● Eventual consistency is acceptable for feed (slight delay is OK). ● Media storage at massive scale (petabytes of images/videos)."},{"id":"capacity-estimation","heading":"Capacity Estimation","level":3,"text":"● Daily active users: 2 Billion (Facebook) ● Posts per day: 500 Million ● Average post size: 1 KB text + 500 KB image = 500 KB ● Storage per day: 500M × 500 KB = 250 TB/day ● Read:Write ratio: 100:1 (people read feed much more than they post) ● News feed QPS: 100M concurrent users × 10 feed refreshes/day = 1B feed requests/day ≈ 12K QPS"},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"News Feed Generation (The Core Problem) Two approaches: Approach 1: Fan out on Write (Push Model) Approach 2: Fan out on Read (Pull Model) Approach 3: Hybrid (What Facebook/Instagram Actually Uses) Feed Ranking Algorithm Image/Media Upload Flow Database Choices Database Schema (Simplified) Feed Cache Structure (Redis) Key Interview Points ● Fan out on Write vs Read — The hybrid approach is the real answer. Know the celebrity problem. ● Social graph — Can use adjacency list in MySQL or a graph DB (Neo4j) for complex queries like \"mutual friends.\" ● Sharding strategy — Shard posts by user id (all posts of a user on same shard). Shard feed cache by user id. ● Hot partition — Celebrity posts can overload one partition. Solution: replicate hot data, or use separate handling. ● Consistency — Feed is eventually consistent (slight delay in seeing a new post is acceptable). Likes/comments can be eventually consistent too. ● Photo storage — Never store images in the database. Use object storage (S3) and serve via CDN."},{"id":"15-cloud-migration-system-design","heading":"15. Cloud Migration System Design","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Migrates an existing on premise application (monolith or distributed) to a cloud platform (AWS, Azure, GCP). ● Involves moving infrastructure, data, applications, and services with minimal downtime and zero data loss."},{"id":"migration-strategies-the-6-rs","heading":"Migration Strategies (The 6 R's)","level":3,"text":"Strategy What It Means When to Use Rehost (Lift & Shift) Move as is to cloud VMs (EC2). No code changes. Quick migration, legacy apps, tight deadline Replatform (Lift & Reshape) Minor optimizations (e.g., switch to managed DB like RDS). No major code changes. Want some cloud benefits without full rewrite Repurchase Replace with SaaS (e.g., move CRM to Salesforce). Off the shelf solution available Refactor (Re architect) Rewrite as cloud native (microservices, containers, serverless). Long term scalability, performance needs Retire Decommission the application (no longer needed). Redundant or unused systems Retain Keep on premise (not ready to migrate). Compliance, dependency, or cost reasons"},{"id":"hld-migration-architecture","heading":"HLD — Migration Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Data Migration Pattern (Zero Downtime) Monolith to Microservices (Strangler Fig Pattern) ● Route requests via API Gateway — old endpoints go to monolith, migrated endpoints go to new microservice. ● Gradually shift traffic until monolith is empty. Containerization (Docker + Kubernetes) Key Interview Points ● Zero downtime migration — Use CDC (Change Data Capture) for continuous replication. Cutover window should be seconds, not hours. ● Strangler Fig Pattern — Incrementally replace monolith components. Safest approach for large systems. ● Dual write problem — During migration, both old and new systems may be active. Ensure idempotency and data consistency. ● Rollback plan — Always have the ability to switch back to on premise if something goes wrong. ● Cost management — Cloud can be expensive. Use reserved instances, auto scaling, spot instances, and right sizing. ● Security — Encrypt data in transit (TLS) and at rest (KMS). IAM policies for least privilege access. VPC for network isolation. ● Compliance — Financial services (JP Morgan) require data residency (data stays in specific regions), audit trails, and encryption standards."},{"id":"16-flash-sale-system-like-amazon-lightning-deals","heading":"16. Flash Sale System (like Amazon Lightning Deals)","level":2,"text":""},{"id":"what-it-does","heading":"What It Does","level":3,"text":"● Sells a limited quantity of items at a discounted price for a short time window. ● Thousands to millions of users compete to buy a few hundred/thousand items simultaneously. ● Must prevent overselling, handle massive traffic spikes, and ensure fairness."},{"id":"functional-requirements","heading":"Functional Requirements","level":3,"text":"● Admin creates a flash sale (product, discounted price, quantity, start time, end time). ● Users see countdown timer and can purchase when sale starts. ● Each user can buy a limited quantity (e.g., max 1 per user). ● Inventory must be accurate — never sell more items than available. ● Show real time stock availability. ● Process payment and confirm order."},{"id":"non-functional-requirements","heading":"Non-Functional Requirements","level":3,"text":"● Handle extreme traffic spikes — 10x 100x normal traffic within seconds. ● No overselling — if 1000 items are available, exactly 1000 orders (not 1001). ● Low latency — purchase flow must complete in < 1 second. ● Fairness — first come first served. ● System should degrade gracefully (static pages, queues) rather than crash."},{"id":"capacity-estimation","heading":"Capacity Estimation","level":3,"text":"● Flash sale: 1000 items available ● Users attempting to buy: 1 Million within first 10 seconds ● QPS spike: 1M / 10 = 100,000 requests/second (100x normal traffic) ● Read:Write ratio: 100:1 (most users just checking if sale is still active)"},{"id":"hld-architecture","heading":"HLD Architecture","level":3,"text":""},{"id":"lld-key-components","heading":"LLD Key Components","level":3,"text":"Inventory Management (The Core Problem) Problem: 1 million users hitting \"Buy\" simultaneously for 1000 items. How to ensure exactly 1000 orders? Solution: Redis Atomic DECR ● Redis is atomic — even with 100K concurrent requests, no race condition. ● No database lock needed. No distributed lock needed. Single Redis command. ● This is the key answer interviewers want. Lua Script for Multi Check (Stock + Per User Limit) Request Queue (Absorbing Traffic Spike) Traffic Control Strategies Purchase Flow (Step by Step) Database Schema Key Interview Points ● Redis atomic DECR — This is THE answer for preventing overselling. Single atomic operation, no locks. ● Never hit the database for stock check — Use Redis. DB is too slow for 100K QPS. ● CDN everything possible — Product page, images, timer. Only the purchase API hits backend. ● Pre scale, don't reactive scale — Flash sale time is known in advance. Scale up servers 30 minutes before. ● Idempotency — If user double clicks \"Buy\", the second request should be rejected (unique constraint on sale id + user id). ● Stock restoration — If payment fails or times out, INCR stock back in Redis. Run a reconciliation job to sync Redis with DB. ● Bot prevention — CAPTCHA before purchase, device fingerprinting, rate limiting per IP/user. ● Fairness — FIFO queue ensures first come first served. No priority based on who has faster internet."},{"id":"17-quick-reference-all-system-designs-summary","heading":"17. Quick Reference — All System Designs Summary","level":2,"text":"System Key Concept to Remember 1 URL Shortener Base62 encoding, 301 vs 302 redirect, cache heavy 2 Rate Limiter Token Bucket algorithm, Redis Lua for atomicity 3 Foundations CAP theorem, ACID vs BASE, Consistent Hashing 4 Notification System Multi channel (Push/Email/SMS), separate queues per channel 5 Payment System Double entry ledger, idempotency key, Saga pattern 6 Stock Trading In memory order book, price time priority matching 7 Distributed Cache Consistent hashing, LRU eviction, cache stampede 8 Message Queue (Kafka) Topics, partitions, consumer groups, at least once 9 API Gateway Routing, auth, rate limiting, SSL termination 10 Task Scheduler Distributed lock, missed job recovery, dead letter queue 11 Distributed Patterns Circuit Breaker, Saga, Event Sourcing, CQRS, Idempotency 12 Interview Checklist Requirements → HLD → LLD → Scaling 13 Netflix Adaptive bitrate streaming, CDN, chunked video, recommendations 14 Facebook/Instagram Fan out on Write vs Read (hybrid), feed ranking, social graph 15 Cloud Migration 6 R's, Strangler Fig, CDC for zero downtime, containerization 16 Flash Sale Redis atomic DECR, pre scaling, virtual waiting room, CDN static pages"}],"qa":[]}};
